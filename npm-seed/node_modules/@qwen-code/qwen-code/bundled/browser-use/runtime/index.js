import { createRequire as __qwenCreateRequire } from 'node:module'; const process = __qwenCreateRequire(import.meta.url)('node:process');
var __defProp = Object.defineProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};

// src/sdk/locator.ts
import { types as utilTypes } from "node:util";
import { Script } from "node:vm";

// src/core/primitives.ts
var FINALIZE_TAB_STATUSES = ["handoff", "deliverable"];
var SNAPSHOT_REF_PATTERN = /^(?:f\d{1,9})?e\d{1,9}$/;

// src/core/serialize-json.ts
function serializeJson(value) {
  const ancestors = /* @__PURE__ */ new Set();
  function encode(item) {
    if (item === null || typeof item === "string" || typeof item === "boolean" || typeof item === "number" && Number.isFinite(item))
      return JSON.stringify(item);
    if (typeof item !== "object" || item === null || ancestors.has(item))
      throw new TypeError("Value must be JSON-serializable");
    const array = Array.isArray(item);
    const prototype = Object.getPrototypeOf(item);
    if (!array && prototype !== null && Object.getPrototypeOf(prototype) !== null || Object.getOwnPropertySymbols(item).some(
      (key) => Object.prototype.propertyIsEnumerable.call(item, key)
    ))
      throw new TypeError("Value must be JSON-serializable");
    ancestors.add(item);
    try {
      if (array) {
        const length = item.length;
        if (Object.keys(item).length !== length)
          throw new TypeError("Value must be JSON-serializable");
        const entries = [];
        for (let index = 0; index < length; index++)
          entries.push(encode(item[index]));
        return "[" + entries.join(",") + "]";
      }
      return "{" + Object.entries(item).map(([key, entry]) => JSON.stringify(key) + ":" + encode(entry)).join(",") + "}";
    } finally {
      ancestors.delete(item);
    }
  }
  return encode(value);
}

// src/sdk/locator.ts
var MAX_NTH_INDEX = 1e4;
var MAX_LOCATOR_STEPS = 32;
function optionRecord(value, label) {
  if (value === void 0) return {};
  if (typeof value !== "object" || value === null || Array.isArray(value))
    throw new TypeError(`${label} must be an object`);
  return value;
}
function matcher(value) {
  if (utilTypes.isRegExp(value))
    return { regex: value.source, flags: value.flags };
  if (typeof value !== "string")
    throw new TypeError("Text matcher must be a string or RegExp");
  return value;
}
function timeoutOptions(options) {
  const value = optionRecord(options, "options");
  return value.timeoutMs === void 0 ? {} : { timeoutMs: value.timeoutMs };
}
function navigationOptions(options) {
  const value = optionRecord(options, "navigation options");
  const result = timeoutOptions(options);
  if (value.waitUntil !== void 0) result.waitUntil = value.waitUntil;
  return result;
}
function actionOptions(options) {
  const value = optionRecord(options, "action options");
  const result = timeoutOptions(options);
  if (value.force !== void 0) result.force = value.force;
  return result;
}
function evaluateArg(value) {
  if (value === void 0) return "undefined";
  try {
    return "JSON.parse(" + JSON.stringify(serializeJson(value)) + ")";
  } catch (error) {
    const wrapped = new TypeError(
      "playwright.evaluate arg must be JSON-serializable"
    );
    wrapped.cause = error;
    throw wrapped;
  }
}
function compiles(expression) {
  try {
    new Script(expression);
    return true;
  } catch {
    return false;
  }
}
function functionExpression(source, method) {
  const prefixed = source.startsWith("async ") ? "async function " + source.slice("async ".length) : "function " + source;
  for (const candidate of [source, prefixed]) {
    const expression = "(" + candidate + ")";
    if (compiles(expression)) return expression;
  }
  throw new TypeError(method + " pageFunction is not serializable");
}
function pageEvaluateScript(pageFunction, arg) {
  const serializedArg = evaluateArg(arg);
  if (typeof pageFunction === "string") {
    if (pageFunction.length === 0)
      throw new TypeError("playwright.evaluate requires a pageFunction");
    return "const arg = " + serializedArg + ";\nreturn eval(" + JSON.stringify(pageFunction) + ");";
  }
  if (typeof pageFunction === "function") {
    return [
      "const arg = " + serializedArg + ";",
      "const __playwrightEvaluate = " + functionExpression(pageFunction.toString(), "playwright.evaluate") + ";",
      "return await __playwrightEvaluate(arg);"
    ].join("\n");
  }
  throw new TypeError("playwright.evaluate requires a string or function");
}
function locatorEvaluateScript(pageFunction, arg, mode) {
  const serializedArg = evaluateArg(arg);
  const method = mode === "all" ? "locator.evaluateAll" : "locator.evaluate";
  if (typeof pageFunction === "string") {
    if (pageFunction.length === 0)
      throw new TypeError(method + " requires a pageFunction");
    return "const arg = " + serializedArg + ";\nreturn eval(" + JSON.stringify(pageFunction) + ");";
  }
  if (typeof pageFunction === "function") {
    return [
      "const arg = " + serializedArg + ";",
      "const __playwrightEvaluate = " + functionExpression(pageFunction.toString(), method) + ";",
      "return await __playwrightEvaluate(" + (mode === "all" ? "elements" : "element") + ", arg);"
    ].join("\n");
  }
  throw new TypeError(method + " requires a string or function");
}
function clickOptions(options) {
  const value = optionRecord(options, "click options");
  const result = { ...timeoutOptions(options) };
  if (value.button !== void 0) result.button = value.button;
  if (value.modifiers !== void 0) result.modifiers = value.modifiers;
  if (value.force !== void 0) result.force = value.force;
  return result;
}
function snapshotRef(nodeId) {
  if (typeof nodeId !== "string")
    throw new TypeError("node_id must be a string");
  const ref = nodeId.trim();
  if (!SNAPSHOT_REF_PATTERN.test(ref))
    throw new TypeError(
      'node_id must be a Playwright snapshot ref such as "e12" or "f1e3" inside a frame'
    );
  return ref;
}
function frameStep(selector) {
  return { kind: "frame", selector };
}
function textStep(kind, text, options) {
  optionRecord(options, "text locator options");
  const step = {
    kind,
    text: matcher(text)
  };
  if (options?.exact !== void 0) step.exact = options.exact;
  return step;
}
function roleStep(role, options) {
  optionRecord(options, "role locator options");
  const step = {
    kind: "getByRole",
    role
  };
  if (options && options.name !== void 0) step.name = matcher(options.name);
  if (options?.exact !== void 0) step.exact = options.exact;
  return step;
}
var LocatorProxy = class _LocatorProxy {
  constructor(context, tabId, steps) {
    Object.defineProperty(this, "context", { value: context });
    Object.defineProperty(this, "tabId", { value: tabId, enumerable: false });
    Object.defineProperty(this, "steps", {
      value: Object.freeze(steps.map((step) => Object.freeze({ ...step }))),
      enumerable: false
    });
  }
  append(step) {
    return new _LocatorProxy(this.context, this.tabId, [...this.steps, step]);
  }
  operandSteps(other, label) {
    if (!(other instanceof _LocatorProxy))
      throw new TypeError(`${label} expects a Locator`);
    if (other.context !== this.context || other.tabId !== this.tabId)
      throw new TypeError(
        `${label} expects a Locator from the same tab and browser session`
      );
    const frames = this.steps.filter((step) => step.kind === "frame");
    const otherFrames = other.steps.filter((step) => step.kind === "frame");
    if (frames.length !== otherFrames.length || frames.some(
      (frame, index) => frame.selector !== otherFrames[index]?.selector
    ))
      throw new TypeError(`${label} expects a Locator from the same frame`);
    return [...other.steps];
  }
  locator(selector, options) {
    const next = this.append({ kind: "locator", selector });
    return options === void 0 ? next : next.filter(options);
  }
  getByRole(role, options) {
    return this.append(roleStep(role, options));
  }
  getByText(text, options) {
    return this.append(textStep("getByText", text, options));
  }
  getByLabel(text, options) {
    return this.append(textStep("getByLabel", text, options));
  }
  getByPlaceholder(text, options) {
    return this.append(textStep("getByPlaceholder", text, options));
  }
  getByTestId(testId) {
    return this.append({ kind: "getByTestId", testId });
  }
  first() {
    return this.append({ kind: "first" });
  }
  last() {
    return this.append({ kind: "last" });
  }
  nth(index) {
    if (!Number.isInteger(index) || index < -MAX_NTH_INDEX || index > MAX_NTH_INDEX)
      throw new TypeError(
        `nth index must be an integer between -${MAX_NTH_INDEX} and ${MAX_NTH_INDEX}`
      );
    return this.append({ kind: "nth", index });
  }
  filter(options = {}) {
    optionRecord(options, "filter options");
    const step = { kind: "filter" };
    if (options.hasText !== void 0) step.hasText = matcher(options.hasText);
    if (options.hasNotText !== void 0)
      step.hasNotText = matcher(options.hasNotText);
    if (options.visible !== void 0) step.visible = options.visible;
    if (options.has !== void 0) {
      step.has = this.operandSteps(options.has, "filter has");
    }
    if (options.hasNot !== void 0) {
      step.hasNot = this.operandSteps(options.hasNot, "filter hasNot");
    }
    return this.append(step);
  }
  and(other) {
    return this.append({ kind: "and", steps: this.operandSteps(other, "and") });
  }
  or(other) {
    return this.append({ kind: "or", steps: this.operandSteps(other, "or") });
  }
  async all() {
    if (this.steps.length >= MAX_LOCATOR_STEPS)
      throw new RangeError(
        `all() cannot extend a locator that already has ${MAX_LOCATOR_STEPS} steps; narrow the locator`
      );
    const count = await this.count();
    if (count > MAX_NTH_INDEX + 1)
      throw new RangeError(
        `all() matched ${count} elements, more than nth() can address; narrow the locator`
      );
    return Array.from({ length: count }, (_, index) => this.nth(index));
  }
  args(extra = {}) {
    return { tabId: this.tabId, steps: this.steps, ...extra };
  }
  count() {
    return this.context.call("locator.count", this.args());
  }
  evaluate(pageFunction, arg, options) {
    return this.context.call(
      "locator.evaluate",
      this.args({
        script: locatorEvaluateScript(pageFunction, arg, "one"),
        ...timeoutOptions(options)
      })
    );
  }
  evaluateAll(pageFunction, arg, options) {
    return this.context.call(
      "locator.evaluateAll",
      this.args({
        script: locatorEvaluateScript(pageFunction, arg, "all"),
        ...timeoutOptions(options)
      })
    );
  }
  allTextContents(options) {
    return this.context.call(
      "locator.allTextContents",
      this.args(timeoutOptions(options))
    );
  }
  innerText(options) {
    return this.context.call(
      "locator.innerText",
      this.args(timeoutOptions(options))
    );
  }
  textContent(options) {
    return this.context.call(
      "locator.textContent",
      this.args(timeoutOptions(options))
    );
  }
  getAttribute(name, options) {
    return this.context.call(
      "locator.getAttribute",
      this.args({ name, ...timeoutOptions(options) })
    );
  }
  isEnabled() {
    return this.context.call("locator.isEnabled", this.args());
  }
  isVisible() {
    return this.context.call("locator.isVisible", this.args());
  }
  click(options) {
    return this.context.call(
      "locator.click",
      this.args(clickOptions(options))
    );
  }
  dblclick(options) {
    return this.context.call(
      "locator.dblclick",
      this.args(clickOptions(options))
    );
  }
  downloadMedia(options = {}) {
    return this.context.call(
      "locator.downloadMedia",
      this.args(timeoutOptions(options))
    );
  }
  fill(value, options) {
    return this.context.call(
      "locator.fill",
      this.args({ value, ...timeoutOptions(options) })
    );
  }
  type(value, options) {
    return this.context.call(
      "locator.type",
      this.args({ value, ...timeoutOptions(options) })
    );
  }
  press(value, options) {
    return this.context.call(
      "locator.press",
      this.args({ value, ...timeoutOptions(options) })
    );
  }
  selectOption(value, options) {
    return this.context.call(
      "locator.selectOption",
      this.args({ value, ...timeoutOptions(options) })
    );
  }
  check(options) {
    return this.context.call(
      "locator.check",
      this.args(actionOptions(options))
    );
  }
  uncheck(options) {
    return this.context.call(
      "locator.uncheck",
      this.args(actionOptions(options))
    );
  }
  setChecked(checked, options) {
    return this.context.call(
      "locator.setChecked",
      this.args({ checked, ...actionOptions(options) })
    );
  }
  waitFor(options) {
    return this.context.call(
      "locator.waitFor",
      this.args({
        state: options.state,
        ...timeoutOptions(options)
      })
    );
  }
  toJSON() {
    return { type: "Locator", tabId: this.tabId, steps: this.steps.length };
  }
};
var FrameLocatorProxy = class _FrameLocatorProxy {
  constructor(context, tabId, steps) {
    Object.defineProperty(this, "context", { value: context });
    Object.defineProperty(this, "tabId", { value: tabId, enumerable: false });
    Object.defineProperty(this, "steps", {
      value: Object.freeze(steps.map((step) => Object.freeze({ ...step }))),
      enumerable: false
    });
  }
  inner(step) {
    return new LocatorProxy(this.context, this.tabId, [...this.steps, step]);
  }
  locator(selector) {
    return this.inner({ kind: "locator", selector });
  }
  getByRole(role, options) {
    return this.inner(roleStep(role, options));
  }
  getByText(text, options) {
    return this.inner(textStep("getByText", text, options));
  }
  getByLabel(text, options) {
    return this.inner(textStep("getByLabel", text, options));
  }
  getByPlaceholder(text, options) {
    return this.inner(textStep("getByPlaceholder", text, options));
  }
  getByTestId(testId) {
    return this.inner({ kind: "getByTestId", testId });
  }
  frameLocator(selector) {
    return new _FrameLocatorProxy(this.context, this.tabId, [
      ...this.steps,
      frameStep(selector)
    ]);
  }
  toJSON() {
    return {
      type: "FrameLocator",
      tabId: this.tabId,
      steps: this.steps.length
    };
  }
};

// src/sdk/tab.ts
var PlaywrightFileChooser = class {
  constructor(context, tabId, descriptor) {
    Object.defineProperty(this, "context", { value: context });
    Object.defineProperty(this, "tabId", { value: tabId, enumerable: false });
    Object.defineProperty(this, "chooserId", {
      value: descriptor.chooserId,
      enumerable: false
    });
    Object.defineProperty(this, "multiple", {
      value: descriptor.multiple === true,
      enumerable: false
    });
  }
  isMultiple() {
    return this.multiple;
  }
  setFiles(files, options = {}) {
    const list = Array.isArray(files) ? files : [files];
    return this.context.call("fileChooser.setFiles", {
      tabId: this.tabId,
      chooserId: this.chooserId,
      files: list,
      ...timeoutOptions(options)
    });
  }
  toJSON() {
    return { type: "PlaywrightFileChooser", multiple: this.multiple };
  }
};
var PlaywrightProxy = class {
  constructor(context, tabId) {
    Object.defineProperty(this, "context", { value: context });
    Object.defineProperty(this, "tabId", { value: tabId });
  }
  locator(selector) {
    return new LocatorProxy(this.context, this.tabId, [
      { kind: "locator", selector }
    ]);
  }
  getByRole(role, options) {
    return new LocatorProxy(this.context, this.tabId, [
      roleStep(role, options)
    ]);
  }
  getByText(text, options) {
    return new LocatorProxy(this.context, this.tabId, [
      textStep("getByText", text, options)
    ]);
  }
  getByLabel(text, options) {
    return new LocatorProxy(this.context, this.tabId, [
      textStep("getByLabel", text, options)
    ]);
  }
  getByPlaceholder(text, options) {
    return new LocatorProxy(this.context, this.tabId, [
      textStep("getByPlaceholder", text, options)
    ]);
  }
  getByTestId(testId) {
    return new LocatorProxy(this.context, this.tabId, [
      { kind: "getByTestId", testId }
    ]);
  }
  frameLocator(selector) {
    return new FrameLocatorProxy(this.context, this.tabId, [
      frameStep(selector)
    ]);
  }
  evaluate(pageFunction, arg, options) {
    return this.context.call(
      "playwright.evaluate",
      {
        tabId: this.tabId,
        script: pageEvaluateScript(pageFunction, arg),
        ...timeoutOptions(options)
      }
    );
  }
  domSnapshot() {
    return this.context.call("playwright.domSnapshot", {
      tabId: this.tabId
    });
  }
  waitForEvent(event, options = {}) {
    if (event !== "download" && event !== "filechooser") {
      throw new TypeError(
        'waitForEvent supports only "download" and "filechooser"'
      );
    }
    const args = {
      tabId: this.tabId,
      event,
      ...timeoutOptions(options)
    };
    if (event === "filechooser") {
      return this.context.call("playwright.waitForEvent", args).then(
        (descriptor) => new PlaywrightFileChooser(this.context, this.tabId, descriptor)
      );
    }
    return this.context.call("playwright.waitForEvent", args);
  }
  waitForURL(url, options = {}) {
    if (typeof url !== "string")
      throw new TypeError("waitForURL expects a string");
    return this.context.call("playwright.waitForURL", {
      tabId: this.tabId,
      url,
      ...navigationOptions(options)
    });
  }
  async expectNavigation(action, options = {}) {
    if (typeof action !== "function")
      throw new TypeError("expectNavigation expects an action function");
    const args = {
      tabId: this.tabId,
      ...navigationOptions(options)
    };
    if (options.url !== void 0) args.url = options.url;
    const waiterId = await this.context.call(
      "playwright.expectNavigation.begin",
      args
    );
    let value;
    try {
      value = await action();
      await this.context.call("playwright.expectNavigation.wait", {
        tabId: this.tabId,
        waiterId
      });
    } catch (error) {
      try {
        await this.context.call("playwright.expectNavigation.cancel", {
          tabId: this.tabId,
          waiterId
        });
      } catch {
      }
      throw error;
    }
    return value;
  }
  waitForLoadState(options = {}) {
    const args = { tabId: this.tabId, ...timeoutOptions(options) };
    if (options.state !== void 0) args.state = options.state;
    return this.context.call("playwright.waitForLoadState", args);
  }
  waitForTimeout(timeoutMs2) {
    return this.context.call("playwright.waitForTimeout", {
      tabId: this.tabId,
      timeoutMs: timeoutMs2
    });
  }
};
var CUAProxy = class {
  constructor(context, tabId) {
    Object.defineProperty(this, "context", { value: context });
    Object.defineProperty(this, "tabId", { value: tabId, enumerable: false });
  }
  click(options) {
    return this.context.call("cua.click", {
      tabId: this.tabId,
      ...options
    });
  }
  double_click(options) {
    return this.context.call("cua.double_click", {
      tabId: this.tabId,
      ...options
    });
  }
  drag(options) {
    return this.context.call("cua.drag", {
      tabId: this.tabId,
      ...options
    });
  }
  keypress(options) {
    return this.context.call("cua.keypress", {
      tabId: this.tabId,
      ...options
    });
  }
  move(options) {
    return this.context.call("cua.move", {
      tabId: this.tabId,
      ...options
    });
  }
  scroll(options) {
    return this.context.call("cua.scroll", {
      tabId: this.tabId,
      ...options
    });
  }
  type(options) {
    return this.context.call("cua.type", {
      tabId: this.tabId,
      ...options
    });
  }
};
var DomCUAProxy = class {
  constructor(context, tabId) {
    Object.defineProperty(this, "context", { value: context });
    Object.defineProperty(this, "tabId", { value: tabId, enumerable: false });
  }
  get_visible_dom() {
    return this.context.call("dom_cua.get_visible_dom", {
      tabId: this.tabId
    });
  }
  click(options) {
    return this.context.call("dom_cua.click", {
      ...options,
      tabId: this.tabId,
      node_id: snapshotRef(options.node_id)
    });
  }
  double_click(options) {
    return this.context.call("dom_cua.double_click", {
      ...options,
      tabId: this.tabId,
      node_id: snapshotRef(options.node_id)
    });
  }
  type(options) {
    return this.context.call("dom_cua.type", {
      ...options,
      tabId: this.tabId
    });
  }
  keypress(options) {
    return this.context.call("dom_cua.keypress", {
      ...options,
      tabId: this.tabId
    });
  }
  scroll(options) {
    return this.context.call("dom_cua.scroll", {
      ...options,
      tabId: this.tabId,
      ...options.node_id === void 0 ? {} : { node_id: snapshotRef(options.node_id) }
    });
  }
};
var DevProxy = class {
  constructor(context, tabId) {
    Object.defineProperty(this, "context", { value: context });
    Object.defineProperty(this, "tabId", { value: tabId, enumerable: false });
  }
  logs(options = {}) {
    const args = { tabId: this.tabId };
    if (options.filter !== void 0) args.filter = options.filter;
    if (options.levels !== void 0) args.levels = options.levels;
    if (options.limit !== void 0) args.limit = options.limit;
    return this.context.call("dev.logs", args);
  }
};
var TabProxy = class {
  constructor(context, browserId, info) {
    Object.defineProperty(this, "context", { value: context });
    Object.defineProperty(this, "browserId", {
      value: browserId,
      enumerable: false
    });
    Object.defineProperty(this, "id", { value: info.id, enumerable: true });
    Object.defineProperty(this, "playwright", {
      value: new PlaywrightProxy(context, info.id),
      enumerable: true
    });
    Object.defineProperty(this, "cua", {
      value: new CUAProxy(context, info.id),
      enumerable: true
    });
    Object.defineProperty(this, "dom_cua", {
      value: new DomCUAProxy(context, info.id),
      enumerable: true
    });
    Object.defineProperty(this, "dev", {
      value: new DevProxy(context, info.id),
      enumerable: true
    });
  }
  goto(url) {
    return this.context.call("tab.goto", {
      tabId: this.id,
      url
    });
  }
  url() {
    return this.context.call("tab.url", { tabId: this.id });
  }
  title() {
    return this.context.call("tab.title", { tabId: this.id });
  }
  back() {
    return this.context.call("tab.back", { tabId: this.id });
  }
  forward() {
    return this.context.call("tab.forward", { tabId: this.id });
  }
  reload() {
    return this.context.call("tab.reload", { tabId: this.id });
  }
  close() {
    return this.context.call("tab.close", { tabId: this.id });
  }
  screenshot(options = {}) {
    return this.context.screenshotCall("tab.screenshot", {
      tabId: this.id,
      ...options
    });
  }
  async getJsDialog() {
    const info = await this.context.call("tab.getJsDialog", { tabId: this.id });
    if (info == null) return void 0;
    const tabId = this.id;
    const dialogId = info.dialogId;
    const context = this.context;
    const dismiss = () => context.call("tab.dialog.dismiss", { tabId, dialogId });
    const base = {
      type: info.type,
      message: info.message,
      dismiss
    };
    if (info.type === "confirm" || info.type === "beforeunload") {
      return Object.freeze({
        ...base,
        type: info.type,
        accept: () => context.call("tab.dialog.accept", { tabId, dialogId })
      });
    }
    if (info.type === "prompt") {
      return Object.freeze({
        ...base,
        type: info.type,
        defaultValue: info.defaultPrompt,
        accept: (text) => {
          if (typeof text !== "string")
            throw new TypeError("Prompt dialog accept expects text");
          return context.call("tab.dialog.accept", {
            tabId,
            dialogId,
            promptText: text
          });
        }
      });
    }
    return Object.freeze({ ...base, type: info.type });
  }
  toJSON() {
    return { type: "Tab", id: this.id };
  }
};

// src/sdk/browser.ts
var TabsProxy = class {
  constructor(context, browserId) {
    Object.defineProperty(this, "context", { value: context });
    Object.defineProperty(this, "browserId", { value: browserId });
  }
  async new() {
    return new TabProxy(
      this.context,
      this.browserId,
      await this.context.call("tabs.new", {
        browserId: this.browserId
      })
    );
  }
  list() {
    return this.context.call("tabs.list", {
      browserId: this.browserId
    });
  }
  async get(tabId) {
    return new TabProxy(
      this.context,
      this.browserId,
      await this.context.call("tabs.get", {
        browserId: this.browserId,
        tabId
      })
    );
  }
  async selected() {
    const info = await this.context.call("tabs.selected", {
      browserId: this.browserId
    });
    return info == null ? void 0 : new TabProxy(this.context, this.browserId, info);
  }
  finalize(options) {
    if (!options || typeof options !== "object") {
      throw new TypeError("tabs.finalize expects an options object");
    }
    return this.context.call("tabs.finalize", {
      browserId: this.browserId,
      ...options.keep === void 0 ? {} : {
        keep: options.keep.map(({ tab, status }) => ({
          tabId: typeof tab === "string" ? tab : tab.id,
          status
        }))
      }
    });
  }
};
var BrowserUserProxy = class {
  constructor(context, browserId) {
    Object.defineProperty(this, "context", { value: context });
    Object.defineProperty(this, "browserId", { value: browserId });
  }
  openTabs() {
    return this.context.call("browser.user.openTabs", {
      browserId: this.browserId
    });
  }
  async claimTab(tab) {
    if (typeof tab !== "string" && (!tab || typeof tab !== "object")) {
      throw new TypeError(
        "claimTab expects a tab returned by browser.user.openTabs()"
      );
    }
    return new TabProxy(
      this.context,
      this.browserId,
      await this.context.call("browser.user.claimTab", {
        browserId: this.browserId,
        tab
      })
    );
  }
  history(options) {
    return this.context.call("browser.user.history", {
      browserId: this.browserId,
      ...options === void 0 ? {} : { options }
    });
  }
};
var BrowserProxy = class {
  constructor(context, info) {
    Object.defineProperty(this, "context", { value: context });
    Object.defineProperty(this, "browserId", {
      value: info.id,
      enumerable: true
    });
    Object.defineProperty(this, "tabs", {
      value: new TabsProxy(context, info.id),
      enumerable: true
    });
    Object.defineProperty(this, "user", {
      value: new BrowserUserProxy(context, info.id),
      enumerable: true
    });
  }
  documentation() {
    return this.context.call("browser.documentation", {
      browserId: this.browserId
    });
  }
  nameSession(name) {
    return this.context.call("browser.nameSession", {
      browserId: this.browserId,
      name
    });
  }
  toJSON() {
    return { type: "Browser", browserId: this.browserId };
  }
};
var BrowsersProxy = class {
  constructor(context) {
    Object.defineProperty(this, "context", { value: context });
  }
  list() {
    return this.context.call("browsers.list", {});
  }
  async get(id2) {
    return new BrowserProxy(
      this.context,
      await this.context.call("browsers.get", { id: id2 })
    );
  }
};
var AgentProxy = class {
  constructor(context) {
    Object.defineProperty(this, "browsers", {
      value: new BrowsersProxy(context),
      enumerable: true
    });
  }
  toJSON() {
    return { type: "Agent" };
  }
};

// src/sdk/context.ts
import { Buffer as Buffer3 } from "node:buffer";

// src/bridge/errors.ts
var BrowserRuntimeError = class extends Error {
  code;
  details;
  constructor(code, message, details) {
    super(message);
    this.name = "BrowserRuntimeError";
    this.code = code;
    this.details = details;
  }
  toJSON() {
    return {
      name: this.name,
      code: this.code,
      message: this.message,
      ...this.details === void 0 ? {} : { details: this.details }
    };
  }
};

// src/bridge/protocol.ts
import { statSync } from "node:fs";
import { posix } from "node:path";
var CHROME_BRIDGE_PROTOCOL_VERSION = 3;
var CHROME_NATIVE_HOST_NAME = "com.qwen.browser_use";
var CHROME_NATIVE_HOST_REVISION = 2;
var CHROME_EXTENSION_ID = "idkijaaipeeinemigojbjkmfmabokbdk";
var CHROME_WEB_STORE_EXTENSION_ID = "hdhmmjclhibojdddmancfgbkleahfaph";
var CHROME_EXTENSION_IDS = [
  CHROME_EXTENSION_ID,
  CHROME_WEB_STORE_EXTENSION_ID
];
var MAX_BRIDGE_FRAME_BYTES = 16 * 1024 * 1024;
var CDP_REQUEST_TIMEOUT_MS = 13e4;
function defaultChromeBridgeSocketPath(environment = process.env) {
  const configured = environment.QWEN_BROWSER_USE_SOCKET_PATH?.trim();
  if (configured) return configured;
  if (process.platform === "win32") {
    const identity = environment.USERNAME?.replace(/[^a-zA-Z0-9_.-]/g, "_") || "default";
    return `\\\\.\\pipe\\qwen-browser-use-${identity}`;
  }
  const uid = typeof process.getuid === "function" ? process.getuid() : "default";
  return posix.join(defaultChromeBridgeSocketDirectory(uid), "bridge.sock");
}
function defaultChromeBridgeSocketDirectory(uid, platform3 = process.platform, stat4 = statDirectory) {
  if (platform3 !== "win32" && typeof uid === "number") {
    const runtimeDir = `/run/user/${uid}`;
    const info = stat4(runtimeDir);
    if (info !== void 0 && info.isDirectory() && info.uid === uid && (info.mode & 63) === 0)
      return runtimeDir;
  }
  const base = platform3 === "darwin" ? "/private/tmp" : "/tmp";
  return posix.join(base, `qwen-browser-use-${uid}`);
}
function statDirectory(path) {
  try {
    return statSync(path);
  } catch {
    return void 0;
  }
}

// src/bridge/transport/chrome-extension-transport.ts
import { connect as connect2 } from "node:net";
import { randomUUID } from "node:crypto";
import { clearTimeout as clearTimeout2, setTimeout as setTimeout2 } from "node:timers";

// src/bridge/discovery.ts
import { lstat as lstat2, mkdir as mkdir2, readdir, readFile, unlink } from "node:fs/promises";
import { connect } from "node:net";
import { homedir } from "node:os";
import { dirname as dirname2, join } from "node:path";

// src/bridge/socket-path.ts
import { lstat, mkdir, realpath, stat } from "node:fs/promises";
import { dirname, resolve } from "node:path";
async function prepareSocketDirectory(socketPath) {
  if (process.platform === "win32") return;
  await mkdir(dirname(socketPath), { mode: 448 }).catch((error) => {
    if (error.code !== "EEXIST") throw error;
  });
  await verifySocketDirectory(socketPath);
}
async function verifySocketDirectory(socketPath) {
  if (process.platform === "win32") return;
  const directory = dirname(socketPath);
  const info = await lstat(directory);
  if (!info.isDirectory() || info.uid !== process.getuid() || (info.mode & 63) !== 0) {
    throw new Error(
      `Chrome bridge requires a private user-owned directory: ${directory}`
    );
  }
  for (const start of [resolve(directory), await realpath(directory)]) {
    let ancestor = dirname(start);
    for (; ; ) {
      const entry = await lstat(ancestor);
      const parent = await stat(ancestor);
      if (entry.uid !== 0 && entry.uid !== process.getuid() || parent.uid !== 0 && parent.uid !== process.getuid() || (parent.mode & 18) !== 0 && !(parent.uid === 0 && (parent.mode & 512) !== 0)) {
        throw new Error(
          `Chrome bridge socket has an unsafe ancestor: ${ancestor}`
        );
      }
      const next = dirname(ancestor);
      if (next === ancestor) break;
      ancestor = next;
    }
  }
}
async function verifySocketPeerPath(socketPath) {
  if (process.platform === "win32") return;
  await verifySocketDirectory(socketPath);
  const info = await lstat(socketPath);
  if (!info.isSocket() || info.uid !== process.getuid()) {
    throw new Error(
      `Chrome bridge requires a user-owned socket: ${socketPath}`
    );
  }
}

// src/bridge/discovery.ts
var DISCOVERY_DIRECTORY_NAME = "qwen-hosts";
function chromeDiscoveryDirectory() {
  return process.env["QWEN_BROWSER_USE_DISCOVERY_DIR"]?.trim() || (process.platform === "win32" ? join(homedir(), ".qwen", "browser-use", "profiles") : join(
    dirname2(defaultChromeBridgeSocketPath()),
    DISCOVERY_DIRECTORY_NAME
  ));
}
function profileSocketPath(directory, hostId) {
  return process.platform === "win32" ? `${defaultChromeBridgeSocketPath()}-${hostId}` : join(directory, `${hostId}.sock`);
}
async function prepareDiscoveryDirectory(directory) {
  if (process.platform === "win32") {
    await mkdir2(directory, { recursive: true });
    return;
  }
  await mkdir2(directory, { recursive: true, mode: 448 });
  await prepareSocketDirectory(join(directory, "discovery"));
}
async function discoverChromeProfiles(directory = chromeDiscoveryDirectory()) {
  await prepareDiscoveryDirectory(directory);
  const entries = await readdir(directory);
  const candidates = await Promise.all(
    entries.filter((entry) => /^[0-9a-f-]{36}\.json$/.test(entry)).map(async (entry) => {
      try {
        const file = join(directory, entry);
        const info = await lstat2(file);
        if (!info.isFile() || process.platform !== "win32" && (info.uid !== process.getuid() || (info.mode & 63) !== 0))
          return;
        const value = JSON.parse(await readFile(file, "utf8"));
        if (!isEndpoint(value) || value.hostInstanceId + ".json" !== entry || value.socketPath !== profileSocketPath(directory, value.hostInstanceId))
          return;
        await verifySocketPeerPath(value.socketPath);
        if (!await socketIsLive(value.socketPath)) {
          if (!processIsAlive(value.pid))
            await removeDeadHost(file, value.socketPath);
          return;
        }
        return { endpoint: value, timestamp: info.mtimeMs };
      } catch {
        return;
      }
    })
  );
  const profiles = /* @__PURE__ */ new Map();
  for (const candidate of candidates.filter((entry) => entry !== void 0).sort((a, b) => b.timestamp - a.timestamp)) {
    if (!profiles.has(candidate.endpoint.extensionInstanceId))
      profiles.set(candidate.endpoint.extensionInstanceId, candidate.endpoint);
  }
  return [...profiles.values()];
}
function isEndpoint(value) {
  if (typeof value !== "object" || value === null) return false;
  const entry = value;
  return typeof entry["extensionInstanceId"] === "string" && entry["extensionInstanceId"].length > 0 && entry["extensionInstanceId"].length <= 128 && typeof entry["hostInstanceId"] === "string" && /^[0-9a-f-]{36}$/.test(entry["hostInstanceId"]) && typeof entry["socketPath"] === "string" && typeof entry["protocolVersion"] === "number" && typeof entry["extensionProtocolVersion"] === "number" && typeof entry["pid"] === "number";
}
function processIsAlive(pid) {
  if (!Number.isInteger(pid) || pid <= 0) return true;
  try {
    process.kill(pid, 0);
    return true;
  } catch (error) {
    return error.code !== "ESRCH";
  }
}
async function removeDeadHost(record3, socketPath) {
  await unlink(record3).catch(() => void 0);
  const info = await lstat2(socketPath).catch(() => void 0);
  if (info?.isSocket() && info.uid === process.getuid())
    await unlink(socketPath).catch(() => void 0);
}
async function socketIsLive(path) {
  return await new Promise((resolve3) => {
    const socket = connect(path);
    const timer = setTimeout(() => finish(false), 200);
    const finish = (live) => {
      clearTimeout(timer);
      socket.destroy();
      resolve3(live);
    };
    socket.once("connect", () => finish(true));
    socket.once("error", () => finish(false));
  });
}

// src/bridge/transport/framing.ts
function encodeFrame(value) {
  const payload = Buffer.from(JSON.stringify(value), "utf8");
  if (payload.byteLength > MAX_BRIDGE_FRAME_BYTES) {
    throw new Error(`Bridge frame exceeds ${MAX_BRIDGE_FRAME_BYTES} bytes`);
  }
  const header = Buffer.allocUnsafe(4);
  header.writeUInt32LE(payload.byteLength, 0);
  return Buffer.concat([header, payload]);
}
var FrameDecoder = class {
  buffered = Buffer.alloc(0);
  push(chunk) {
    this.buffered = this.buffered.byteLength === 0 ? chunk : Buffer.concat([this.buffered, chunk]);
    const messages = [];
    while (this.buffered.byteLength >= 4) {
      const length = this.buffered.readUInt32LE(0);
      if (length > MAX_BRIDGE_FRAME_BYTES) {
        throw new Error(
          `Bridge frame declares ${length} bytes; maximum is ${MAX_BRIDGE_FRAME_BYTES}`
        );
      }
      if (this.buffered.byteLength < length + 4) break;
      const payload = this.buffered.subarray(4, length + 4);
      this.buffered = this.buffered.subarray(length + 4);
      messages.push(JSON.parse(payload.toString("utf8")));
    }
    return messages;
  }
};

// src/bridge/transport/chrome-extension-transport.ts
var ChromeExtensionTransport = class {
  socketPath;
  explicitSocketPath;
  connectTimeoutMs;
  requestTimeoutMs;
  describeProfiles;
  socket;
  hello;
  selectedExtensionInstanceId;
  // Requested by selectProfile; becomes the binding only once a Host answers.
  requestedExtensionInstanceId;
  startPromise;
  stopPromise;
  pending = /* @__PURE__ */ new Map();
  eventListeners = /* @__PURE__ */ new Set();
  connectionListeners = /* @__PURE__ */ new Set();
  constructor(options = {}) {
    this.socketPath = options.socketPath ?? defaultChromeBridgeSocketPath();
    this.explicitSocketPath = Boolean(
      options.socketPath ?? process.env["QWEN_BROWSER_USE_SOCKET_PATH"]?.trim()
    );
    this.connectTimeoutMs = options.connectTimeoutMs ?? 35e3;
    this.requestTimeoutMs = options.requestTimeoutMs ?? 3e4;
    this.describeProfiles = options.describeProfiles;
  }
  async start() {
    if (this.stopPromise !== void 0) await this.stopPromise;
    if (this.isConnected()) return;
    const attempt = this.startPromise ??= this.startInternal();
    try {
      return await attempt;
    } finally {
      if (this.startPromise === attempt) this.startPromise = void 0;
    }
  }
  isConnected() {
    return this.socket !== void 0 && !this.socket.destroyed && this.hello !== void 0;
  }
  onEvent(listener) {
    this.eventListeners.add(listener);
    return () => {
      this.eventListeners.delete(listener);
    };
  }
  onConnectionChange(listener) {
    this.connectionListeners.add(listener);
    return () => {
      this.connectionListeners.delete(listener);
    };
  }
  async request(method, params = {}, timeoutMs2) {
    if (this.stopPromise !== void 0) throw disconnectedError();
    return await this.sendRequest(method, params, timeoutMs2);
  }
  async sendRequest(method, params, timeoutMs2) {
    const budget = timeoutMs2 ?? (method === "cdp.send" ? CDP_REQUEST_TIMEOUT_MS : this.requestTimeoutMs);
    const socket = this.socket;
    if (socket === void 0 || socket.destroyed) {
      throw disconnectedError();
    }
    const id2 = randomUUID();
    const frame = encodeFrame({
      type: "request",
      browserSessionId: this.hello?.browserSessionId,
      id: id2,
      method,
      params
    });
    const response = new Promise((resolve3, reject) => {
      const timer = setTimeout2(() => {
        this.pending.delete(id2);
        reject(
          new BrowserRuntimeError(
            "OPERATION_TIMEOUT",
            `Chrome bridge request timed out: ${method}`
          )
        );
      }, budget);
      this.pending.set(id2, {
        resolve: resolve3,
        reject,
        timer,
        ...method === "cdp.send" && typeof params.tabId === "number" && (params.sessionId === void 0 || typeof params.sessionId === "string") && (params.method === "Input.dispatchMouseEvent" || params.method === "Input.dispatchKeyEvent" || params.method === "Input.insertText") ? {
          inputTarget: {
            tabId: params.tabId,
            ...typeof params.sessionId === "string" ? { sessionId: params.sessionId } : {}
          }
        } : {}
      });
    });
    try {
      socket.write(frame);
    } catch (error) {
      const pending = this.pending.get(id2);
      if (pending !== void 0) clearTimeout2(pending.timer);
      this.pending.delete(id2);
      throw error;
    }
    return await response;
  }
  async stop() {
    const attempt = this.stopPromise ??= this.stopInternal(this.startPromise);
    try {
      return await attempt;
    } finally {
      if (this.stopPromise === attempt) this.stopPromise = void 0;
    }
  }
  async profiles() {
    if (!this.explicitSocketPath) {
      const deadline = Date.now() + this.connectTimeoutMs;
      for (; ; ) {
        const profiles = await discoverChromeProfiles();
        const compatible = profiles.filter(isCompatible);
        if (compatible.length > 0) return await this.describe(compatible);
        if (Date.now() >= deadline) {
          if (profiles.length > 0) throw versionMismatch();
          return [];
        }
        await delay(Math.min(POLL_INTERVAL_MS, deadline - Date.now()));
      }
    }
    try {
      await this.start();
    } catch (error) {
      if (error instanceof BrowserRuntimeError && error.code === "BROWSER_DISCONNECTED")
        return [];
      throw error;
    }
    return [
      {
        extensionInstanceId: this.hello.extensionInstanceId,
        hostInstanceId: this.hello.hostInstanceId,
        protocolVersion: CHROME_BRIDGE_PROTOCOL_VERSION,
        extensionProtocolVersion: CHROME_BRIDGE_PROTOCOL_VERSION,
        socketPath: this.socketPath,
        pid: 0
      }
    ];
  }
  selectProfile(id2) {
    if (id2 === "chrome" || id2 === "extension") return;
    if (!id2.startsWith("chrome:") || id2.length <= 7)
      throw new BrowserRuntimeError("NOT_FOUND", "Unknown Chrome profile");
    const profile = id2.slice(7);
    if (this.selectedExtensionInstanceId !== void 0 && this.selectedExtensionInstanceId !== profile)
      throw new BrowserRuntimeError(
        "INVALID_ARGUMENT",
        "This Browser Use runtime is already bound to another Chrome profile. To select a different profile, reset the Node REPL kernel and run the Browser Use setup again."
      );
    if (this.selectedExtensionInstanceId === void 0)
      this.requestedExtensionInstanceId = profile;
  }
  async startInternal() {
    try {
      await this.connectWithinDeadline(
        this.selectedExtensionInstanceId ?? this.requestedExtensionInstanceId
      );
    } finally {
      this.requestedExtensionInstanceId = void 0;
    }
  }
  async connectWithinDeadline(wanted) {
    const deadline = Date.now() + this.connectTimeoutMs;
    let lastError;
    let mismatch;
    do {
      try {
        let endpoint;
        if (!this.explicitSocketPath) {
          const profiles = await discoverChromeProfiles();
          endpoint = wanted === void 0 ? await this.defaultProfile(profiles) : profiles.find(
            (profile) => profile.extensionInstanceId === wanted
          );
          if (endpoint === void 0) throw disconnectedError();
          if (!isCompatible(endpoint)) throw versionMismatch();
          this.socketPath = endpoint.socketPath;
        }
        await verifySocketPeerPath(this.socketPath);
        await this.connectHost(
          endpoint,
          wanted,
          Math.max(1, deadline - Date.now())
        );
        return;
      } catch (error) {
        lastError = error;
        if (error instanceof BrowserRuntimeError && error.code === "EXTENSION_VERSION_MISMATCH")
          mismatch = error;
        const code = error.code;
        if (code !== "ENOENT" && code !== "ECONNREFUSED" && code !== "ECONNRESET" && code !== "EPIPE" && !(error instanceof BrowserRuntimeError))
          throw new BrowserRuntimeError(
            "TRANSPORT_UNAVAILABLE",
            `Could not connect to the local Chrome Host: ${errorMessage(error)}`
          );
      }
      if (Date.now() < deadline)
        await delay(Math.min(POLL_INTERVAL_MS, deadline - Date.now()));
    } while (Date.now() < deadline);
    if (mismatch !== void 0) throw mismatch;
    throw disconnectedError(
      `Qwen Chrome extension is not connected. Open Chrome and install or enable the Qwen extension in the profile you want to use (chrome://extensions), then retry. ${errorMessage(lastError)}`
    );
  }
  /**
   * Newest compatible Host by default; with several compatible profiles,
   * prefer the one Chrome last used, as a user would expect.
   */
  async defaultProfile(profiles) {
    const compatible = profiles.filter(isCompatible);
    if (compatible.length > 1)
      return (await this.describe(compatible)).find((profile) => profile.lastUsed) ?? compatible[0];
    return compatible[0] ?? profiles[0];
  }
  async describe(profiles) {
    if (this.describeProfiles === void 0 || profiles.length === 0)
      return profiles;
    const described = await this.describeProfiles(
      profiles.map((profile) => profile.extensionInstanceId)
    ).catch(() => /* @__PURE__ */ new Map());
    return profiles.map((profile) => {
      const description = described.get(profile.extensionInstanceId);
      return description === void 0 ? profile : {
        ...profile,
        profileName: description.name,
        lastUsed: description.lastUsed
      };
    });
  }
  async connectHost(endpoint, wanted, timeoutMs2) {
    await new Promise((resolve3, reject) => {
      const socket = connect2(this.socketPath);
      const decoder = new FrameDecoder();
      const inbound = [];
      let validated = false;
      let draining = false;
      const timer = setTimeout2(
        () => fail(disconnectedError("Chrome Host hello timed out")),
        timeoutMs2
      );
      const fail = (error) => {
        clearTimeout2(timer);
        socket.destroy();
        reject(error);
      };
      socket.once(
        "connect",
        () => socket.write(
          encodeFrame({
            type: "client.hello",
            protocolVersion: CHROME_BRIDGE_PROTOCOL_VERSION,
            extensionInstanceId: wanted ?? endpoint?.extensionInstanceId,
            hostInstanceId: endpoint?.hostInstanceId
          })
        )
      );
      const drain = () => {
        while (inbound.length > 0 && !socket.destroyed) {
          const message = inbound.shift();
          if (!isObject(message)) {
            fail(disconnectedError("Invalid Chrome Host message"));
            break;
          }
          if (!validated) {
            if (message.type === "error" && message.code === "EXTENSION_VERSION_MISMATCH") {
              fail(versionMismatch());
              break;
            }
            if (message.type !== "hello" || !CHROME_EXTENSION_IDS.includes(message.extensionId) || message.protocolVersion !== CHROME_BRIDGE_PROTOCOL_VERSION || typeof message.extensionInstanceId !== "string" || !message.extensionInstanceId || typeof message.hostInstanceId !== "string" || !message.hostInstanceId || typeof message.browserSessionId !== "string" || !message.browserSessionId || wanted !== void 0 && wanted !== message.extensionInstanceId || endpoint !== void 0 && (message.extensionInstanceId !== endpoint.extensionInstanceId || message.hostInstanceId !== endpoint.hostInstanceId)) {
              fail(
                disconnectedError(
                  "Chrome Host profile or protocol did not match"
                )
              );
              break;
            }
            validated = true;
            clearTimeout2(timer);
            this.selectedExtensionInstanceId ??= message.extensionInstanceId;
            this.socket = socket;
            this.hello = message;
            this.notifyConnectionChange(true);
            resolve3();
            continue;
          }
          if (this.socket !== socket) break;
          if (message.browserSessionId !== this.hello?.browserSessionId)
            continue;
          const settles = message.type === "response" && typeof message.id === "string" && this.pending.has(message.id);
          this.handleMessage(message);
          if (settles && inbound.length > 0) {
            setImmediate(drain);
            return;
          }
        }
        draining = false;
      };
      socket.on("data", (chunk) => {
        try {
          inbound.push(...decoder.push(chunk));
        } catch {
          fail(disconnectedError("Invalid Chrome Host frame"));
          return;
        }
        if (!draining) {
          draining = true;
          drain();
        }
      });
      socket.on("error", (error) => {
        if (!validated) fail(error);
      });
      socket.on("close", () => {
        inbound.length = 0;
        clearTimeout2(timer);
        if (!validated) reject(disconnectedError());
        if (this.socket === socket) this.disconnect(disconnectedError());
      });
    });
  }
  handleMessage(message) {
    if (!isObject(message) || typeof message.type !== "string") return;
    if (message.type === "hello") return;
    if (this.hello === void 0) return;
    if (message.type === "event") {
      if (typeof message.tabId !== "number" || typeof message.method !== "string" || "sessionId" in message && (typeof message.sessionId !== "string" || message.sessionId === ""))
        return;
      const event = {
        type: "event",
        tabId: message.tabId,
        method: message.method,
        params: message.params,
        ...typeof message.sessionId === "string" && message.sessionId !== "" ? { sessionId: message.sessionId } : {}
      };
      for (const listener of this.eventListeners) {
        try {
          listener(event);
        } catch {
        }
      }
      if (event.method === "Page.javascriptDialogOpening") {
        for (const [id2, pending2] of this.pending) {
          if (pending2.inputTarget?.tabId === event.tabId && pending2.inputTarget.sessionId === event.sessionId) {
            this.pending.delete(id2);
            clearTimeout2(pending2.timer);
            pending2.resolve({});
          }
        }
      }
      return;
    }
    if (message.type !== "response" || typeof message.id !== "string") return;
    const response = message;
    const pending = this.pending.get(response.id);
    if (pending === void 0) return;
    this.pending.delete(response.id);
    clearTimeout2(pending.timer);
    if (response.ok) {
      pending.resolve(response.result);
    } else {
      const code = response.error?.code;
      pending.reject(
        new BrowserRuntimeError(
          bridgeRuntimeErrorCode(code),
          response.error?.message || "Chrome extension operation failed"
        )
      );
    }
  }
  disconnect(error) {
    const wasConnected = this.hello !== void 0;
    const socket = this.socket;
    this.socket = void 0;
    this.hello = void 0;
    if (socket !== void 0 && !socket.destroyed) socket.destroy();
    for (const pending of this.pending.values()) {
      clearTimeout2(pending.timer);
      pending.reject(error);
    }
    this.pending.clear();
    if (wasConnected) this.notifyConnectionChange(false);
  }
  notifyConnectionChange(connected) {
    for (const listener of this.connectionListeners) {
      try {
        listener(connected);
      } catch {
      }
    }
  }
  async stopInternal(starting) {
    await starting?.catch(() => void 0);
    if (this.isConnected())
      await this.sendRequest("session.close", {}, 2e3).catch(() => void 0);
    this.disconnect(disconnectedError("Chrome bridge stopped"));
    this.selectedExtensionInstanceId = void 0;
    this.requestedExtensionInstanceId = void 0;
  }
};
var POLL_INTERVAL_MS = 100;
function delay(ms) {
  return new Promise((resolve3) => setTimeout2(resolve3, ms));
}
function isCompatible(profile) {
  return profile.protocolVersion === CHROME_BRIDGE_PROTOCOL_VERSION && profile.extensionProtocolVersion === CHROME_BRIDGE_PROTOCOL_VERSION;
}
function versionMismatch() {
  return new BrowserRuntimeError(
    "EXTENSION_VERSION_MISMATCH",
    "Browser Use CLI, Native Host and Chrome extension versions must match. Update Qwen Code, run native-host-setup.js install, and reload the extension at chrome://extensions."
  );
}
function bridgeRuntimeErrorCode(code) {
  switch (code) {
    case "NOT_GRANTED":
      return "TAB_NOT_GRANTED";
    case "STALE_TAB":
      return "STALE_TAB";
    case "UNSUPPORTED_TAB":
      return "UNSUPPORTED_TAB";
    case "PERMISSION_REQUIRED":
      return "PERMISSION_REQUIRED";
    case "TAB_OWNERSHIP_CONFLICT":
      return "TAB_OWNERSHIP_CONFLICT";
    case "TAB_DEBUGGER_CONFLICT":
      return "TAB_DEBUGGER_CONFLICT";
    default:
      return "OPERATION_FAILED";
  }
}
function disconnectedError(message = "Chrome extension disconnected") {
  return new BrowserRuntimeError("BROWSER_DISCONNECTED", message);
}
function isObject(value) {
  return typeof value === "object" && value !== null && !Array.isArray(value);
}
function errorMessage(error) {
  return isObject(error) && typeof error.message === "string" && error.message !== "" ? error.message : String(error);
}

// src/core/errors.ts
function invalidArguments(method, error) {
  const issues = error.issues.map((issue) => ({
    code: issue.code,
    path: issue.path.join("."),
    message: issue.message
  }));
  const summary = issues.slice(0, 3).map(
    ({ path, message }) => `${path || "options"}: ${message}`.slice(0, 200)
  ).join("; ");
  const hint = method === "cua.keypress" || method === "dom_cua.keypress" ? ` Use ${method}({ keys: ["Enter"] }); a chord uses an array such as ["Control", "a"].` : method === "dom_cua.type" ? " Use dom_cua.click({ node_id }) followed by dom_cua.type({ text }) to type at the current focus." : "";
  return new BrowserRuntimeError(
    "INVALID_ARGUMENT",
    `Invalid arguments for ${method}: ${summary}.${hint}`,
    { issues }
  );
}
function staleSessionError() {
  return new BrowserRuntimeError(
    "STALE_BROWSER_SESSION",
    "This Browser Use session is stale; initialize Browser Use and claim the tab again"
  );
}
function sanitizeOperationError(method, error) {
  if (error instanceof BrowserRuntimeError) return error;
  const rawMessage = operationErrorMessage(error);
  const message = rawMessage ? `${method} failed: ${rawMessage}` : `${method} failed`;
  const apiName = /^[a-zA-Z][\w$]*(?:\.[\w$]+)*:\s/.exec(rawMessage)?.[0];
  const failure = rawMessage.slice(apiName?.length ?? 0);
  const name = error instanceof Error ? error.name : "";
  if (name === "TimeoutError")
    return new BrowserRuntimeError("OPERATION_TIMEOUT", message);
  const firstLine = failure.split("\n", 1)[0] ?? "";
  const pageChannel = apiName !== void 0 && /evaluate/i.test(apiName);
  if (!pageChannel && /^(?:target|page) crashed/i.test(firstLine))
    return new BrowserRuntimeError("STALE_TAB", message);
  if (!pageChannel && /^(?:Error: )?strict mode violation: .* resolved to \d+ elements:/i.test(
    firstLine
  ))
    return new BrowserRuntimeError("LOCATOR_NOT_UNIQUE", message);
  if (!pageChannel && /^(?:target (?:page|context|browser).*closed|page has been closed|no tab with id)/i.test(
    firstLine
  ))
    return new BrowserRuntimeError("STALE_TAB", message);
  if (!pageChannel && /^frame was detached/i.test(firstLine))
    return new BrowserRuntimeError("INVALID_LOCATOR", message);
  return new BrowserRuntimeError("OPERATION_FAILED", message);
}
function operationErrorMessage(error) {
  const message = error instanceof Error ? error.message : typeof error === "object" && error !== null && "message" in error && typeof error.message === "string" ? error.message : "";
  return message.trim().slice(0, 4e3);
}

// src/runtime.ts
import { fileURLToPath } from "node:url";

// src/core/chrome-runtime-documentation.ts
var DEFAULT_CHROME_DOCUMENTATION = `# Qwen Browser Use

This runtime controls the user's existing Chrome through an explicitly installed extension and a local Native Messaging bridge.

## Control boundary

- Multiple sessions share the profile while controlling separate tabs. Claiming a tab held by another session returns TAB_OWNERSHIP_CONFLICT; wait for its release or choose another tab.
- This runtime is bound to the Chrome profile it first connected to; setup's browserAgent.browsers.get('chrome') picks the default. browserAgent.browsers.list() lists the available profiles. To work in a different one, reset the Node REPL kernel and pass its id to browserAgent.browsers.get(id) during setup instead.

- Installing the extension is the user's consent. browser.user.openTabs() lists every open top-level http(s) tab across the user's Chrome windows, ordered by lastOpened descending.
- To take over an already-open tab, call browser.user.openTabs(), choose the matching returned object by its visible title, url, lastOpened and tabGroup, then pass that exact object to browser.user.claimTab().
- Claiming controls the chosen tab in place, without moving it into an agent tab group, and returns a normal controllable Tab.
- A claim fails closed if the tab title or URL changed after discovery; list open tabs again instead of claiming a different tab.
- Do not guess tab ids. Only claim ids that came from the current openTabs() result.
- tabs.new() creates a new agent tab in the user's Chrome; it shares the profile's cookies and signed-in state.
- browser.user.history({ queries?, from?, to?, limit? }) lists recent browsing history ordered by dateVisited descending. Without from, Chrome searches the last 24 hours; pass from explicitly to include older visits.
- Kernel reset loses JavaScript handles but does not close Chrome or erase its profile. Re-run setup and claim the tab again.

## API

- Tab: goto, url, title, back, forward, reload, close, screenshot (viewport, clip, or fullPage). back and forward return when history navigation commits, without waiting for another load event.
- Coordinate CUA (tab.cua): click({ x, y, button?, keypress? }), double_click({ x, y, keypress? }), drag({ path: [{ x, y }, ...], keys? }), move({ x, y, keys? }) and scroll({ x, y, scrollX, scrollY, keypress? }) use viewport CSS-pixel coordinates read from a screenshot. click button values are 1=left, 2=middle, 3=right, 4=back and 5=forward. type({ text }) and keypress({ keys: ["Enter"] }) act at the current keyboard focus, not at coordinates. Keys and modifiers are arrays, e.g. ["Control", "a"] for a chord.
- goto navigates to http(s) URLs only, waits for Playwright's default load state and tolerates redirects; read tab.url() afterwards for the final address.
- Semantic locator: CSS, role, text, label, placeholder, test id, filter, first/last/nth, and/or. getByText resolves to the innermost matching element.
- Locator reads: count, allTextContents, innerText, textContent, getAttribute, isEnabled and isVisible. Reads other than count accept timeoutMs where the SDK signature provides it. Reads default to a 1s timeout (actions 5s): innerText, textContent, getAttribute, isEnabled and allTextContents wait up to that long for the element, so pass timeoutMs or call waitFor() first when content renders after an action; count and isVisible return immediately.
- Evaluation: tab.playwright.evaluate(pageFunction, arg?, options?), locator.evaluate(pageFunction, arg?, options?) and locator.evaluateAll(pageFunction, arg?, options?). Use these for inspection and use action APIs for page changes. Page functions may be JavaScript functions or script strings and must use JSON-serializable arguments and results. Strings return their last expression's value; use an async function for await and parentheses around object literals. Function-valued strings are not invoked.
- Locator actions: click, dblclick, downloadMedia, fill, type, press, selectOption, check, uncheck and setChecked. downloadMedia triggers a download for the matched media or file link; it reads the resource from the page origin, so a cross-origin resource whose server sends no CORS headers fails with an error naming that cause instead of downloading \u2014 do not retry the same call for it. Actions use strict locator matching, actionability checks, auto-waiting, and a default 5s timeout; use an explicit timeoutMs or waitFor() when a page needs longer.
- click/dblclick/check scroll the element into view and send real input events (options: button, modifiers, force where applicable). A covered element is reported instead of clicked unless force is set.
- Click and keypress actions return after input is dispatched, without waiting for a resulting navigation. Success does not mean the destination has loaded. Use expectNavigation(action, options) when the action should navigate; its navigation timeout is independent of the action timeout.
- locator.press("Enter"), locator.press("Control+a") and locator.type("text") send real keyboard events to the located element. locator.type() inserts at the current selection without clearing existing text and accepts up to 60000 characters per call; use locator.fill() to replace a value or for longer text. fill follows Playwright input/change event behavior; it does not dispatch an extra change event. If typing produces no observable change in the original focused editable element, locator.type() raises INPUT_BLOCKED so the target state can be inspected before continuing. This heuristic does not apply to cua.type() or dom_cua.type().
- For uploads, arm playwright.waitForEvent("filechooser") before clicking, then call chooser.isMultiple() and chooser.setFiles(paths). Paths must name existing absolute files and each call is limited to 100 MB.
- tab.playwright.domSnapshot() and tab.dom_cua.get_visible_dom() return the same Playwright AI accessibility snapshot, including static text and containers alongside controls. Neither filters by role or cursor style. Node ids such as [ref=e12] identify nodes, not a guarantee that clicking them performs an action. Use dom_cua.click({ node_id: "e12" }) or double_click({ node_id: "e12" }) for a listed node, then dom_cua.type({ text }) or dom_cua.keypress({ keys: ["Enter"] }) at the current focus. type and keypress do not accept node_id; focus the target with click first. dom_cua.scroll({ node_id?, x, y }) scrolls the page or a listed node by x/y wheel deltas in CSS pixels (negative scrolls up or left); unlike cua.scroll, its x/y are not viewport coordinates. Take a new snapshot after navigation \u2014 node ids from before a navigation are rejected \u2014 or when a node id becomes stale.
- iframes: snapshots expand same-origin and cross-origin iframes beneath their "iframe" line; elements inside a frame use Playwright refs such as [ref=f1e3], which dom_cua accepts. tab.playwright.frameLocator(selector), chainable on frame locators, scopes getByRole/getByText/getByLabel/getByPlaceholder/getByTestId/locator to that frame's document.
- Waits live on tab.playwright (not on tab): locator.waitFor({ state }), tab.playwright.expectNavigation(action, options), tab.playwright.waitForEvent("filechooser" | "download"), tab.playwright.waitForURL(url), tab.playwright.waitForLoadState(options), tab.playwright.waitForTimeout(ms). Arm event waiters before the triggering action. A download event confirms that the browser started a download but does not expose its local path. expectNavigation arms its waiter before invoking the action, so fast click-triggered navigations are not missed.
- tab.dev.logs({ filter?, levels?, limit? }) returns console messages and uncaught exceptions captured since the tab was claimed.
- JavaScript dialogs: opening a dialog does not by itself make an already-dispatched click or keypress fail. Call tab.getJsDialog() to read the dialog type and message. While a dialog is open, page operations fail with DIALOG_OPEN instead of waiting for a timeout. Alerts can be dismissed, confirms and before-unload dialogs can be accepted or dismissed, and prompts require text when accepted. A dialog handle applies only to the dialog that was read; get a new handle if it has closed or been replaced. Claiming a tab that already shows a JavaScript dialog fails with DIALOG_OPEN: Chrome shows that dialog to the user and only the user can close it, so ask them to close it, then list open tabs and claim again. Releasing or finalizing a tab while its dialog is open leaves the dialog for the user.
- A claimed tab that is closed, crashed, or whose debugger the user revoked reports STALE_TAB on the next command; claim a tab again to continue.
- Screenshots return JPEG bytes, mimeType and metadata with the original image dimensions, viewport, device pixel ratio and CSS-pixel coordinate space. Pass the complete object to nodeRepl.emitImage(await tab.screenshot()) so metadata stays attached to the image. Only a viewport screenshot is directly usable as cua coordinate space; for clip and fullPage captures the metadata origin names the document point of the image's top-left pixel.
- Clipboard and raw CDP are not exposed.

- Browser transport, serialization, snapshot truncation and screenshot budgets are runtime details rather than model-facing controls.`;

// src/native-host-installer.ts
import { createHash, randomUUID as randomUUID2 } from "node:crypto";
import { constants } from "node:fs";
import {
  access,
  chmod,
  mkdir as mkdir3,
  readFile as readFile2,
  readdir as readdir2,
  rename,
  rm,
  stat as stat2,
  writeFile
} from "node:fs/promises";
import { homedir as homedir2 } from "node:os";
import { dirname as dirname3, isAbsolute, join as join2, resolve as resolve2 } from "node:path";
var LAUNCHER_MARKER = "# qwen-browser-use native host";
var INSTALLATION_MARKER = "# installation ";
var MANIFEST_FILE = CHROME_NATIVE_HOST_NAME + ".json";
var ALLOWED_ORIGINS = CHROME_EXTENSION_IDS.map(
  (id2) => "chrome-extension://" + id2 + "/"
);
async function installChromeNativeHost(options) {
  const resolved = resolveOptions(options);
  const hostContents = await readFile2(resolved.nativeHostPath, "utf8");
  await access(resolved.nodePath, constants.X_OK);
  const existingLauncher = await readExistingFile(resolved.launcherPath);
  if (existingLauncher !== null && !isOwnedLauncher(existingLauncher)) {
    throw new Error(
      "Refusing to replace a foreign Native Host launcher: " + resolved.launcherPath
    );
  }
  await mkdir3(dirname3(resolved.launcherPath), {
    recursive: true,
    mode: 448
  });
  const installedHostPath = join2(
    dirname3(resolved.launcherPath),
    "hosts",
    createHash("sha256").update(hostContents).digest("hex"),
    "native-host.mjs"
  );
  await mkdir3(dirname3(installedHostPath), { recursive: true, mode: 448 });
  const existingHost = await readExistingFile(installedHostPath);
  if (existingHost !== null && existingHost !== hostContents) {
    throw new Error(
      "Installed Native Host content has changed: " + installedHostPath
    );
  }
  await atomicWrite(installedHostPath, hostContents, 384, existingHost);
  const installation = {
    protocolVersion: CHROME_BRIDGE_PROTOCOL_VERSION,
    revision: CHROME_NATIVE_HOST_REVISION,
    nativeHostPath: installedHostPath,
    nodePath: resolved.nodePath
  };
  await atomicWrite(
    resolved.launcherPath,
    [
      "#!/bin/sh",
      LAUNCHER_MARKER,
      INSTALLATION_MARKER + JSON.stringify(installation),
      "exec " + shellQuote(resolved.nodePath) + " " + shellQuote(installedHostPath) + ' "$@"',
      ""
    ].join("\n"),
    448,
    existingLauncher
  );
  await writeManifests(resolved);
  return await statusChromeNativeHost(options);
}
var NativeHostNewerError = class extends Error {
  constructor(installedProtocolVersion) {
    super(
      "The installed Browser Use Native Host speaks protocol " + installedProtocolVersion + ", newer than this Qwen Code (protocol " + CHROME_BRIDGE_PROTOCOL_VERSION + "). Update Qwen Code; the installed Host was left unchanged."
    );
    this.installedProtocolVersion = installedProtocolVersion;
    this.name = "NativeHostNewerError";
  }
};
async function ensureChromeNativeHost(options) {
  const status = await statusChromeNativeHost(options);
  if (status.protocolVersion !== null && status.protocolVersion > CHROME_BRIDGE_PROTOCOL_VERSION)
    throw new NativeHostNewerError(status.protocolVersion);
  if (!status.ready || status.protocolVersion !== CHROME_BRIDGE_PROTOCOL_VERSION || (status.hostRevision ?? 0) < CHROME_NATIVE_HOST_REVISION) {
    return { ...await installChromeNativeHost(options), action: "installed" };
  }
  await writeManifests(resolveOptions(options));
  return { ...await statusChromeNativeHost(options), action: "reused" };
}
async function statusChromeNativeHost(options) {
  const resolved = resolveOptions(options);
  const installedPaths = [];
  const skippedForeignPaths = [];
  for (const manifestPath of resolved.manifestPaths) {
    const contents = await readExistingFile(manifestPath);
    if (contents === null) continue;
    if (isOwnedManifest(contents, resolved.launcherPath)) {
      installedPaths.push(manifestPath);
    } else {
      skippedForeignPaths.push(manifestPath);
    }
  }
  const launcher = await readExistingFile(resolved.launcherPath);
  if (launcher !== null && isOwnedLauncher(launcher)) {
    installedPaths.push(resolved.launcherPath);
  } else if (launcher !== null) {
    skippedForeignPaths.push(resolved.launcherPath);
  }
  const installation = readInstallation(launcher);
  return {
    launcherPath: resolved.launcherPath,
    manifestPaths: resolved.manifestPaths,
    installedPaths,
    skippedForeignPaths,
    protocolVersion: installation?.protocolVersion ?? null,
    hostRevision: installation?.revision ?? null,
    nativeHostPath: installation?.nativeHostPath ?? null,
    ready: installation !== null && installedPaths.length > 1 && await pathExists(installation.nativeHostPath, constants.R_OK) && await pathExists(resolved.launcherPath, constants.X_OK) && await pathExists(installation.nodePath, constants.X_OK)
  };
}
function readInstallation(launcher) {
  if (launcher === null || !isOwnedLauncher(launcher)) return null;
  const line = launcher.split("\n")[2];
  if (!line?.startsWith(INSTALLATION_MARKER)) return null;
  try {
    const value = JSON.parse(
      line.slice(INSTALLATION_MARKER.length)
    );
    if (Number.isInteger(value.protocolVersion) && Number.isInteger(value.revision) && typeof value.nativeHostPath === "string" && isAbsolute(value.nativeHostPath) && typeof value.nodePath === "string" && isAbsolute(value.nodePath))
      return value;
  } catch {
  }
  return null;
}
var MAX_STORAGE_FILE_BYTES = 8 * 1024 * 1024;
async function describeChromeProfiles(options, instanceIds) {
  const described = /* @__PURE__ */ new Map();
  if (instanceIds.length === 0) return described;
  const platform3 = options.platform ?? process.platform;
  if (platform3 !== "darwin" && platform3 !== "linux") return described;
  const { manifestPaths } = resolveOptions({
    ...options,
    nativeHostPath: "/unused",
    nodePath: "/unused"
  });
  for (const manifestPath of manifestPaths) {
    const browserRoot = dirname3(dirname3(manifestPath));
    const localState = await readExistingFile(join2(browserRoot, "Local State")).then(
      (contents) => contents === null ? null : JSON.parse(contents)
    ).catch(() => null);
    if (localState === null) continue;
    const browser = browserLabel(browserRoot);
    const infoCache = localState.profile?.info_cache ?? {};
    for (const profileDirectory of Object.keys(infoCache)) {
      if (!/^(Default|Profile \d+)$/.test(profileDirectory)) continue;
      const storages = CHROME_EXTENSION_IDS.map(
        (id2) => join2(browserRoot, profileDirectory, "Local Extension Settings", id2)
      );
      const files = [];
      for (const storage of storages) {
        for (const file of await readdir2(storage).catch(() => []))
          files.push(join2(storage, file));
      }
      for (const path of files) {
        if (!/\.(log|ldb)$/.test(path)) continue;
        const info = await stat2(path).catch(() => void 0);
        if (!info?.isFile() || info.size > MAX_STORAGE_FILE_BYTES) continue;
        const bytes = await readFile2(path).catch(() => void 0);
        if (bytes === void 0) continue;
        for (const id2 of instanceIds) {
          if (described.has(id2) || !bytes.includes(id2)) continue;
          const name = infoCache[profileDirectory]?.name;
          const profileName = typeof name === "string" && name.trim() !== "" ? name.trim() : profileDirectory;
          described.set(id2, {
            name: browser + " \xB7 " + profileName,
            lastUsed: localState.profile?.last_used === profileDirectory
          });
        }
      }
    }
  }
  return described;
}
function browserLabel(browserRoot) {
  if (/Chrome for Testing|google-chrome-for-testing/.test(browserRoot))
    return "Chrome for Testing";
  if (/chromium/i.test(browserRoot)) return "Chromium";
  return "Chrome";
}
function nativeHostInstallHome(environment = process.env) {
  const configured = environment["QWEN_BROWSER_USE_INSTALL_HOME"]?.trim();
  return resolve2(configured ? configured : homedir2());
}
function resolveOptions(options) {
  const platform3 = options.platform ?? process.platform;
  if (platform3 !== "darwin" && platform3 !== "linux") {
    throw new Error(
      "Automatic Native Messaging installation supports macOS and Linux"
    );
  }
  const homeDir = resolve2(options.homeDir ?? nativeHostInstallHome());
  const nativeHostPath = options.nativeHostPath;
  const nodePath = options.nodePath ?? process.execPath;
  if (!isAbsolute(nativeHostPath) || !isAbsolute(nodePath)) {
    throw new Error("Native Host and Node paths must be absolute");
  }
  const manifestRoots = platform3 === "darwin" ? [
    "Library/Application Support/Google/Chrome/NativeMessagingHosts",
    "Library/Application Support/Google/Chrome for Testing/NativeMessagingHosts",
    "Library/Application Support/Chromium/NativeMessagingHosts"
  ] : [
    ".config/google-chrome/NativeMessagingHosts",
    ".config/google-chrome-for-testing/NativeMessagingHosts",
    ".config/chromium/NativeMessagingHosts"
  ];
  return {
    nativeHostPath,
    nodePath,
    // Not `native-host.sh`: protocol 2 installers own and rewrite that path.
    launcherPath: join2(homeDir, ".qwen/browser-use/host.sh"),
    manifestPaths: manifestRoots.map(
      (root) => join2(homeDir, root, MANIFEST_FILE)
    )
  };
}
async function writeManifests(resolved) {
  const manifest = JSON.stringify(
    {
      name: CHROME_NATIVE_HOST_NAME,
      description: "Qwen Browser Use",
      path: resolved.launcherPath,
      type: "stdio",
      allowed_origins: ALLOWED_ORIGINS
    },
    null,
    2
  ) + "\n";
  for (const manifestPath of resolved.manifestPaths) {
    const existing = await readExistingFile(manifestPath);
    if (existing === null ? !await pathExists(dirname3(dirname3(manifestPath))) : !isOwnedManifest(existing, resolved.launcherPath))
      continue;
    await mkdir3(dirname3(manifestPath), { recursive: true });
    await atomicWrite(manifestPath, manifest, 384, existing);
  }
}
function isOwnedManifest(contents, launcherPath) {
  try {
    const value = JSON.parse(contents);
    return value["name"] === CHROME_NATIVE_HOST_NAME && value["type"] === "stdio" && value["path"] === launcherPath && Array.isArray(value["allowed_origins"]) && value["allowed_origins"].length > 0 && // A registration listing only some of our origins is an older install of
    // ours to upgrade; one naming any other extension belongs to someone else.
    value["allowed_origins"].every(
      (origin) => ALLOWED_ORIGINS.includes(origin)
    );
  } catch {
    return false;
  }
}
function isOwnedLauncher(contents) {
  return contents.startsWith("#!/bin/sh\n" + LAUNCHER_MARKER + "\n");
}
async function readExistingFile(path) {
  try {
    return await readFile2(path, "utf8");
  } catch (error) {
    if (error.code === "ENOENT") return null;
    throw error;
  }
}
async function atomicWrite(target, contents, mode, existingContents) {
  if (existingContents === contents) {
    if (((await stat2(target)).mode & 511) !== mode) {
      await chmod(target, mode);
    }
    return;
  }
  const temporary = target + "." + randomUUID2() + ".tmp";
  try {
    await writeFile(temporary, contents, { mode });
    await chmod(temporary, mode);
    await rename(temporary, target);
  } finally {
    await rm(temporary, { force: true });
  }
}
async function pathExists(path, mode = constants.F_OK) {
  return await access(path, mode).then(
    () => true,
    () => false
  );
}
function shellQuote(value) {
  return "'" + value.replaceAll("'", "'\\''") + "'";
}

// src/playwright/playwright-runtime.ts
import { randomUUID as randomUUID4 } from "node:crypto";
import { realpath as realpath2, stat as stat3 } from "node:fs/promises";
import { isAbsolute as isAbsolute2 } from "node:path";
import { clearTimeout as clearTimeout5, setTimeout as setTimeout5 } from "node:timers";

// ../../node_modules/zod/v3/external.js
var external_exports = {};
__export(external_exports, {
  BRAND: () => BRAND,
  DIRTY: () => DIRTY,
  EMPTY_PATH: () => EMPTY_PATH,
  INVALID: () => INVALID,
  NEVER: () => NEVER,
  OK: () => OK,
  ParseStatus: () => ParseStatus,
  Schema: () => ZodType,
  ZodAny: () => ZodAny,
  ZodArray: () => ZodArray,
  ZodBigInt: () => ZodBigInt,
  ZodBoolean: () => ZodBoolean,
  ZodBranded: () => ZodBranded,
  ZodCatch: () => ZodCatch,
  ZodDate: () => ZodDate,
  ZodDefault: () => ZodDefault,
  ZodDiscriminatedUnion: () => ZodDiscriminatedUnion,
  ZodEffects: () => ZodEffects,
  ZodEnum: () => ZodEnum,
  ZodError: () => ZodError,
  ZodFirstPartyTypeKind: () => ZodFirstPartyTypeKind,
  ZodFunction: () => ZodFunction,
  ZodIntersection: () => ZodIntersection,
  ZodIssueCode: () => ZodIssueCode,
  ZodLazy: () => ZodLazy,
  ZodLiteral: () => ZodLiteral,
  ZodMap: () => ZodMap,
  ZodNaN: () => ZodNaN,
  ZodNativeEnum: () => ZodNativeEnum,
  ZodNever: () => ZodNever,
  ZodNull: () => ZodNull,
  ZodNullable: () => ZodNullable,
  ZodNumber: () => ZodNumber,
  ZodObject: () => ZodObject,
  ZodOptional: () => ZodOptional,
  ZodParsedType: () => ZodParsedType,
  ZodPipeline: () => ZodPipeline,
  ZodPromise: () => ZodPromise,
  ZodReadonly: () => ZodReadonly,
  ZodRecord: () => ZodRecord,
  ZodSchema: () => ZodType,
  ZodSet: () => ZodSet,
  ZodString: () => ZodString,
  ZodSymbol: () => ZodSymbol,
  ZodTransformer: () => ZodEffects,
  ZodTuple: () => ZodTuple,
  ZodType: () => ZodType,
  ZodUndefined: () => ZodUndefined,
  ZodUnion: () => ZodUnion,
  ZodUnknown: () => ZodUnknown,
  ZodVoid: () => ZodVoid,
  addIssueToContext: () => addIssueToContext,
  any: () => anyType,
  array: () => arrayType,
  bigint: () => bigIntType,
  boolean: () => booleanType,
  coerce: () => coerce,
  custom: () => custom,
  date: () => dateType,
  datetimeRegex: () => datetimeRegex,
  defaultErrorMap: () => en_default,
  discriminatedUnion: () => discriminatedUnionType,
  effect: () => effectsType,
  enum: () => enumType,
  function: () => functionType,
  getErrorMap: () => getErrorMap,
  getParsedType: () => getParsedType,
  instanceof: () => instanceOfType,
  intersection: () => intersectionType,
  isAborted: () => isAborted,
  isAsync: () => isAsync,
  isDirty: () => isDirty,
  isValid: () => isValid,
  late: () => late,
  lazy: () => lazyType,
  literal: () => literalType,
  makeIssue: () => makeIssue,
  map: () => mapType,
  nan: () => nanType,
  nativeEnum: () => nativeEnumType,
  never: () => neverType,
  null: () => nullType,
  nullable: () => nullableType,
  number: () => numberType,
  object: () => objectType,
  objectUtil: () => objectUtil,
  oboolean: () => oboolean,
  onumber: () => onumber,
  optional: () => optionalType,
  ostring: () => ostring,
  pipeline: () => pipelineType,
  preprocess: () => preprocessType,
  promise: () => promiseType,
  quotelessJson: () => quotelessJson,
  record: () => recordType,
  set: () => setType,
  setErrorMap: () => setErrorMap,
  strictObject: () => strictObjectType,
  string: () => stringType,
  symbol: () => symbolType,
  transformer: () => effectsType,
  tuple: () => tupleType,
  undefined: () => undefinedType,
  union: () => unionType,
  unknown: () => unknownType,
  util: () => util,
  void: () => voidType
});

// ../../node_modules/zod/v3/helpers/util.js
var util;
(function(util2) {
  util2.assertEqual = (_) => {
  };
  function assertIs(_arg) {
  }
  util2.assertIs = assertIs;
  function assertNever(_x) {
    throw new Error();
  }
  util2.assertNever = assertNever;
  util2.arrayToEnum = (items) => {
    const obj = {};
    for (const item of items) {
      obj[item] = item;
    }
    return obj;
  };
  util2.getValidEnumValues = (obj) => {
    const validKeys = util2.objectKeys(obj).filter((k) => typeof obj[obj[k]] !== "number");
    const filtered = {};
    for (const k of validKeys) {
      filtered[k] = obj[k];
    }
    return util2.objectValues(filtered);
  };
  util2.objectValues = (obj) => {
    return util2.objectKeys(obj).map(function(e) {
      return obj[e];
    });
  };
  util2.objectKeys = typeof Object.keys === "function" ? (obj) => Object.keys(obj) : (object) => {
    const keys = [];
    for (const key in object) {
      if (Object.prototype.hasOwnProperty.call(object, key)) {
        keys.push(key);
      }
    }
    return keys;
  };
  util2.find = (arr, checker) => {
    for (const item of arr) {
      if (checker(item))
        return item;
    }
    return void 0;
  };
  util2.isInteger = typeof Number.isInteger === "function" ? (val) => Number.isInteger(val) : (val) => typeof val === "number" && Number.isFinite(val) && Math.floor(val) === val;
  function joinValues(array, separator = " | ") {
    return array.map((val) => typeof val === "string" ? `'${val}'` : val).join(separator);
  }
  util2.joinValues = joinValues;
  util2.jsonStringifyReplacer = (_, value) => {
    if (typeof value === "bigint") {
      return value.toString();
    }
    return value;
  };
})(util || (util = {}));
var objectUtil;
(function(objectUtil2) {
  objectUtil2.mergeShapes = (first, second) => {
    return {
      ...first,
      ...second
      // second overwrites first
    };
  };
})(objectUtil || (objectUtil = {}));
var ZodParsedType = util.arrayToEnum([
  "string",
  "nan",
  "number",
  "integer",
  "float",
  "boolean",
  "date",
  "bigint",
  "symbol",
  "function",
  "undefined",
  "null",
  "array",
  "object",
  "unknown",
  "promise",
  "void",
  "never",
  "map",
  "set"
]);
var getParsedType = (data) => {
  const t = typeof data;
  switch (t) {
    case "undefined":
      return ZodParsedType.undefined;
    case "string":
      return ZodParsedType.string;
    case "number":
      return Number.isNaN(data) ? ZodParsedType.nan : ZodParsedType.number;
    case "boolean":
      return ZodParsedType.boolean;
    case "function":
      return ZodParsedType.function;
    case "bigint":
      return ZodParsedType.bigint;
    case "symbol":
      return ZodParsedType.symbol;
    case "object":
      if (Array.isArray(data)) {
        return ZodParsedType.array;
      }
      if (data === null) {
        return ZodParsedType.null;
      }
      if (data.then && typeof data.then === "function" && data.catch && typeof data.catch === "function") {
        return ZodParsedType.promise;
      }
      if (typeof Map !== "undefined" && data instanceof Map) {
        return ZodParsedType.map;
      }
      if (typeof Set !== "undefined" && data instanceof Set) {
        return ZodParsedType.set;
      }
      if (typeof Date !== "undefined" && data instanceof Date) {
        return ZodParsedType.date;
      }
      return ZodParsedType.object;
    default:
      return ZodParsedType.unknown;
  }
};

// ../../node_modules/zod/v3/ZodError.js
var ZodIssueCode = util.arrayToEnum([
  "invalid_type",
  "invalid_literal",
  "custom",
  "invalid_union",
  "invalid_union_discriminator",
  "invalid_enum_value",
  "unrecognized_keys",
  "invalid_arguments",
  "invalid_return_type",
  "invalid_date",
  "invalid_string",
  "too_small",
  "too_big",
  "invalid_intersection_types",
  "not_multiple_of",
  "not_finite"
]);
var quotelessJson = (obj) => {
  const json = JSON.stringify(obj, null, 2);
  return json.replace(/"([^"]+)":/g, "$1:");
};
var ZodError = class _ZodError extends Error {
  get errors() {
    return this.issues;
  }
  constructor(issues) {
    super();
    this.issues = [];
    this.addIssue = (sub) => {
      this.issues = [...this.issues, sub];
    };
    this.addIssues = (subs = []) => {
      this.issues = [...this.issues, ...subs];
    };
    const actualProto = new.target.prototype;
    if (Object.setPrototypeOf) {
      Object.setPrototypeOf(this, actualProto);
    } else {
      this.__proto__ = actualProto;
    }
    this.name = "ZodError";
    this.issues = issues;
  }
  format(_mapper) {
    const mapper = _mapper || function(issue) {
      return issue.message;
    };
    const fieldErrors = { _errors: [] };
    const processError = (error) => {
      for (const issue of error.issues) {
        if (issue.code === "invalid_union") {
          issue.unionErrors.map(processError);
        } else if (issue.code === "invalid_return_type") {
          processError(issue.returnTypeError);
        } else if (issue.code === "invalid_arguments") {
          processError(issue.argumentsError);
        } else if (issue.path.length === 0) {
          fieldErrors._errors.push(mapper(issue));
        } else {
          let curr = fieldErrors;
          let i = 0;
          while (i < issue.path.length) {
            const el = issue.path[i];
            const terminal = i === issue.path.length - 1;
            if (!terminal) {
              curr[el] = curr[el] || { _errors: [] };
            } else {
              curr[el] = curr[el] || { _errors: [] };
              curr[el]._errors.push(mapper(issue));
            }
            curr = curr[el];
            i++;
          }
        }
      }
    };
    processError(this);
    return fieldErrors;
  }
  static assert(value) {
    if (!(value instanceof _ZodError)) {
      throw new Error(`Not a ZodError: ${value}`);
    }
  }
  toString() {
    return this.message;
  }
  get message() {
    return JSON.stringify(this.issues, util.jsonStringifyReplacer, 2);
  }
  get isEmpty() {
    return this.issues.length === 0;
  }
  flatten(mapper = (issue) => issue.message) {
    const fieldErrors = {};
    const formErrors = [];
    for (const sub of this.issues) {
      if (sub.path.length > 0) {
        const firstEl = sub.path[0];
        fieldErrors[firstEl] = fieldErrors[firstEl] || [];
        fieldErrors[firstEl].push(mapper(sub));
      } else {
        formErrors.push(mapper(sub));
      }
    }
    return { formErrors, fieldErrors };
  }
  get formErrors() {
    return this.flatten();
  }
};
ZodError.create = (issues) => {
  const error = new ZodError(issues);
  return error;
};

// ../../node_modules/zod/v3/locales/en.js
var errorMap = (issue, _ctx) => {
  let message;
  switch (issue.code) {
    case ZodIssueCode.invalid_type:
      if (issue.received === ZodParsedType.undefined) {
        message = "Required";
      } else {
        message = `Expected ${issue.expected}, received ${issue.received}`;
      }
      break;
    case ZodIssueCode.invalid_literal:
      message = `Invalid literal value, expected ${JSON.stringify(issue.expected, util.jsonStringifyReplacer)}`;
      break;
    case ZodIssueCode.unrecognized_keys:
      message = `Unrecognized key(s) in object: ${util.joinValues(issue.keys, ", ")}`;
      break;
    case ZodIssueCode.invalid_union:
      message = `Invalid input`;
      break;
    case ZodIssueCode.invalid_union_discriminator:
      message = `Invalid discriminator value. Expected ${util.joinValues(issue.options)}`;
      break;
    case ZodIssueCode.invalid_enum_value:
      message = `Invalid enum value. Expected ${util.joinValues(issue.options)}, received '${issue.received}'`;
      break;
    case ZodIssueCode.invalid_arguments:
      message = `Invalid function arguments`;
      break;
    case ZodIssueCode.invalid_return_type:
      message = `Invalid function return type`;
      break;
    case ZodIssueCode.invalid_date:
      message = `Invalid date`;
      break;
    case ZodIssueCode.invalid_string:
      if (typeof issue.validation === "object") {
        if ("includes" in issue.validation) {
          message = `Invalid input: must include "${issue.validation.includes}"`;
          if (typeof issue.validation.position === "number") {
            message = `${message} at one or more positions greater than or equal to ${issue.validation.position}`;
          }
        } else if ("startsWith" in issue.validation) {
          message = `Invalid input: must start with "${issue.validation.startsWith}"`;
        } else if ("endsWith" in issue.validation) {
          message = `Invalid input: must end with "${issue.validation.endsWith}"`;
        } else {
          util.assertNever(issue.validation);
        }
      } else if (issue.validation !== "regex") {
        message = `Invalid ${issue.validation}`;
      } else {
        message = "Invalid";
      }
      break;
    case ZodIssueCode.too_small:
      if (issue.type === "array")
        message = `Array must contain ${issue.exact ? "exactly" : issue.inclusive ? `at least` : `more than`} ${issue.minimum} element(s)`;
      else if (issue.type === "string")
        message = `String must contain ${issue.exact ? "exactly" : issue.inclusive ? `at least` : `over`} ${issue.minimum} character(s)`;
      else if (issue.type === "number")
        message = `Number must be ${issue.exact ? `exactly equal to ` : issue.inclusive ? `greater than or equal to ` : `greater than `}${issue.minimum}`;
      else if (issue.type === "bigint")
        message = `Number must be ${issue.exact ? `exactly equal to ` : issue.inclusive ? `greater than or equal to ` : `greater than `}${issue.minimum}`;
      else if (issue.type === "date")
        message = `Date must be ${issue.exact ? `exactly equal to ` : issue.inclusive ? `greater than or equal to ` : `greater than `}${new Date(Number(issue.minimum))}`;
      else
        message = "Invalid input";
      break;
    case ZodIssueCode.too_big:
      if (issue.type === "array")
        message = `Array must contain ${issue.exact ? `exactly` : issue.inclusive ? `at most` : `less than`} ${issue.maximum} element(s)`;
      else if (issue.type === "string")
        message = `String must contain ${issue.exact ? `exactly` : issue.inclusive ? `at most` : `under`} ${issue.maximum} character(s)`;
      else if (issue.type === "number")
        message = `Number must be ${issue.exact ? `exactly` : issue.inclusive ? `less than or equal to` : `less than`} ${issue.maximum}`;
      else if (issue.type === "bigint")
        message = `BigInt must be ${issue.exact ? `exactly` : issue.inclusive ? `less than or equal to` : `less than`} ${issue.maximum}`;
      else if (issue.type === "date")
        message = `Date must be ${issue.exact ? `exactly` : issue.inclusive ? `smaller than or equal to` : `smaller than`} ${new Date(Number(issue.maximum))}`;
      else
        message = "Invalid input";
      break;
    case ZodIssueCode.custom:
      message = `Invalid input`;
      break;
    case ZodIssueCode.invalid_intersection_types:
      message = `Intersection results could not be merged`;
      break;
    case ZodIssueCode.not_multiple_of:
      message = `Number must be a multiple of ${issue.multipleOf}`;
      break;
    case ZodIssueCode.not_finite:
      message = "Number must be finite";
      break;
    default:
      message = _ctx.defaultError;
      util.assertNever(issue);
  }
  return { message };
};
var en_default = errorMap;

// ../../node_modules/zod/v3/errors.js
var overrideErrorMap = en_default;
function setErrorMap(map) {
  overrideErrorMap = map;
}
function getErrorMap() {
  return overrideErrorMap;
}

// ../../node_modules/zod/v3/helpers/parseUtil.js
var makeIssue = (params) => {
  const { data, path, errorMaps, issueData } = params;
  const fullPath = [...path, ...issueData.path || []];
  const fullIssue = {
    ...issueData,
    path: fullPath
  };
  if (issueData.message !== void 0) {
    return {
      ...issueData,
      path: fullPath,
      message: issueData.message
    };
  }
  let errorMessage2 = "";
  const maps = errorMaps.filter((m) => !!m).slice().reverse();
  for (const map of maps) {
    errorMessage2 = map(fullIssue, { data, defaultError: errorMessage2 }).message;
  }
  return {
    ...issueData,
    path: fullPath,
    message: errorMessage2
  };
};
var EMPTY_PATH = [];
function addIssueToContext(ctx, issueData) {
  const overrideMap = getErrorMap();
  const issue = makeIssue({
    issueData,
    data: ctx.data,
    path: ctx.path,
    errorMaps: [
      ctx.common.contextualErrorMap,
      // contextual error map is first priority
      ctx.schemaErrorMap,
      // then schema-bound map if available
      overrideMap,
      // then global override map
      overrideMap === en_default ? void 0 : en_default
      // then global default map
    ].filter((x) => !!x)
  });
  ctx.common.issues.push(issue);
}
var ParseStatus = class _ParseStatus {
  constructor() {
    this.value = "valid";
  }
  dirty() {
    if (this.value === "valid")
      this.value = "dirty";
  }
  abort() {
    if (this.value !== "aborted")
      this.value = "aborted";
  }
  static mergeArray(status, results) {
    const arrayValue = [];
    for (const s of results) {
      if (s.status === "aborted")
        return INVALID;
      if (s.status === "dirty")
        status.dirty();
      arrayValue.push(s.value);
    }
    return { status: status.value, value: arrayValue };
  }
  static async mergeObjectAsync(status, pairs) {
    const syncPairs = [];
    for (const pair of pairs) {
      const key = await pair.key;
      const value = await pair.value;
      syncPairs.push({
        key,
        value
      });
    }
    return _ParseStatus.mergeObjectSync(status, syncPairs);
  }
  static mergeObjectSync(status, pairs) {
    const finalObject = {};
    for (const pair of pairs) {
      const { key, value } = pair;
      if (key.status === "aborted")
        return INVALID;
      if (value.status === "aborted")
        return INVALID;
      if (key.status === "dirty")
        status.dirty();
      if (value.status === "dirty")
        status.dirty();
      if (key.value !== "__proto__" && (typeof value.value !== "undefined" || pair.alwaysSet)) {
        finalObject[key.value] = value.value;
      }
    }
    return { status: status.value, value: finalObject };
  }
};
var INVALID = Object.freeze({
  status: "aborted"
});
var DIRTY = (value) => ({ status: "dirty", value });
var OK = (value) => ({ status: "valid", value });
var isAborted = (x) => x.status === "aborted";
var isDirty = (x) => x.status === "dirty";
var isValid = (x) => x.status === "valid";
var isAsync = (x) => typeof Promise !== "undefined" && x instanceof Promise;

// ../../node_modules/zod/v3/helpers/errorUtil.js
var errorUtil;
(function(errorUtil2) {
  errorUtil2.errToObj = (message) => typeof message === "string" ? { message } : message || {};
  errorUtil2.toString = (message) => typeof message === "string" ? message : message?.message;
})(errorUtil || (errorUtil = {}));

// ../../node_modules/zod/v3/types.js
var ParseInputLazyPath = class {
  constructor(parent, value, path, key) {
    this._cachedPath = [];
    this.parent = parent;
    this.data = value;
    this._path = path;
    this._key = key;
  }
  get path() {
    if (!this._cachedPath.length) {
      if (Array.isArray(this._key)) {
        this._cachedPath.push(...this._path, ...this._key);
      } else {
        this._cachedPath.push(...this._path, this._key);
      }
    }
    return this._cachedPath;
  }
};
var handleResult = (ctx, result) => {
  if (isValid(result)) {
    return { success: true, data: result.value };
  } else {
    if (!ctx.common.issues.length) {
      throw new Error("Validation failed but no issues detected.");
    }
    return {
      success: false,
      get error() {
        if (this._error)
          return this._error;
        const error = new ZodError(ctx.common.issues);
        this._error = error;
        return this._error;
      }
    };
  }
};
function processCreateParams(params) {
  if (!params)
    return {};
  const { errorMap: errorMap2, invalid_type_error, required_error, description } = params;
  if (errorMap2 && (invalid_type_error || required_error)) {
    throw new Error(`Can't use "invalid_type_error" or "required_error" in conjunction with custom error map.`);
  }
  if (errorMap2)
    return { errorMap: errorMap2, description };
  const customMap = (iss, ctx) => {
    const { message } = params;
    if (iss.code === "invalid_enum_value") {
      return { message: message ?? ctx.defaultError };
    }
    if (typeof ctx.data === "undefined") {
      return { message: message ?? required_error ?? ctx.defaultError };
    }
    if (iss.code !== "invalid_type")
      return { message: ctx.defaultError };
    return { message: message ?? invalid_type_error ?? ctx.defaultError };
  };
  return { errorMap: customMap, description };
}
var ZodType = class {
  get description() {
    return this._def.description;
  }
  _getType(input) {
    return getParsedType(input.data);
  }
  _getOrReturnCtx(input, ctx) {
    return ctx || {
      common: input.parent.common,
      data: input.data,
      parsedType: getParsedType(input.data),
      schemaErrorMap: this._def.errorMap,
      path: input.path,
      parent: input.parent
    };
  }
  _processInputParams(input) {
    return {
      status: new ParseStatus(),
      ctx: {
        common: input.parent.common,
        data: input.data,
        parsedType: getParsedType(input.data),
        schemaErrorMap: this._def.errorMap,
        path: input.path,
        parent: input.parent
      }
    };
  }
  _parseSync(input) {
    const result = this._parse(input);
    if (isAsync(result)) {
      throw new Error("Synchronous parse encountered promise.");
    }
    return result;
  }
  _parseAsync(input) {
    const result = this._parse(input);
    return Promise.resolve(result);
  }
  parse(data, params) {
    const result = this.safeParse(data, params);
    if (result.success)
      return result.data;
    throw result.error;
  }
  safeParse(data, params) {
    const ctx = {
      common: {
        issues: [],
        async: params?.async ?? false,
        contextualErrorMap: params?.errorMap
      },
      path: params?.path || [],
      schemaErrorMap: this._def.errorMap,
      parent: null,
      data,
      parsedType: getParsedType(data)
    };
    const result = this._parseSync({ data, path: ctx.path, parent: ctx });
    return handleResult(ctx, result);
  }
  "~validate"(data) {
    const ctx = {
      common: {
        issues: [],
        async: !!this["~standard"].async
      },
      path: [],
      schemaErrorMap: this._def.errorMap,
      parent: null,
      data,
      parsedType: getParsedType(data)
    };
    if (!this["~standard"].async) {
      try {
        const result = this._parseSync({ data, path: [], parent: ctx });
        return isValid(result) ? {
          value: result.value
        } : {
          issues: ctx.common.issues
        };
      } catch (err) {
        if (err?.message?.toLowerCase()?.includes("encountered")) {
          this["~standard"].async = true;
        }
        ctx.common = {
          issues: [],
          async: true
        };
      }
    }
    return this._parseAsync({ data, path: [], parent: ctx }).then((result) => isValid(result) ? {
      value: result.value
    } : {
      issues: ctx.common.issues
    });
  }
  async parseAsync(data, params) {
    const result = await this.safeParseAsync(data, params);
    if (result.success)
      return result.data;
    throw result.error;
  }
  async safeParseAsync(data, params) {
    const ctx = {
      common: {
        issues: [],
        contextualErrorMap: params?.errorMap,
        async: true
      },
      path: params?.path || [],
      schemaErrorMap: this._def.errorMap,
      parent: null,
      data,
      parsedType: getParsedType(data)
    };
    const maybeAsyncResult = this._parse({ data, path: ctx.path, parent: ctx });
    const result = await (isAsync(maybeAsyncResult) ? maybeAsyncResult : Promise.resolve(maybeAsyncResult));
    return handleResult(ctx, result);
  }
  refine(check, message) {
    const getIssueProperties = (val) => {
      if (typeof message === "string" || typeof message === "undefined") {
        return { message };
      } else if (typeof message === "function") {
        return message(val);
      } else {
        return message;
      }
    };
    return this._refinement((val, ctx) => {
      const result = check(val);
      const setError = () => ctx.addIssue({
        code: ZodIssueCode.custom,
        ...getIssueProperties(val)
      });
      if (typeof Promise !== "undefined" && result instanceof Promise) {
        return result.then((data) => {
          if (!data) {
            setError();
            return false;
          } else {
            return true;
          }
        });
      }
      if (!result) {
        setError();
        return false;
      } else {
        return true;
      }
    });
  }
  refinement(check, refinementData) {
    return this._refinement((val, ctx) => {
      if (!check(val)) {
        ctx.addIssue(typeof refinementData === "function" ? refinementData(val, ctx) : refinementData);
        return false;
      } else {
        return true;
      }
    });
  }
  _refinement(refinement) {
    return new ZodEffects({
      schema: this,
      typeName: ZodFirstPartyTypeKind.ZodEffects,
      effect: { type: "refinement", refinement }
    });
  }
  superRefine(refinement) {
    return this._refinement(refinement);
  }
  constructor(def) {
    this.spa = this.safeParseAsync;
    this._def = def;
    this.parse = this.parse.bind(this);
    this.safeParse = this.safeParse.bind(this);
    this.parseAsync = this.parseAsync.bind(this);
    this.safeParseAsync = this.safeParseAsync.bind(this);
    this.spa = this.spa.bind(this);
    this.refine = this.refine.bind(this);
    this.refinement = this.refinement.bind(this);
    this.superRefine = this.superRefine.bind(this);
    this.optional = this.optional.bind(this);
    this.nullable = this.nullable.bind(this);
    this.nullish = this.nullish.bind(this);
    this.array = this.array.bind(this);
    this.promise = this.promise.bind(this);
    this.or = this.or.bind(this);
    this.and = this.and.bind(this);
    this.transform = this.transform.bind(this);
    this.brand = this.brand.bind(this);
    this.default = this.default.bind(this);
    this.catch = this.catch.bind(this);
    this.describe = this.describe.bind(this);
    this.pipe = this.pipe.bind(this);
    this.readonly = this.readonly.bind(this);
    this.isNullable = this.isNullable.bind(this);
    this.isOptional = this.isOptional.bind(this);
    this["~standard"] = {
      version: 1,
      vendor: "zod",
      validate: (data) => this["~validate"](data)
    };
  }
  optional() {
    return ZodOptional.create(this, this._def);
  }
  nullable() {
    return ZodNullable.create(this, this._def);
  }
  nullish() {
    return this.nullable().optional();
  }
  array() {
    return ZodArray.create(this);
  }
  promise() {
    return ZodPromise.create(this, this._def);
  }
  or(option) {
    return ZodUnion.create([this, option], this._def);
  }
  and(incoming) {
    return ZodIntersection.create(this, incoming, this._def);
  }
  transform(transform) {
    return new ZodEffects({
      ...processCreateParams(this._def),
      schema: this,
      typeName: ZodFirstPartyTypeKind.ZodEffects,
      effect: { type: "transform", transform }
    });
  }
  default(def) {
    const defaultValueFunc = typeof def === "function" ? def : () => def;
    return new ZodDefault({
      ...processCreateParams(this._def),
      innerType: this,
      defaultValue: defaultValueFunc,
      typeName: ZodFirstPartyTypeKind.ZodDefault
    });
  }
  brand() {
    return new ZodBranded({
      typeName: ZodFirstPartyTypeKind.ZodBranded,
      type: this,
      ...processCreateParams(this._def)
    });
  }
  catch(def) {
    const catchValueFunc = typeof def === "function" ? def : () => def;
    return new ZodCatch({
      ...processCreateParams(this._def),
      innerType: this,
      catchValue: catchValueFunc,
      typeName: ZodFirstPartyTypeKind.ZodCatch
    });
  }
  describe(description) {
    const This = this.constructor;
    return new This({
      ...this._def,
      description
    });
  }
  pipe(target) {
    return ZodPipeline.create(this, target);
  }
  readonly() {
    return ZodReadonly.create(this);
  }
  isOptional() {
    return this.safeParse(void 0).success;
  }
  isNullable() {
    return this.safeParse(null).success;
  }
};
var cuidRegex = /^c[^\s-]{8,}$/i;
var cuid2Regex = /^[0-9a-z]+$/;
var ulidRegex = /^[0-9A-HJKMNP-TV-Z]{26}$/i;
var uuidRegex = /^[0-9a-fA-F]{8}\b-[0-9a-fA-F]{4}\b-[0-9a-fA-F]{4}\b-[0-9a-fA-F]{4}\b-[0-9a-fA-F]{12}$/i;
var nanoidRegex = /^[a-z0-9_-]{21}$/i;
var jwtRegex = /^[A-Za-z0-9-_]+\.[A-Za-z0-9-_]+\.[A-Za-z0-9-_]*$/;
var durationRegex = /^[-+]?P(?!$)(?:(?:[-+]?\d+Y)|(?:[-+]?\d+[.,]\d+Y$))?(?:(?:[-+]?\d+M)|(?:[-+]?\d+[.,]\d+M$))?(?:(?:[-+]?\d+W)|(?:[-+]?\d+[.,]\d+W$))?(?:(?:[-+]?\d+D)|(?:[-+]?\d+[.,]\d+D$))?(?:T(?=[\d+-])(?:(?:[-+]?\d+H)|(?:[-+]?\d+[.,]\d+H$))?(?:(?:[-+]?\d+M)|(?:[-+]?\d+[.,]\d+M$))?(?:[-+]?\d+(?:[.,]\d+)?S)?)??$/;
var emailRegex = /^(?!\.)(?!.*\.\.)([A-Z0-9_'+\-\.]*)[A-Z0-9_+-]@([A-Z0-9][A-Z0-9\-]*\.)+[A-Z]{2,}$/i;
var _emojiRegex = `^(\\p{Extended_Pictographic}|\\p{Emoji_Component})+$`;
var emojiRegex;
var ipv4Regex = /^(?:(?:25[0-5]|2[0-4][0-9]|1[0-9][0-9]|[1-9][0-9]|[0-9])\.){3}(?:25[0-5]|2[0-4][0-9]|1[0-9][0-9]|[1-9][0-9]|[0-9])$/;
var ipv4CidrRegex = /^(?:(?:25[0-5]|2[0-4][0-9]|1[0-9][0-9]|[1-9][0-9]|[0-9])\.){3}(?:25[0-5]|2[0-4][0-9]|1[0-9][0-9]|[1-9][0-9]|[0-9])\/(3[0-2]|[12]?[0-9])$/;
var ipv6Regex = /^(([0-9a-fA-F]{1,4}:){7,7}[0-9a-fA-F]{1,4}|([0-9a-fA-F]{1,4}:){1,7}:|([0-9a-fA-F]{1,4}:){1,6}:[0-9a-fA-F]{1,4}|([0-9a-fA-F]{1,4}:){1,5}(:[0-9a-fA-F]{1,4}){1,2}|([0-9a-fA-F]{1,4}:){1,4}(:[0-9a-fA-F]{1,4}){1,3}|([0-9a-fA-F]{1,4}:){1,3}(:[0-9a-fA-F]{1,4}){1,4}|([0-9a-fA-F]{1,4}:){1,2}(:[0-9a-fA-F]{1,4}){1,5}|[0-9a-fA-F]{1,4}:((:[0-9a-fA-F]{1,4}){1,6})|:((:[0-9a-fA-F]{1,4}){1,7}|:)|fe80:(:[0-9a-fA-F]{0,4}){0,4}%[0-9a-zA-Z]{1,}|::(ffff(:0{1,4}){0,1}:){0,1}((25[0-5]|(2[0-4]|1{0,1}[0-9]){0,1}[0-9])\.){3,3}(25[0-5]|(2[0-4]|1{0,1}[0-9]){0,1}[0-9])|([0-9a-fA-F]{1,4}:){1,4}:((25[0-5]|(2[0-4]|1{0,1}[0-9]){0,1}[0-9])\.){3,3}(25[0-5]|(2[0-4]|1{0,1}[0-9]){0,1}[0-9]))$/;
var ipv6CidrRegex = /^(([0-9a-fA-F]{1,4}:){7,7}[0-9a-fA-F]{1,4}|([0-9a-fA-F]{1,4}:){1,7}:|([0-9a-fA-F]{1,4}:){1,6}:[0-9a-fA-F]{1,4}|([0-9a-fA-F]{1,4}:){1,5}(:[0-9a-fA-F]{1,4}){1,2}|([0-9a-fA-F]{1,4}:){1,4}(:[0-9a-fA-F]{1,4}){1,3}|([0-9a-fA-F]{1,4}:){1,3}(:[0-9a-fA-F]{1,4}){1,4}|([0-9a-fA-F]{1,4}:){1,2}(:[0-9a-fA-F]{1,4}){1,5}|[0-9a-fA-F]{1,4}:((:[0-9a-fA-F]{1,4}){1,6})|:((:[0-9a-fA-F]{1,4}){1,7}|:)|fe80:(:[0-9a-fA-F]{0,4}){0,4}%[0-9a-zA-Z]{1,}|::(ffff(:0{1,4}){0,1}:){0,1}((25[0-5]|(2[0-4]|1{0,1}[0-9]){0,1}[0-9])\.){3,3}(25[0-5]|(2[0-4]|1{0,1}[0-9]){0,1}[0-9])|([0-9a-fA-F]{1,4}:){1,4}:((25[0-5]|(2[0-4]|1{0,1}[0-9]){0,1}[0-9])\.){3,3}(25[0-5]|(2[0-4]|1{0,1}[0-9]){0,1}[0-9]))\/(12[0-8]|1[01][0-9]|[1-9]?[0-9])$/;
var base64Regex = /^([0-9a-zA-Z+/]{4})*(([0-9a-zA-Z+/]{2}==)|([0-9a-zA-Z+/]{3}=))?$/;
var base64urlRegex = /^([0-9a-zA-Z-_]{4})*(([0-9a-zA-Z-_]{2}(==)?)|([0-9a-zA-Z-_]{3}(=)?))?$/;
var dateRegexSource = `((\\d\\d[2468][048]|\\d\\d[13579][26]|\\d\\d0[48]|[02468][048]00|[13579][26]00)-02-29|\\d{4}-((0[13578]|1[02])-(0[1-9]|[12]\\d|3[01])|(0[469]|11)-(0[1-9]|[12]\\d|30)|(02)-(0[1-9]|1\\d|2[0-8])))`;
var dateRegex = new RegExp(`^${dateRegexSource}$`);
function timeRegexSource(args) {
  let secondsRegexSource = `[0-5]\\d`;
  if (args.precision) {
    secondsRegexSource = `${secondsRegexSource}\\.\\d{${args.precision}}`;
  } else if (args.precision == null) {
    secondsRegexSource = `${secondsRegexSource}(\\.\\d+)?`;
  }
  const secondsQuantifier = args.precision ? "+" : "?";
  return `([01]\\d|2[0-3]):[0-5]\\d(:${secondsRegexSource})${secondsQuantifier}`;
}
function timeRegex(args) {
  return new RegExp(`^${timeRegexSource(args)}$`);
}
function datetimeRegex(args) {
  let regex = `${dateRegexSource}T${timeRegexSource(args)}`;
  const opts = [];
  opts.push(args.local ? `Z?` : `Z`);
  if (args.offset)
    opts.push(`([+-]\\d{2}:?\\d{2})`);
  regex = `${regex}(${opts.join("|")})`;
  return new RegExp(`^${regex}$`);
}
function isValidIP(ip, version) {
  if ((version === "v4" || !version) && ipv4Regex.test(ip)) {
    return true;
  }
  if ((version === "v6" || !version) && ipv6Regex.test(ip)) {
    return true;
  }
  return false;
}
function isValidJWT(jwt, alg) {
  if (!jwtRegex.test(jwt))
    return false;
  try {
    const [header] = jwt.split(".");
    if (!header)
      return false;
    const base64 = header.replace(/-/g, "+").replace(/_/g, "/").padEnd(header.length + (4 - header.length % 4) % 4, "=");
    const decoded = JSON.parse(atob(base64));
    if (typeof decoded !== "object" || decoded === null)
      return false;
    if ("typ" in decoded && decoded?.typ !== "JWT")
      return false;
    if (!decoded.alg)
      return false;
    if (alg && decoded.alg !== alg)
      return false;
    return true;
  } catch {
    return false;
  }
}
function isValidCidr(ip, version) {
  if ((version === "v4" || !version) && ipv4CidrRegex.test(ip)) {
    return true;
  }
  if ((version === "v6" || !version) && ipv6CidrRegex.test(ip)) {
    return true;
  }
  return false;
}
var ZodString = class _ZodString extends ZodType {
  _parse(input) {
    if (this._def.coerce) {
      input.data = String(input.data);
    }
    const parsedType = this._getType(input);
    if (parsedType !== ZodParsedType.string) {
      const ctx2 = this._getOrReturnCtx(input);
      addIssueToContext(ctx2, {
        code: ZodIssueCode.invalid_type,
        expected: ZodParsedType.string,
        received: ctx2.parsedType
      });
      return INVALID;
    }
    const status = new ParseStatus();
    let ctx = void 0;
    for (const check of this._def.checks) {
      if (check.kind === "min") {
        if (input.data.length < check.value) {
          ctx = this._getOrReturnCtx(input, ctx);
          addIssueToContext(ctx, {
            code: ZodIssueCode.too_small,
            minimum: check.value,
            type: "string",
            inclusive: true,
            exact: false,
            message: check.message
          });
          status.dirty();
        }
      } else if (check.kind === "max") {
        if (input.data.length > check.value) {
          ctx = this._getOrReturnCtx(input, ctx);
          addIssueToContext(ctx, {
            code: ZodIssueCode.too_big,
            maximum: check.value,
            type: "string",
            inclusive: true,
            exact: false,
            message: check.message
          });
          status.dirty();
        }
      } else if (check.kind === "length") {
        const tooBig = input.data.length > check.value;
        const tooSmall = input.data.length < check.value;
        if (tooBig || tooSmall) {
          ctx = this._getOrReturnCtx(input, ctx);
          if (tooBig) {
            addIssueToContext(ctx, {
              code: ZodIssueCode.too_big,
              maximum: check.value,
              type: "string",
              inclusive: true,
              exact: true,
              message: check.message
            });
          } else if (tooSmall) {
            addIssueToContext(ctx, {
              code: ZodIssueCode.too_small,
              minimum: check.value,
              type: "string",
              inclusive: true,
              exact: true,
              message: check.message
            });
          }
          status.dirty();
        }
      } else if (check.kind === "email") {
        if (!emailRegex.test(input.data)) {
          ctx = this._getOrReturnCtx(input, ctx);
          addIssueToContext(ctx, {
            validation: "email",
            code: ZodIssueCode.invalid_string,
            message: check.message
          });
          status.dirty();
        }
      } else if (check.kind === "emoji") {
        if (!emojiRegex) {
          emojiRegex = new RegExp(_emojiRegex, "u");
        }
        if (!emojiRegex.test(input.data)) {
          ctx = this._getOrReturnCtx(input, ctx);
          addIssueToContext(ctx, {
            validation: "emoji",
            code: ZodIssueCode.invalid_string,
            message: check.message
          });
          status.dirty();
        }
      } else if (check.kind === "uuid") {
        if (!uuidRegex.test(input.data)) {
          ctx = this._getOrReturnCtx(input, ctx);
          addIssueToContext(ctx, {
            validation: "uuid",
            code: ZodIssueCode.invalid_string,
            message: check.message
          });
          status.dirty();
        }
      } else if (check.kind === "nanoid") {
        if (!nanoidRegex.test(input.data)) {
          ctx = this._getOrReturnCtx(input, ctx);
          addIssueToContext(ctx, {
            validation: "nanoid",
            code: ZodIssueCode.invalid_string,
            message: check.message
          });
          status.dirty();
        }
      } else if (check.kind === "cuid") {
        if (!cuidRegex.test(input.data)) {
          ctx = this._getOrReturnCtx(input, ctx);
          addIssueToContext(ctx, {
            validation: "cuid",
            code: ZodIssueCode.invalid_string,
            message: check.message
          });
          status.dirty();
        }
      } else if (check.kind === "cuid2") {
        if (!cuid2Regex.test(input.data)) {
          ctx = this._getOrReturnCtx(input, ctx);
          addIssueToContext(ctx, {
            validation: "cuid2",
            code: ZodIssueCode.invalid_string,
            message: check.message
          });
          status.dirty();
        }
      } else if (check.kind === "ulid") {
        if (!ulidRegex.test(input.data)) {
          ctx = this._getOrReturnCtx(input, ctx);
          addIssueToContext(ctx, {
            validation: "ulid",
            code: ZodIssueCode.invalid_string,
            message: check.message
          });
          status.dirty();
        }
      } else if (check.kind === "url") {
        try {
          new URL(input.data);
        } catch {
          ctx = this._getOrReturnCtx(input, ctx);
          addIssueToContext(ctx, {
            validation: "url",
            code: ZodIssueCode.invalid_string,
            message: check.message
          });
          status.dirty();
        }
      } else if (check.kind === "regex") {
        check.regex.lastIndex = 0;
        const testResult = check.regex.test(input.data);
        if (!testResult) {
          ctx = this._getOrReturnCtx(input, ctx);
          addIssueToContext(ctx, {
            validation: "regex",
            code: ZodIssueCode.invalid_string,
            message: check.message
          });
          status.dirty();
        }
      } else if (check.kind === "trim") {
        input.data = input.data.trim();
      } else if (check.kind === "includes") {
        if (!input.data.includes(check.value, check.position)) {
          ctx = this._getOrReturnCtx(input, ctx);
          addIssueToContext(ctx, {
            code: ZodIssueCode.invalid_string,
            validation: { includes: check.value, position: check.position },
            message: check.message
          });
          status.dirty();
        }
      } else if (check.kind === "toLowerCase") {
        input.data = input.data.toLowerCase();
      } else if (check.kind === "toUpperCase") {
        input.data = input.data.toUpperCase();
      } else if (check.kind === "startsWith") {
        if (!input.data.startsWith(check.value)) {
          ctx = this._getOrReturnCtx(input, ctx);
          addIssueToContext(ctx, {
            code: ZodIssueCode.invalid_string,
            validation: { startsWith: check.value },
            message: check.message
          });
          status.dirty();
        }
      } else if (check.kind === "endsWith") {
        if (!input.data.endsWith(check.value)) {
          ctx = this._getOrReturnCtx(input, ctx);
          addIssueToContext(ctx, {
            code: ZodIssueCode.invalid_string,
            validation: { endsWith: check.value },
            message: check.message
          });
          status.dirty();
        }
      } else if (check.kind === "datetime") {
        const regex = datetimeRegex(check);
        if (!regex.test(input.data)) {
          ctx = this._getOrReturnCtx(input, ctx);
          addIssueToContext(ctx, {
            code: ZodIssueCode.invalid_string,
            validation: "datetime",
            message: check.message
          });
          status.dirty();
        }
      } else if (check.kind === "date") {
        const regex = dateRegex;
        if (!regex.test(input.data)) {
          ctx = this._getOrReturnCtx(input, ctx);
          addIssueToContext(ctx, {
            code: ZodIssueCode.invalid_string,
            validation: "date",
            message: check.message
          });
          status.dirty();
        }
      } else if (check.kind === "time") {
        const regex = timeRegex(check);
        if (!regex.test(input.data)) {
          ctx = this._getOrReturnCtx(input, ctx);
          addIssueToContext(ctx, {
            code: ZodIssueCode.invalid_string,
            validation: "time",
            message: check.message
          });
          status.dirty();
        }
      } else if (check.kind === "duration") {
        if (!durationRegex.test(input.data)) {
          ctx = this._getOrReturnCtx(input, ctx);
          addIssueToContext(ctx, {
            validation: "duration",
            code: ZodIssueCode.invalid_string,
            message: check.message
          });
          status.dirty();
        }
      } else if (check.kind === "ip") {
        if (!isValidIP(input.data, check.version)) {
          ctx = this._getOrReturnCtx(input, ctx);
          addIssueToContext(ctx, {
            validation: "ip",
            code: ZodIssueCode.invalid_string,
            message: check.message
          });
          status.dirty();
        }
      } else if (check.kind === "jwt") {
        if (!isValidJWT(input.data, check.alg)) {
          ctx = this._getOrReturnCtx(input, ctx);
          addIssueToContext(ctx, {
            validation: "jwt",
            code: ZodIssueCode.invalid_string,
            message: check.message
          });
          status.dirty();
        }
      } else if (check.kind === "cidr") {
        if (!isValidCidr(input.data, check.version)) {
          ctx = this._getOrReturnCtx(input, ctx);
          addIssueToContext(ctx, {
            validation: "cidr",
            code: ZodIssueCode.invalid_string,
            message: check.message
          });
          status.dirty();
        }
      } else if (check.kind === "base64") {
        if (!base64Regex.test(input.data)) {
          ctx = this._getOrReturnCtx(input, ctx);
          addIssueToContext(ctx, {
            validation: "base64",
            code: ZodIssueCode.invalid_string,
            message: check.message
          });
          status.dirty();
        }
      } else if (check.kind === "base64url") {
        if (!base64urlRegex.test(input.data)) {
          ctx = this._getOrReturnCtx(input, ctx);
          addIssueToContext(ctx, {
            validation: "base64url",
            code: ZodIssueCode.invalid_string,
            message: check.message
          });
          status.dirty();
        }
      } else {
        util.assertNever(check);
      }
    }
    return { status: status.value, value: input.data };
  }
  _regex(regex, validation, message) {
    return this.refinement((data) => regex.test(data), {
      validation,
      code: ZodIssueCode.invalid_string,
      ...errorUtil.errToObj(message)
    });
  }
  _addCheck(check) {
    return new _ZodString({
      ...this._def,
      checks: [...this._def.checks, check]
    });
  }
  email(message) {
    return this._addCheck({ kind: "email", ...errorUtil.errToObj(message) });
  }
  url(message) {
    return this._addCheck({ kind: "url", ...errorUtil.errToObj(message) });
  }
  emoji(message) {
    return this._addCheck({ kind: "emoji", ...errorUtil.errToObj(message) });
  }
  uuid(message) {
    return this._addCheck({ kind: "uuid", ...errorUtil.errToObj(message) });
  }
  nanoid(message) {
    return this._addCheck({ kind: "nanoid", ...errorUtil.errToObj(message) });
  }
  cuid(message) {
    return this._addCheck({ kind: "cuid", ...errorUtil.errToObj(message) });
  }
  cuid2(message) {
    return this._addCheck({ kind: "cuid2", ...errorUtil.errToObj(message) });
  }
  ulid(message) {
    return this._addCheck({ kind: "ulid", ...errorUtil.errToObj(message) });
  }
  base64(message) {
    return this._addCheck({ kind: "base64", ...errorUtil.errToObj(message) });
  }
  base64url(message) {
    return this._addCheck({
      kind: "base64url",
      ...errorUtil.errToObj(message)
    });
  }
  jwt(options) {
    return this._addCheck({ kind: "jwt", ...errorUtil.errToObj(options) });
  }
  ip(options) {
    return this._addCheck({ kind: "ip", ...errorUtil.errToObj(options) });
  }
  cidr(options) {
    return this._addCheck({ kind: "cidr", ...errorUtil.errToObj(options) });
  }
  datetime(options) {
    if (typeof options === "string") {
      return this._addCheck({
        kind: "datetime",
        precision: null,
        offset: false,
        local: false,
        message: options
      });
    }
    return this._addCheck({
      kind: "datetime",
      precision: typeof options?.precision === "undefined" ? null : options?.precision,
      offset: options?.offset ?? false,
      local: options?.local ?? false,
      ...errorUtil.errToObj(options?.message)
    });
  }
  date(message) {
    return this._addCheck({ kind: "date", message });
  }
  time(options) {
    if (typeof options === "string") {
      return this._addCheck({
        kind: "time",
        precision: null,
        message: options
      });
    }
    return this._addCheck({
      kind: "time",
      precision: typeof options?.precision === "undefined" ? null : options?.precision,
      ...errorUtil.errToObj(options?.message)
    });
  }
  duration(message) {
    return this._addCheck({ kind: "duration", ...errorUtil.errToObj(message) });
  }
  regex(regex, message) {
    return this._addCheck({
      kind: "regex",
      regex,
      ...errorUtil.errToObj(message)
    });
  }
  includes(value, options) {
    return this._addCheck({
      kind: "includes",
      value,
      position: options?.position,
      ...errorUtil.errToObj(options?.message)
    });
  }
  startsWith(value, message) {
    return this._addCheck({
      kind: "startsWith",
      value,
      ...errorUtil.errToObj(message)
    });
  }
  endsWith(value, message) {
    return this._addCheck({
      kind: "endsWith",
      value,
      ...errorUtil.errToObj(message)
    });
  }
  min(minLength, message) {
    return this._addCheck({
      kind: "min",
      value: minLength,
      ...errorUtil.errToObj(message)
    });
  }
  max(maxLength, message) {
    return this._addCheck({
      kind: "max",
      value: maxLength,
      ...errorUtil.errToObj(message)
    });
  }
  length(len, message) {
    return this._addCheck({
      kind: "length",
      value: len,
      ...errorUtil.errToObj(message)
    });
  }
  /**
   * Equivalent to `.min(1)`
   */
  nonempty(message) {
    return this.min(1, errorUtil.errToObj(message));
  }
  trim() {
    return new _ZodString({
      ...this._def,
      checks: [...this._def.checks, { kind: "trim" }]
    });
  }
  toLowerCase() {
    return new _ZodString({
      ...this._def,
      checks: [...this._def.checks, { kind: "toLowerCase" }]
    });
  }
  toUpperCase() {
    return new _ZodString({
      ...this._def,
      checks: [...this._def.checks, { kind: "toUpperCase" }]
    });
  }
  get isDatetime() {
    return !!this._def.checks.find((ch) => ch.kind === "datetime");
  }
  get isDate() {
    return !!this._def.checks.find((ch) => ch.kind === "date");
  }
  get isTime() {
    return !!this._def.checks.find((ch) => ch.kind === "time");
  }
  get isDuration() {
    return !!this._def.checks.find((ch) => ch.kind === "duration");
  }
  get isEmail() {
    return !!this._def.checks.find((ch) => ch.kind === "email");
  }
  get isURL() {
    return !!this._def.checks.find((ch) => ch.kind === "url");
  }
  get isEmoji() {
    return !!this._def.checks.find((ch) => ch.kind === "emoji");
  }
  get isUUID() {
    return !!this._def.checks.find((ch) => ch.kind === "uuid");
  }
  get isNANOID() {
    return !!this._def.checks.find((ch) => ch.kind === "nanoid");
  }
  get isCUID() {
    return !!this._def.checks.find((ch) => ch.kind === "cuid");
  }
  get isCUID2() {
    return !!this._def.checks.find((ch) => ch.kind === "cuid2");
  }
  get isULID() {
    return !!this._def.checks.find((ch) => ch.kind === "ulid");
  }
  get isIP() {
    return !!this._def.checks.find((ch) => ch.kind === "ip");
  }
  get isCIDR() {
    return !!this._def.checks.find((ch) => ch.kind === "cidr");
  }
  get isBase64() {
    return !!this._def.checks.find((ch) => ch.kind === "base64");
  }
  get isBase64url() {
    return !!this._def.checks.find((ch) => ch.kind === "base64url");
  }
  get minLength() {
    let min = null;
    for (const ch of this._def.checks) {
      if (ch.kind === "min") {
        if (min === null || ch.value > min)
          min = ch.value;
      }
    }
    return min;
  }
  get maxLength() {
    let max = null;
    for (const ch of this._def.checks) {
      if (ch.kind === "max") {
        if (max === null || ch.value < max)
          max = ch.value;
      }
    }
    return max;
  }
};
ZodString.create = (params) => {
  return new ZodString({
    checks: [],
    typeName: ZodFirstPartyTypeKind.ZodString,
    coerce: params?.coerce ?? false,
    ...processCreateParams(params)
  });
};
function floatSafeRemainder(val, step) {
  const valDecCount = (val.toString().split(".")[1] || "").length;
  const stepDecCount = (step.toString().split(".")[1] || "").length;
  const decCount = valDecCount > stepDecCount ? valDecCount : stepDecCount;
  const valInt = Number.parseInt(val.toFixed(decCount).replace(".", ""));
  const stepInt = Number.parseInt(step.toFixed(decCount).replace(".", ""));
  return valInt % stepInt / 10 ** decCount;
}
var ZodNumber = class _ZodNumber extends ZodType {
  constructor() {
    super(...arguments);
    this.min = this.gte;
    this.max = this.lte;
    this.step = this.multipleOf;
  }
  _parse(input) {
    if (this._def.coerce) {
      input.data = Number(input.data);
    }
    const parsedType = this._getType(input);
    if (parsedType !== ZodParsedType.number) {
      const ctx2 = this._getOrReturnCtx(input);
      addIssueToContext(ctx2, {
        code: ZodIssueCode.invalid_type,
        expected: ZodParsedType.number,
        received: ctx2.parsedType
      });
      return INVALID;
    }
    let ctx = void 0;
    const status = new ParseStatus();
    for (const check of this._def.checks) {
      if (check.kind === "int") {
        if (!util.isInteger(input.data)) {
          ctx = this._getOrReturnCtx(input, ctx);
          addIssueToContext(ctx, {
            code: ZodIssueCode.invalid_type,
            expected: "integer",
            received: "float",
            message: check.message
          });
          status.dirty();
        }
      } else if (check.kind === "min") {
        const tooSmall = check.inclusive ? input.data < check.value : input.data <= check.value;
        if (tooSmall) {
          ctx = this._getOrReturnCtx(input, ctx);
          addIssueToContext(ctx, {
            code: ZodIssueCode.too_small,
            minimum: check.value,
            type: "number",
            inclusive: check.inclusive,
            exact: false,
            message: check.message
          });
          status.dirty();
        }
      } else if (check.kind === "max") {
        const tooBig = check.inclusive ? input.data > check.value : input.data >= check.value;
        if (tooBig) {
          ctx = this._getOrReturnCtx(input, ctx);
          addIssueToContext(ctx, {
            code: ZodIssueCode.too_big,
            maximum: check.value,
            type: "number",
            inclusive: check.inclusive,
            exact: false,
            message: check.message
          });
          status.dirty();
        }
      } else if (check.kind === "multipleOf") {
        if (floatSafeRemainder(input.data, check.value) !== 0) {
          ctx = this._getOrReturnCtx(input, ctx);
          addIssueToContext(ctx, {
            code: ZodIssueCode.not_multiple_of,
            multipleOf: check.value,
            message: check.message
          });
          status.dirty();
        }
      } else if (check.kind === "finite") {
        if (!Number.isFinite(input.data)) {
          ctx = this._getOrReturnCtx(input, ctx);
          addIssueToContext(ctx, {
            code: ZodIssueCode.not_finite,
            message: check.message
          });
          status.dirty();
        }
      } else {
        util.assertNever(check);
      }
    }
    return { status: status.value, value: input.data };
  }
  gte(value, message) {
    return this.setLimit("min", value, true, errorUtil.toString(message));
  }
  gt(value, message) {
    return this.setLimit("min", value, false, errorUtil.toString(message));
  }
  lte(value, message) {
    return this.setLimit("max", value, true, errorUtil.toString(message));
  }
  lt(value, message) {
    return this.setLimit("max", value, false, errorUtil.toString(message));
  }
  setLimit(kind, value, inclusive, message) {
    return new _ZodNumber({
      ...this._def,
      checks: [
        ...this._def.checks,
        {
          kind,
          value,
          inclusive,
          message: errorUtil.toString(message)
        }
      ]
    });
  }
  _addCheck(check) {
    return new _ZodNumber({
      ...this._def,
      checks: [...this._def.checks, check]
    });
  }
  int(message) {
    return this._addCheck({
      kind: "int",
      message: errorUtil.toString(message)
    });
  }
  positive(message) {
    return this._addCheck({
      kind: "min",
      value: 0,
      inclusive: false,
      message: errorUtil.toString(message)
    });
  }
  negative(message) {
    return this._addCheck({
      kind: "max",
      value: 0,
      inclusive: false,
      message: errorUtil.toString(message)
    });
  }
  nonpositive(message) {
    return this._addCheck({
      kind: "max",
      value: 0,
      inclusive: true,
      message: errorUtil.toString(message)
    });
  }
  nonnegative(message) {
    return this._addCheck({
      kind: "min",
      value: 0,
      inclusive: true,
      message: errorUtil.toString(message)
    });
  }
  multipleOf(value, message) {
    return this._addCheck({
      kind: "multipleOf",
      value,
      message: errorUtil.toString(message)
    });
  }
  finite(message) {
    return this._addCheck({
      kind: "finite",
      message: errorUtil.toString(message)
    });
  }
  safe(message) {
    return this._addCheck({
      kind: "min",
      inclusive: true,
      value: Number.MIN_SAFE_INTEGER,
      message: errorUtil.toString(message)
    })._addCheck({
      kind: "max",
      inclusive: true,
      value: Number.MAX_SAFE_INTEGER,
      message: errorUtil.toString(message)
    });
  }
  get minValue() {
    let min = null;
    for (const ch of this._def.checks) {
      if (ch.kind === "min") {
        if (min === null || ch.value > min)
          min = ch.value;
      }
    }
    return min;
  }
  get maxValue() {
    let max = null;
    for (const ch of this._def.checks) {
      if (ch.kind === "max") {
        if (max === null || ch.value < max)
          max = ch.value;
      }
    }
    return max;
  }
  get isInt() {
    return !!this._def.checks.find((ch) => ch.kind === "int" || ch.kind === "multipleOf" && util.isInteger(ch.value));
  }
  get isFinite() {
    let max = null;
    let min = null;
    for (const ch of this._def.checks) {
      if (ch.kind === "finite" || ch.kind === "int" || ch.kind === "multipleOf") {
        return true;
      } else if (ch.kind === "min") {
        if (min === null || ch.value > min)
          min = ch.value;
      } else if (ch.kind === "max") {
        if (max === null || ch.value < max)
          max = ch.value;
      }
    }
    return Number.isFinite(min) && Number.isFinite(max);
  }
};
ZodNumber.create = (params) => {
  return new ZodNumber({
    checks: [],
    typeName: ZodFirstPartyTypeKind.ZodNumber,
    coerce: params?.coerce || false,
    ...processCreateParams(params)
  });
};
var ZodBigInt = class _ZodBigInt extends ZodType {
  constructor() {
    super(...arguments);
    this.min = this.gte;
    this.max = this.lte;
  }
  _parse(input) {
    if (this._def.coerce) {
      try {
        input.data = BigInt(input.data);
      } catch {
        return this._getInvalidInput(input);
      }
    }
    const parsedType = this._getType(input);
    if (parsedType !== ZodParsedType.bigint) {
      return this._getInvalidInput(input);
    }
    let ctx = void 0;
    const status = new ParseStatus();
    for (const check of this._def.checks) {
      if (check.kind === "min") {
        const tooSmall = check.inclusive ? input.data < check.value : input.data <= check.value;
        if (tooSmall) {
          ctx = this._getOrReturnCtx(input, ctx);
          addIssueToContext(ctx, {
            code: ZodIssueCode.too_small,
            type: "bigint",
            minimum: check.value,
            inclusive: check.inclusive,
            message: check.message
          });
          status.dirty();
        }
      } else if (check.kind === "max") {
        const tooBig = check.inclusive ? input.data > check.value : input.data >= check.value;
        if (tooBig) {
          ctx = this._getOrReturnCtx(input, ctx);
          addIssueToContext(ctx, {
            code: ZodIssueCode.too_big,
            type: "bigint",
            maximum: check.value,
            inclusive: check.inclusive,
            message: check.message
          });
          status.dirty();
        }
      } else if (check.kind === "multipleOf") {
        if (input.data % check.value !== BigInt(0)) {
          ctx = this._getOrReturnCtx(input, ctx);
          addIssueToContext(ctx, {
            code: ZodIssueCode.not_multiple_of,
            multipleOf: check.value,
            message: check.message
          });
          status.dirty();
        }
      } else {
        util.assertNever(check);
      }
    }
    return { status: status.value, value: input.data };
  }
  _getInvalidInput(input) {
    const ctx = this._getOrReturnCtx(input);
    addIssueToContext(ctx, {
      code: ZodIssueCode.invalid_type,
      expected: ZodParsedType.bigint,
      received: ctx.parsedType
    });
    return INVALID;
  }
  gte(value, message) {
    return this.setLimit("min", value, true, errorUtil.toString(message));
  }
  gt(value, message) {
    return this.setLimit("min", value, false, errorUtil.toString(message));
  }
  lte(value, message) {
    return this.setLimit("max", value, true, errorUtil.toString(message));
  }
  lt(value, message) {
    return this.setLimit("max", value, false, errorUtil.toString(message));
  }
  setLimit(kind, value, inclusive, message) {
    return new _ZodBigInt({
      ...this._def,
      checks: [
        ...this._def.checks,
        {
          kind,
          value,
          inclusive,
          message: errorUtil.toString(message)
        }
      ]
    });
  }
  _addCheck(check) {
    return new _ZodBigInt({
      ...this._def,
      checks: [...this._def.checks, check]
    });
  }
  positive(message) {
    return this._addCheck({
      kind: "min",
      value: BigInt(0),
      inclusive: false,
      message: errorUtil.toString(message)
    });
  }
  negative(message) {
    return this._addCheck({
      kind: "max",
      value: BigInt(0),
      inclusive: false,
      message: errorUtil.toString(message)
    });
  }
  nonpositive(message) {
    return this._addCheck({
      kind: "max",
      value: BigInt(0),
      inclusive: true,
      message: errorUtil.toString(message)
    });
  }
  nonnegative(message) {
    return this._addCheck({
      kind: "min",
      value: BigInt(0),
      inclusive: true,
      message: errorUtil.toString(message)
    });
  }
  multipleOf(value, message) {
    return this._addCheck({
      kind: "multipleOf",
      value,
      message: errorUtil.toString(message)
    });
  }
  get minValue() {
    let min = null;
    for (const ch of this._def.checks) {
      if (ch.kind === "min") {
        if (min === null || ch.value > min)
          min = ch.value;
      }
    }
    return min;
  }
  get maxValue() {
    let max = null;
    for (const ch of this._def.checks) {
      if (ch.kind === "max") {
        if (max === null || ch.value < max)
          max = ch.value;
      }
    }
    return max;
  }
};
ZodBigInt.create = (params) => {
  return new ZodBigInt({
    checks: [],
    typeName: ZodFirstPartyTypeKind.ZodBigInt,
    coerce: params?.coerce ?? false,
    ...processCreateParams(params)
  });
};
var ZodBoolean = class extends ZodType {
  _parse(input) {
    if (this._def.coerce) {
      input.data = Boolean(input.data);
    }
    const parsedType = this._getType(input);
    if (parsedType !== ZodParsedType.boolean) {
      const ctx = this._getOrReturnCtx(input);
      addIssueToContext(ctx, {
        code: ZodIssueCode.invalid_type,
        expected: ZodParsedType.boolean,
        received: ctx.parsedType
      });
      return INVALID;
    }
    return OK(input.data);
  }
};
ZodBoolean.create = (params) => {
  return new ZodBoolean({
    typeName: ZodFirstPartyTypeKind.ZodBoolean,
    coerce: params?.coerce || false,
    ...processCreateParams(params)
  });
};
var ZodDate = class _ZodDate extends ZodType {
  _parse(input) {
    if (this._def.coerce) {
      input.data = new Date(input.data);
    }
    const parsedType = this._getType(input);
    if (parsedType !== ZodParsedType.date) {
      const ctx2 = this._getOrReturnCtx(input);
      addIssueToContext(ctx2, {
        code: ZodIssueCode.invalid_type,
        expected: ZodParsedType.date,
        received: ctx2.parsedType
      });
      return INVALID;
    }
    if (Number.isNaN(input.data.getTime())) {
      const ctx2 = this._getOrReturnCtx(input);
      addIssueToContext(ctx2, {
        code: ZodIssueCode.invalid_date
      });
      return INVALID;
    }
    const status = new ParseStatus();
    let ctx = void 0;
    for (const check of this._def.checks) {
      if (check.kind === "min") {
        if (input.data.getTime() < check.value) {
          ctx = this._getOrReturnCtx(input, ctx);
          addIssueToContext(ctx, {
            code: ZodIssueCode.too_small,
            message: check.message,
            inclusive: true,
            exact: false,
            minimum: check.value,
            type: "date"
          });
          status.dirty();
        }
      } else if (check.kind === "max") {
        if (input.data.getTime() > check.value) {
          ctx = this._getOrReturnCtx(input, ctx);
          addIssueToContext(ctx, {
            code: ZodIssueCode.too_big,
            message: check.message,
            inclusive: true,
            exact: false,
            maximum: check.value,
            type: "date"
          });
          status.dirty();
        }
      } else {
        util.assertNever(check);
      }
    }
    return {
      status: status.value,
      value: new Date(input.data.getTime())
    };
  }
  _addCheck(check) {
    return new _ZodDate({
      ...this._def,
      checks: [...this._def.checks, check]
    });
  }
  min(minDate, message) {
    return this._addCheck({
      kind: "min",
      value: minDate.getTime(),
      message: errorUtil.toString(message)
    });
  }
  max(maxDate, message) {
    return this._addCheck({
      kind: "max",
      value: maxDate.getTime(),
      message: errorUtil.toString(message)
    });
  }
  get minDate() {
    let min = null;
    for (const ch of this._def.checks) {
      if (ch.kind === "min") {
        if (min === null || ch.value > min)
          min = ch.value;
      }
    }
    return min != null ? new Date(min) : null;
  }
  get maxDate() {
    let max = null;
    for (const ch of this._def.checks) {
      if (ch.kind === "max") {
        if (max === null || ch.value < max)
          max = ch.value;
      }
    }
    return max != null ? new Date(max) : null;
  }
};
ZodDate.create = (params) => {
  return new ZodDate({
    checks: [],
    coerce: params?.coerce || false,
    typeName: ZodFirstPartyTypeKind.ZodDate,
    ...processCreateParams(params)
  });
};
var ZodSymbol = class extends ZodType {
  _parse(input) {
    const parsedType = this._getType(input);
    if (parsedType !== ZodParsedType.symbol) {
      const ctx = this._getOrReturnCtx(input);
      addIssueToContext(ctx, {
        code: ZodIssueCode.invalid_type,
        expected: ZodParsedType.symbol,
        received: ctx.parsedType
      });
      return INVALID;
    }
    return OK(input.data);
  }
};
ZodSymbol.create = (params) => {
  return new ZodSymbol({
    typeName: ZodFirstPartyTypeKind.ZodSymbol,
    ...processCreateParams(params)
  });
};
var ZodUndefined = class extends ZodType {
  _parse(input) {
    const parsedType = this._getType(input);
    if (parsedType !== ZodParsedType.undefined) {
      const ctx = this._getOrReturnCtx(input);
      addIssueToContext(ctx, {
        code: ZodIssueCode.invalid_type,
        expected: ZodParsedType.undefined,
        received: ctx.parsedType
      });
      return INVALID;
    }
    return OK(input.data);
  }
};
ZodUndefined.create = (params) => {
  return new ZodUndefined({
    typeName: ZodFirstPartyTypeKind.ZodUndefined,
    ...processCreateParams(params)
  });
};
var ZodNull = class extends ZodType {
  _parse(input) {
    const parsedType = this._getType(input);
    if (parsedType !== ZodParsedType.null) {
      const ctx = this._getOrReturnCtx(input);
      addIssueToContext(ctx, {
        code: ZodIssueCode.invalid_type,
        expected: ZodParsedType.null,
        received: ctx.parsedType
      });
      return INVALID;
    }
    return OK(input.data);
  }
};
ZodNull.create = (params) => {
  return new ZodNull({
    typeName: ZodFirstPartyTypeKind.ZodNull,
    ...processCreateParams(params)
  });
};
var ZodAny = class extends ZodType {
  constructor() {
    super(...arguments);
    this._any = true;
  }
  _parse(input) {
    return OK(input.data);
  }
};
ZodAny.create = (params) => {
  return new ZodAny({
    typeName: ZodFirstPartyTypeKind.ZodAny,
    ...processCreateParams(params)
  });
};
var ZodUnknown = class extends ZodType {
  constructor() {
    super(...arguments);
    this._unknown = true;
  }
  _parse(input) {
    return OK(input.data);
  }
};
ZodUnknown.create = (params) => {
  return new ZodUnknown({
    typeName: ZodFirstPartyTypeKind.ZodUnknown,
    ...processCreateParams(params)
  });
};
var ZodNever = class extends ZodType {
  _parse(input) {
    const ctx = this._getOrReturnCtx(input);
    addIssueToContext(ctx, {
      code: ZodIssueCode.invalid_type,
      expected: ZodParsedType.never,
      received: ctx.parsedType
    });
    return INVALID;
  }
};
ZodNever.create = (params) => {
  return new ZodNever({
    typeName: ZodFirstPartyTypeKind.ZodNever,
    ...processCreateParams(params)
  });
};
var ZodVoid = class extends ZodType {
  _parse(input) {
    const parsedType = this._getType(input);
    if (parsedType !== ZodParsedType.undefined) {
      const ctx = this._getOrReturnCtx(input);
      addIssueToContext(ctx, {
        code: ZodIssueCode.invalid_type,
        expected: ZodParsedType.void,
        received: ctx.parsedType
      });
      return INVALID;
    }
    return OK(input.data);
  }
};
ZodVoid.create = (params) => {
  return new ZodVoid({
    typeName: ZodFirstPartyTypeKind.ZodVoid,
    ...processCreateParams(params)
  });
};
var ZodArray = class _ZodArray extends ZodType {
  _parse(input) {
    const { ctx, status } = this._processInputParams(input);
    const def = this._def;
    if (ctx.parsedType !== ZodParsedType.array) {
      addIssueToContext(ctx, {
        code: ZodIssueCode.invalid_type,
        expected: ZodParsedType.array,
        received: ctx.parsedType
      });
      return INVALID;
    }
    if (def.exactLength !== null) {
      const tooBig = ctx.data.length > def.exactLength.value;
      const tooSmall = ctx.data.length < def.exactLength.value;
      if (tooBig || tooSmall) {
        addIssueToContext(ctx, {
          code: tooBig ? ZodIssueCode.too_big : ZodIssueCode.too_small,
          minimum: tooSmall ? def.exactLength.value : void 0,
          maximum: tooBig ? def.exactLength.value : void 0,
          type: "array",
          inclusive: true,
          exact: true,
          message: def.exactLength.message
        });
        status.dirty();
      }
    }
    if (def.minLength !== null) {
      if (ctx.data.length < def.minLength.value) {
        addIssueToContext(ctx, {
          code: ZodIssueCode.too_small,
          minimum: def.minLength.value,
          type: "array",
          inclusive: true,
          exact: false,
          message: def.minLength.message
        });
        status.dirty();
      }
    }
    if (def.maxLength !== null) {
      if (ctx.data.length > def.maxLength.value) {
        addIssueToContext(ctx, {
          code: ZodIssueCode.too_big,
          maximum: def.maxLength.value,
          type: "array",
          inclusive: true,
          exact: false,
          message: def.maxLength.message
        });
        status.dirty();
      }
    }
    if (ctx.common.async) {
      return Promise.all([...ctx.data].map((item, i) => {
        return def.type._parseAsync(new ParseInputLazyPath(ctx, item, ctx.path, i));
      })).then((result2) => {
        return ParseStatus.mergeArray(status, result2);
      });
    }
    const result = [...ctx.data].map((item, i) => {
      return def.type._parseSync(new ParseInputLazyPath(ctx, item, ctx.path, i));
    });
    return ParseStatus.mergeArray(status, result);
  }
  get element() {
    return this._def.type;
  }
  min(minLength, message) {
    return new _ZodArray({
      ...this._def,
      minLength: { value: minLength, message: errorUtil.toString(message) }
    });
  }
  max(maxLength, message) {
    return new _ZodArray({
      ...this._def,
      maxLength: { value: maxLength, message: errorUtil.toString(message) }
    });
  }
  length(len, message) {
    return new _ZodArray({
      ...this._def,
      exactLength: { value: len, message: errorUtil.toString(message) }
    });
  }
  nonempty(message) {
    return this.min(1, message);
  }
};
ZodArray.create = (schema, params) => {
  return new ZodArray({
    type: schema,
    minLength: null,
    maxLength: null,
    exactLength: null,
    typeName: ZodFirstPartyTypeKind.ZodArray,
    ...processCreateParams(params)
  });
};
function deepPartialify(schema) {
  if (schema instanceof ZodObject) {
    const newShape = {};
    for (const key in schema.shape) {
      const fieldSchema = schema.shape[key];
      newShape[key] = ZodOptional.create(deepPartialify(fieldSchema));
    }
    return new ZodObject({
      ...schema._def,
      shape: () => newShape
    });
  } else if (schema instanceof ZodArray) {
    return new ZodArray({
      ...schema._def,
      type: deepPartialify(schema.element)
    });
  } else if (schema instanceof ZodOptional) {
    return ZodOptional.create(deepPartialify(schema.unwrap()));
  } else if (schema instanceof ZodNullable) {
    return ZodNullable.create(deepPartialify(schema.unwrap()));
  } else if (schema instanceof ZodTuple) {
    return ZodTuple.create(schema.items.map((item) => deepPartialify(item)));
  } else {
    return schema;
  }
}
var ZodObject = class _ZodObject extends ZodType {
  constructor() {
    super(...arguments);
    this._cached = null;
    this.nonstrict = this.passthrough;
    this.augment = this.extend;
  }
  _getCached() {
    if (this._cached !== null)
      return this._cached;
    const shape = this._def.shape();
    const keys = util.objectKeys(shape);
    this._cached = { shape, keys };
    return this._cached;
  }
  _parse(input) {
    const parsedType = this._getType(input);
    if (parsedType !== ZodParsedType.object) {
      const ctx2 = this._getOrReturnCtx(input);
      addIssueToContext(ctx2, {
        code: ZodIssueCode.invalid_type,
        expected: ZodParsedType.object,
        received: ctx2.parsedType
      });
      return INVALID;
    }
    const { status, ctx } = this._processInputParams(input);
    const { shape, keys: shapeKeys } = this._getCached();
    const extraKeys = [];
    if (!(this._def.catchall instanceof ZodNever && this._def.unknownKeys === "strip")) {
      for (const key in ctx.data) {
        if (!shapeKeys.includes(key)) {
          extraKeys.push(key);
        }
      }
    }
    const pairs = [];
    for (const key of shapeKeys) {
      const keyValidator = shape[key];
      const value = ctx.data[key];
      pairs.push({
        key: { status: "valid", value: key },
        value: keyValidator._parse(new ParseInputLazyPath(ctx, value, ctx.path, key)),
        alwaysSet: key in ctx.data
      });
    }
    if (this._def.catchall instanceof ZodNever) {
      const unknownKeys = this._def.unknownKeys;
      if (unknownKeys === "passthrough") {
        for (const key of extraKeys) {
          pairs.push({
            key: { status: "valid", value: key },
            value: { status: "valid", value: ctx.data[key] }
          });
        }
      } else if (unknownKeys === "strict") {
        if (extraKeys.length > 0) {
          addIssueToContext(ctx, {
            code: ZodIssueCode.unrecognized_keys,
            keys: extraKeys
          });
          status.dirty();
        }
      } else if (unknownKeys === "strip") {
      } else {
        throw new Error(`Internal ZodObject error: invalid unknownKeys value.`);
      }
    } else {
      const catchall = this._def.catchall;
      for (const key of extraKeys) {
        const value = ctx.data[key];
        pairs.push({
          key: { status: "valid", value: key },
          value: catchall._parse(
            new ParseInputLazyPath(ctx, value, ctx.path, key)
            //, ctx.child(key), value, getParsedType(value)
          ),
          alwaysSet: key in ctx.data
        });
      }
    }
    if (ctx.common.async) {
      return Promise.resolve().then(async () => {
        const syncPairs = [];
        for (const pair of pairs) {
          const key = await pair.key;
          const value = await pair.value;
          syncPairs.push({
            key,
            value,
            alwaysSet: pair.alwaysSet
          });
        }
        return syncPairs;
      }).then((syncPairs) => {
        return ParseStatus.mergeObjectSync(status, syncPairs);
      });
    } else {
      return ParseStatus.mergeObjectSync(status, pairs);
    }
  }
  get shape() {
    return this._def.shape();
  }
  strict(message) {
    errorUtil.errToObj;
    return new _ZodObject({
      ...this._def,
      unknownKeys: "strict",
      ...message !== void 0 ? {
        errorMap: (issue, ctx) => {
          const defaultError = this._def.errorMap?.(issue, ctx).message ?? ctx.defaultError;
          if (issue.code === "unrecognized_keys")
            return {
              message: errorUtil.errToObj(message).message ?? defaultError
            };
          return {
            message: defaultError
          };
        }
      } : {}
    });
  }
  strip() {
    return new _ZodObject({
      ...this._def,
      unknownKeys: "strip"
    });
  }
  passthrough() {
    return new _ZodObject({
      ...this._def,
      unknownKeys: "passthrough"
    });
  }
  // const AugmentFactory =
  //   <Def extends ZodObjectDef>(def: Def) =>
  //   <Augmentation extends ZodRawShape>(
  //     augmentation: Augmentation
  //   ): ZodObject<
  //     extendShape<ReturnType<Def["shape"]>, Augmentation>,
  //     Def["unknownKeys"],
  //     Def["catchall"]
  //   > => {
  //     return new ZodObject({
  //       ...def,
  //       shape: () => ({
  //         ...def.shape(),
  //         ...augmentation,
  //       }),
  //     }) as any;
  //   };
  extend(augmentation) {
    return new _ZodObject({
      ...this._def,
      shape: () => ({
        ...this._def.shape(),
        ...augmentation
      })
    });
  }
  /**
   * Prior to zod@1.0.12 there was a bug in the
   * inferred type of merged objects. Please
   * upgrade if you are experiencing issues.
   */
  merge(merging) {
    const merged = new _ZodObject({
      unknownKeys: merging._def.unknownKeys,
      catchall: merging._def.catchall,
      shape: () => ({
        ...this._def.shape(),
        ...merging._def.shape()
      }),
      typeName: ZodFirstPartyTypeKind.ZodObject
    });
    return merged;
  }
  // merge<
  //   Incoming extends AnyZodObject,
  //   Augmentation extends Incoming["shape"],
  //   NewOutput extends {
  //     [k in keyof Augmentation | keyof Output]: k extends keyof Augmentation
  //       ? Augmentation[k]["_output"]
  //       : k extends keyof Output
  //       ? Output[k]
  //       : never;
  //   },
  //   NewInput extends {
  //     [k in keyof Augmentation | keyof Input]: k extends keyof Augmentation
  //       ? Augmentation[k]["_input"]
  //       : k extends keyof Input
  //       ? Input[k]
  //       : never;
  //   }
  // >(
  //   merging: Incoming
  // ): ZodObject<
  //   extendShape<T, ReturnType<Incoming["_def"]["shape"]>>,
  //   Incoming["_def"]["unknownKeys"],
  //   Incoming["_def"]["catchall"],
  //   NewOutput,
  //   NewInput
  // > {
  //   const merged: any = new ZodObject({
  //     unknownKeys: merging._def.unknownKeys,
  //     catchall: merging._def.catchall,
  //     shape: () =>
  //       objectUtil.mergeShapes(this._def.shape(), merging._def.shape()),
  //     typeName: ZodFirstPartyTypeKind.ZodObject,
  //   }) as any;
  //   return merged;
  // }
  setKey(key, schema) {
    return this.augment({ [key]: schema });
  }
  // merge<Incoming extends AnyZodObject>(
  //   merging: Incoming
  // ): //ZodObject<T & Incoming["_shape"], UnknownKeys, Catchall> = (merging) => {
  // ZodObject<
  //   extendShape<T, ReturnType<Incoming["_def"]["shape"]>>,
  //   Incoming["_def"]["unknownKeys"],
  //   Incoming["_def"]["catchall"]
  // > {
  //   // const mergedShape = objectUtil.mergeShapes(
  //   //   this._def.shape(),
  //   //   merging._def.shape()
  //   // );
  //   const merged: any = new ZodObject({
  //     unknownKeys: merging._def.unknownKeys,
  //     catchall: merging._def.catchall,
  //     shape: () =>
  //       objectUtil.mergeShapes(this._def.shape(), merging._def.shape()),
  //     typeName: ZodFirstPartyTypeKind.ZodObject,
  //   }) as any;
  //   return merged;
  // }
  catchall(index) {
    return new _ZodObject({
      ...this._def,
      catchall: index
    });
  }
  pick(mask) {
    const shape = {};
    for (const key of util.objectKeys(mask)) {
      if (mask[key] && this.shape[key]) {
        shape[key] = this.shape[key];
      }
    }
    return new _ZodObject({
      ...this._def,
      shape: () => shape
    });
  }
  omit(mask) {
    const shape = {};
    for (const key of util.objectKeys(this.shape)) {
      if (!mask[key]) {
        shape[key] = this.shape[key];
      }
    }
    return new _ZodObject({
      ...this._def,
      shape: () => shape
    });
  }
  /**
   * @deprecated
   */
  deepPartial() {
    return deepPartialify(this);
  }
  partial(mask) {
    const newShape = {};
    for (const key of util.objectKeys(this.shape)) {
      const fieldSchema = this.shape[key];
      if (mask && !mask[key]) {
        newShape[key] = fieldSchema;
      } else {
        newShape[key] = fieldSchema.optional();
      }
    }
    return new _ZodObject({
      ...this._def,
      shape: () => newShape
    });
  }
  required(mask) {
    const newShape = {};
    for (const key of util.objectKeys(this.shape)) {
      if (mask && !mask[key]) {
        newShape[key] = this.shape[key];
      } else {
        const fieldSchema = this.shape[key];
        let newField = fieldSchema;
        while (newField instanceof ZodOptional) {
          newField = newField._def.innerType;
        }
        newShape[key] = newField;
      }
    }
    return new _ZodObject({
      ...this._def,
      shape: () => newShape
    });
  }
  keyof() {
    return createZodEnum(util.objectKeys(this.shape));
  }
};
ZodObject.create = (shape, params) => {
  return new ZodObject({
    shape: () => shape,
    unknownKeys: "strip",
    catchall: ZodNever.create(),
    typeName: ZodFirstPartyTypeKind.ZodObject,
    ...processCreateParams(params)
  });
};
ZodObject.strictCreate = (shape, params) => {
  return new ZodObject({
    shape: () => shape,
    unknownKeys: "strict",
    catchall: ZodNever.create(),
    typeName: ZodFirstPartyTypeKind.ZodObject,
    ...processCreateParams(params)
  });
};
ZodObject.lazycreate = (shape, params) => {
  return new ZodObject({
    shape,
    unknownKeys: "strip",
    catchall: ZodNever.create(),
    typeName: ZodFirstPartyTypeKind.ZodObject,
    ...processCreateParams(params)
  });
};
var ZodUnion = class extends ZodType {
  _parse(input) {
    const { ctx } = this._processInputParams(input);
    const options = this._def.options;
    function handleResults(results) {
      for (const result of results) {
        if (result.result.status === "valid") {
          return result.result;
        }
      }
      for (const result of results) {
        if (result.result.status === "dirty") {
          ctx.common.issues.push(...result.ctx.common.issues);
          return result.result;
        }
      }
      const unionErrors = results.map((result) => new ZodError(result.ctx.common.issues));
      addIssueToContext(ctx, {
        code: ZodIssueCode.invalid_union,
        unionErrors
      });
      return INVALID;
    }
    if (ctx.common.async) {
      return Promise.all(options.map(async (option) => {
        const childCtx = {
          ...ctx,
          common: {
            ...ctx.common,
            issues: []
          },
          parent: null
        };
        return {
          result: await option._parseAsync({
            data: ctx.data,
            path: ctx.path,
            parent: childCtx
          }),
          ctx: childCtx
        };
      })).then(handleResults);
    } else {
      let dirty = void 0;
      const issues = [];
      for (const option of options) {
        const childCtx = {
          ...ctx,
          common: {
            ...ctx.common,
            issues: []
          },
          parent: null
        };
        const result = option._parseSync({
          data: ctx.data,
          path: ctx.path,
          parent: childCtx
        });
        if (result.status === "valid") {
          return result;
        } else if (result.status === "dirty" && !dirty) {
          dirty = { result, ctx: childCtx };
        }
        if (childCtx.common.issues.length) {
          issues.push(childCtx.common.issues);
        }
      }
      if (dirty) {
        ctx.common.issues.push(...dirty.ctx.common.issues);
        return dirty.result;
      }
      const unionErrors = issues.map((issues2) => new ZodError(issues2));
      addIssueToContext(ctx, {
        code: ZodIssueCode.invalid_union,
        unionErrors
      });
      return INVALID;
    }
  }
  get options() {
    return this._def.options;
  }
};
ZodUnion.create = (types, params) => {
  return new ZodUnion({
    options: types,
    typeName: ZodFirstPartyTypeKind.ZodUnion,
    ...processCreateParams(params)
  });
};
var getDiscriminator = (type) => {
  if (type instanceof ZodLazy) {
    return getDiscriminator(type.schema);
  } else if (type instanceof ZodEffects) {
    return getDiscriminator(type.innerType());
  } else if (type instanceof ZodLiteral) {
    return [type.value];
  } else if (type instanceof ZodEnum) {
    return type.options;
  } else if (type instanceof ZodNativeEnum) {
    return util.objectValues(type.enum);
  } else if (type instanceof ZodDefault) {
    return getDiscriminator(type._def.innerType);
  } else if (type instanceof ZodUndefined) {
    return [void 0];
  } else if (type instanceof ZodNull) {
    return [null];
  } else if (type instanceof ZodOptional) {
    return [void 0, ...getDiscriminator(type.unwrap())];
  } else if (type instanceof ZodNullable) {
    return [null, ...getDiscriminator(type.unwrap())];
  } else if (type instanceof ZodBranded) {
    return getDiscriminator(type.unwrap());
  } else if (type instanceof ZodReadonly) {
    return getDiscriminator(type.unwrap());
  } else if (type instanceof ZodCatch) {
    return getDiscriminator(type._def.innerType);
  } else {
    return [];
  }
};
var ZodDiscriminatedUnion = class _ZodDiscriminatedUnion extends ZodType {
  _parse(input) {
    const { ctx } = this._processInputParams(input);
    if (ctx.parsedType !== ZodParsedType.object) {
      addIssueToContext(ctx, {
        code: ZodIssueCode.invalid_type,
        expected: ZodParsedType.object,
        received: ctx.parsedType
      });
      return INVALID;
    }
    const discriminator = this.discriminator;
    const discriminatorValue = ctx.data[discriminator];
    const option = this.optionsMap.get(discriminatorValue);
    if (!option) {
      addIssueToContext(ctx, {
        code: ZodIssueCode.invalid_union_discriminator,
        options: Array.from(this.optionsMap.keys()),
        path: [discriminator]
      });
      return INVALID;
    }
    if (ctx.common.async) {
      return option._parseAsync({
        data: ctx.data,
        path: ctx.path,
        parent: ctx
      });
    } else {
      return option._parseSync({
        data: ctx.data,
        path: ctx.path,
        parent: ctx
      });
    }
  }
  get discriminator() {
    return this._def.discriminator;
  }
  get options() {
    return this._def.options;
  }
  get optionsMap() {
    return this._def.optionsMap;
  }
  /**
   * The constructor of the discriminated union schema. Its behaviour is very similar to that of the normal z.union() constructor.
   * However, it only allows a union of objects, all of which need to share a discriminator property. This property must
   * have a different value for each object in the union.
   * @param discriminator the name of the discriminator property
   * @param types an array of object schemas
   * @param params
   */
  static create(discriminator, options, params) {
    const optionsMap = /* @__PURE__ */ new Map();
    for (const type of options) {
      const discriminatorValues = getDiscriminator(type.shape[discriminator]);
      if (!discriminatorValues.length) {
        throw new Error(`A discriminator value for key \`${discriminator}\` could not be extracted from all schema options`);
      }
      for (const value of discriminatorValues) {
        if (optionsMap.has(value)) {
          throw new Error(`Discriminator property ${String(discriminator)} has duplicate value ${String(value)}`);
        }
        optionsMap.set(value, type);
      }
    }
    return new _ZodDiscriminatedUnion({
      typeName: ZodFirstPartyTypeKind.ZodDiscriminatedUnion,
      discriminator,
      options,
      optionsMap,
      ...processCreateParams(params)
    });
  }
};
function mergeValues(a, b) {
  const aType = getParsedType(a);
  const bType = getParsedType(b);
  if (a === b) {
    return { valid: true, data: a };
  } else if (aType === ZodParsedType.object && bType === ZodParsedType.object) {
    const bKeys = util.objectKeys(b);
    const sharedKeys = util.objectKeys(a).filter((key) => bKeys.indexOf(key) !== -1);
    const newObj = { ...a, ...b };
    for (const key of sharedKeys) {
      const sharedValue = mergeValues(a[key], b[key]);
      if (!sharedValue.valid) {
        return { valid: false };
      }
      newObj[key] = sharedValue.data;
    }
    return { valid: true, data: newObj };
  } else if (aType === ZodParsedType.array && bType === ZodParsedType.array) {
    if (a.length !== b.length) {
      return { valid: false };
    }
    const newArray = [];
    for (let index = 0; index < a.length; index++) {
      const itemA = a[index];
      const itemB = b[index];
      const sharedValue = mergeValues(itemA, itemB);
      if (!sharedValue.valid) {
        return { valid: false };
      }
      newArray.push(sharedValue.data);
    }
    return { valid: true, data: newArray };
  } else if (aType === ZodParsedType.date && bType === ZodParsedType.date && +a === +b) {
    return { valid: true, data: a };
  } else {
    return { valid: false };
  }
}
var ZodIntersection = class extends ZodType {
  _parse(input) {
    const { status, ctx } = this._processInputParams(input);
    const handleParsed = (parsedLeft, parsedRight) => {
      if (isAborted(parsedLeft) || isAborted(parsedRight)) {
        return INVALID;
      }
      const merged = mergeValues(parsedLeft.value, parsedRight.value);
      if (!merged.valid) {
        addIssueToContext(ctx, {
          code: ZodIssueCode.invalid_intersection_types
        });
        return INVALID;
      }
      if (isDirty(parsedLeft) || isDirty(parsedRight)) {
        status.dirty();
      }
      return { status: status.value, value: merged.data };
    };
    if (ctx.common.async) {
      return Promise.all([
        this._def.left._parseAsync({
          data: ctx.data,
          path: ctx.path,
          parent: ctx
        }),
        this._def.right._parseAsync({
          data: ctx.data,
          path: ctx.path,
          parent: ctx
        })
      ]).then(([left, right]) => handleParsed(left, right));
    } else {
      return handleParsed(this._def.left._parseSync({
        data: ctx.data,
        path: ctx.path,
        parent: ctx
      }), this._def.right._parseSync({
        data: ctx.data,
        path: ctx.path,
        parent: ctx
      }));
    }
  }
};
ZodIntersection.create = (left, right, params) => {
  return new ZodIntersection({
    left,
    right,
    typeName: ZodFirstPartyTypeKind.ZodIntersection,
    ...processCreateParams(params)
  });
};
var ZodTuple = class _ZodTuple extends ZodType {
  _parse(input) {
    const { status, ctx } = this._processInputParams(input);
    if (ctx.parsedType !== ZodParsedType.array) {
      addIssueToContext(ctx, {
        code: ZodIssueCode.invalid_type,
        expected: ZodParsedType.array,
        received: ctx.parsedType
      });
      return INVALID;
    }
    if (ctx.data.length < this._def.items.length) {
      addIssueToContext(ctx, {
        code: ZodIssueCode.too_small,
        minimum: this._def.items.length,
        inclusive: true,
        exact: false,
        type: "array"
      });
      return INVALID;
    }
    const rest = this._def.rest;
    if (!rest && ctx.data.length > this._def.items.length) {
      addIssueToContext(ctx, {
        code: ZodIssueCode.too_big,
        maximum: this._def.items.length,
        inclusive: true,
        exact: false,
        type: "array"
      });
      status.dirty();
    }
    const items = [...ctx.data].map((item, itemIndex) => {
      const schema = this._def.items[itemIndex] || this._def.rest;
      if (!schema)
        return null;
      return schema._parse(new ParseInputLazyPath(ctx, item, ctx.path, itemIndex));
    }).filter((x) => !!x);
    if (ctx.common.async) {
      return Promise.all(items).then((results) => {
        return ParseStatus.mergeArray(status, results);
      });
    } else {
      return ParseStatus.mergeArray(status, items);
    }
  }
  get items() {
    return this._def.items;
  }
  rest(rest) {
    return new _ZodTuple({
      ...this._def,
      rest
    });
  }
};
ZodTuple.create = (schemas, params) => {
  if (!Array.isArray(schemas)) {
    throw new Error("You must pass an array of schemas to z.tuple([ ... ])");
  }
  return new ZodTuple({
    items: schemas,
    typeName: ZodFirstPartyTypeKind.ZodTuple,
    rest: null,
    ...processCreateParams(params)
  });
};
var ZodRecord = class _ZodRecord extends ZodType {
  get keySchema() {
    return this._def.keyType;
  }
  get valueSchema() {
    return this._def.valueType;
  }
  _parse(input) {
    const { status, ctx } = this._processInputParams(input);
    if (ctx.parsedType !== ZodParsedType.object) {
      addIssueToContext(ctx, {
        code: ZodIssueCode.invalid_type,
        expected: ZodParsedType.object,
        received: ctx.parsedType
      });
      return INVALID;
    }
    const pairs = [];
    const keyType = this._def.keyType;
    const valueType = this._def.valueType;
    for (const key in ctx.data) {
      pairs.push({
        key: keyType._parse(new ParseInputLazyPath(ctx, key, ctx.path, key)),
        value: valueType._parse(new ParseInputLazyPath(ctx, ctx.data[key], ctx.path, key)),
        alwaysSet: key in ctx.data
      });
    }
    if (ctx.common.async) {
      return ParseStatus.mergeObjectAsync(status, pairs);
    } else {
      return ParseStatus.mergeObjectSync(status, pairs);
    }
  }
  get element() {
    return this._def.valueType;
  }
  static create(first, second, third) {
    if (second instanceof ZodType) {
      return new _ZodRecord({
        keyType: first,
        valueType: second,
        typeName: ZodFirstPartyTypeKind.ZodRecord,
        ...processCreateParams(third)
      });
    }
    return new _ZodRecord({
      keyType: ZodString.create(),
      valueType: first,
      typeName: ZodFirstPartyTypeKind.ZodRecord,
      ...processCreateParams(second)
    });
  }
};
var ZodMap = class extends ZodType {
  get keySchema() {
    return this._def.keyType;
  }
  get valueSchema() {
    return this._def.valueType;
  }
  _parse(input) {
    const { status, ctx } = this._processInputParams(input);
    if (ctx.parsedType !== ZodParsedType.map) {
      addIssueToContext(ctx, {
        code: ZodIssueCode.invalid_type,
        expected: ZodParsedType.map,
        received: ctx.parsedType
      });
      return INVALID;
    }
    const keyType = this._def.keyType;
    const valueType = this._def.valueType;
    const pairs = [...ctx.data.entries()].map(([key, value], index) => {
      return {
        key: keyType._parse(new ParseInputLazyPath(ctx, key, ctx.path, [index, "key"])),
        value: valueType._parse(new ParseInputLazyPath(ctx, value, ctx.path, [index, "value"]))
      };
    });
    if (ctx.common.async) {
      const finalMap = /* @__PURE__ */ new Map();
      return Promise.resolve().then(async () => {
        for (const pair of pairs) {
          const key = await pair.key;
          const value = await pair.value;
          if (key.status === "aborted" || value.status === "aborted") {
            return INVALID;
          }
          if (key.status === "dirty" || value.status === "dirty") {
            status.dirty();
          }
          finalMap.set(key.value, value.value);
        }
        return { status: status.value, value: finalMap };
      });
    } else {
      const finalMap = /* @__PURE__ */ new Map();
      for (const pair of pairs) {
        const key = pair.key;
        const value = pair.value;
        if (key.status === "aborted" || value.status === "aborted") {
          return INVALID;
        }
        if (key.status === "dirty" || value.status === "dirty") {
          status.dirty();
        }
        finalMap.set(key.value, value.value);
      }
      return { status: status.value, value: finalMap };
    }
  }
};
ZodMap.create = (keyType, valueType, params) => {
  return new ZodMap({
    valueType,
    keyType,
    typeName: ZodFirstPartyTypeKind.ZodMap,
    ...processCreateParams(params)
  });
};
var ZodSet = class _ZodSet extends ZodType {
  _parse(input) {
    const { status, ctx } = this._processInputParams(input);
    if (ctx.parsedType !== ZodParsedType.set) {
      addIssueToContext(ctx, {
        code: ZodIssueCode.invalid_type,
        expected: ZodParsedType.set,
        received: ctx.parsedType
      });
      return INVALID;
    }
    const def = this._def;
    if (def.minSize !== null) {
      if (ctx.data.size < def.minSize.value) {
        addIssueToContext(ctx, {
          code: ZodIssueCode.too_small,
          minimum: def.minSize.value,
          type: "set",
          inclusive: true,
          exact: false,
          message: def.minSize.message
        });
        status.dirty();
      }
    }
    if (def.maxSize !== null) {
      if (ctx.data.size > def.maxSize.value) {
        addIssueToContext(ctx, {
          code: ZodIssueCode.too_big,
          maximum: def.maxSize.value,
          type: "set",
          inclusive: true,
          exact: false,
          message: def.maxSize.message
        });
        status.dirty();
      }
    }
    const valueType = this._def.valueType;
    function finalizeSet(elements2) {
      const parsedSet = /* @__PURE__ */ new Set();
      for (const element of elements2) {
        if (element.status === "aborted")
          return INVALID;
        if (element.status === "dirty")
          status.dirty();
        parsedSet.add(element.value);
      }
      return { status: status.value, value: parsedSet };
    }
    const elements = [...ctx.data.values()].map((item, i) => valueType._parse(new ParseInputLazyPath(ctx, item, ctx.path, i)));
    if (ctx.common.async) {
      return Promise.all(elements).then((elements2) => finalizeSet(elements2));
    } else {
      return finalizeSet(elements);
    }
  }
  min(minSize, message) {
    return new _ZodSet({
      ...this._def,
      minSize: { value: minSize, message: errorUtil.toString(message) }
    });
  }
  max(maxSize, message) {
    return new _ZodSet({
      ...this._def,
      maxSize: { value: maxSize, message: errorUtil.toString(message) }
    });
  }
  size(size, message) {
    return this.min(size, message).max(size, message);
  }
  nonempty(message) {
    return this.min(1, message);
  }
};
ZodSet.create = (valueType, params) => {
  return new ZodSet({
    valueType,
    minSize: null,
    maxSize: null,
    typeName: ZodFirstPartyTypeKind.ZodSet,
    ...processCreateParams(params)
  });
};
var ZodFunction = class _ZodFunction extends ZodType {
  constructor() {
    super(...arguments);
    this.validate = this.implement;
  }
  _parse(input) {
    const { ctx } = this._processInputParams(input);
    if (ctx.parsedType !== ZodParsedType.function) {
      addIssueToContext(ctx, {
        code: ZodIssueCode.invalid_type,
        expected: ZodParsedType.function,
        received: ctx.parsedType
      });
      return INVALID;
    }
    function makeArgsIssue(args, error) {
      return makeIssue({
        data: args,
        path: ctx.path,
        errorMaps: [ctx.common.contextualErrorMap, ctx.schemaErrorMap, getErrorMap(), en_default].filter((x) => !!x),
        issueData: {
          code: ZodIssueCode.invalid_arguments,
          argumentsError: error
        }
      });
    }
    function makeReturnsIssue(returns, error) {
      return makeIssue({
        data: returns,
        path: ctx.path,
        errorMaps: [ctx.common.contextualErrorMap, ctx.schemaErrorMap, getErrorMap(), en_default].filter((x) => !!x),
        issueData: {
          code: ZodIssueCode.invalid_return_type,
          returnTypeError: error
        }
      });
    }
    const params = { errorMap: ctx.common.contextualErrorMap };
    const fn = ctx.data;
    if (this._def.returns instanceof ZodPromise) {
      const me = this;
      return OK(async function(...args) {
        const error = new ZodError([]);
        const parsedArgs = await me._def.args.parseAsync(args, params).catch((e) => {
          error.addIssue(makeArgsIssue(args, e));
          throw error;
        });
        const result = await Reflect.apply(fn, this, parsedArgs);
        const parsedReturns = await me._def.returns._def.type.parseAsync(result, params).catch((e) => {
          error.addIssue(makeReturnsIssue(result, e));
          throw error;
        });
        return parsedReturns;
      });
    } else {
      const me = this;
      return OK(function(...args) {
        const parsedArgs = me._def.args.safeParse(args, params);
        if (!parsedArgs.success) {
          throw new ZodError([makeArgsIssue(args, parsedArgs.error)]);
        }
        const result = Reflect.apply(fn, this, parsedArgs.data);
        const parsedReturns = me._def.returns.safeParse(result, params);
        if (!parsedReturns.success) {
          throw new ZodError([makeReturnsIssue(result, parsedReturns.error)]);
        }
        return parsedReturns.data;
      });
    }
  }
  parameters() {
    return this._def.args;
  }
  returnType() {
    return this._def.returns;
  }
  args(...items) {
    return new _ZodFunction({
      ...this._def,
      args: ZodTuple.create(items).rest(ZodUnknown.create())
    });
  }
  returns(returnType) {
    return new _ZodFunction({
      ...this._def,
      returns: returnType
    });
  }
  implement(func) {
    const validatedFunc = this.parse(func);
    return validatedFunc;
  }
  strictImplement(func) {
    const validatedFunc = this.parse(func);
    return validatedFunc;
  }
  static create(args, returns, params) {
    return new _ZodFunction({
      args: args ? args : ZodTuple.create([]).rest(ZodUnknown.create()),
      returns: returns || ZodUnknown.create(),
      typeName: ZodFirstPartyTypeKind.ZodFunction,
      ...processCreateParams(params)
    });
  }
};
var ZodLazy = class extends ZodType {
  get schema() {
    return this._def.getter();
  }
  _parse(input) {
    const { ctx } = this._processInputParams(input);
    const lazySchema = this._def.getter();
    return lazySchema._parse({ data: ctx.data, path: ctx.path, parent: ctx });
  }
};
ZodLazy.create = (getter, params) => {
  return new ZodLazy({
    getter,
    typeName: ZodFirstPartyTypeKind.ZodLazy,
    ...processCreateParams(params)
  });
};
var ZodLiteral = class extends ZodType {
  _parse(input) {
    if (input.data !== this._def.value) {
      const ctx = this._getOrReturnCtx(input);
      addIssueToContext(ctx, {
        received: ctx.data,
        code: ZodIssueCode.invalid_literal,
        expected: this._def.value
      });
      return INVALID;
    }
    return { status: "valid", value: input.data };
  }
  get value() {
    return this._def.value;
  }
};
ZodLiteral.create = (value, params) => {
  return new ZodLiteral({
    value,
    typeName: ZodFirstPartyTypeKind.ZodLiteral,
    ...processCreateParams(params)
  });
};
function createZodEnum(values, params) {
  return new ZodEnum({
    values,
    typeName: ZodFirstPartyTypeKind.ZodEnum,
    ...processCreateParams(params)
  });
}
var ZodEnum = class _ZodEnum extends ZodType {
  _parse(input) {
    if (typeof input.data !== "string") {
      const ctx = this._getOrReturnCtx(input);
      const expectedValues = this._def.values;
      addIssueToContext(ctx, {
        expected: util.joinValues(expectedValues),
        received: ctx.parsedType,
        code: ZodIssueCode.invalid_type
      });
      return INVALID;
    }
    if (!this._cache) {
      this._cache = new Set(this._def.values);
    }
    if (!this._cache.has(input.data)) {
      const ctx = this._getOrReturnCtx(input);
      const expectedValues = this._def.values;
      addIssueToContext(ctx, {
        received: ctx.data,
        code: ZodIssueCode.invalid_enum_value,
        options: expectedValues
      });
      return INVALID;
    }
    return OK(input.data);
  }
  get options() {
    return this._def.values;
  }
  get enum() {
    const enumValues = {};
    for (const val of this._def.values) {
      enumValues[val] = val;
    }
    return enumValues;
  }
  get Values() {
    const enumValues = {};
    for (const val of this._def.values) {
      enumValues[val] = val;
    }
    return enumValues;
  }
  get Enum() {
    const enumValues = {};
    for (const val of this._def.values) {
      enumValues[val] = val;
    }
    return enumValues;
  }
  extract(values, newDef = this._def) {
    return _ZodEnum.create(values, {
      ...this._def,
      ...newDef
    });
  }
  exclude(values, newDef = this._def) {
    return _ZodEnum.create(this.options.filter((opt) => !values.includes(opt)), {
      ...this._def,
      ...newDef
    });
  }
};
ZodEnum.create = createZodEnum;
var ZodNativeEnum = class extends ZodType {
  _parse(input) {
    const nativeEnumValues = util.getValidEnumValues(this._def.values);
    const ctx = this._getOrReturnCtx(input);
    if (ctx.parsedType !== ZodParsedType.string && ctx.parsedType !== ZodParsedType.number) {
      const expectedValues = util.objectValues(nativeEnumValues);
      addIssueToContext(ctx, {
        expected: util.joinValues(expectedValues),
        received: ctx.parsedType,
        code: ZodIssueCode.invalid_type
      });
      return INVALID;
    }
    if (!this._cache) {
      this._cache = new Set(util.getValidEnumValues(this._def.values));
    }
    if (!this._cache.has(input.data)) {
      const expectedValues = util.objectValues(nativeEnumValues);
      addIssueToContext(ctx, {
        received: ctx.data,
        code: ZodIssueCode.invalid_enum_value,
        options: expectedValues
      });
      return INVALID;
    }
    return OK(input.data);
  }
  get enum() {
    return this._def.values;
  }
};
ZodNativeEnum.create = (values, params) => {
  return new ZodNativeEnum({
    values,
    typeName: ZodFirstPartyTypeKind.ZodNativeEnum,
    ...processCreateParams(params)
  });
};
var ZodPromise = class extends ZodType {
  unwrap() {
    return this._def.type;
  }
  _parse(input) {
    const { ctx } = this._processInputParams(input);
    if (ctx.parsedType !== ZodParsedType.promise && ctx.common.async === false) {
      addIssueToContext(ctx, {
        code: ZodIssueCode.invalid_type,
        expected: ZodParsedType.promise,
        received: ctx.parsedType
      });
      return INVALID;
    }
    const promisified = ctx.parsedType === ZodParsedType.promise ? ctx.data : Promise.resolve(ctx.data);
    return OK(promisified.then((data) => {
      return this._def.type.parseAsync(data, {
        path: ctx.path,
        errorMap: ctx.common.contextualErrorMap
      });
    }));
  }
};
ZodPromise.create = (schema, params) => {
  return new ZodPromise({
    type: schema,
    typeName: ZodFirstPartyTypeKind.ZodPromise,
    ...processCreateParams(params)
  });
};
var ZodEffects = class extends ZodType {
  innerType() {
    return this._def.schema;
  }
  sourceType() {
    return this._def.schema._def.typeName === ZodFirstPartyTypeKind.ZodEffects ? this._def.schema.sourceType() : this._def.schema;
  }
  _parse(input) {
    const { status, ctx } = this._processInputParams(input);
    const effect = this._def.effect || null;
    const checkCtx = {
      addIssue: (arg) => {
        addIssueToContext(ctx, arg);
        if (arg.fatal) {
          status.abort();
        } else {
          status.dirty();
        }
      },
      get path() {
        return ctx.path;
      }
    };
    checkCtx.addIssue = checkCtx.addIssue.bind(checkCtx);
    if (effect.type === "preprocess") {
      const processed = effect.transform(ctx.data, checkCtx);
      if (ctx.common.async) {
        return Promise.resolve(processed).then(async (processed2) => {
          if (status.value === "aborted")
            return INVALID;
          const result = await this._def.schema._parseAsync({
            data: processed2,
            path: ctx.path,
            parent: ctx
          });
          if (result.status === "aborted")
            return INVALID;
          if (result.status === "dirty")
            return DIRTY(result.value);
          if (status.value === "dirty")
            return DIRTY(result.value);
          return result;
        });
      } else {
        if (status.value === "aborted")
          return INVALID;
        const result = this._def.schema._parseSync({
          data: processed,
          path: ctx.path,
          parent: ctx
        });
        if (result.status === "aborted")
          return INVALID;
        if (result.status === "dirty")
          return DIRTY(result.value);
        if (status.value === "dirty")
          return DIRTY(result.value);
        return result;
      }
    }
    if (effect.type === "refinement") {
      const executeRefinement = (acc) => {
        const result = effect.refinement(acc, checkCtx);
        if (ctx.common.async) {
          return Promise.resolve(result);
        }
        if (result instanceof Promise) {
          throw new Error("Async refinement encountered during synchronous parse operation. Use .parseAsync instead.");
        }
        return acc;
      };
      if (ctx.common.async === false) {
        const inner = this._def.schema._parseSync({
          data: ctx.data,
          path: ctx.path,
          parent: ctx
        });
        if (inner.status === "aborted")
          return INVALID;
        if (inner.status === "dirty")
          status.dirty();
        executeRefinement(inner.value);
        return { status: status.value, value: inner.value };
      } else {
        return this._def.schema._parseAsync({ data: ctx.data, path: ctx.path, parent: ctx }).then((inner) => {
          if (inner.status === "aborted")
            return INVALID;
          if (inner.status === "dirty")
            status.dirty();
          return executeRefinement(inner.value).then(() => {
            return { status: status.value, value: inner.value };
          });
        });
      }
    }
    if (effect.type === "transform") {
      if (ctx.common.async === false) {
        const base = this._def.schema._parseSync({
          data: ctx.data,
          path: ctx.path,
          parent: ctx
        });
        if (!isValid(base))
          return INVALID;
        const result = effect.transform(base.value, checkCtx);
        if (result instanceof Promise) {
          throw new Error(`Asynchronous transform encountered during synchronous parse operation. Use .parseAsync instead.`);
        }
        return { status: status.value, value: result };
      } else {
        return this._def.schema._parseAsync({ data: ctx.data, path: ctx.path, parent: ctx }).then((base) => {
          if (!isValid(base))
            return INVALID;
          return Promise.resolve(effect.transform(base.value, checkCtx)).then((result) => ({
            status: status.value,
            value: result
          }));
        });
      }
    }
    util.assertNever(effect);
  }
};
ZodEffects.create = (schema, effect, params) => {
  return new ZodEffects({
    schema,
    typeName: ZodFirstPartyTypeKind.ZodEffects,
    effect,
    ...processCreateParams(params)
  });
};
ZodEffects.createWithPreprocess = (preprocess, schema, params) => {
  return new ZodEffects({
    schema,
    effect: { type: "preprocess", transform: preprocess },
    typeName: ZodFirstPartyTypeKind.ZodEffects,
    ...processCreateParams(params)
  });
};
var ZodOptional = class extends ZodType {
  _parse(input) {
    const parsedType = this._getType(input);
    if (parsedType === ZodParsedType.undefined) {
      return OK(void 0);
    }
    return this._def.innerType._parse(input);
  }
  unwrap() {
    return this._def.innerType;
  }
};
ZodOptional.create = (type, params) => {
  return new ZodOptional({
    innerType: type,
    typeName: ZodFirstPartyTypeKind.ZodOptional,
    ...processCreateParams(params)
  });
};
var ZodNullable = class extends ZodType {
  _parse(input) {
    const parsedType = this._getType(input);
    if (parsedType === ZodParsedType.null) {
      return OK(null);
    }
    return this._def.innerType._parse(input);
  }
  unwrap() {
    return this._def.innerType;
  }
};
ZodNullable.create = (type, params) => {
  return new ZodNullable({
    innerType: type,
    typeName: ZodFirstPartyTypeKind.ZodNullable,
    ...processCreateParams(params)
  });
};
var ZodDefault = class extends ZodType {
  _parse(input) {
    const { ctx } = this._processInputParams(input);
    let data = ctx.data;
    if (ctx.parsedType === ZodParsedType.undefined) {
      data = this._def.defaultValue();
    }
    return this._def.innerType._parse({
      data,
      path: ctx.path,
      parent: ctx
    });
  }
  removeDefault() {
    return this._def.innerType;
  }
};
ZodDefault.create = (type, params) => {
  return new ZodDefault({
    innerType: type,
    typeName: ZodFirstPartyTypeKind.ZodDefault,
    defaultValue: typeof params.default === "function" ? params.default : () => params.default,
    ...processCreateParams(params)
  });
};
var ZodCatch = class extends ZodType {
  _parse(input) {
    const { ctx } = this._processInputParams(input);
    const newCtx = {
      ...ctx,
      common: {
        ...ctx.common,
        issues: []
      }
    };
    const result = this._def.innerType._parse({
      data: newCtx.data,
      path: newCtx.path,
      parent: {
        ...newCtx
      }
    });
    if (isAsync(result)) {
      return result.then((result2) => {
        return {
          status: "valid",
          value: result2.status === "valid" ? result2.value : this._def.catchValue({
            get error() {
              return new ZodError(newCtx.common.issues);
            },
            input: newCtx.data
          })
        };
      });
    } else {
      return {
        status: "valid",
        value: result.status === "valid" ? result.value : this._def.catchValue({
          get error() {
            return new ZodError(newCtx.common.issues);
          },
          input: newCtx.data
        })
      };
    }
  }
  removeCatch() {
    return this._def.innerType;
  }
};
ZodCatch.create = (type, params) => {
  return new ZodCatch({
    innerType: type,
    typeName: ZodFirstPartyTypeKind.ZodCatch,
    catchValue: typeof params.catch === "function" ? params.catch : () => params.catch,
    ...processCreateParams(params)
  });
};
var ZodNaN = class extends ZodType {
  _parse(input) {
    const parsedType = this._getType(input);
    if (parsedType !== ZodParsedType.nan) {
      const ctx = this._getOrReturnCtx(input);
      addIssueToContext(ctx, {
        code: ZodIssueCode.invalid_type,
        expected: ZodParsedType.nan,
        received: ctx.parsedType
      });
      return INVALID;
    }
    return { status: "valid", value: input.data };
  }
};
ZodNaN.create = (params) => {
  return new ZodNaN({
    typeName: ZodFirstPartyTypeKind.ZodNaN,
    ...processCreateParams(params)
  });
};
var BRAND = Symbol("zod_brand");
var ZodBranded = class extends ZodType {
  _parse(input) {
    const { ctx } = this._processInputParams(input);
    const data = ctx.data;
    return this._def.type._parse({
      data,
      path: ctx.path,
      parent: ctx
    });
  }
  unwrap() {
    return this._def.type;
  }
};
var ZodPipeline = class _ZodPipeline extends ZodType {
  _parse(input) {
    const { status, ctx } = this._processInputParams(input);
    if (ctx.common.async) {
      const handleAsync = async () => {
        const inResult = await this._def.in._parseAsync({
          data: ctx.data,
          path: ctx.path,
          parent: ctx
        });
        if (inResult.status === "aborted")
          return INVALID;
        if (inResult.status === "dirty") {
          status.dirty();
          return DIRTY(inResult.value);
        } else {
          return this._def.out._parseAsync({
            data: inResult.value,
            path: ctx.path,
            parent: ctx
          });
        }
      };
      return handleAsync();
    } else {
      const inResult = this._def.in._parseSync({
        data: ctx.data,
        path: ctx.path,
        parent: ctx
      });
      if (inResult.status === "aborted")
        return INVALID;
      if (inResult.status === "dirty") {
        status.dirty();
        return {
          status: "dirty",
          value: inResult.value
        };
      } else {
        return this._def.out._parseSync({
          data: inResult.value,
          path: ctx.path,
          parent: ctx
        });
      }
    }
  }
  static create(a, b) {
    return new _ZodPipeline({
      in: a,
      out: b,
      typeName: ZodFirstPartyTypeKind.ZodPipeline
    });
  }
};
var ZodReadonly = class extends ZodType {
  _parse(input) {
    const result = this._def.innerType._parse(input);
    const freeze = (data) => {
      if (isValid(data)) {
        data.value = Object.freeze(data.value);
      }
      return data;
    };
    return isAsync(result) ? result.then((data) => freeze(data)) : freeze(result);
  }
  unwrap() {
    return this._def.innerType;
  }
};
ZodReadonly.create = (type, params) => {
  return new ZodReadonly({
    innerType: type,
    typeName: ZodFirstPartyTypeKind.ZodReadonly,
    ...processCreateParams(params)
  });
};
function cleanParams(params, data) {
  const p = typeof params === "function" ? params(data) : typeof params === "string" ? { message: params } : params;
  const p2 = typeof p === "string" ? { message: p } : p;
  return p2;
}
function custom(check, _params = {}, fatal) {
  if (check)
    return ZodAny.create().superRefine((data, ctx) => {
      const r = check(data);
      if (r instanceof Promise) {
        return r.then((r2) => {
          if (!r2) {
            const params = cleanParams(_params, data);
            const _fatal = params.fatal ?? fatal ?? true;
            ctx.addIssue({ code: "custom", ...params, fatal: _fatal });
          }
        });
      }
      if (!r) {
        const params = cleanParams(_params, data);
        const _fatal = params.fatal ?? fatal ?? true;
        ctx.addIssue({ code: "custom", ...params, fatal: _fatal });
      }
      return;
    });
  return ZodAny.create();
}
var late = {
  object: ZodObject.lazycreate
};
var ZodFirstPartyTypeKind;
(function(ZodFirstPartyTypeKind2) {
  ZodFirstPartyTypeKind2["ZodString"] = "ZodString";
  ZodFirstPartyTypeKind2["ZodNumber"] = "ZodNumber";
  ZodFirstPartyTypeKind2["ZodNaN"] = "ZodNaN";
  ZodFirstPartyTypeKind2["ZodBigInt"] = "ZodBigInt";
  ZodFirstPartyTypeKind2["ZodBoolean"] = "ZodBoolean";
  ZodFirstPartyTypeKind2["ZodDate"] = "ZodDate";
  ZodFirstPartyTypeKind2["ZodSymbol"] = "ZodSymbol";
  ZodFirstPartyTypeKind2["ZodUndefined"] = "ZodUndefined";
  ZodFirstPartyTypeKind2["ZodNull"] = "ZodNull";
  ZodFirstPartyTypeKind2["ZodAny"] = "ZodAny";
  ZodFirstPartyTypeKind2["ZodUnknown"] = "ZodUnknown";
  ZodFirstPartyTypeKind2["ZodNever"] = "ZodNever";
  ZodFirstPartyTypeKind2["ZodVoid"] = "ZodVoid";
  ZodFirstPartyTypeKind2["ZodArray"] = "ZodArray";
  ZodFirstPartyTypeKind2["ZodObject"] = "ZodObject";
  ZodFirstPartyTypeKind2["ZodUnion"] = "ZodUnion";
  ZodFirstPartyTypeKind2["ZodDiscriminatedUnion"] = "ZodDiscriminatedUnion";
  ZodFirstPartyTypeKind2["ZodIntersection"] = "ZodIntersection";
  ZodFirstPartyTypeKind2["ZodTuple"] = "ZodTuple";
  ZodFirstPartyTypeKind2["ZodRecord"] = "ZodRecord";
  ZodFirstPartyTypeKind2["ZodMap"] = "ZodMap";
  ZodFirstPartyTypeKind2["ZodSet"] = "ZodSet";
  ZodFirstPartyTypeKind2["ZodFunction"] = "ZodFunction";
  ZodFirstPartyTypeKind2["ZodLazy"] = "ZodLazy";
  ZodFirstPartyTypeKind2["ZodLiteral"] = "ZodLiteral";
  ZodFirstPartyTypeKind2["ZodEnum"] = "ZodEnum";
  ZodFirstPartyTypeKind2["ZodEffects"] = "ZodEffects";
  ZodFirstPartyTypeKind2["ZodNativeEnum"] = "ZodNativeEnum";
  ZodFirstPartyTypeKind2["ZodOptional"] = "ZodOptional";
  ZodFirstPartyTypeKind2["ZodNullable"] = "ZodNullable";
  ZodFirstPartyTypeKind2["ZodDefault"] = "ZodDefault";
  ZodFirstPartyTypeKind2["ZodCatch"] = "ZodCatch";
  ZodFirstPartyTypeKind2["ZodPromise"] = "ZodPromise";
  ZodFirstPartyTypeKind2["ZodBranded"] = "ZodBranded";
  ZodFirstPartyTypeKind2["ZodPipeline"] = "ZodPipeline";
  ZodFirstPartyTypeKind2["ZodReadonly"] = "ZodReadonly";
})(ZodFirstPartyTypeKind || (ZodFirstPartyTypeKind = {}));
var instanceOfType = (cls, params = {
  message: `Input not instance of ${cls.name}`
}) => custom((data) => data instanceof cls, params);
var stringType = ZodString.create;
var numberType = ZodNumber.create;
var nanType = ZodNaN.create;
var bigIntType = ZodBigInt.create;
var booleanType = ZodBoolean.create;
var dateType = ZodDate.create;
var symbolType = ZodSymbol.create;
var undefinedType = ZodUndefined.create;
var nullType = ZodNull.create;
var anyType = ZodAny.create;
var unknownType = ZodUnknown.create;
var neverType = ZodNever.create;
var voidType = ZodVoid.create;
var arrayType = ZodArray.create;
var objectType = ZodObject.create;
var strictObjectType = ZodObject.strictCreate;
var unionType = ZodUnion.create;
var discriminatedUnionType = ZodDiscriminatedUnion.create;
var intersectionType = ZodIntersection.create;
var tupleType = ZodTuple.create;
var recordType = ZodRecord.create;
var mapType = ZodMap.create;
var setType = ZodSet.create;
var functionType = ZodFunction.create;
var lazyType = ZodLazy.create;
var literalType = ZodLiteral.create;
var enumType = ZodEnum.create;
var nativeEnumType = ZodNativeEnum.create;
var promiseType = ZodPromise.create;
var effectsType = ZodEffects.create;
var optionalType = ZodOptional.create;
var nullableType = ZodNullable.create;
var preprocessType = ZodEffects.createWithPreprocess;
var pipelineType = ZodPipeline.create;
var ostring = () => stringType().optional();
var onumber = () => numberType().optional();
var oboolean = () => booleanType().optional();
var coerce = {
  string: ((arg) => ZodString.create({ ...arg, coerce: true })),
  number: ((arg) => ZodNumber.create({ ...arg, coerce: true })),
  boolean: ((arg) => ZodBoolean.create({
    ...arg,
    coerce: true
  })),
  bigint: ((arg) => ZodBigInt.create({ ...arg, coerce: true })),
  date: ((arg) => ZodDate.create({ ...arg, coerce: true }))
};
var NEVER = INVALID;

// src/core/screenshot-budget.ts
var MAX_SCREENSHOT_PIXELS = 2097152;
var MAX_SCREENSHOT_EDGE = 8192;
var MAX_FULLPAGE_SCREENSHOT_PIXELS = 16777216;
var MAX_SCREENSHOT_BYTES = 4 * 1024 * 1024;
function assertScreenshotDimensions(width, height, context) {
  if (!Number.isFinite(width) || !Number.isFinite(height) || width <= 0 || height <= 0) {
    throw new BrowserRuntimeError(
      "OPERATION_FAILED",
      `Chrome returned invalid ${context} screenshot dimensions`
    );
  }
}
function assertScreenshotBudget(width, height, context, maxPixels = MAX_SCREENSHOT_PIXELS) {
  assertScreenshotDimensions(width, height, context);
  if (width > MAX_SCREENSHOT_EDGE || height > MAX_SCREENSHOT_EDGE || width * height > maxPixels) {
    throw new BrowserRuntimeError(
      "INVALID_ARGUMENT",
      `${context} screenshot dimensions ${Math.ceil(width)}x${Math.ceil(height)} exceed the ${maxPixels}-pixel capture budget; use a smaller viewport or clip`,
      {
        width,
        height,
        maxPixels,
        maxEdge: MAX_SCREENSHOT_EDGE
      }
    );
  }
}

// src/core/schemas.ts
var id = external_exports.string().min(1).max(200);
var timeoutMs = external_exports.number().int().positive().max(12e4);
var waitUntil = external_exports.enum(["commit", "domcontentloaded", "load", "networkidle"]);
var loadState = external_exports.enum(["domcontentloaded", "load", "networkidle"]);
var matcher2 = external_exports.union([
  external_exports.string().min(1).max(2e4),
  external_exports.object({
    regex: external_exports.string().min(1).max(2e4),
    flags: external_exports.string().regex(/^[dimsuv]*$/, "Locator matcher flags must not include g or y").optional()
  }).strict()
]);
function locatorStepSchema(depth) {
  if (depth > 32) return external_exports.never();
  const nestedSteps = external_exports.array(external_exports.lazy(() => locatorStepSchema(depth + 1))).min(1).max(32);
  const step = external_exports.discriminatedUnion("kind", [
    external_exports.object({
      kind: external_exports.literal("locator"),
      selector: external_exports.string().min(1).max(2e4)
    }).strict(),
    external_exports.object({
      kind: external_exports.literal("frame"),
      selector: external_exports.string().min(1).max(2e4)
    }).strict(),
    external_exports.object({
      kind: external_exports.literal("getByRole"),
      role: external_exports.string().min(1).max(100),
      name: matcher2.optional(),
      exact: external_exports.boolean().optional()
    }).strict(),
    external_exports.object({
      kind: external_exports.literal("getByText"),
      text: matcher2,
      exact: external_exports.boolean().optional()
    }).strict(),
    external_exports.object({
      kind: external_exports.literal("getByLabel"),
      text: matcher2,
      exact: external_exports.boolean().optional()
    }).strict(),
    external_exports.object({
      kind: external_exports.literal("getByPlaceholder"),
      text: matcher2,
      exact: external_exports.boolean().optional()
    }).strict(),
    external_exports.object({
      kind: external_exports.literal("getByTestId"),
      testId: external_exports.string().max(2e4)
    }).strict(),
    external_exports.object({
      kind: external_exports.literal("filter"),
      hasText: matcher2.optional(),
      hasNotText: matcher2.optional(),
      has: nestedSteps.optional(),
      hasNot: nestedSteps.optional(),
      visible: external_exports.boolean().optional()
    }).strict(),
    external_exports.object({ kind: external_exports.literal("first") }).strict(),
    external_exports.object({ kind: external_exports.literal("last") }).strict(),
    external_exports.object({
      kind: external_exports.literal("nth"),
      index: external_exports.number().int().min(-1e4).max(1e4)
    }).strict(),
    external_exports.object({
      kind: external_exports.literal("and"),
      steps: nestedSteps
    }).strict(),
    external_exports.object({
      kind: external_exports.literal("or"),
      steps: nestedSteps
    }).strict()
  ]);
  return step.superRefine((value, ctx) => {
    if ((value.kind === "getByRole" || value.kind === "getByText" || value.kind === "getByLabel" || value.kind === "getByPlaceholder") && value.exact === true) {
      const text = value.kind === "getByRole" ? value.name : value.text;
      if (typeof text === "object") {
        ctx.addIssue({
          code: external_exports.ZodIssueCode.custom,
          message: "exact matching requires a plain string, not a regex"
        });
      }
    }
  });
}
var locatorStepsSchema = external_exports.array(locatorStepSchema(1)).min(1).max(32);
var browserArgs = external_exports.object({ browserId: id }).strict();
var userTabInfo = external_exports.object({
  id,
  title: external_exports.string().max(2e4).nullable().optional(),
  url: external_exports.string().max(2e4).nullable().optional(),
  lastOpened: external_exports.string().max(200).optional(),
  tabGroup: external_exports.string().max(1e3).optional()
}).strict();
var historyQuery = external_exports.string().max(1e3).refine(
  (value) => value.trim() !== "",
  "History query terms must not be empty"
);
var historyTimestamp = external_exports.union([external_exports.string().max(200), external_exports.date()]).refine((value) => {
  const timestamp = value instanceof Date ? value.getTime() : Date.parse(value);
  return Number.isFinite(timestamp);
}, "History timestamps must be valid dates");
var historyOptions = external_exports.object({
  limit: external_exports.number().int().positive().max(500).optional(),
  queries: external_exports.array(historyQuery).min(1).max(20).optional(),
  from: historyTimestamp.optional(),
  to: historyTimestamp.optional()
}).strict();
var tabArgs = external_exports.object({ tabId: id }).strict();
var locatorFields = {
  tabId: id,
  steps: locatorStepsSchema
};
var locatorArgs = external_exports.object(locatorFields).strict();
var locatorTimeoutArgs = external_exports.object({ ...locatorFields, timeoutMs: timeoutMs.optional() }).strict();
var navigationOptions2 = {
  waitUntil: waitUntil.optional(),
  timeoutMs: timeoutMs.optional()
};
var locatorActionOptions = {
  timeoutMs: timeoutMs.optional()
};
var screenshotClip = external_exports.object({
  x: external_exports.number().finite().nonnegative(),
  y: external_exports.number().finite().nonnegative(),
  width: external_exports.number().finite().positive().max(MAX_SCREENSHOT_EDGE),
  height: external_exports.number().finite().positive().max(MAX_SCREENSHOT_EDGE)
}).strict().refine((value) => value.width * value.height <= MAX_SCREENSHOT_PIXELS, {
  message: `Screenshot clip must not exceed ${MAX_SCREENSHOT_PIXELS} pixels`
});
var selectOptionDescriptor = external_exports.object({
  index: external_exports.number().int().nonnegative().max(1e5).optional(),
  label: external_exports.string().max(2e4).optional(),
  value: external_exports.string().max(2e4).optional()
}).strict().refine(
  (value) => value.index !== void 0 || value.label !== void 0 || value.value !== void 0,
  {
    message: "A select option descriptor must include index, label, or value"
  }
);
var selectOption = external_exports.union([external_exports.string().max(2e4), selectOptionDescriptor]);
var mouseButtonName = external_exports.enum(["left", "middle", "right"]);
var modifierNames = external_exports.array(external_exports.enum(["Alt", "Control", "ControlOrMeta", "Meta", "Shift"])).max(4);
var scrollDelta = external_exports.number().finite().min(-1e5).max(1e5);
var coordinate = external_exports.number().finite().nonnegative().max(1e5);
var coordinatePoint = external_exports.object({ x: coordinate, y: coordinate }).strict();
var keyNames = external_exports.array(external_exports.string().min(1).max(100)).min(1).max(32);
var optionalKeyNames = external_exports.array(external_exports.string().min(1).max(100)).max(8).optional();
var domNodeId = external_exports.string().regex(SNAPSHOT_REF_PATTERN);
var evaluateScript = external_exports.string().min(1).max(512e3);
var pageEvaluateArgs = external_exports.object({
  tabId: id,
  script: evaluateScript,
  timeoutMs: timeoutMs.optional()
}).strict();
var locatorEvaluateArgs = external_exports.object({
  ...locatorFields,
  script: evaluateScript,
  timeoutMs: timeoutMs.optional()
}).strict();
var commandSchemas = {
  "browsers.list": external_exports.object({}).strict(),
  "browsers.get": external_exports.object({ id }).strict(),
  "browser.documentation": browserArgs,
  "browser.nameSession": external_exports.object({ browserId: id, name: external_exports.string().min(1).max(200) }).strict(),
  "browser.user.openTabs": browserArgs,
  "browser.user.claimTab": external_exports.object({ browserId: id, tab: external_exports.union([id, userTabInfo]) }).strict(),
  "browser.user.history": external_exports.object({ browserId: id, options: historyOptions.optional() }).strict(),
  "tabs.new": browserArgs,
  "tabs.list": browserArgs,
  "tabs.get": external_exports.object({ browserId: id, tabId: id }).strict(),
  "tabs.selected": browserArgs,
  "tabs.finalize": external_exports.object({
    browserId: id,
    keep: external_exports.array(
      external_exports.object({
        tabId: id,
        status: external_exports.enum(FINALIZE_TAB_STATUSES)
      }).strict()
    ).max(100).optional()
  }).strict(),
  "tab.goto": external_exports.object({
    tabId: id,
    url: external_exports.string().min(1).max(2e4)
  }).strict(),
  "tab.url": tabArgs,
  "tab.title": tabArgs,
  "tab.back": tabArgs,
  "tab.forward": tabArgs,
  "tab.reload": tabArgs,
  "tab.close": tabArgs,
  "tab.screenshot": external_exports.object({
    tabId: id,
    clip: screenshotClip.optional(),
    fullPage: external_exports.boolean().optional()
  }).strict().refine((value) => value.clip === void 0 || value.fullPage !== true, {
    message: "Screenshot clip and fullPage cannot be used together"
  }),
  "cua.click": external_exports.object({
    tabId: id,
    x: coordinate,
    y: coordinate,
    button: external_exports.number().int().min(1).max(5).optional(),
    keypress: optionalKeyNames
  }).strict(),
  "cua.double_click": external_exports.object({
    tabId: id,
    x: coordinate,
    y: coordinate,
    keypress: optionalKeyNames
  }).strict(),
  "cua.drag": external_exports.object({
    tabId: id,
    path: external_exports.array(coordinatePoint).min(2).max(512),
    keys: optionalKeyNames
  }).strict(),
  "cua.keypress": external_exports.object({ tabId: id, keys: keyNames }).strict(),
  "cua.move": external_exports.object({ tabId: id, x: coordinate, y: coordinate, keys: optionalKeyNames }).strict(),
  "cua.scroll": external_exports.object({
    tabId: id,
    x: coordinate,
    y: coordinate,
    scrollX: scrollDelta,
    scrollY: scrollDelta,
    keypress: optionalKeyNames
  }).strict(),
  "cua.type": external_exports.object({ tabId: id, text: external_exports.string().max(1e6) }).strict(),
  "dom_cua.get_visible_dom": tabArgs,
  "dom_cua.click": external_exports.object({
    tabId: id,
    node_id: domNodeId
  }).strict(),
  "dom_cua.double_click": external_exports.object({
    tabId: id,
    node_id: domNodeId
  }).strict(),
  "dom_cua.type": external_exports.object({
    tabId: id,
    text: external_exports.string().max(1e6)
  }).strict(),
  "dom_cua.keypress": external_exports.object({
    tabId: id,
    keys: keyNames
  }).strict(),
  "dom_cua.scroll": external_exports.object({
    tabId: id,
    node_id: domNodeId.optional(),
    x: scrollDelta,
    y: scrollDelta
  }).strict(),
  "tab.getJsDialog": tabArgs,
  "tab.dialog.accept": external_exports.object({
    tabId: id,
    dialogId: id,
    promptText: external_exports.string().max(2e4).optional()
  }).strict(),
  "tab.dialog.dismiss": external_exports.object({ tabId: id, dialogId: id }).strict(),
  "dev.logs": external_exports.object({
    tabId: id,
    filter: external_exports.string().max(1e3).optional(),
    levels: external_exports.array(external_exports.enum(["debug", "info", "log", "warn", "warning", "error"])).max(6).optional(),
    limit: external_exports.number().int().min(1).max(1e3).optional()
  }).strict(),
  "playwright.domSnapshot": tabArgs,
  "playwright.evaluate": pageEvaluateArgs,
  "playwright.waitForEvent": external_exports.object({
    tabId: id,
    event: external_exports.enum(["download", "filechooser"]),
    timeoutMs: timeoutMs.optional()
  }).strict(),
  "fileChooser.setFiles": external_exports.object({
    tabId: id,
    chooserId: id,
    files: external_exports.array(external_exports.string().min(1).max(4096)).min(1).max(50),
    timeoutMs: timeoutMs.optional()
  }).strict(),
  "playwright.waitForURL": external_exports.object({
    tabId: id,
    url: external_exports.string().min(1).max(2e4),
    ...navigationOptions2
  }).strict(),
  "playwright.expectNavigation.begin": external_exports.object({
    tabId: id,
    url: external_exports.string().min(1).max(2e4).optional(),
    waitUntil: loadState.optional(),
    timeoutMs: timeoutMs.optional()
  }).strict(),
  "playwright.expectNavigation.wait": external_exports.object({ tabId: id, waiterId: id }).strict(),
  "playwright.expectNavigation.cancel": external_exports.object({ tabId: id, waiterId: id }).strict(),
  "playwright.waitForLoadState": external_exports.object({
    tabId: id,
    state: loadState.optional(),
    timeoutMs: timeoutMs.optional()
  }).strict(),
  "playwright.waitForTimeout": external_exports.object({
    tabId: id,
    timeoutMs: external_exports.number().int().nonnegative().max(12e4)
  }).strict(),
  "locator.count": locatorArgs,
  "locator.evaluate": locatorEvaluateArgs,
  "locator.evaluateAll": locatorEvaluateArgs,
  "locator.allTextContents": locatorTimeoutArgs,
  "locator.innerText": locatorTimeoutArgs,
  "locator.textContent": locatorTimeoutArgs,
  "locator.getAttribute": external_exports.object({
    ...locatorFields,
    name: external_exports.string().min(1).max(1e3),
    timeoutMs: timeoutMs.optional()
  }).strict(),
  "locator.isEnabled": locatorArgs,
  "locator.isVisible": locatorArgs,
  "locator.click": external_exports.object({
    ...locatorFields,
    button: mouseButtonName.optional(),
    modifiers: modifierNames.optional(),
    force: external_exports.boolean().optional(),
    ...locatorActionOptions
  }).strict(),
  "locator.dblclick": external_exports.object({
    ...locatorFields,
    button: mouseButtonName.optional(),
    modifiers: modifierNames.optional(),
    force: external_exports.boolean().optional(),
    ...locatorActionOptions
  }).strict(),
  "locator.downloadMedia": locatorTimeoutArgs,
  "locator.fill": external_exports.object({
    ...locatorFields,
    value: external_exports.string().max(1e6),
    timeoutMs: timeoutMs.optional()
  }).strict(),
  "locator.type": external_exports.object({
    ...locatorFields,
    // pressSequentially pays a CDP round trip per character, so past this
    // length the per-character budget cannot fit inside the 120s ceiling
    // and typing would abort mid-string; fill has no per-character cost.
    value: external_exports.string().max(6e4),
    timeoutMs: timeoutMs.optional()
  }).strict(),
  "locator.press": external_exports.object({
    ...locatorFields,
    value: external_exports.string().min(1).max(200),
    timeoutMs: timeoutMs.optional()
  }).strict(),
  "locator.selectOption": external_exports.object({
    ...locatorFields,
    value: external_exports.union([selectOption, external_exports.array(selectOption).min(1).max(1e3)]),
    timeoutMs: timeoutMs.optional()
  }).strict(),
  "locator.check": external_exports.object({
    ...locatorFields,
    force: external_exports.boolean().optional(),
    ...locatorActionOptions
  }).strict(),
  "locator.uncheck": external_exports.object({
    ...locatorFields,
    force: external_exports.boolean().optional(),
    ...locatorActionOptions
  }).strict(),
  "locator.setChecked": external_exports.object({
    ...locatorFields,
    checked: external_exports.boolean(),
    force: external_exports.boolean().optional(),
    ...locatorActionOptions
  }).strict(),
  "locator.waitFor": external_exports.object({
    ...locatorFields,
    state: external_exports.enum(["attached", "detached", "visible", "hidden"]),
    timeoutMs: timeoutMs.optional()
  }).strict()
};

// src/playwright/input-operations.ts
import { platform } from "node:os";

// src/playwright/runtime-helpers.ts
import { clearTimeout as clearTimeout3, setTimeout as setTimeout3 } from "node:timers";
var DEFAULT_TIMEOUT_MS = 3e4;
var MAX_TAB_TEXT_LENGTH = 2e4;
function providerTabs(value) {
  if (!Array.isArray(value))
    throw new BrowserRuntimeError(
      "OPERATION_FAILED",
      "Chrome extension returned an invalid tab list"
    );
  return value.map(providerTab);
}
function providerTab(value) {
  const tab = record(value);
  if (typeof tab.providerTabId !== "number")
    throw new BrowserRuntimeError(
      "OPERATION_FAILED",
      "Chrome extension returned an invalid tab"
    );
  return {
    providerTabId: tab.providerTabId,
    title: typeof tab.title === "string" ? tab.title.slice(0, MAX_TAB_TEXT_LENGTH) : null,
    url: typeof tab.url === "string" ? tab.url.slice(0, MAX_TAB_TEXT_LENGTH) : null,
    ...typeof tab.active === "boolean" ? { active: tab.active } : {},
    ...typeof tab.lastOpened === "string" ? { lastOpened: tab.lastOpened } : {},
    ...typeof tab.tabGroup === "string" ? { tabGroup: tab.tabGroup } : {},
    ...typeof tab.derivedFromProviderTabId === "number" ? { derivedFromProviderTabId: tab.derivedFromProviderTabId } : {}
  };
}
function orderOpenTabs(tabs) {
  const opened = (tab) => {
    const time = tab.lastOpened === void 0 ? NaN : Date.parse(tab.lastOpened);
    return Number.isNaN(time) ? -Infinity : time;
  };
  return tabs.filter((tab) => tab.url === null || /^https?:/.test(tab.url)).sort((left, right) => {
    const a = opened(left);
    const b = opened(right);
    return a === b ? 0 : a > b ? -1 : 1;
  });
}
function matcher3(value) {
  if (typeof value === "string") return value;
  try {
    return new RegExp(value.regex, value.flags ?? "");
  } catch (error) {
    throw new BrowserRuntimeError(
      "INVALID_ARGUMENT",
      `Invalid locator regex: ${error.message}`
    );
  }
}
function navigationOptions3(args) {
  return {
    timeout: timeoutArg(args),
    waitUntil: args.waitUntil === "commit" || args.waitUntil === "domcontentloaded" || args.waitUntil === "networkidle" ? args.waitUntil : "load"
  };
}
function loadState2(value) {
  return value === "domcontentloaded" || value === "networkidle" ? value : "load";
}
function timeoutArg(args, fallback = DEFAULT_TIMEOUT_MS) {
  return typeof args.timeoutMs === "number" ? args.timeoutMs : fallback;
}
function timeoutOption(args) {
  return { timeout: timeoutArg(args) };
}
function clickOptions2(args, fallback = DEFAULT_TIMEOUT_MS) {
  return {
    timeout: timeoutArg(args, fallback),
    button: mouseButton(args.button),
    modifiers: modifiers(args.modifiers),
    ...args.force === true ? { force: true } : {}
  };
}
function mouseButton(value) {
  switch (value) {
    case void 0:
    case 1:
    case "left":
      return "left";
    case 2:
    case "middle":
      return "middle";
    case 3:
    case "right":
      return "right";
    default:
      throw new BrowserRuntimeError(
        "INVALID_ARGUMENT",
        "Playwright supports left, middle and right mouse buttons"
      );
  }
}
function modifiers(value) {
  if (value === void 0) return [];
  const values = stringArray(value);
  return values.map((item) => {
    const normalized = item.replace(/[\s_-]/g, "").toLowerCase();
    if (normalized === "alt" || normalized === "option") return "Alt";
    if (normalized === "control" || normalized === "ctrl") return "Control";
    if (normalized === "meta" || normalized === "command" || normalized === "cmd")
      return "Meta";
    if (normalized === "shift") return "Shift";
    if (normalized === "controlormeta" || normalized === "ctrlormeta")
      return "ControlOrMeta";
    throw new BrowserRuntimeError(
      "INVALID_ARGUMENT",
      `Unsupported modifier key: ${item}`
    );
  });
}
async function withModifiers(page, value, action) {
  const keys = modifiers(value);
  const pressed = [];
  try {
    for (const key of keys) {
      pressed.push(key);
      await page.keyboard.down(key);
    }
    await action();
  } finally {
    await Promise.allSettled(
      pressed.reverse().map((key) => page.keyboard.up(key))
    );
  }
}
async function pressKeyChord(page, value) {
  const keys = stringArray(value);
  const chord = keys.join("+");
  try {
    await page.keyboard.press(chord);
  } catch (error) {
    await releaseChordKeys(page, chordTokens(chord));
    throw error;
  }
}
function chordTokens(chord) {
  const tokens = [];
  let building = "";
  for (const char of chord) {
    if (char === "+" && building !== "") {
      tokens.push(building);
      building = "";
    } else {
      building += char;
    }
  }
  tokens.push(building);
  return tokens;
}
async function releaseChordKeys(page, chord) {
  await Promise.allSettled(
    [...chord].reverse().map((key) => page.keyboard.up(key))
  );
}
function selectOptions(value) {
  if (typeof value === "string") return value;
  if (Array.isArray(value)) {
    const mapped = value.map(
      (item) => typeof item === "string" ? item : selectOptionRecord(item)
    );
    if (mapped.some((item) => typeof item !== "string"))
      return mapped.map(
        (item) => typeof item === "string" ? { value: item } : item
      );
    return mapped;
  }
  return selectOptionRecord(value);
}
function selectOptionRecord(value) {
  const item = record(value);
  return {
    ...typeof item.value === "string" ? { value: item.value } : {},
    ...typeof item.label === "string" ? { label: item.label } : {},
    ...typeof item.index === "number" ? { index: item.index } : {}
  };
}
function consoleLevel(value) {
  if (value === "warning") return "warn";
  if (value === "debug" || value === "info" || value === "warn" || value === "error")
    return value;
  return "log";
}
function isClip(value) {
  const clip = record(value);
  return typeof clip.x === "number" && typeof clip.y === "number" && typeof clip.width === "number" && typeof clip.height === "number";
}
function jpegDimensions(buffer) {
  if (buffer.length >= 4 && buffer.readUInt16BE(0) === 65496) {
    let offset = 2;
    while (offset + 4 <= buffer.length && buffer[offset] === 255) {
      const marker = buffer[offset + 1];
      if (marker === 255) {
        offset++;
        continue;
      }
      if (marker === 218 || marker === 217) break;
      const length = buffer.readUInt16BE(offset + 2);
      if (length < 2 || offset + 2 + length > buffer.length) break;
      if (marker === 192 || marker === 193 || marker === 194) {
        if (length < 8) break;
        return {
          width: buffer.readUInt16BE(offset + 7),
          height: buffer.readUInt16BE(offset + 5)
        };
      }
      offset += 2 + length;
    }
  }
  throw new BrowserRuntimeError(
    "OPERATION_FAILED",
    "Chrome returned an invalid JPEG screenshot"
  );
}
function staleTabError() {
  return new BrowserRuntimeError(
    "STALE_TAB",
    "The Chrome tab is closed or stale; claim a tab again to continue"
  );
}
function isoTimestamp(value) {
  if (value instanceof Date) return value.toISOString();
  if (typeof value === "string") return new Date(value).toISOString();
  throw new BrowserRuntimeError(
    "INVALID_ARGUMENT",
    "Expected an ISO date string"
  );
}
function stringArg(args, name) {
  const value = args[name];
  if (typeof value !== "string")
    throw new BrowserRuntimeError(
      "INVALID_ARGUMENT",
      `Missing string argument: ${name}`
    );
  return value;
}
function numberArg(args, name) {
  const value = args[name];
  if (typeof value !== "number")
    throw new BrowserRuntimeError(
      "INVALID_ARGUMENT",
      `Missing number argument: ${name}`
    );
  return value;
}
function stringArray(value) {
  if (!Array.isArray(value) || value.some((item) => typeof item !== "string"))
    throw new BrowserRuntimeError(
      "INVALID_ARGUMENT",
      "Expected a string array"
    );
  return value;
}
function record(value) {
  return value !== null && typeof value === "object" ? value : {};
}
function pushBounded(array, value, limit = 1e3) {
  array.push(value);
  if (array.length > limit) array.splice(0, array.length - limit);
}
function jsonResult(value) {
  if (value === void 0) return null;
  try {
    return JSON.parse(JSON.stringify(value));
  } catch {
    throw new BrowserRuntimeError(
      "OPERATION_FAILED",
      "Browser evaluation returned a non-serializable value"
    );
  }
}
async function withTimeout(promise, timeout) {
  let timer;
  try {
    return await Promise.race([
      promise,
      new Promise((_resolve, reject) => {
        timer = setTimeout3(
          () => reject(
            new BrowserRuntimeError(
              "OPERATION_TIMEOUT",
              `Operation timed out after ${timeout}ms`
            )
          ),
          timeout
        );
      })
    ]);
  } finally {
    if (timer !== void 0) clearTimeout3(timer);
  }
}
var TITLE_TIMEOUT_MS = 5e3;
async function pageTitle(page) {
  return await withTimeout(page.title(), TITLE_TIMEOUT_MS);
}

// src/playwright/snapshot.ts
async function snapshotTab(tab) {
  const raw = await tab.page.ariaSnapshot({ mode: "ai" });
  const text = truncateLines(raw, 2e4);
  tab.snapshotRefs = collectRefs(text);
  return text;
}
async function snapshotRefLocator(tab, ref) {
  if (!SNAPSHOT_REF_PATTERN.test(ref) || tab.snapshotRefs?.has(ref) !== true)
    throw invalidRef(ref);
  const locator = tab.page.locator(`aria-ref=${ref}`);
  if (await locator.count() !== 1) throw invalidRef(ref);
  return locator;
}
function invalidRef(ref) {
  return new BrowserRuntimeError(
    "INVALID_LOCATOR",
    `Snapshot ref ${ref} is stale or unknown; take a new domSnapshot`
  );
}
function collectRefs(text) {
  const refs = /* @__PURE__ */ new Set();
  for (const match of text.matchAll(/\[ref=([^\s\]]+)\]/g)) {
    const ref = match[1];
    if (ref !== void 0 && SNAPSHOT_REF_PATTERN.test(ref)) refs.add(ref);
  }
  return refs;
}
function truncateLines(text, maxChars) {
  if (text.length <= maxChars) return text;
  const marker = `[truncated: snapshot exceeded ${maxChars} characters]`;
  const lines = [];
  let chars = marker.length + 1;
  let truncated = false;
  for (const line of text.split("\n")) {
    if (chars + line.length + 1 > maxChars) {
      truncated = true;
      continue;
    }
    lines.push(line);
    chars += line.length + 1;
  }
  if (truncated) lines.push(marker);
  return lines.join("\n");
}

// src/playwright/input-operations.ts
async function executeDomCuaOperation(method, args, tab) {
  if (method === "dom_cua.get_visible_dom") return await snapshotTab(tab);
  switch (method) {
    case "dom_cua.click": {
      const locator = await domCuaLocator(tab, args);
      await locator.click({ ...clickOptions2(args), noWaitAfter: true });
      return null;
    }
    case "dom_cua.double_click": {
      const locator = await domCuaLocator(tab, args);
      await locator.dblclick(clickOptions2(args));
      return null;
    }
    case "dom_cua.type":
      await tab.page.keyboard.insertText(stringArg(args, "text"));
      return null;
    case "dom_cua.keypress":
      await pressKeyChord(tab.page, args.keys);
      return null;
    case "dom_cua.scroll": {
      if (typeof args.node_id === "string") {
        const locator = await domCuaLocator(tab, args);
        await locator.hover();
      }
      await tab.page.mouse.wheel(numberArg(args, "x"), numberArg(args, "y"));
      return null;
    }
    default:
      throw new BrowserRuntimeError(
        "UNKNOWN_METHOD",
        `Unknown DOM CUA method: ${method}`
      );
  }
}
async function executeCuaOperation(method, args, tab) {
  const page = tab.page;
  switch (method) {
    case "cua.click": {
      const button = args.button === void 0 ? 1 : numberArg(args, "button");
      if (button <= 3) {
        await withModifiers(page, args.keypress, async () => {
          await page.mouse.click(numberArg(args, "x"), numberArg(args, "y"), {
            button: mouseButton(button)
          });
        });
      } else {
        await dispatchAuxiliaryClick(page, args, button);
      }
      return null;
    }
    case "cua.double_click":
      await withModifiers(page, args.keypress, async () => {
        await page.mouse.dblclick(numberArg(args, "x"), numberArg(args, "y"));
      });
      return null;
    case "cua.drag": {
      const path = args.path;
      await withModifiers(page, args.keys, async () => {
        await page.mouse.move(path[0]?.x ?? 0, path[0]?.y ?? 0);
        await page.mouse.down();
        try {
          for (const point of path.slice(1))
            await page.mouse.move(point.x, point.y);
        } finally {
          await page.mouse.up().catch(() => void 0);
        }
      });
      return null;
    }
    case "cua.keypress":
      await pressKeyChord(page, args.keys);
      return null;
    case "cua.move":
      await withModifiers(page, args.keys, async () => {
        await page.mouse.move(numberArg(args, "x"), numberArg(args, "y"));
      });
      return null;
    case "cua.scroll":
      await withModifiers(page, args.keypress, async () => {
        await page.mouse.move(numberArg(args, "x"), numberArg(args, "y"));
        await page.mouse.wheel(
          numberArg(args, "scrollX"),
          numberArg(args, "scrollY")
        );
      });
      return null;
    case "cua.type":
      await page.keyboard.insertText(stringArg(args, "text"));
      return null;
    default:
      throw new BrowserRuntimeError(
        "UNKNOWN_METHOD",
        `Unknown CUA method: ${method}`
      );
  }
}
async function domCuaLocator(tab, args) {
  return await snapshotRefLocator(tab, stringArg(args, "node_id"));
}
async function dispatchAuxiliaryClick(page, args, buttonNumber) {
  const button = buttonNumber === 4 ? "back" : "forward";
  const buttons = buttonNumber === 4 ? 8 : 16;
  const x = numberArg(args, "x");
  const y = numberArg(args, "y");
  const held = modifiers(args.keypress);
  const modifierMask = held.reduce((mask, key) => {
    switch (key) {
      case "Alt":
        return mask | 1;
      case "Control":
        return mask | 2;
      case "Meta":
        return mask | 4;
      case "ControlOrMeta":
        return mask | (platform() === "darwin" ? 4 : 2);
      case "Shift":
        return mask | 8;
      default:
        return mask;
    }
  }, 0);
  const session = await page.context().newCDPSession(page);
  try {
    for (const key of held) await page.keyboard.down(key);
    await page.mouse.move(x, y);
    await session.send("Input.dispatchMouseEvent", {
      type: "mousePressed",
      x,
      y,
      button,
      buttons,
      clickCount: 1,
      modifiers: modifierMask
    });
    await session.send("Input.dispatchMouseEvent", {
      type: "mouseReleased",
      x,
      y,
      button,
      buttons: 0,
      clickCount: 1,
      modifiers: modifierMask
    });
  } finally {
    for (const key of [...held].reverse())
      await page.keyboard.up(key).catch(() => void 0);
    await session.detach().catch(() => void 0);
  }
}

// src/playwright/locator-operations.ts
var DEFAULT_ACTION_TIMEOUT_MS = 5e3;
var DEFAULT_READ_TIMEOUT_MS = 1e3;
var DEFAULT_WAIT_TIMEOUT_MS = 3e4;
var TYPE_CHAR_BUDGET_MS = 2;
var MAX_TYPE_TIMEOUT_MS = 12e4;
async function executeLocatorOperation(method, args, tab) {
  const locator = buildLocator(tab.page, args.steps);
  const timeout = timeoutArg(args, defaultLocatorTimeout(method));
  const options = { timeout };
  switch (method) {
    case "locator.count":
      return await withTimeout(locator.count(), timeout);
    case "locator.evaluate":
      return jsonResult(
        await evaluateLocator(
          locator,
          stringArg(args, "script"),
          false,
          timeout
        )
      );
    case "locator.evaluateAll":
      return jsonResult(
        await evaluateLocator(
          locator,
          stringArg(args, "script"),
          true,
          timeout
        )
      );
    case "locator.allTextContents": {
      const deadline = Date.now() + timeout;
      const attached = await locator.first().waitFor({ state: "attached", timeout }).then(() => true).catch((error) => {
        if (error instanceof Error && error.name === "TimeoutError")
          return false;
        throw error;
      });
      if (!attached) return [];
      return await withTimeout(
        locator.allTextContents(),
        deadline - Date.now()
      );
    }
    case "locator.innerText":
      return await locator.innerText(options);
    case "locator.textContent":
      return await locator.textContent(options);
    case "locator.getAttribute":
      return await locator.getAttribute(stringArg(args, "name"), options);
    case "locator.isEnabled":
      return await locator.isEnabled(options);
    case "locator.isVisible":
      return await locator.isVisible(options);
    case "locator.click":
      await locator.click({
        ...clickOptions2(args, DEFAULT_ACTION_TIMEOUT_MS),
        noWaitAfter: true
      });
      return null;
    case "locator.dblclick":
      await locator.dblclick(clickOptions2(args, DEFAULT_ACTION_TIMEOUT_MS));
      return null;
    case "locator.downloadMedia":
      await withTimeout(
        locator.evaluate(
          async (element, deadline) => {
            const remainingMs = () => {
              const remaining = deadline - Date.now();
              if (remaining <= 0)
                throw new DOMException(
                  "Media download timed out",
                  "TimeoutError"
                );
              return remaining;
            };
            const signal = AbortSignal.timeout(remainingMs());
            element.scrollIntoView({ block: "center", inline: "nearest" });
            const candidates = [
              element,
              ...element.querySelectorAll("img, video, source"),
              ...element.querySelectorAll("a[href]"),
              element.closest("img, video, source, a[href]")
            ].filter((node) => node !== null);
            const readString = (node, name) => {
              const value = Reflect.get(node, name);
              return typeof value === "string" && value !== "" ? value : null;
            };
            const readSrcset = (node) => {
              const srcset = readString(node, "srcset");
              if (srcset === null) return null;
              const candidate = /^[\s,]*(\S+)/.exec(srcset)?.[1]?.replace(/,+$/, "");
              return candidate ? new URL(candidate, document.baseURI).href : null;
            };
            const fetchMedia = async (target) => {
              remainingMs();
              let fetched;
              try {
                fetched = await fetch(target, { signal });
              } catch (error) {
                if (error instanceof TypeError)
                  throw new Error(
                    `Media download requires reading the resource, but the page origin cannot read it (cross-origin without CORS): ${target.slice(0, 200)}`
                  );
                throw error;
              }
              if (!fetched.ok)
                throw new Error(
                  `Media download failed: HTTP ${fetched.status}`
                );
              return fetched;
            };
            let picked = null;
            const ownHref = readString(element, "href");
            if (ownHref !== null && /^(?:https?|blob|data):/.test(ownHref)) {
              const probe = await fetchMedia(ownHref);
              const type = probe.headers.get("content-type") ?? "";
              if (type.toLowerCase().startsWith("text/html")) {
                void probe.body?.cancel().catch(() => void 0);
              } else {
                picked = { url: ownHref, response: probe };
              }
            }
            if (picked === null) {
              let scanned = null;
              for (const candidate of candidates) {
                scanned = readString(candidate, "currentSrc");
                if (scanned !== null) break;
              }
              if (scanned === null)
                for (const candidate of candidates) {
                  scanned = readString(candidate, "src") ?? readSrcset(candidate);
                  if (scanned !== null) break;
                }
              if (scanned === null)
                for (const candidate of candidates) {
                  scanned = readString(candidate, "href");
                  if (scanned !== null) break;
                }
              if (scanned === null)
                throw new Error(
                  "Matched element does not expose a downloadable URL"
                );
              if (!/^(?:https?|blob|data):/.test(scanned))
                throw new Error(
                  `Unsupported media URL scheme: ${scanned.slice(0, 200)}`
                );
              picked = { url: scanned, response: await fetchMedia(scanned) };
            }
            const { url, response } = picked;
            const blob = await response.blob();
            remainingMs();
            const objectUrl = URL.createObjectURL(blob);
            const anchor = document.createElement("a");
            anchor.href = objectUrl;
            anchor.download = /^https?:/.test(url) ? new URL(url).pathname.split("/").pop() || "download" : "";
            anchor.rel = "noopener";
            anchor.style.display = "none";
            try {
              document.body.append(anchor);
              remainingMs();
              anchor.click();
            } finally {
              anchor.remove();
              setTimeout(() => URL.revokeObjectURL(objectUrl), 1e4);
            }
          },
          Date.now() + timeout,
          options
        ),
        timeout
      );
      return null;
    case "locator.fill":
      await locator.fill(stringArg(args, "value"), options);
      return null;
    case "locator.type": {
      const value = stringArg(args, "value");
      const scaled = args.timeoutMs === void 0 ? Math.min(
        Math.max(options.timeout, value.length * TYPE_CHAR_BUDGET_MS),
        MAX_TYPE_TIMEOUT_MS
      ) : options.timeout;
      await typeIntoLocator(locator, value, { timeout: scaled });
      return null;
    }
    case "locator.press": {
      const value = stringArg(args, "value");
      try {
        await locator.press(value, {
          ...options,
          noWaitAfter: true
        });
      } catch (error) {
        await releaseChordKeys(tab.page, chordTokens(value));
        throw error;
      }
      return null;
    }
    case "locator.selectOption":
      await locator.selectOption(selectOptions(args.value), options);
      return null;
    case "locator.check":
      await locator.check({
        ...options,
        ...args.force === true ? { force: true } : {}
      });
      return null;
    case "locator.uncheck":
      await locator.uncheck({
        ...options,
        ...args.force === true ? { force: true } : {}
      });
      return null;
    case "locator.setChecked":
      await locator.setChecked(args.checked === true, {
        ...options,
        ...args.force === true ? { force: true } : {}
      });
      return null;
    case "locator.waitFor":
      await locator.waitFor({
        state: args.state,
        timeout
      });
      return null;
    default:
      throw new BrowserRuntimeError(
        "UNKNOWN_METHOD",
        `Unknown locator method: ${method}`
      );
  }
}
function defaultLocatorTimeout(method) {
  switch (method) {
    case "locator.evaluate":
    case "locator.evaluateAll":
    case "locator.waitFor":
      return DEFAULT_WAIT_TIMEOUT_MS;
    case "locator.count":
    case "locator.allTextContents":
    case "locator.innerText":
    case "locator.textContent":
    case "locator.getAttribute":
    case "locator.isEnabled":
    case "locator.isVisible":
      return DEFAULT_READ_TIMEOUT_MS;
    default:
      return DEFAULT_ACTION_TIMEOUT_MS;
  }
}
async function typeIntoLocator(locator, value, options) {
  const unchanged = value === "" ? void 0 : await locator.evaluateHandle(
    (element) => {
      const hasValue = element instanceof HTMLInputElement || element instanceof HTMLTextAreaElement || element instanceof HTMLSelectElement;
      const before = hasValue ? element.value : element instanceof HTMLElement && element.isContentEditable ? element.textContent ?? "" : void 0;
      return () => before !== void 0 && element.isConnected && element.matches(":focus-within") && (hasValue ? element.value : element.textContent ?? "") === before;
    },
    void 0,
    options
  );
  try {
    await locator.pressSequentially(value, options);
    if (unchanged !== void 0 && await withTimeout(
      unchanged.evaluate((check) => check()),
      options.timeout
    ).catch(() => false))
      throw new BrowserRuntimeError(
        "INPUT_BLOCKED",
        "Typing produced no observable change; inspect the target state before continuing"
      );
  } finally {
    await unchanged?.dispose().catch(() => void 0);
  }
}
function buildLocator(page, steps) {
  let scope = page;
  let locator;
  for (const step of steps) {
    switch (step.kind) {
      case "locator":
        locator = scope.locator(step.selector);
        scope = locator;
        break;
      case "frame":
        scope = scope.locator(step.selector).contentFrame();
        locator = void 0;
        break;
      case "getByRole":
        locator = scope.getByRole(step.role, {
          ...step.name === void 0 ? {} : { name: matcher3(step.name) },
          ...step.exact === void 0 ? {} : { exact: step.exact }
        });
        scope = locator;
        break;
      case "getByText":
        locator = scope.getByText(matcher3(step.text), {
          ...step.exact === void 0 ? {} : { exact: step.exact }
        });
        scope = locator;
        break;
      case "getByLabel":
        locator = scope.getByLabel(matcher3(step.text), {
          ...step.exact === void 0 ? {} : { exact: step.exact }
        });
        scope = locator;
        break;
      case "getByPlaceholder":
        locator = scope.getByPlaceholder(matcher3(step.text), {
          ...step.exact === void 0 ? {} : { exact: step.exact }
        });
        scope = locator;
        break;
      case "getByTestId":
        locator = scope.getByTestId(step.testId);
        scope = locator;
        break;
      case "filter":
        locator = requireLocator(locator).filter({
          ...step.hasText === void 0 ? {} : { hasText: matcher3(step.hasText) },
          ...step.hasNotText === void 0 ? {} : { hasNotText: matcher3(step.hasNotText) },
          ...step.has === void 0 ? {} : { has: buildLocator(page, step.has) },
          ...step.hasNot === void 0 ? {} : { hasNot: buildLocator(page, step.hasNot) },
          ...step.visible === void 0 ? {} : { visible: step.visible }
        });
        scope = locator;
        break;
      case "first":
        locator = requireLocator(locator).first();
        scope = locator;
        break;
      case "last":
        locator = requireLocator(locator).last();
        scope = locator;
        break;
      case "nth":
        locator = requireLocator(locator).nth(step.index);
        scope = locator;
        break;
      case "and":
        locator = requireLocator(locator).and(buildLocator(page, step.steps));
        scope = locator;
        break;
      case "or":
        locator = requireLocator(locator).or(buildLocator(page, step.steps));
        scope = locator;
        break;
      default:
        throw new BrowserRuntimeError(
          "INVALID_LOCATOR",
          "The locator plan contains an unsupported step"
        );
    }
  }
  return requireLocator(locator);
}
function requireLocator(locator) {
  if (locator !== void 0) return locator;
  throw new BrowserRuntimeError(
    "INVALID_LOCATOR",
    "The locator plan must end with an element selector"
  );
}
async function evaluateScript2(page, script, timeout) {
  return JSON.parse(
    await withTimeout(
      page.evaluate(async (source) => {
        const AsyncFunction = Object.getPrototypeOf(async () => void 0).constructor;
        return await new AsyncFunction(source)();
      }, jsonEvaluationScript(script)),
      timeout
    )
  );
}
function jsonEvaluationScript(script) {
  return `return (${serializeJson.toString()})((await (async () => {
${script}
})()) ?? null);`;
}
async function evaluateLocator(locator, script, all, timeout) {
  if (all) {
    const deadline = Date.now() + timeout;
    const attached = await locator.first().waitFor({ state: "attached", timeout }).then(() => true).catch((error) => {
      if (error instanceof Error && error.name === "TimeoutError")
        return false;
      throw error;
    });
    if (!attached) return [];
    return JSON.parse(
      await withTimeout(
        locator.evaluateAll(async (elements, source) => {
          const AsyncFunction = Object.getPrototypeOf(async () => void 0).constructor;
          return await new AsyncFunction("elements", source)(elements);
        }, jsonEvaluationScript(script)),
        deadline - Date.now()
      )
    );
  }
  return JSON.parse(
    await withTimeout(
      locator.evaluate(
        async (element, source) => {
          const AsyncFunction = Object.getPrototypeOf(async () => void 0).constructor;
          return await new AsyncFunction("element", source)(element);
        },
        jsonEvaluationScript(script),
        { timeout }
      ),
      timeout
    )
  );
}

// src/playwright/screenshot.ts
import { Buffer as Buffer2 } from "node:buffer";
import { clearTimeout as clearTimeout4, setTimeout as setTimeout4 } from "node:timers";
var FRAME_TIMEOUT_MS = 2e3;
var CAPTURE_TIMEOUT_MS = 5e3;
var CLEANUP_TIMEOUT_MS = 1e3;
var MIN_DEVICE_PIXEL_RATIO = 0.25;
var MAX_DEVICE_PIXEL_RATIO = 8;
var screenshotQueues = /* @__PURE__ */ new WeakMap();
async function captureTabScreenshot(tab, args, bridge) {
  const pending = (screenshotQueues.get(tab) ?? Promise.resolve()).catch(() => void 0).then(() => capture(tab, args, bridge));
  screenshotQueues.set(tab, pending);
  try {
    return await pending;
  } finally {
    if (screenshotQueues.get(tab) === pending) screenshotQueues.delete(tab);
  }
}
async function capture(tab, args, bridge) {
  const send = async (method, params = {}, timeoutMs2 = CAPTURE_TIMEOUT_MS) => record(
    await bridge.request(
      "cdp.send",
      { tabId: tab.providerTabId, method, params },
      timeoutMs2
    )
  );
  let probedRatio;
  try {
    const pixelRatio = record(
      (await send(
        "Runtime.evaluate",
        {
          expression: `new Promise(resolve => {
              let first = 0, second = 0;
              const timer = setTimeout(finish, 250);
              function finish() {
                clearTimeout(timer);
                cancelAnimationFrame(first);
                cancelAnimationFrame(second);
                resolve(window.devicePixelRatio);
              }
              first = requestAnimationFrame(() => {
                second = requestAnimationFrame(finish);
              });
            })`,
          awaitPromise: true,
          returnByValue: true
        },
        FRAME_TIMEOUT_MS
      )).result
    ).value;
    if (isPlausibleRatio(pixelRatio)) probedRatio = pixelRatio;
  } catch {
    probedRatio = void 0;
  }
  let devicePixelRatio = probedRatio ?? 1;
  const layout = await send("Page.getLayoutMetrics");
  const viewport = record(layout.cssVisualViewport);
  const content = record(layout.cssContentSize);
  const viewportWidth = numberArg(viewport, "clientWidth");
  const viewportHeight = numberArg(viewport, "clientHeight");
  assertScreenshotDimensions(viewportWidth, viewportHeight, "Viewport");
  const clip = isClip(args.clip) ? args.clip : void 0;
  const fullPage = args.fullPage === true;
  const constrained = fullPage || clip !== void 0;
  const width = clip?.width ?? (fullPage ? numberArg(content, "width") : viewportWidth);
  const height = clip?.height ?? (fullPage ? numberArg(content, "height") : viewportHeight);
  const origin = {
    x: fullPage ? numberArg(content, "x") : numberArg(viewport, "pageX") + (clip?.x ?? 0),
    y: fullPage ? numberArg(content, "y") : numberArg(viewport, "pageY") + (clip?.y ?? 0)
  };
  if (constrained)
    assertScreenshotBudget(
      width,
      height,
      fullPage ? "Full-page" : "Clip",
      fullPage ? MAX_FULLPAGE_SCREENSHOT_PIXELS : MAX_SCREENSHOT_PIXELS
    );
  let data;
  let frameOrigin;
  if (!constrained && devicePixelRatio >= 1) {
    const frame = await viewportFrame(
      tab.providerTabId,
      bridge,
      send,
      width,
      height,
      origin
    );
    if (frame !== void 0) {
      data = frame.data;
      frameOrigin = frame.origin;
    }
  }
  if (data === void 0) {
    const scale = 1 / devicePixelRatio;
    data = await captureRegion(send, constrained, origin, width, height, scale);
    const measured = jpegDimensions(Buffer2.from(data, "base64"));
    const implied = measured.width / (width * scale);
    if (regionMismatch(measured, width, height) && isPlausibleRatio(implied) && Math.abs(implied - devicePixelRatio) > 0.01) {
      devicePixelRatio = implied;
      data = await captureRegion(
        send,
        constrained,
        origin,
        width,
        height,
        1 / implied
      );
    }
  }
  const buffer = Buffer2.from(data, "base64");
  const dimensions = jpegDimensions(buffer);
  if (regionMismatch(dimensions, width, height))
    throw new BrowserRuntimeError(
      "OPERATION_FAILED",
      "Chrome returned a screenshot that does not match the viewport; retry"
    );
  if (constrained)
    assertScreenshotBudget(
      dimensions.width,
      dimensions.height,
      "Captured",
      fullPage ? MAX_FULLPAGE_SCREENSHOT_PIXELS : MAX_SCREENSHOT_PIXELS
    );
  else
    assertScreenshotDimensions(dimensions.width, dimensions.height, "Captured");
  if (buffer.length > MAX_SCREENSHOT_BYTES)
    throw new BrowserRuntimeError(
      "OPERATION_FAILED",
      "Chrome screenshot exceeded the encoded byte budget; use a smaller clip"
    );
  return {
    base64: data,
    mimeType: "image/jpeg",
    ...dimensions,
    viewport: { width: viewportWidth, height: viewportHeight },
    devicePixelRatio,
    coordinateSpace: "css-pixels",
    origin: frameOrigin ?? origin
  };
}
async function captureRegion(send, constrained, origin, width, height, scale) {
  const result = await send("Page.captureScreenshot", {
    format: "jpeg",
    quality: 80,
    captureBeyondViewport: constrained,
    clip: { x: origin.x, y: origin.y, width, height, scale }
  });
  if (typeof result.data !== "string" || result.data.length === 0)
    throw new BrowserRuntimeError(
      "OPERATION_FAILED",
      "Chrome returned no screenshot data"
    );
  return result.data;
}
async function viewportFrame(tabId, bridge, send, width, height, origin) {
  let settle = () => void 0;
  const nextFrame = new Promise((resolve3) => {
    settle = resolve3;
  });
  const timer = setTimeout4(() => settle(void 0), FRAME_TIMEOUT_MS);
  const startedAt = Date.now() / 1e3;
  const removeListener = bridge.onEvent((event) => {
    if (event.tabId !== tabId || event.sessionId !== void 0) return;
    const params = record(event.params);
    if (event.method === "Page.screencastVisibilityChanged" && params.visible === false) {
      settle(void 0);
      return;
    }
    if (event.method !== "Page.screencastFrame") return;
    const sessionId = params.sessionId;
    if (typeof sessionId !== "number") return;
    const metadata = record(params.metadata);
    const timestamp = metadata.timestamp;
    const fresh = typeof timestamp === "number" && Number.isFinite(timestamp) && timestamp >= startedAt;
    if (fresh && typeof params.data === "string" && params.data.length > 0) {
      settle({
        data: params.data,
        sessionId,
        origin: {
          x: scrollOffset(metadata.scrollOffsetX, origin.x),
          y: scrollOffset(metadata.scrollOffsetY, origin.y)
        }
      });
      return;
    }
    void send(
      "Page.screencastFrameAck",
      { sessionId },
      CLEANUP_TIMEOUT_MS
    ).catch(() => void 0);
  });
  let frame;
  try {
    await send(
      "Page.startScreencast",
      {
        format: "jpeg",
        quality: 80,
        everyNthFrame: 1,
        maxWidth: Math.round(width),
        maxHeight: Math.round(height)
      },
      FRAME_TIMEOUT_MS
    );
    frame = await nextFrame;
    if (frame === void 0) return void 0;
    const dimensions = jpegDimensions(Buffer2.from(frame.data, "base64"));
    return dimensions.width === Math.round(width) && dimensions.height === Math.round(height) ? frame : void 0;
  } catch {
    return void 0;
  } finally {
    clearTimeout4(timer);
    removeListener();
    settle(void 0);
    await send("Page.stopScreencast", {}, CLEANUP_TIMEOUT_MS).catch(
      () => void 0
    );
    if (frame !== void 0)
      await send(
        "Page.screencastFrameAck",
        { sessionId: frame.sessionId },
        CLEANUP_TIMEOUT_MS
      ).catch(() => void 0);
  }
}
function scrollOffset(value, fallback) {
  return typeof value === "number" && Number.isFinite(value) ? value : fallback;
}
function isPlausibleRatio(value) {
  return typeof value === "number" && Number.isFinite(value) && value >= MIN_DEVICE_PIXEL_RATIO && value <= MAX_DEVICE_PIXEL_RATIO;
}
function regionMismatch(actual, width, height) {
  return Math.abs(actual.width - Math.round(width)) > 1 || Math.abs(actual.height - Math.round(height)) > 1;
}

// src/playwright/playwright-session.ts
import { randomUUID as randomUUID3 } from "node:crypto";
import {
  chromium
} from "playwright-core";

// src/playwright/qwen-playwright-transport.ts
import { platform as platform2 } from "node:os";
import { runInThisContext } from "node:vm";

// src/playwright/browser-model.ts
var BrowserModel = class {
  bridge;
  tabSessions = /* @__PURE__ */ new Map();
  browserSessions = /* @__PURE__ */ new Set();
  explicitPageSessions = /* @__PURE__ */ new Map();
  sendToPlaywright;
  nextSessionId = 1;
  ownedTabs = /* @__PURE__ */ new Set();
  operations = /* @__PURE__ */ new Map();
  closing;
  constructor(bridge) {
    this.bridge = bridge;
  }
  connect(send) {
    this.sendToPlaywright = send;
  }
  async registerTab(tabId) {
    return await this.serializeTab(tabId, async () => {
      this.assertOpen();
      try {
        return (await this.attachTab(tabId)).targetInfo;
      } catch (error) {
        await this.releaseTab(tabId);
        throw error;
      }
    });
  }
  async unregisterTab(tabId) {
    await this.serializeTab(tabId, async () => this.releaseTab(tabId));
  }
  close() {
    if (this.closing !== void 0) return this.closing;
    if (!this.bridge.isConnected()) this.ownedTabs.clear();
    const tabIds = /* @__PURE__ */ new Set([...this.ownedTabs, ...this.operations.keys()]);
    this.closing = Promise.allSettled(
      [...tabIds].map(async (tabId) => this.unregisterTab(tabId))
    ).then(() => void 0);
    return this.closing;
  }
  serializeTab(tabId, operation) {
    const previous = this.operations.get(tabId) ?? Promise.resolve();
    const result = previous.then(operation);
    const settled = result.then(
      () => void 0,
      () => void 0
    );
    this.operations.set(tabId, settled);
    void settled.then(() => {
      if (this.operations.get(tabId) === settled) this.operations.delete(tabId);
    });
    return result;
  }
  assertOpen() {
    if (this.closing !== void 0)
      throw new Error("Playwright transport is closed");
  }
  assertOwned(tabId) {
    if (!this.ownedTabs.has(tabId))
      throw new Error(`Tab ${tabId} was removed during attachment`);
  }
  async releaseTab(tabId) {
    this.detachTab(tabId);
    if (this.ownedTabs.delete(tabId) && this.bridge.isConnected())
      await this.bridge.request("tabs.detach", { tabId }, 2e3).catch(() => void 0);
  }
  async onBridgeEvent(event) {
    if (this.closing !== void 0) return;
    if (event.method === "qwenBrowser.tabRemoved" || event.method === "qwenBrowser.detached") {
      this.ownedTabs.delete(event.tabId);
      this.detachTab(event.tabId);
      return;
    }
    const tabSession = this.tabSessions.get(event.tabId);
    if (tabSession === void 0) return;
    if (event.method === "Page.downloadWillBegin" || event.method === "Page.downloadProgress") {
      this.emit({
        method: event.method.replace("Page.", "Browser."),
        params: record2(event.params)
      });
      return;
    }
    if (event.method === "qwenBrowser.sessionDetached" && event.sessionId !== void 0) {
      const child = tabSession.childSessions.get(event.sessionId);
      tabSession.childSessions.delete(event.sessionId);
      this.detachExplicitSessions(
        (session) => session.tabSession === tabSession && session.sourceSessionId === event.sessionId
      );
      this.emit({
        sessionId: tabSession.sessionId,
        method: "Target.detachedFromTarget",
        params: {
          sessionId: event.sessionId,
          ...child === void 0 ? {} : { targetId: child.targetId }
        }
      });
      return;
    }
    const params = { ...record2(event.params) };
    const childSessionId = stringOrUndefined(params.sessionId);
    if (event.method === "Target.attachedToTarget" && childSessionId) {
      const info = targetInfo(params.targetInfo);
      tabSession.childSessions.set(childSessionId, info);
      params.targetInfo = info;
    }
    if (event.method === "Target.detachedFromTarget" && childSessionId) {
      tabSession.childSessions.delete(childSessionId);
      this.detachExplicitSessions(
        (session) => session.tabSession === tabSession && session.sourceSessionId === childSessionId
      );
    }
    const sourceSessionId = event.sessionId;
    const message = {
      sessionId: sourceSessionId || tabSession.sessionId,
      method: event.method,
      params
    };
    this.emit(message);
    for (const [sessionId, session] of this.explicitPageSessions) {
      if (session.tabSession === tabSession && session.sourceSessionId === sourceSessionId)
        this.emit({ ...message, sessionId });
    }
  }
  getTargetInfo(sessionId) {
    if (sessionId === void 0) return void 0;
    const explicit = this.explicitPageSessions.get(sessionId);
    if (explicit !== void 0) return explicit.targetInfo;
    const root = this.findSession(
      (candidate) => candidate.sessionId === sessionId
    );
    if (root !== void 0) return root.targetInfo;
    for (const session of this.tabSessions.values()) {
      const child = session.childSessions.get(sessionId);
      if (child !== void 0) return child;
    }
    return void 0;
  }
  attachToBrowserTarget() {
    const sessionId = `pw-browser-${this.nextSessionId++}`;
    this.browserSessions.add(sessionId);
    return { sessionId };
  }
  isBrowserSession(sessionId) {
    return this.browserSessions.has(sessionId);
  }
  attachToTarget(parentSessionId, targetId) {
    if (!this.browserSessions.has(parentSessionId))
      throw new Error(`Unknown Playwright browser session: ${parentSessionId}`);
    const target = this.findTarget(targetId);
    if (target === void 0)
      throw new Error(`No controlled tab found for CDP target: ${targetId}`);
    const sessionId = `pw-cdp-${this.nextSessionId++}`;
    this.explicitPageSessions.set(sessionId, {
      parentSessionId,
      ...target
    });
    return { sessionId };
  }
  detachFromTarget(parentSessionId, sessionId) {
    const session = this.explicitPageSessions.get(sessionId);
    if (session?.parentSessionId !== parentSessionId)
      throw new Error(`Unknown Playwright target session: ${sessionId}`);
    this.detachExplicitSessions((candidate) => candidate === session);
  }
  async sendCommand(sessionId, method, params) {
    const explicit = this.explicitPageSessions.get(sessionId);
    if (explicit !== void 0) {
      return await this.bridge.request(
        "cdp.send",
        {
          tabId: explicit.tabSession.tabId,
          method,
          params,
          ...explicit.sourceSessionId === void 0 ? {} : { sessionId: explicit.sourceSessionId }
        },
        CDP_REQUEST_TIMEOUT_MS
      );
    }
    let session = this.findSession(
      (candidate) => candidate.sessionId === sessionId
    );
    let childSessionId;
    if (session === void 0) {
      session = this.findSession(
        (candidate) => candidate.childSessions.has(sessionId)
      );
      childSessionId = sessionId;
    }
    if (session === void 0)
      throw new Error(`No tab found for CDP session: ${sessionId}`);
    return await this.bridge.request(
      "cdp.send",
      {
        tabId: session.tabId,
        method,
        params,
        ...childSessionId === void 0 ? {} : { sessionId: childSessionId }
      },
      CDP_REQUEST_TIMEOUT_MS
    );
  }
  async attachTab(tabId) {
    const existing = this.tabSessions.get(tabId);
    if (existing !== void 0) return existing;
    this.ownedTabs.add(tabId);
    await this.bridge.request("tabs.attach", { tabId });
    this.assertOpen();
    this.assertOwned(tabId);
    const response = record2(
      await this.bridge.request("cdp.send", {
        tabId,
        method: "Target.getTargetInfo",
        params: {}
      })
    );
    this.assertOpen();
    this.assertOwned(tabId);
    const info = targetInfo(response.targetInfo);
    const session = {
      tabId,
      sessionId: `pw-tab-${this.nextSessionId++}`,
      targetInfo: info,
      childSessions: /* @__PURE__ */ new Map()
    };
    this.tabSessions.set(tabId, session);
    this.emit({
      method: "Target.attachedToTarget",
      params: {
        sessionId: session.sessionId,
        targetInfo: { ...info, attached: true },
        waitingForDebugger: false
      }
    });
    return session;
  }
  detachTab(tabId) {
    const session = this.tabSessions.get(tabId);
    if (session === void 0) return;
    this.tabSessions.delete(tabId);
    this.detachExplicitSessions(
      (candidate) => candidate.tabSession === session
    );
    this.emit({
      method: "Target.detachedFromTarget",
      params: {
        sessionId: session.sessionId,
        targetId: session.targetInfo.targetId
      }
    });
  }
  findSession(predicate) {
    for (const session of this.tabSessions.values()) {
      if (predicate(session)) return session;
    }
    return void 0;
  }
  findTarget(targetId) {
    for (const tabSession of this.tabSessions.values()) {
      if (tabSession.targetInfo.targetId === targetId)
        return { tabSession, targetInfo: tabSession.targetInfo };
      for (const [sourceSessionId, info] of tabSession.childSessions) {
        if (info.targetId === targetId)
          return { tabSession, targetInfo: info, sourceSessionId };
      }
    }
    return void 0;
  }
  detachExplicitSessions(predicate) {
    for (const [sessionId, session] of this.explicitPageSessions) {
      if (!predicate(session)) continue;
      this.explicitPageSessions.delete(sessionId);
      this.emit({
        sessionId: session.parentSessionId,
        method: "Target.detachedFromTarget",
        params: { sessionId, targetId: session.targetInfo.targetId }
      });
    }
  }
  emit(message) {
    this.sendToPlaywright?.(message);
  }
};
function targetInfo(value) {
  const info = record2(value);
  if (typeof info.targetId !== "string" || typeof info.type !== "string" || typeof info.title !== "string" || typeof info.url !== "string" || info.browserContextId !== void 0 && (typeof info.browserContextId !== "string" || info.browserContextId === ""))
    throw new Error("Chrome extension returned invalid target information");
  return {
    ...info,
    browserContextId: info.browserContextId ?? "qwen-default-context"
  };
}
function record2(value) {
  return value !== null && typeof value === "object" ? value : {};
}
function stringOrUndefined(value) {
  return typeof value === "string" && value !== "" ? value : void 0;
}

// src/playwright/qwen-playwright-transport.ts
var QwenPlaywrightTransport = class {
  onmessage;
  onclose;
  model;
  removeEventListener;
  removeConnectionListener;
  closed = false;
  constructor(bridge) {
    this.model = new BrowserModel(bridge);
    this.model.connect((message) => this.emit(message));
    this.removeEventListener = bridge.onEvent((event) => {
      if (event.method === "Page.screencastFrame" || event.method === "Page.screencastVisibilityChanged")
        return;
      void this.model.onBridgeEvent(event).catch((error) => {
        this.closeWithReason(
          error instanceof Error ? error.message : "Chrome target attachment failed"
        );
      });
    });
    this.removeConnectionListener = bridge.onConnectionChange((connected) => {
      if (!connected) this.closeWithReason("Chrome extension disconnected");
    });
  }
  open() {
  }
  send(message) {
    if (this.closed) return;
    let request;
    try {
      request = command(message);
    } catch (error) {
      this.closeWithReason(
        error instanceof Error ? error.message : "Invalid CDP command"
      );
      return;
    }
    const response = {
      id: request.id,
      ...request.sessionId === void 0 ? {} : { sessionId: request.sessionId }
    };
    void this.handle(request).then(
      (result) => this.emit({ ...response, result }),
      (error) => this.emit({
        ...response,
        error: {
          // Playwright swallows an orphan error response (its callback is
          // gone, e.g. after a target crash) only when it carries this
          // code; without it the response trips an internal assert whose
          // rejection would tear down the whole transport.
          code: -32001,
          message: error instanceof Error ? error.message : "Chrome extension operation failed"
        }
      })
    );
  }
  close() {
    return this.closeWithReason("Browser Use session closed");
  }
  async registerTab(tabId) {
    const info = await this.model.registerTab(tabId);
    return info.targetId;
  }
  async unregisterTab(tabId) {
    await this.model.unregisterTab(tabId);
  }
  async handle(message) {
    const params = message.params ?? {};
    switch (message.method) {
      case "Browser.getVersion":
        return {
          protocolVersion: "1.3",
          product: "Chrome/151.0.0.0",
          userAgent: `Mozilla/5.0 (${hostUserAgentPlatform()}) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/151.0.0.0 Safari/537.36`,
          jsVersion: ""
        };
      case "Browser.setDownloadBehavior":
        return {};
      case "Target.attachToBrowserTarget":
        if (message.sessionId !== void 0)
          throw new Error("Target.attachToBrowserTarget requires root session");
        return this.model.attachToBrowserTarget();
      case "Target.attachToTarget": {
        const parentSessionId = requiredSessionId(message);
        if (this.model.isBrowserSession(parentSessionId))
          return this.model.attachToTarget(
            parentSessionId,
            string(params.targetId, "targetId")
          );
        return await this.model.sendCommand(
          parentSessionId,
          message.method,
          params
        );
      }
      case "Target.detachFromTarget": {
        const parentSessionId = requiredSessionId(message);
        if (!this.model.isBrowserSession(parentSessionId))
          return await this.model.sendCommand(
            parentSessionId,
            message.method,
            params
          );
        this.model.detachFromTarget(
          parentSessionId,
          string(params.sessionId, "sessionId")
        );
        return {};
      }
      case "Target.setAutoAttach":
        if (message.sessionId !== void 0)
          return await this.model.sendCommand(
            message.sessionId,
            message.method,
            params
          );
        return {};
      case "Target.getTargetInfo":
        return { targetInfo: this.model.getTargetInfo(message.sessionId) };
      default:
        if (message.sessionId === void 0)
          throw new Error(
            `Unsupported browser-level CDP command: ${message.method}`
          );
        return await this.model.sendCommand(
          message.sessionId,
          message.method,
          params
        );
    }
  }
  emit(message) {
    if (this.closed) return;
    const failed = (error) => this.closeWithReason(
      error instanceof Error ? error.message : "Playwright rejected a CDP message"
    );
    try {
      void Promise.resolve(this.onmessage?.(message)).catch(failed);
    } catch (error) {
      void failed(error);
    }
  }
  closeWithReason(reason) {
    if (this.closed) return this.model.close();
    this.closed = true;
    this.removeEventListener();
    this.removeConnectionListener();
    const cleanup = this.model.close();
    this.onclose?.(reason);
    return cleanup;
  }
};
function playwrightTransportAdapter(transport) {
  const adapter = runInThisContext("({})");
  Object.defineProperties(adapter, {
    open: { value: () => transport.open?.() },
    send: { value: (message) => transport.send(message) },
    close: { value: () => transport.close() },
    onmessage: {
      get: () => transport.onmessage,
      set: (listener) => {
        transport.onmessage = listener;
      }
    },
    onclose: {
      get: () => transport.onclose,
      set: (listener) => {
        transport.onclose = listener;
      }
    }
  });
  return adapter;
}
function hostUserAgentPlatform() {
  switch (platform2()) {
    case "darwin":
      return "Macintosh; Intel Mac OS X 15_0_0";
    case "win32":
      return "Windows NT 10.0; Win64; x64";
    default:
      return "X11; Linux x86_64";
  }
}
function command(value) {
  const message = value;
  if (typeof message.id !== "number" || typeof message.method !== "string")
    throw new Error("Playwright sent an invalid CDP command");
  return message;
}
function requiredSessionId(message) {
  if (message.sessionId === void 0)
    throw new Error(`${message.method} requires a parent session`);
  return message.sessionId;
}
function string(value, name) {
  if (typeof value !== "string" || value === "")
    throw new Error(`Missing CDP ${name}`);
  return value;
}

// src/playwright/playwright-session.ts
var PlaywrightSession = class {
  bridge;
  transport;
  browser;
  context;
  discoveredTabs = /* @__PURE__ */ new Map();
  tabs = /* @__PURE__ */ new Map();
  tabIdPrefix = newTabIdPrefix();
  selectedTabId;
  started = false;
  stopped = false;
  starting;
  registration = Promise.resolve();
  stopping;
  removeEventListener;
  removeConnectionListener;
  constructor(options) {
    this.bridge = options.bridge;
    this.removeEventListener = this.bridge.onEvent((event) => {
      if (this.stopped) return;
      if (event.method === "Page.javascriptDialogOpening") {
        for (const tab of this.tabs.values())
          if (tab.providerTabId === event.tabId) traceDialogOpening(tab);
      }
      if (event.method === "Page.javascriptDialogClosed") {
        for (const tab of this.tabs.values()) {
          if (tab.providerTabId !== event.tabId) continue;
          tab.dialogBlocked = false;
          traceDialogClosed(tab);
          queueMicrotask(() => {
            tab.dialog = void 0;
          });
        }
      }
      if (event.method === "qwenBrowser.derivedTabTracked") {
        const parent = record(event.params).openerTabId;
        if (typeof parent === "number" && [...this.tabs.values()].some(
          (tab) => tab.providerTabId === parent && !tab.stale
        )) {
          const tabIdPrefix = this.tabIdPrefix;
          void this.bridge.request("tabs.get", { tabId: event.tabId }).then(async (value) => {
            if (this.stopped || tabIdPrefix !== this.tabIdPrefix) return;
            const provider = providerTab(value);
            await this.registerTab(provider, "created", false);
          }).catch(() => void 0);
        }
      }
    });
    this.removeConnectionListener = this.bridge.onConnectionChange(
      (connected) => {
        if (!connected && !this.stopped) this.invalidateSession();
      }
    );
  }
  async start() {
    if (this.stopped)
      throw new BrowserRuntimeError(
        "NOT_RUNNING",
        "A stopped Browser Use runtime cannot be restarted"
      );
    if (this.started && this.browser?.isConnected()) return;
    const attempt = this.starting ??= this.startInternal();
    try {
      await attempt;
    } finally {
      if (this.starting === attempt) this.starting = void 0;
    }
  }
  async startInternal() {
    await this.transport?.close();
    this.assertRunning();
    const tabIdPrefix = this.tabIdPrefix;
    await this.bridge.start();
    const transport = new QwenPlaywrightTransport(this.bridge);
    try {
      const browser = await chromium.connectOverCDP(
        playwrightTransportAdapter(transport),
        {
          noDefaults: true,
          timeout: 1e4
        }
      );
      const context = browser.contexts()[0];
      if (context === void 0)
        throw new BrowserRuntimeError(
          "OPERATION_FAILED",
          "Playwright did not expose the Chrome default context"
        );
      if (this.stopped)
        throw new BrowserRuntimeError(
          "NOT_RUNNING",
          "Browser Use stopped while Playwright was connecting"
        );
      if (tabIdPrefix !== this.tabIdPrefix)
        throw new BrowserRuntimeError(
          "BROWSER_DISCONNECTED",
          "Chrome extension disconnected while Playwright was connecting"
        );
      this.transport = transport;
      this.browser = browser;
      this.context = context;
      this.started = true;
      browser.on("disconnected", () => {
        if (this.browser === browser) this.invalidateSession();
      });
    } catch (error) {
      await transport.close();
      throw error;
    }
  }
  stop() {
    return this.stopping ??= this.stopInternal();
  }
  async stopInternal() {
    this.stopped = true;
    this.removeEventListener();
    this.removeConnectionListener();
    await this.starting?.catch(() => void 0);
    await this.registration;
    if (this.bridge.isConnected()) {
      await this.finalizeTabs([]).catch(() => void 0);
    }
    await this.transport?.close();
    this.invalidateSession();
    await this.bridge.stop();
  }
  invalidateSession() {
    for (const tab of this.tabs.values()) tab.stale = "session";
    this.tabIdPrefix = newTabIdPrefix();
    this.tabs.clear();
    this.discoveredTabs.clear();
    this.selectedTabId = void 0;
    this.browser = void 0;
    this.context = void 0;
    this.started = false;
  }
  assertRunning() {
    if (this.stopped)
      throw new BrowserRuntimeError(
        "NOT_RUNNING",
        "Browser Use runtime stopped"
      );
  }
  isSessionStale(id2) {
    return id2.startsWith("tab-") && !id2.startsWith(this.tabIdPrefix);
  }
  async newTab() {
    const provider = providerTab(await this.bridge.request("tabs.create"));
    return await this.registerTab(provider, "created", true);
  }
  async listTabs() {
    await this.syncDerivedTabs("list");
    return await Promise.all(
      [...this.tabs.values()].filter((tab) => !tab.stale).map(async (tab) => await this.tabInfo(tab))
    );
  }
  async getTabInfo(id2) {
    return await this.tabInfo(this.claimed(id2));
  }
  async selectedTabInfo() {
    const selected = this.selectedTabId === void 0 ? void 0 : this.tabs.get(this.selectedTabId);
    return selected === void 0 || selected.stale ? null : await this.tabInfo(selected);
  }
  async openTabs() {
    const providers = orderOpenTabs(
      providerTabs(await this.bridge.request("tabs.queryOpen"))
    );
    this.discoveredTabs.clear();
    return providers.map((provider) => {
      const tab = {
        id: `open-${randomUUID3()}`,
        providerTabId: provider.providerTabId,
        title: provider.title,
        url: provider.url,
        ...provider.lastOpened === void 0 ? {} : { lastOpened: provider.lastOpened },
        ...provider.tabGroup === void 0 ? {} : { tabGroup: provider.tabGroup }
      };
      this.discoveredTabs.set(tab.id, tab);
      return {
        id: tab.id,
        title: tab.title,
        url: tab.url,
        ...tab.lastOpened === void 0 ? {} : { lastOpened: tab.lastOpened },
        ...tab.tabGroup === void 0 ? {} : { tabGroup: tab.tabGroup }
      };
    });
  }
  async claimTab(value) {
    const supplied = typeof value === "string" ? { id: value } : record(value);
    const id2 = typeof supplied.id === "string" ? supplied.id : "";
    const discovered = this.discoveredTabs.get(id2);
    if (discovered === void 0)
      throw new BrowserRuntimeError(
        "TAB_NOT_GRANTED",
        "The tab must come from a fresh browser.user.openTabs() result"
      );
    if ("title" in supplied && supplied.title !== discovered.title || "url" in supplied && supplied.url !== discovered.url)
      throw new BrowserRuntimeError(
        "STALE_TAB",
        "The supplied tab no longer matches the discovery result"
      );
    const current = providerTab(
      await this.bridge.request("tabs.get", {
        tabId: discovered.providerTabId
      })
    );
    if (current.title !== discovered.title || current.url !== discovered.url)
      throw new BrowserRuntimeError(
        "STALE_TAB",
        "The Chrome tab changed after discovery; list open tabs again"
      );
    const controlled = [...this.tabs.values()].some(
      (tab) => tab.providerTabId === current.providerTabId && !tab.stale
    );
    if (!controlled) await this.assertClaimableRenderer(current.providerTabId);
    return await this.registerTab(current, "claimed", true);
  }
  /**
   * CDP cannot answer a dialog that opened while no Browser Use debugger was
   * attached, and its blocked renderer would only time registration out.
   * Probe first and hand the tab back with an actionable error instead.
   */
  async assertClaimableRenderer(providerTabId) {
    await this.bridge.request("tabs.attach", { tabId: providerTabId });
    try {
      await withTimeout(
        this.bridge.request("cdp.send", {
          tabId: providerTabId,
          method: "Runtime.evaluate",
          params: { expression: "0", returnByValue: true }
        }),
        ATTACH_PROBE_TIMEOUT_MS
      );
      return;
    } catch (error) {
      await this.bridge.request("tabs.release", { tabId: providerTabId }).catch(() => void 0);
      if (!(error instanceof BrowserRuntimeError) || error.code !== "OPERATION_TIMEOUT")
        throw error;
    }
    throw new BrowserRuntimeError(
      "DIALOG_OPEN",
      "The tab is not responding, most likely because it shows a JavaScript dialog that only the user can close. Ask the user to close the dialog in Chrome, then list open tabs and claim the tab again."
    );
  }
  async registerTab(provider, ownership, select) {
    this.assertRunning();
    const tabIdPrefix = this.tabIdPrefix;
    const existing = [...this.tabs.values()].find(
      (tab) => tab.providerTabId === provider.providerTabId && !tab.stale
    );
    if (existing !== void 0) {
      if (ownership === "created") existing.ownership = "created";
      if (select) this.selectedTabId = existing.id;
      const info = await this.tabInfo(existing);
      this.assertRunning();
      if (tabIdPrefix !== this.tabIdPrefix) throw staleSessionError();
      return info;
    }
    return await this.serializeRegistration(async () => {
      this.assertRunning();
      if (tabIdPrefix !== this.tabIdPrefix) throw staleSessionError();
      const registered = [...this.tabs.values()].find(
        (tab2) => tab2.providerTabId === provider.providerTabId && !tab2.stale
      );
      if (registered !== void 0) {
        if (ownership === "created") registered.ownership = "created";
        if (select) this.selectedTabId = registered.id;
        const info2 = await this.tabInfo(registered);
        this.assertRunning();
        if (tabIdPrefix !== this.tabIdPrefix) throw staleSessionError();
        return info2;
      }
      const context = this.requireContext();
      const transport = this.requireTransport();
      const targetIdPromise = transport.registerTab(provider.providerTabId);
      const pagePromise = context.waitForEvent("page", {
        timeout: 1e4,
        predicate: async (candidate) => await pageTargetId(candidate) === await targetIdPromise
      });
      let page;
      try {
        [, page] = await Promise.all([targetIdPromise, pagePromise]);
        this.assertRunning();
        if (tabIdPrefix !== this.tabIdPrefix) throw staleSessionError();
        await this.bridge.request("cdp.send", {
          tabId: provider.providerTabId,
          method: "Emulation.setFocusEmulationEnabled",
          params: { enabled: true }
        });
        this.assertRunning();
        if (tabIdPrefix !== this.tabIdPrefix) throw staleSessionError();
        if (page.isClosed()) throw staleTabError();
      } catch (error) {
        await transport.unregisterTab(provider.providerTabId);
        throw error;
      }
      const tab = {
        id: `${tabIdPrefix}${randomUUID3()}`,
        providerTabId: provider.providerTabId,
        page,
        stale: false,
        logs: [],
        dialogTrace: [],
        fileChoosers: /* @__PURE__ */ new Map(),
        navigationWaiters: /* @__PURE__ */ new Map(),
        ownership
      };
      for (const previous of this.tabs.values()) {
        if (previous.providerTabId !== tab.providerTabId) continue;
        if (previous.ownership === "created") tab.ownership = "created";
        this.tabs.delete(previous.id);
      }
      this.installPageObservers(tab, transport);
      this.tabs.set(tab.id, tab);
      if (select) this.selectedTabId = tab.id;
      const blocked = await withTimeout(
        this.bridge.request("cdp.send", {
          tabId: provider.providerTabId,
          method: "Runtime.evaluate",
          params: { expression: "0", returnByValue: true }
        }),
        ATTACH_PROBE_TIMEOUT_MS
      ).then(
        () => false,
        (error) => error instanceof BrowserRuntimeError && error.code === "OPERATION_TIMEOUT"
      );
      if (blocked) tab.dialogBlocked = true;
      const info = await this.tabInfo(tab);
      this.assertRunning();
      if (tabIdPrefix !== this.tabIdPrefix) throw staleSessionError();
      return info;
    });
  }
  async serializeRegistration(operation) {
    const previous = this.registration;
    let release;
    this.registration = new Promise((resolvePromise) => {
      release = resolvePromise;
    });
    await previous;
    try {
      return await operation();
    } finally {
      release?.();
    }
  }
  installPageObservers(tab, transport) {
    const { page } = tab;
    let released = false;
    const release = () => {
      if (released) return;
      released = true;
      if (tab.stale === false) tab.stale = "tab";
      tab.dialog = void 0;
      tab.dialogTrace.length = 0;
      tab.fileChoosers.clear();
      tab.navigationWaiters.clear();
      if (this.selectedTabId === tab.id) this.selectedTabId = void 0;
      void transport.unregisterTab(tab.providerTabId).catch(() => void 0);
    };
    page.on("close", () => {
      if (!released) this.tabs.delete(tab.id);
      release();
    });
    page.on("crash", release);
    page.on("dialog", (dialog) => {
      if (takeDeliveredDialog(tab)) return;
      tab.dialog = dialog;
    });
    page.on("framenavigated", (frame) => {
      if (frame !== page.mainFrame()) return;
      tab.dialog = void 0;
      tab.dialogBlocked = false;
      tab.snapshotRefs = void 0;
    });
    page.on("console", (message) => {
      const location = message.location();
      pushBounded(tab.logs, {
        level: consoleLevel(message.type()),
        message: message.text().slice(0, 2e4),
        timestamp: (/* @__PURE__ */ new Date()).toISOString(),
        ...location.url === "" ? {} : { url: location.url }
      });
    });
    page.on("pageerror", (error) => {
      pushBounded(tab.logs, {
        level: "error",
        message: error.message.slice(0, 2e4),
        timestamp: (/* @__PURE__ */ new Date()).toISOString()
      });
    });
  }
  async syncDerivedTabs(mode) {
    const providers = providerTabs(
      await this.bridge.request("tabs.queryDerived")
    );
    const results = await Promise.allSettled(
      providers.map((provider) => {
        if (this.stopped && mode === "finalize") {
          const tab = [...this.tabs.values()].find(
            (tab2) => tab2.providerTabId === provider.providerTabId
          );
          if (tab !== void 0) {
            tab.ownership = "created";
            return;
          }
          return this.bridge.request("tabs.close", {
            tabId: provider.providerTabId
          });
        }
        return this.registerTab(provider, "created", false);
      })
    );
    const failed = results.find(
      (result) => result.status === "rejected" && (mode === "finalize" || result.reason instanceof BrowserRuntimeError && (result.reason.code === "STALE_BROWSER_SESSION" || result.reason.code === "BROWSER_DISCONNECTED"))
    );
    if (failed !== void 0) throw failed.reason;
  }
  async tabInfo(tab) {
    if (tab.stale === "session") throw staleSessionError();
    if (tab.stale === "tab" || tab.page.isClosed()) throw staleTabError();
    return {
      id: tab.id,
      title: tab.dialog === void 0 && tab.dialogBlocked !== true ? await pageTitle(tab.page).catch(() => null) : null,
      url: tab.page.url() || null
    };
  }
  async closeTab(tab) {
    const transport = this.requireTransport();
    await this.bridge.request("tabs.close", { tabId: tab.providerTabId }).catch((error) => {
      if (!(error instanceof BrowserRuntimeError) || error.code !== "STALE_TAB")
        throw error;
    });
    tab.stale = "tab";
    this.tabs.delete(tab.id);
    await transport.unregisterTab(tab.providerTabId);
    if (this.selectedTabId === tab.id) this.selectedTabId = void 0;
  }
  async finalizeTabs(keep) {
    await this.registration;
    const results = await Promise.allSettled([
      this.syncDerivedTabs("finalize")
    ]);
    await this.registration;
    const dispositions = /* @__PURE__ */ new Map();
    for (const { tabId, status } of keep) {
      if (dispositions.has(tabId)) {
        throw new BrowserRuntimeError(
          "INVALID_ARGUMENT",
          `Tab appears more than once in finalize(): ${tabId}`
        );
      }
      this.claimed(tabId);
      dispositions.set(tabId, status);
    }
    const operations = [...this.tabs.values()].filter((tab) => tab.stale !== "session").map(async (tab) => {
      const status = dispositions.get(tab.id);
      if (status === "handoff") return;
      if (status === "deliverable" || tab.ownership === "claimed") {
        await this.releaseTab(tab);
        return;
      }
      await this.closeTab(tab);
    });
    results.push(...await Promise.allSettled(operations));
    const failed = results.find(
      (result) => result.status === "rejected"
    );
    if (failed !== void 0) throw failed.reason;
  }
  async releaseTab(tab) {
    await this.bridge.request("tabs.release", { tabId: tab.providerTabId }).catch((error) => {
      if (!(error instanceof BrowserRuntimeError) || error.code !== "STALE_TAB")
        throw error;
    });
    tab.stale = "tab";
    this.tabs.delete(tab.id);
    await this.transport?.unregisterTab(tab.providerTabId);
    if (this.selectedTabId === tab.id) this.selectedTabId = void 0;
  }
  claimed(id2) {
    const tab = this.tabs.get(id2);
    if (tab?.stale === "session") throw staleSessionError();
    if (tab === void 0 || tab.stale === "tab" || tab.page.isClosed())
      throw staleTabError();
    this.selectedTabId = id2;
    return tab;
  }
  requireContext() {
    if (this.context !== void 0) return this.context;
    throw new BrowserRuntimeError(
      "NOT_RUNNING",
      "The Playwright browser context is unavailable"
    );
  }
  requireTransport() {
    if (this.transport !== void 0) return this.transport;
    throw new BrowserRuntimeError(
      "NOT_RUNNING",
      "The Playwright transport is unavailable"
    );
  }
};
var ATTACH_PROBE_TIMEOUT_MS = 1e3;
function newTabIdPrefix() {
  return `tab-${randomUUID3()}-`;
}
async function pageTargetId(page) {
  let session;
  try {
    session = await page.context().newCDPSession(page);
    const result = record(await session.send("Target.getTargetInfo"));
    const info = record(result.targetInfo);
    return typeof info.targetId === "string" ? info.targetId : void 0;
  } catch {
    return void 0;
  } finally {
    await session?.detach().catch(() => void 0);
  }
}
var MAX_DIALOG_TRACE = 16;
function traceDialogOpening(tab) {
  pushBounded(
    tab.dialogTrace,
    { closed: false, delivered: false },
    MAX_DIALOG_TRACE
  );
}
function traceDialogClosed(tab) {
  const entry = tab.dialogTrace.find((item) => !item.closed);
  if (entry !== void 0) {
    entry.closed = true;
    setImmediate(() => {
      setImmediate(() => {
        if (entry.delivered) return;
        const index = tab.dialogTrace.indexOf(entry);
        if (index !== -1) tab.dialogTrace.splice(index, 1);
      });
    });
  }
  pruneDialogTrace(tab);
}
function takeDeliveredDialog(tab) {
  const entry = tab.dialogTrace.find((item) => !item.delivered);
  if (entry === void 0) return false;
  entry.delivered = true;
  const { closed } = entry;
  pruneDialogTrace(tab);
  return closed;
}
function pruneDialogTrace(tab) {
  while (tab.dialogTrace[0]?.closed && tab.dialogTrace[0]?.delivered)
    tab.dialogTrace.shift();
}

// src/playwright/playwright-runtime.ts
var BROWSER_ID = "chrome";
var ATTACH_TIME_DIALOG_ID = "dialog-open-at-attach";
var MAX_UPLOAD_BYTES = 100 * 1024 * 1024;
var MAX_PENDING_TAB_RESOURCES = 100;
var INPUT_DRAIN_TIMEOUT_MS = 250;
var LOCATOR_INPUT_METHODS = /* @__PURE__ */ new Set([
  "locator.click",
  "locator.dblclick",
  "locator.downloadMedia",
  "locator.fill",
  "locator.type",
  "locator.press",
  "locator.selectOption",
  "locator.check",
  "locator.uncheck",
  "locator.setChecked"
]);
var PlaywrightRuntime = class {
  selectedBrowserId = BROWSER_ID;
  profileChosen = false;
  stopped = false;
  bridge;
  documentationText;
  session;
  sessionName;
  dialogIds = /* @__PURE__ */ new WeakMap();
  constructor(options = {}) {
    this.bridge = options.bridge ?? new ChromeExtensionTransport(options);
    this.documentationText = options.documentation ?? DEFAULT_CHROME_DOCUMENTATION;
    this.session = new PlaywrightSession({ bridge: this.bridge });
  }
  get browserId() {
    return this.selectedBrowserId;
  }
  async start() {
    await this.session.start();
    this.profileChosen = true;
  }
  async stop() {
    this.stopped = true;
    await this.session.stop();
  }
  async dispatch(method, input) {
    if (this.stopped)
      throw new BrowserRuntimeError(
        "NOT_RUNNING",
        "Browser Use runtime stopped"
      );
    const schema = Object.hasOwn(commandSchemas, method) ? commandSchemas[method] : void 0;
    if (schema === void 0)
      throw new BrowserRuntimeError(
        "UNKNOWN_METHOD",
        `Unknown browser method: ${method}`
      );
    let args;
    try {
      args = schema.parse(input);
    } catch (error) {
      if (error instanceof ZodError) throw invalidArguments(method, error);
      throw error;
    }
    let tab;
    let requested;
    try {
      if (typeof args.tabId === "string" && this.session.isSessionStale(args.tabId))
        throw staleSessionError();
      if (method === "browsers.list" && this.bridge.profiles !== void 0) {
        return (await this.bridge.profiles()).map((profile) => ({
          ...this.browserInfo(),
          id: `chrome:${profile.extensionInstanceId}`,
          name: profile.profileName ?? `Chrome \xB7 ${profile.extensionInstanceId}`
        }));
      }
      if (method === "browsers.get" && this.bridge.selectProfile !== void 0) {
        const id2 = stringArg(args, "id");
        this.bridge.selectProfile(id2);
        if (!this.profileChosen && id2.startsWith("chrome:")) requested = id2;
      }
      await this.start();
      if (requested !== void 0) {
        this.bridge.selectProfile?.(requested);
        this.selectedBrowserId = requested;
      }
      tab = typeof args.tabId === "string" ? this.session.claimed(args.tabId) : void 0;
      if (tab !== void 0) assertDialogAllows(method, tab);
      const result = await this.execute(method, args);
      return result;
    } catch (error) {
      if (tab?.stale === "session") throw staleSessionError();
      if (error instanceof BrowserRuntimeError) throw error;
      if (tab !== void 0 && (tab.stale === "tab" || tab.page.isClosed()))
        throw staleTabError();
      throw sanitizeOperationError(method, error);
    }
  }
  async execute(method, args) {
    switch (method) {
      case "browsers.list":
        try {
          await this.bridge.request("ping");
          return [this.browserInfo()];
        } catch (error) {
          if (error instanceof BrowserRuntimeError && error.code === "BROWSER_DISCONNECTED")
            return [];
          throw error;
        }
      case "browsers.get":
        this.assertBrowserSelector(stringArg(args, "id"));
        await this.bridge.request("ping");
        return this.browserInfo();
      case "browser.documentation":
        this.assertBrowser(stringArg(args, "browserId"));
        return this.documentation();
      case "browser.nameSession": {
        this.assertBrowser(stringArg(args, "browserId"));
        const name = stringArg(args, "name");
        await this.bridge.request("session.name", { name });
        this.sessionName = name;
        return null;
      }
      case "browser.user.openTabs":
        this.assertBrowser(stringArg(args, "browserId"));
        return await this.session.openTabs();
      case "browser.user.claimTab":
        this.assertBrowser(stringArg(args, "browserId"));
        return await this.session.claimTab(args.tab);
      case "browser.user.history":
        this.assertBrowser(stringArg(args, "browserId"));
        return await this.history(args.options);
      case "tabs.new":
        this.assertBrowser(stringArg(args, "browserId"));
        return await this.session.newTab();
      case "tabs.list":
        this.assertBrowser(stringArg(args, "browserId"));
        return await this.session.listTabs();
      case "tabs.get":
        this.assertBrowser(stringArg(args, "browserId"));
        return await this.session.getTabInfo(stringArg(args, "tabId"));
      case "tabs.selected":
        this.assertBrowser(stringArg(args, "browserId"));
        return await this.session.selectedTabInfo();
      case "tabs.finalize":
        this.assertBrowser(stringArg(args, "browserId"));
        await this.session.finalizeTabs(
          Array.isArray(args.keep) ? args.keep : []
        );
        return null;
      case "tab.goto": {
        const tab = this.tab(args);
        const url = stringArg(args, "url");
        if (!/^https?:/i.test(url))
          throw new BrowserRuntimeError(
            "INVALID_ARGUMENT",
            "Only http(s) URLs can be navigated"
          );
        await tab.page.goto(url);
        return null;
      }
      case "tab.url":
        return this.tab(args).page.url();
      case "tab.title":
        return await pageTitle(this.tab(args).page);
      case "tab.back":
        await this.tab(args).page.goBack({
          waitUntil: "commit",
          timeout: 3e4
        });
        return null;
      case "tab.forward":
        await this.tab(args).page.goForward({
          waitUntil: "commit",
          timeout: 3e4
        });
        return null;
      case "tab.reload":
        await this.tab(args).page.reload();
        return null;
      case "tab.close":
        await this.session.closeTab(this.tab(args));
        return null;
      case "tab.screenshot":
        return await captureTabScreenshot(this.tab(args), args, this.bridge);
      case "tab.getJsDialog": {
        const tab = this.tab(args);
        const dialog = tab.dialog;
        if (dialog !== void 0)
          return {
            dialogId: this.dialogId(dialog),
            type: dialog.type(),
            message: dialog.message(),
            defaultPrompt: dialog.defaultValue()
          };
        if (tab.dialogBlocked === true)
          return {
            dialogId: ATTACH_TIME_DIALOG_ID,
            type: "alert",
            message: "",
            defaultPrompt: ""
          };
        return null;
      }
      case "tab.dialog.accept": {
        const tab = this.tab(args);
        if (tab.dialog === void 0 && tab.dialogBlocked === true) {
          await this.handleAttachTimeDialog(tab, args, true);
          return null;
        }
        const dialog = this.requireDialog(tab, args);
        try {
          await dialog.accept(
            typeof args.promptText === "string" ? args.promptText : void 0
          );
        } finally {
          if (tab.dialog === dialog) {
            tab.dialog = void 0;
            tab.dialogBlocked = false;
          }
        }
        return null;
      }
      case "tab.dialog.dismiss": {
        const tab = this.tab(args);
        if (tab.dialog === void 0 && tab.dialogBlocked === true) {
          await this.handleAttachTimeDialog(tab, args, false);
          return null;
        }
        const dialog = this.requireDialog(tab, args);
        try {
          await dialog.dismiss();
        } finally {
          if (tab.dialog === dialog) {
            tab.dialog = void 0;
            tab.dialogBlocked = false;
          }
        }
        return null;
      }
      case "dev.logs":
        return jsonResult(this.readLogs(this.tab(args), args));
      case "playwright.domSnapshot":
        return await snapshotTab(this.tab(args));
      case "playwright.evaluate":
        return jsonResult(
          await evaluateScript2(
            this.tab(args).page,
            stringArg(args, "script"),
            timeoutArg(args)
          )
        );
      case "playwright.waitForEvent":
        return await this.waitForEvent(this.tab(args), args);
      case "fileChooser.setFiles":
        await this.setChooserFiles(this.tab(args), args);
        return null;
      case "playwright.waitForURL":
        await this.tab(args).page.waitForURL(
          stringArg(args, "url"),
          navigationOptions3(args)
        );
        return null;
      case "playwright.expectNavigation.begin":
        return this.beginNavigationWait(this.tab(args), args);
      case "playwright.expectNavigation.wait":
        await this.finishNavigationWait(this.tab(args), args);
        return null;
      case "playwright.expectNavigation.cancel":
        this.tab(args).navigationWaiters.delete(stringArg(args, "waiterId"));
        return null;
      case "playwright.waitForLoadState":
        await this.tab(args).page.waitForLoadState(
          loadState2(args.state),
          timeoutOption(args)
        );
        return null;
      case "playwright.waitForTimeout":
        await this.tab(args).page.waitForTimeout(numberArg(args, "timeoutMs"));
        return null;
      default:
        if (method.startsWith("locator.")) {
          const tab = this.tab(args);
          return LOCATOR_INPUT_METHODS.has(method) ? await this.executeInput(
            tab,
            async () => executeLocatorOperation(method, args, tab)
          ) : await executeLocatorOperation(method, args, tab);
        }
        if (method.startsWith("dom_cua.")) {
          const tab = this.tab(args);
          return method === "dom_cua.get_visible_dom" ? await executeDomCuaOperation(method, args, tab) : await this.executeInput(
            tab,
            async () => executeDomCuaOperation(method, args, tab)
          );
        }
        if (method.startsWith("cua.")) {
          const tab = this.tab(args);
          return await this.executeInput(
            tab,
            async () => executeCuaOperation(method, args, tab)
          );
        }
        throw new BrowserRuntimeError(
          "UNKNOWN_METHOD",
          `Unknown browser method: ${method}`
        );
    }
  }
  async executeInput(tab, action) {
    const result = await action();
    if (tab.dialog !== void 0 || tab.page.isClosed()) return result;
    let timer;
    try {
      await Promise.race([
        tab.page.evaluate(
          () => new Promise((resolve3) => globalThis.setTimeout(resolve3, 0))
        ),
        new Promise((resolve3) => {
          timer = setTimeout5(resolve3, INPUT_DRAIN_TIMEOUT_MS);
        })
      ]);
    } catch {
    } finally {
      clearTimeout5(timer);
    }
    return result;
  }
  async history(value) {
    const options = record(value);
    const entries = await this.bridge.request("history.query", {
      ...options.limit === void 0 ? {} : { limit: options.limit },
      ...options.queries === void 0 ? {} : { queries: options.queries },
      ...options.from === void 0 ? {} : { from: isoTimestamp(options.from) },
      ...options.to === void 0 ? {} : { to: isoTimestamp(options.to) }
    });
    if (!Array.isArray(entries))
      throw new BrowserRuntimeError(
        "OPERATION_FAILED",
        "Chrome extension returned an invalid history list"
      );
    return entries.map((entry) => {
      const item = record(entry);
      return {
        url: typeof item.url === "string" ? item.url : "",
        title: typeof item.title === "string" ? item.title : null,
        dateVisited: typeof item.dateVisited === "string" ? item.dateVisited : ""
      };
    });
  }
  readLogs(tab, args) {
    const levels = Array.isArray(args.levels) ? new Set(
      args.levels.map((level) => level === "warning" ? "warn" : level)
    ) : void 0;
    const filter = typeof args.filter === "string" ? args.filter : void 0;
    const limit = typeof args.limit === "number" ? args.limit : 100;
    const result = tab.logs.filter(
      (entry) => (levels === void 0 || levels.has(entry.level)) && (filter === void 0 || entry.message.includes(filter) || entry.url?.includes(filter) === true)
    ).slice(-limit);
    return result;
  }
  dialogId(dialog) {
    let id2 = this.dialogIds.get(dialog);
    if (id2 === void 0) {
      id2 = `dialog-${randomUUID4()}`;
      this.dialogIds.set(dialog, id2);
    }
    return id2;
  }
  // A dialog known only through the attach-time probe has no Playwright
  // handle; accept or dismiss it over CDP and release the gate either way —
  // a failure means the modal is already gone.
  async handleAttachTimeDialog(tab, args, accept) {
    if (stringArg(args, "dialogId") !== ATTACH_TIME_DIALOG_ID)
      throw new BrowserRuntimeError(
        "NOT_FOUND",
        "The JavaScript dialog is stale or unknown"
      );
    try {
      await this.bridge.request("cdp.send", {
        tabId: tab.providerTabId,
        method: "Page.handleJavaScriptDialog",
        params: {
          accept,
          ...typeof args.promptText === "string" ? { promptText: args.promptText } : {}
        }
      });
    } finally {
      tab.dialogBlocked = false;
    }
  }
  requireDialog(tab, args) {
    if (tab.dialog !== void 0 && this.dialogIds.get(tab.dialog) === stringArg(args, "dialogId"))
      return tab.dialog;
    throw new BrowserRuntimeError(
      "NOT_FOUND",
      "The JavaScript dialog is stale or unknown"
    );
  }
  async waitForEvent(tab, args) {
    const timeout = timeoutArg(args);
    if (args.event === "filechooser") {
      const chooser = await tab.page.waitForEvent("filechooser", { timeout });
      const chooserId = `chooser-${randomUUID4()}`;
      setBounded(
        tab.fileChoosers,
        chooserId,
        chooser,
        MAX_PENDING_TAB_RESOURCES
      );
      return { chooserId, multiple: chooser.isMultiple() };
    }
    await tab.page.waitForEvent("download", { timeout });
    return {};
  }
  async setChooserFiles(tab, args) {
    const chooserId = stringArg(args, "chooserId");
    const chooser = tab.fileChoosers.get(chooserId);
    if (chooser === void 0)
      throw new BrowserRuntimeError(
        "NOT_FOUND",
        "The file chooser is stale or unknown"
      );
    await chooser.setFiles(await this.uploadFiles(args.files), {
      timeout: timeoutArg(args)
    });
    tab.fileChoosers.delete(chooserId);
  }
  beginNavigationWait(tab, args) {
    const id2 = `navigation-${randomUUID4()}`;
    const options = {
      timeout: timeoutArg(args),
      waitUntil: loadState2(args.waitUntil),
      ...typeof args.url === "string" ? { url: args.url } : {}
    };
    const waiter = tab.page.waitForNavigation(options);
    waiter.catch(() => void 0);
    setBounded(tab.navigationWaiters, id2, waiter, MAX_PENDING_TAB_RESOURCES);
    return id2;
  }
  async finishNavigationWait(tab, args) {
    const id2 = stringArg(args, "waiterId");
    const waiter = tab.navigationWaiters.get(id2);
    if (waiter === void 0)
      throw new BrowserRuntimeError(
        "NOT_FOUND",
        "The navigation waiter is stale or unknown"
      );
    try {
      await waiter;
    } finally {
      tab.navigationWaiters.delete(id2);
    }
  }
  async uploadFiles(value) {
    const paths = stringArray(value);
    let totalBytes = 0;
    const files = [];
    for (const path of paths) {
      if (!isAbsolute2(path))
        throw new BrowserRuntimeError(
          "INVALID_ARGUMENT",
          "Upload paths must be absolute"
        );
      const resolved = await realpath2(path).catch(() => void 0);
      if (resolved === void 0)
        throw new BrowserRuntimeError(
          "INVALID_ARGUMENT",
          `Upload path does not exist: ${path}`
        );
      const info = await stat3(resolved);
      if (!info.isFile())
        throw new BrowserRuntimeError(
          "INVALID_ARGUMENT",
          `Upload path is not a file: ${path}`
        );
      totalBytes += info.size;
      if (totalBytes > MAX_UPLOAD_BYTES)
        throw new BrowserRuntimeError(
          "INVALID_ARGUMENT",
          `Uploads are limited to ${MAX_UPLOAD_BYTES} bytes per call`
        );
      files.push(resolved);
    }
    return files;
  }
  tab(args) {
    return this.session.claimed(stringArg(args, "tabId"));
  }
  assertBrowser(id2) {
    if (id2 !== this.browserId)
      throw new BrowserRuntimeError(
        "NOT_FOUND",
        "No Chrome browser exists for the supplied id"
      );
  }
  assertBrowserSelector(id2) {
    if (this.bridge.selectProfile !== void 0) return;
    if (id2 !== this.browserId && id2 !== "extension")
      throw new BrowserRuntimeError(
        "NOT_FOUND",
        "No Chrome browser exists for the supplied id"
      );
  }
  browserInfo() {
    return {
      id: this.browserId,
      name: this.sessionName === void 0 ? "Chrome" : `Chrome \xB7 ${this.sessionName}`,
      type: "extension",
      family: "chrome"
    };
  }
  documentation() {
    return this.documentationText;
  }
};
function assertDialogAllows(method, tab) {
  if (tab.dialog === void 0 && tab.dialogBlocked !== true || method === "tabs.get" || method === "tab.getJsDialog" || method === "tab.dialog.accept" || method === "tab.dialog.dismiss" || method === "tab.close" || method === "tab.url" || method === "dev.logs" || method === "playwright.expectNavigation.cancel")
    return;
  throw new BrowserRuntimeError(
    "DIALOG_OPEN",
    "A JavaScript dialog is open; call tab.getJsDialog() and accept or dismiss it before continuing"
  );
}
function setBounded(map, key, value, limit) {
  map.set(key, value);
  if (map.size <= limit) return;
  const oldest = map.keys().next().value;
  if (oldest !== void 0) map.delete(oldest);
}

// src/runtime.ts
async function createBrowserBackend() {
  let describeProfiles;
  if (!process.env["QWEN_BROWSER_USE_SOCKET_PATH"]?.trim() && !process.env["QWEN_BROWSER_USE_DISCOVERY_DIR"]?.trim() && (process.platform === "darwin" || process.platform === "linux")) {
    const options = {
      homeDir: nativeHostInstallHome(),
      nativeHostPath: fileURLToPath(
        new URL("./native-host.js", import.meta.url)
      )
    };
    const installed = await ensureChromeNativeHost(options);
    if (installed.skippedForeignPaths.length > 0) {
      process.stderr.write(
        "Browser Use: another program owns the Chrome Native Messaging manifest at " + installed.skippedForeignPaths.join(", ") + ". It was left unchanged; if that browser is the one you use, Chrome will launch that host instead of Qwen Code. Remove or move the file, then retry Browser Use.\n"
      );
    }
    if (!installed.ready) {
      throw new Error(
        "Browser Use could not register its Native Host with any Chrome profile root. " + (installed.skippedForeignPaths.length > 0 ? "Another program owns " + installed.skippedForeignPaths.join(", ") + "; remove or move it, then retry Browser Use." : "Start Chrome once so its profile directory exists, then retry Browser Use.")
      );
    }
    describeProfiles = (ids) => describeChromeProfiles({ homeDir: options.homeDir }, ids);
  }
  return new PlaywrightRuntime({
    bridge: new ChromeExtensionTransport({ describeProfiles }),
    documentation: DEFAULT_CHROME_DOCUMENTATION
  });
}

// src/sdk/context.ts
function jsonBoundary(value, label) {
  if (value === void 0) return void 0;
  try {
    const serialized = JSON.stringify(value);
    if (serialized !== void 0) return JSON.parse(serialized);
  } catch (error) {
    const wrapped = new TypeError(label + " must be JSON-serializable");
    wrapped.cause = error;
    throw wrapped;
  }
  throw new TypeError(label + " must be JSON-serializable");
}
var BrowserSdkContext = class {
  constructor(backend) {
    this.backend = backend;
  }
  active = true;
  closePromise;
  async call(method, args) {
    this.assertActive();
    let result;
    try {
      result = await this.backend.dispatch(
        method,
        jsonBoundary(args, "Browser operation arguments")
      );
    } catch (error) {
      if (!this.active) throw staleSessionError();
      throw error;
    }
    this.assertActive();
    return jsonBoundary(result, "Browser operation result");
  }
  async screenshotCall(method, args) {
    const image = await this.call(method, args);
    return {
      bytes: Uint8Array.from(Buffer3.from(image.base64, "base64")),
      mimeType: image.mimeType,
      metadata: {
        width: image.width,
        height: image.height,
        viewport: image.viewport,
        devicePixelRatio: image.devicePixelRatio,
        coordinateSpace: image.coordinateSpace,
        origin: image.origin
      }
    };
  }
  close() {
    this.active = false;
    return this.closePromise ??= this.backend.stop();
  }
  assertActive() {
    if (!this.active) throw staleSessionError();
  }
};
async function createBrowserSdkContext() {
  return new BrowserSdkContext(await createBrowserBackend());
}

// src/index.ts
var singletonSession;
async function setupBrowserRuntime() {
  const pending = singletonSession ??= createBrowserRuntimeSession();
  try {
    return (await pending).agent;
  } catch (error) {
    if (singletonSession === pending) singletonSession = void 0;
    throw error;
  }
}
async function closeBrowserRuntime() {
  const pending = singletonSession;
  singletonSession = void 0;
  if (pending === void 0) return;
  const session = await pending.catch(() => void 0);
  await session?.context.close();
}
async function createBrowserRuntimeSession() {
  const context = await createBrowserSdkContext();
  return { context, agent: new AgentProxy(context) };
}
export {
  closeBrowserRuntime,
  setupBrowserRuntime
};
/**
 * @license
 * Copyright 2026 Qwen Team
 * SPDX-License-Identifier: Apache-2.0
 */
