#!/usr/bin/env node
import { createRequire as __qwenCreateRequire } from 'node:module'; import { fileURLToPath as __qwenFileURLToPath } from 'node:url'; import { dirname as __qwenDirname } from 'node:path'; const require = __qwenCreateRequire(import.meta.url); const __filename = __qwenFileURLToPath(import.meta.url); const __dirname = __qwenDirname(__filename);

// scripts/native-host-setup.ts
import { fileURLToPath } from "node:url";

// src/native-host-installer.ts
import { createHash, randomUUID } from "node:crypto";
import { constants } from "node:fs";
import {
  access,
  chmod,
  mkdir,
  readFile,
  readdir,
  rename,
  rm,
  stat,
  writeFile
} from "node:fs/promises";
import { homedir } from "node:os";
import { dirname, isAbsolute, join, resolve } from "node:path";

// src/bridge/protocol.ts
var CHROME_BRIDGE_PROTOCOL_VERSION = 3;
var CHROME_NATIVE_HOST_NAME = "com.qwen.browser_use";
var CHROME_NATIVE_HOST_REVISION = 2;
var CHROME_EXTENSION_ID = "idkijaaipeeinemigojbjkmfmabokbdk";
var CHROME_WEB_STORE_EXTENSION_ID = "hdhmmjclhibojdddmancfgbkleahfaph";
var CHROME_EXTENSION_IDS = [
  CHROME_EXTENSION_ID,
  CHROME_WEB_STORE_EXTENSION_ID
];
var MAX_BRIDGE_FRAME_BYTES = 16 * 1024 * 1024;

// src/native-host-installer.ts
var LAUNCHER_MARKER = "# qwen-browser-use native host";
var INSTALLATION_MARKER = "# installation ";
var MANIFEST_FILE = CHROME_NATIVE_HOST_NAME + ".json";
var ALLOWED_ORIGINS = CHROME_EXTENSION_IDS.map(
  (id) => "chrome-extension://" + id + "/"
);
async function installChromeNativeHost(options2) {
  const resolved = resolveOptions(options2);
  const hostContents = await readFile(resolved.nativeHostPath, "utf8");
  await access(resolved.nodePath, constants.X_OK);
  const existingLauncher = await readExistingFile(resolved.launcherPath);
  if (existingLauncher !== null && !isOwnedLauncher(existingLauncher)) {
    throw new Error(
      "Refusing to replace a foreign Native Host launcher: " + resolved.launcherPath
    );
  }
  await mkdir(dirname(resolved.launcherPath), {
    recursive: true,
    mode: 448
  });
  const installedHostPath = join(
    dirname(resolved.launcherPath),
    "hosts",
    createHash("sha256").update(hostContents).digest("hex"),
    "native-host.mjs"
  );
  await mkdir(dirname(installedHostPath), { recursive: true, mode: 448 });
  const existingHost = await readExistingFile(installedHostPath);
  if (existingHost !== null && existingHost !== hostContents) {
    throw new Error(
      "Installed Native Host content has changed: " + installedHostPath
    );
  }
  await atomicWrite(installedHostPath, hostContents, 384, existingHost);
  const installation = {
    protocolVersion: CHROME_BRIDGE_PROTOCOL_VERSION,
    revision: CHROME_NATIVE_HOST_REVISION,
    nativeHostPath: installedHostPath,
    nodePath: resolved.nodePath
  };
  await atomicWrite(
    resolved.launcherPath,
    [
      "#!/bin/sh",
      LAUNCHER_MARKER,
      INSTALLATION_MARKER + JSON.stringify(installation),
      "exec " + shellQuote(resolved.nodePath) + " " + shellQuote(installedHostPath) + ' "$@"',
      ""
    ].join("\n"),
    448,
    existingLauncher
  );
  await writeManifests(resolved);
  return await statusChromeNativeHost(options2);
}
async function uninstallChromeNativeHost(options2) {
  const resolved = resolveOptions(options2);
  const skippedForeignPaths = [];
  for (const manifestPath of resolved.manifestPaths) {
    const contents = await readExistingFile(manifestPath);
    if (contents === null) continue;
    if (isOwnedManifest(contents, resolved.launcherPath)) {
      await rm(manifestPath, { force: true });
    } else {
      skippedForeignPaths.push(manifestPath);
    }
  }
  const launcher = await readExistingFile(resolved.launcherPath);
  if (launcher !== null) {
    if (isOwnedLauncher(launcher)) {
      await rm(resolved.launcherPath, { force: true });
      await rm(join(dirname(resolved.launcherPath), "hosts"), {
        recursive: true,
        force: true
      });
    } else {
      skippedForeignPaths.push(resolved.launcherPath);
    }
  }
  return {
    launcherPath: resolved.launcherPath,
    manifestPaths: resolved.manifestPaths,
    skippedForeignPaths
  };
}
async function statusChromeNativeHost(options2) {
  const resolved = resolveOptions(options2);
  const installedPaths = [];
  const skippedForeignPaths = [];
  for (const manifestPath of resolved.manifestPaths) {
    const contents = await readExistingFile(manifestPath);
    if (contents === null) continue;
    if (isOwnedManifest(contents, resolved.launcherPath)) {
      installedPaths.push(manifestPath);
    } else {
      skippedForeignPaths.push(manifestPath);
    }
  }
  const launcher = await readExistingFile(resolved.launcherPath);
  if (launcher !== null && isOwnedLauncher(launcher)) {
    installedPaths.push(resolved.launcherPath);
  } else if (launcher !== null) {
    skippedForeignPaths.push(resolved.launcherPath);
  }
  const installation = readInstallation(launcher);
  return {
    launcherPath: resolved.launcherPath,
    manifestPaths: resolved.manifestPaths,
    installedPaths,
    skippedForeignPaths,
    protocolVersion: installation?.protocolVersion ?? null,
    hostRevision: installation?.revision ?? null,
    nativeHostPath: installation?.nativeHostPath ?? null,
    ready: installation !== null && installedPaths.length > 1 && await pathExists(installation.nativeHostPath, constants.R_OK) && await pathExists(resolved.launcherPath, constants.X_OK) && await pathExists(installation.nodePath, constants.X_OK)
  };
}
function readInstallation(launcher) {
  if (launcher === null || !isOwnedLauncher(launcher)) return null;
  const line = launcher.split("\n")[2];
  if (!line?.startsWith(INSTALLATION_MARKER)) return null;
  try {
    const value = JSON.parse(
      line.slice(INSTALLATION_MARKER.length)
    );
    if (Number.isInteger(value.protocolVersion) && Number.isInteger(value.revision) && typeof value.nativeHostPath === "string" && isAbsolute(value.nativeHostPath) && typeof value.nodePath === "string" && isAbsolute(value.nodePath))
      return value;
  } catch {
  }
  return null;
}
var MAX_STORAGE_FILE_BYTES = 8 * 1024 * 1024;
function nativeHostInstallHome(environment = process.env) {
  const configured = environment["QWEN_BROWSER_USE_INSTALL_HOME"]?.trim();
  return resolve(configured ? configured : homedir());
}
function resolveOptions(options2) {
  const platform = options2.platform ?? process.platform;
  if (platform !== "darwin" && platform !== "linux") {
    throw new Error(
      "Automatic Native Messaging installation supports macOS and Linux"
    );
  }
  const homeDir = resolve(options2.homeDir ?? nativeHostInstallHome());
  const nativeHostPath = options2.nativeHostPath;
  const nodePath = options2.nodePath ?? process.execPath;
  if (!isAbsolute(nativeHostPath) || !isAbsolute(nodePath)) {
    throw new Error("Native Host and Node paths must be absolute");
  }
  const manifestRoots = platform === "darwin" ? [
    "Library/Application Support/Google/Chrome/NativeMessagingHosts",
    "Library/Application Support/Google/Chrome for Testing/NativeMessagingHosts",
    "Library/Application Support/Chromium/NativeMessagingHosts"
  ] : [
    ".config/google-chrome/NativeMessagingHosts",
    ".config/google-chrome-for-testing/NativeMessagingHosts",
    ".config/chromium/NativeMessagingHosts"
  ];
  return {
    nativeHostPath,
    nodePath,
    // Not `native-host.sh`: protocol 2 installers own and rewrite that path.
    launcherPath: join(homeDir, ".qwen/browser-use/host.sh"),
    manifestPaths: manifestRoots.map(
      (root) => join(homeDir, root, MANIFEST_FILE)
    )
  };
}
async function writeManifests(resolved) {
  const manifest = JSON.stringify(
    {
      name: CHROME_NATIVE_HOST_NAME,
      description: "Qwen Browser Use",
      path: resolved.launcherPath,
      type: "stdio",
      allowed_origins: ALLOWED_ORIGINS
    },
    null,
    2
  ) + "\n";
  for (const manifestPath of resolved.manifestPaths) {
    const existing = await readExistingFile(manifestPath);
    if (existing === null ? !await pathExists(dirname(dirname(manifestPath))) : !isOwnedManifest(existing, resolved.launcherPath))
      continue;
    await mkdir(dirname(manifestPath), { recursive: true });
    await atomicWrite(manifestPath, manifest, 384, existing);
  }
}
function isOwnedManifest(contents, launcherPath) {
  try {
    const value = JSON.parse(contents);
    return value["name"] === CHROME_NATIVE_HOST_NAME && value["type"] === "stdio" && value["path"] === launcherPath && Array.isArray(value["allowed_origins"]) && value["allowed_origins"].length > 0 && // A registration listing only some of our origins is an older install of
    // ours to upgrade; one naming any other extension belongs to someone else.
    value["allowed_origins"].every(
      (origin) => ALLOWED_ORIGINS.includes(origin)
    );
  } catch {
    return false;
  }
}
function isOwnedLauncher(contents) {
  return contents.startsWith("#!/bin/sh\n" + LAUNCHER_MARKER + "\n");
}
async function readExistingFile(path) {
  try {
    return await readFile(path, "utf8");
  } catch (error) {
    if (error.code === "ENOENT") return null;
    throw error;
  }
}
async function atomicWrite(target, contents, mode, existingContents) {
  if (existingContents === contents) {
    if (((await stat(target)).mode & 511) !== mode) {
      await chmod(target, mode);
    }
    return;
  }
  const temporary = target + "." + randomUUID() + ".tmp";
  try {
    await writeFile(temporary, contents, { mode });
    await chmod(temporary, mode);
    await rename(temporary, target);
  } finally {
    await rm(temporary, { force: true });
  }
}
async function pathExists(path, mode = constants.F_OK) {
  return await access(path, mode).then(
    () => true,
    () => false
  );
}
function shellQuote(value) {
  return "'" + value.replaceAll("'", "'\\''") + "'";
}

// scripts/native-host-setup.ts
var command = process.argv[2] ?? "status";
var options = {
  homeDir: nativeHostInstallHome(),
  nativeHostPath: fileURLToPath(new URL("../native-host.js", import.meta.url))
};
var result = command === "install" ? await installChromeNativeHost(options) : command === "uninstall" ? await uninstallChromeNativeHost(options) : command === "status" ? await statusChromeNativeHost(options) : null;
if (!result) {
  process.stderr.write(
    "Usage: native-host-setup.js install|status|uninstall\n"
  );
  process.exit(2);
}
process.stdout.write(
  JSON.stringify({
    command,
    ...result,
    ...command === "install" ? {
      activation: "next-host-start",
      notice: "Running Native Hosts and sessions remain active. Reconnect the extension after those sessions finish to use the installed Host."
    } : {}
  }) + "\n"
);
/**
 * @license
 * Copyright 2026 Qwen Team
 * SPDX-License-Identifier: Apache-2.0
 */
