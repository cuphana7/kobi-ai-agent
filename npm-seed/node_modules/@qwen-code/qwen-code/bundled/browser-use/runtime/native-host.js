#!/usr/bin/env node
import { createRequire as __qwenCreateRequire } from 'node:module'; import { fileURLToPath as __qwenFileURLToPath } from 'node:url'; import { dirname as __qwenDirname } from 'node:path'; const require = __qwenCreateRequire(import.meta.url); const __filename = __qwenFileURLToPath(import.meta.url); const __dirname = __qwenDirname(__filename);

// src/bridge/native-host/index.ts
import { randomUUID } from "node:crypto";
import { chmod, lstat as lstat4, rename, unlink as unlink3, writeFile } from "node:fs/promises";
import { connect as connect2, createServer } from "node:net";
import { dirname as dirname3, join as join2 } from "node:path";

// src/bridge/protocol.ts
import { statSync } from "node:fs";
import { posix } from "node:path";
var CHROME_BRIDGE_PROTOCOL_VERSION = 3;
var CHROME_EXTENSION_ID = "idkijaaipeeinemigojbjkmfmabokbdk";
var CHROME_WEB_STORE_EXTENSION_ID = "hdhmmjclhibojdddmancfgbkleahfaph";
var CHROME_EXTENSION_IDS = [
  CHROME_EXTENSION_ID,
  CHROME_WEB_STORE_EXTENSION_ID
];
var MAX_BRIDGE_FRAME_BYTES = 16 * 1024 * 1024;
function defaultChromeBridgeSocketPath(environment = process.env) {
  const configured = environment.QWEN_BROWSER_USE_SOCKET_PATH?.trim();
  if (configured) return configured;
  if (process.platform === "win32") {
    const identity2 = environment.USERNAME?.replace(/[^a-zA-Z0-9_.-]/g, "_") || "default";
    return `\\\\.\\pipe\\qwen-browser-use-${identity2}`;
  }
  const uid = typeof process.getuid === "function" ? process.getuid() : "default";
  return posix.join(defaultChromeBridgeSocketDirectory(uid), "bridge.sock");
}
function defaultChromeBridgeSocketDirectory(uid, platform = process.platform, stat2 = statDirectory) {
  if (platform !== "win32" && typeof uid === "number") {
    const runtimeDir = `/run/user/${uid}`;
    const info = stat2(runtimeDir);
    if (info !== void 0 && info.isDirectory() && info.uid === uid && (info.mode & 63) === 0)
      return runtimeDir;
  }
  const base = platform === "darwin" ? "/private/tmp" : "/tmp";
  return posix.join(base, `qwen-browser-use-${uid}`);
}
function statDirectory(path) {
  try {
    return statSync(path);
  } catch {
    return void 0;
  }
}

// src/bridge/discovery.ts
import { lstat as lstat2, mkdir as mkdir2, readdir, readFile, unlink } from "node:fs/promises";
import { homedir } from "node:os";
import { dirname as dirname2, join } from "node:path";

// src/bridge/socket-path.ts
import { lstat, mkdir, realpath, stat } from "node:fs/promises";
import { dirname, resolve } from "node:path";
async function prepareSocketDirectory(socketPath2) {
  if (process.platform === "win32") return;
  await mkdir(dirname(socketPath2), { mode: 448 }).catch((error) => {
    if (error.code !== "EEXIST") throw error;
  });
  await verifySocketDirectory(socketPath2);
}
async function verifySocketDirectory(socketPath2) {
  if (process.platform === "win32") return;
  const directory2 = dirname(socketPath2);
  const info = await lstat(directory2);
  if (!info.isDirectory() || info.uid !== process.getuid() || (info.mode & 63) !== 0) {
    throw new Error(
      `Chrome bridge requires a private user-owned directory: ${directory2}`
    );
  }
  for (const start2 of [resolve(directory2), await realpath(directory2)]) {
    let ancestor = dirname(start2);
    for (; ; ) {
      const entry = await lstat(ancestor);
      const parent = await stat(ancestor);
      if (entry.uid !== 0 && entry.uid !== process.getuid() || parent.uid !== 0 && parent.uid !== process.getuid() || (parent.mode & 18) !== 0 && !(parent.uid === 0 && (parent.mode & 512) !== 0)) {
        throw new Error(
          `Chrome bridge socket has an unsafe ancestor: ${ancestor}`
        );
      }
      const next = dirname(ancestor);
      if (next === ancestor) break;
      ancestor = next;
    }
  }
}
async function verifySocketPeerPath(socketPath2) {
  if (process.platform === "win32") return;
  await verifySocketDirectory(socketPath2);
  const info = await lstat(socketPath2);
  if (!info.isSocket() || info.uid !== process.getuid()) {
    throw new Error(
      `Chrome bridge requires a user-owned socket: ${socketPath2}`
    );
  }
}

// src/bridge/discovery.ts
var DISCOVERY_DIRECTORY_NAME = "qwen-hosts";
function chromeDiscoveryDirectory() {
  return process.env["QWEN_BROWSER_USE_DISCOVERY_DIR"]?.trim() || (process.platform === "win32" ? join(homedir(), ".qwen", "browser-use", "profiles") : join(
    dirname2(defaultChromeBridgeSocketPath()),
    DISCOVERY_DIRECTORY_NAME
  ));
}
function profileSocketPath(directory2, hostId) {
  return process.platform === "win32" ? `${defaultChromeBridgeSocketPath()}-${hostId}` : join(directory2, `${hostId}.sock`);
}
async function prepareDiscoveryDirectory(directory2) {
  if (process.platform === "win32") {
    await mkdir2(directory2, { recursive: true });
    return;
  }
  await mkdir2(directory2, { recursive: true, mode: 448 });
  await prepareSocketDirectory(join(directory2, "discovery"));
}

// src/bridge/transport/framing.ts
function encodeFrame(value) {
  const payload = Buffer.from(JSON.stringify(value), "utf8");
  if (payload.byteLength > MAX_BRIDGE_FRAME_BYTES) {
    throw new Error(`Bridge frame exceeds ${MAX_BRIDGE_FRAME_BYTES} bytes`);
  }
  const header = Buffer.allocUnsafe(4);
  header.writeUInt32LE(payload.byteLength, 0);
  return Buffer.concat([header, payload]);
}
var FrameDecoder = class {
  buffered = Buffer.alloc(0);
  push(chunk) {
    this.buffered = this.buffered.byteLength === 0 ? chunk : Buffer.concat([this.buffered, chunk]);
    const messages = [];
    while (this.buffered.byteLength >= 4) {
      const length = this.buffered.readUInt32LE(0);
      if (length > MAX_BRIDGE_FRAME_BYTES) {
        throw new Error(
          `Bridge frame declares ${length} bytes; maximum is ${MAX_BRIDGE_FRAME_BYTES}`
        );
      }
      if (this.buffered.byteLength < length + 4) break;
      const payload = this.buffered.subarray(4, length + 4);
      this.buffered = this.buffered.subarray(length + 4);
      messages.push(JSON.parse(payload.toString("utf8")));
    }
    return messages;
  }
};

// src/bridge/native-host/native-messaging-output.ts
var NATIVE_MESSAGE_CHUNK_TYPE = "qwen.browser.chunk";
var MAX_NATIVE_MESSAGE_BYTES = 1048576;
var CHUNK_BYTES = 7e5;
function encodeNativeMessagingOutput(message, chunkId) {
  const direct = encodeFrame(message);
  if (direct.length <= MAX_NATIVE_MESSAGE_BYTES) return [direct];
  const payload = Buffer.from(JSON.stringify(message));
  const total = Math.ceil(payload.length / CHUNK_BYTES);
  return Array.from(
    { length: total },
    (_, index) => encodeFrame({
      type: NATIVE_MESSAGE_CHUNK_TYPE,
      id: chunkId,
      index,
      total,
      data: payload.subarray(index * CHUNK_BYTES, (index + 1) * CHUNK_BYTES).toString("base64")
    })
  );
}

// src/bridge/native-host/listener.ts
import { lstat as lstat3, unlink as unlink2 } from "node:fs/promises";
import { connect } from "node:net";
function isObject(value) {
  return typeof value === "object" && value !== null && !Array.isArray(value);
}
async function listen(server2, socketPath2) {
  await new Promise((resolve2, reject) => {
    const onError = (error) => reject(error);
    server2.once("error", onError);
    server2.listen(socketPath2, () => {
      server2.off("error", onError);
      resolve2();
    });
  });
}
function isAddressInUse(error) {
  return hasErrorCode(error, "EADDRINUSE");
}
function hasErrorCode(error, code) {
  return isObject(error) && error.code === code;
}
async function recoverStaleSocketAndListen(server2, socketPath2) {
  if (!await removeStaleSocket(socketPath2)) return false;
  await listen(server2, socketPath2);
  return true;
}
async function removeStaleSocket(socketPath2) {
  if (process.platform === "win32") return false;
  const info = await lstat3(socketPath2).catch((error) => {
    if (hasErrorCode(error, "ENOENT")) return void 0;
    throw error;
  });
  if (info === void 0) return true;
  if (!info.isSocket()) return false;
  if (typeof process.getuid === "function" && info.uid !== process.getuid())
    return false;
  if (await socketAcceptsConnections(socketPath2)) return false;
  const current = await lstat3(socketPath2).catch((error) => {
    if (hasErrorCode(error, "ENOENT")) return void 0;
    throw error;
  });
  if (current === void 0) return true;
  if (!current.isSocket() || current.dev !== info.dev || current.ino !== info.ino)
    return false;
  await unlink2(socketPath2).catch((error) => {
    if (!hasErrorCode(error, "ENOENT")) throw error;
  });
  return true;
}
async function currentSocketIdentity(socketPath2) {
  if (process.platform === "win32") return void 0;
  const info = await lstat3(socketPath2).catch(() => void 0);
  if (info === void 0 || !info.isSocket()) return void 0;
  return { dev: info.dev, ino: info.ino };
}
async function unlinkOwnedSocket(socketPath2, identity2) {
  if (process.platform === "win32" || identity2 === void 0) return;
  const current = await currentSocketIdentity(socketPath2);
  if (current === void 0 || current.dev !== identity2.dev || current.ino !== identity2.ino)
    return;
  await unlink2(socketPath2).catch((error) => {
    if (!hasErrorCode(error, "ENOENT")) throw error;
  });
}
async function socketAcceptsConnections(socketPath2) {
  return await new Promise((resolve2) => {
    const candidate = connect(socketPath2);
    let settled = false;
    const finish = (active) => {
      if (settled) return;
      settled = true;
      clearTimeout(timer);
      candidate.destroy();
      resolve2(active);
    };
    const timer = setTimeout(() => finish(true), 500);
    candidate.once("connect", () => finish(true));
    candidate.once("error", (error) => {
      finish(
        !hasErrorCode(error, "ECONNREFUSED") && !hasErrorCode(error, "ENOENT")
      );
    });
  });
}

// src/bridge/native-host/index.ts
var hostInstanceId = randomUUID();
var directory = chromeDiscoveryDirectory();
var overridden = Boolean(process.env["QWEN_BROWSER_USE_SOCKET_PATH"]?.trim());
var socketPath = overridden ? defaultChromeBridgeSocketPath() : profileSocketPath(directory, hostInstanceId);
var discoveryPath = overridden ? void 0 : join2(directory, `${hostInstanceId}.json`);
var decoder = new FrameDecoder();
var clients = /* @__PURE__ */ new Map();
var sockets = /* @__PURE__ */ new Set();
var pending = /* @__PURE__ */ new Map();
var server = createServer(accept);
var hello;
var identity;
var outputSequence = 0;
var closing = false;
var starting;
var startupTimer = setTimeout(() => void shutdown(1), 1e4);
var legacySocketPath = overridden ? void 0 : join2(dirname3(directory), "bridge.sock");
var legacyTimer;
var greetedLegacySocket;
function sendNative(message) {
  if (closing) return;
  for (const frame of encodeNativeMessagingOutput(
    message,
    String(++outputSequence)
  ))
    process.stdout.write(frame);
}
function request(client, method, params, originalId) {
  const id = randomUUID();
  const timer = setTimeout(() => {
    pending.delete(id);
    if (method === "session.open") {
      client.reason ??= "disconnected";
      client.state = "closing";
      request(client, "session.close", { reason: client.reason });
      client.socket.destroy();
      return;
    }
    if (method === "session.close") {
      client.state = "closed";
      clients.delete(client.id);
      client.socket.destroy();
    }
    if (originalId !== void 0)
      respond(client, originalId, false, void 0, {
        code: "OPERATION_TIMEOUT",
        message: `Browser Host request timed out: ${method}`
      });
  }, 135e3);
  timer.unref();
  pending.set(id, { client, method, originalId, timer });
  sendNative({
    type: "request",
    id,
    browserSessionId: client.id,
    method,
    params
  });
}
function respond(client, id, ok, result, error) {
  if (!client.socket.destroyed)
    client.socket.write(
      encodeFrame({
        type: "response",
        browserSessionId: client.id,
        id,
        ok,
        result,
        error
      })
    );
}
function closeClient(client, reason, originalId) {
  client.reason ??= reason;
  if (client.state === "opening" || client.state === "closing" || client.state === "closed")
    return;
  client.state = "closing";
  request(client, "session.close", { reason: client.reason }, originalId);
}
function accept(socket) {
  if (closing) {
    socket.destroy();
    return;
  }
  sockets.add(socket);
  const frames = new FrameDecoder();
  let client;
  const timer = setTimeout(() => socket.destroy(), 1e4);
  timer.unref();
  socket.on("error", () => void 0);
  socket.on("close", () => {
    clearTimeout(timer);
    sockets.delete(socket);
    if (client !== void 0 && !closing) closeClient(client, "disconnected");
  });
  socket.on("data", (chunk) => {
    try {
      for (const message of frames.push(chunk)) {
        if (!isObject2(message)) {
          socket.destroy();
          return;
        }
        if (client === void 0) {
          if (message.type !== "client.hello" || hello === void 0) {
            socket.destroy();
            return;
          }
          clearTimeout(timer);
          if (message.protocolVersion !== CHROME_BRIDGE_PROTOCOL_VERSION || hello.protocolVersion !== CHROME_BRIDGE_PROTOCOL_VERSION) {
            socket.end(
              encodeFrame({
                type: "error",
                code: "EXTENSION_VERSION_MISMATCH",
                message: "Browser Use CLI, Native Host and Chrome extension versions must match. Update Qwen Code, run native-host-setup.js install, and reload the extension at chrome://extensions."
              })
            );
            return;
          }
          if (message.extensionInstanceId !== void 0 && message.extensionInstanceId !== hello.extensionInstanceId || message.hostInstanceId !== void 0 && message.hostInstanceId !== hostInstanceId) {
            socket.destroy();
            return;
          }
          client = { socket, id: randomUUID(), state: "opening" };
          clients.set(client.id, client);
          request(client, "session.open", {});
          continue;
        }
        if (client.state !== "active" || message.type !== "request" || typeof message.id !== "string" || typeof message.method !== "string" || !isObject2(message.params) || message.browserSessionId !== client.id || message.method === "session.open") {
          socket.destroy();
          return;
        }
        if (message.method === "session.close")
          closeClient(client, "graceful", message.id);
        else request(client, message.method, message.params, message.id);
      }
    } catch {
      socket.destroy();
    }
  });
}
async function start(extensionHello) {
  hello = extensionHello;
  if (!overridden) await prepareDiscoveryDirectory(directory);
  await prepareSocketDirectory(socketPath);
  try {
    await listen(server, socketPath);
  } catch (error) {
    if (!isAddressInUse(error) || !await recoverStaleSocketAndListen(server, socketPath))
      throw error;
  }
  identity = await currentSocketIdentity(socketPath);
  if (process.platform !== "win32") await chmod(socketPath, 384);
  if (discoveryPath !== void 0) {
    const temporary = `${discoveryPath}.tmp`;
    try {
      await writeFile(
        temporary,
        JSON.stringify({
          extensionInstanceId: hello.extensionInstanceId,
          hostInstanceId,
          protocolVersion: CHROME_BRIDGE_PROTOCOL_VERSION,
          extensionProtocolVersion: hello.protocolVersion,
          socketPath,
          pid: process.pid
        }),
        { mode: 384 }
      );
      await rename(temporary, discoveryPath);
    } finally {
      await unlink3(temporary).catch(() => void 0);
    }
  }
  clearTimeout(startupTimer);
  if (legacySocketPath !== void 0 && hello.protocolVersion === CHROME_BRIDGE_PROTOCOL_VERSION) {
    legacyTimer = setInterval(() => void greetLegacyClient(), 1e3);
    legacyTimer.unref();
    void greetLegacyClient();
  }
}
async function greetLegacyClient() {
  if (legacySocketPath === void 0 || hello === void 0 || closing) return;
  const info = await lstat4(legacySocketPath).catch(() => void 0);
  if (!info?.isSocket() || info.uid !== process.getuid?.()) return;
  const key = `${info.dev}:${info.ino}:${info.birthtimeMs || info.ctimeMs}`;
  if (key === greetedLegacySocket) return;
  greetedLegacySocket = key;
  try {
    await verifySocketPeerPath(legacySocketPath);
  } catch {
    return;
  }
  const legacy = connect2(legacySocketPath);
  legacy.on("error", () => void 0);
  legacy.setTimeout(5e3, () => legacy.destroy());
  legacy.once(
    "connect",
    () => legacy.end(
      encodeFrame({
        type: "hello",
        protocolVersion: CHROME_BRIDGE_PROTOCOL_VERSION,
        extensionId: hello.extensionId,
        extensionInstanceId: hello.extensionInstanceId
      })
    )
  );
}
async function shutdown(code = 0) {
  if (closing) return;
  closing = true;
  clearTimeout(startupTimer);
  clearInterval(legacyTimer);
  for (const item of pending.values()) clearTimeout(item.timer);
  pending.clear();
  for (const socket of sockets) socket.destroy();
  process.stdin.destroy();
  const deadline = setTimeout(() => process.exit(code), 2e3);
  deadline.unref();
  await starting?.catch(() => void 0);
  await unlinkOwnedSocket(socketPath, identity);
  if (discoveryPath !== void 0)
    await unlink3(discoveryPath).catch(() => void 0);
  process.exit(code);
}
process.stdin.on("data", (chunk) => {
  try {
    for (const message of decoder.push(chunk)) {
      if (!isObject2(message)) continue;
      if (hello === void 0 && starting === void 0) {
        if (message.type !== "hello" || !CHROME_EXTENSION_IDS.includes(message.extensionId) || typeof message.protocolVersion !== "number" || typeof message.extensionInstanceId !== "string" || message.extensionInstanceId.length === 0 || message.extensionInstanceId.length > 128) {
          process.stderr.write(
            "Qwen Browser Use Host: refusing a hello from extension " + JSON.stringify(message.extensionId) + " with protocol " + JSON.stringify(message.protocolVersion) + "\n"
          );
          void shutdown(1);
          return;
        }
        starting = start(message);
        void starting.catch(() => shutdown(1));
        continue;
      }
      if (message.type === "event" && typeof message.browserSessionId === "string") {
        const client2 = clients.get(message.browserSessionId);
        if (client2?.state === "active" && !client2.socket.destroyed)
          client2.socket.write(encodeFrame(message));
        continue;
      }
      if (message.type !== "response" || typeof message.id !== "string")
        continue;
      const item = pending.get(message.id);
      if (item === void 0 || item.client.id !== message.browserSessionId)
        continue;
      pending.delete(message.id);
      clearTimeout(item.timer);
      const client = item.client;
      if (item.method === "session.open") {
        if (!message.ok) {
          clients.delete(client.id);
          client.state = "closed";
          client.socket.destroy();
          continue;
        }
        client.state = "active";
        if (client.reason !== void 0 || client.socket.destroyed)
          closeClient(client, client.reason ?? "disconnected");
        else
          client.socket.write(
            encodeFrame({
              ...hello,
              hostInstanceId,
              browserSessionId: client.id
            })
          );
      } else {
        if (item.originalId !== void 0)
          respond(
            client,
            item.originalId,
            message.ok === true,
            message.result,
            message.error
          );
        if (item.method === "session.close") {
          client.state = "closed";
          clients.delete(client.id);
          client.socket.end();
        }
      }
    }
  } catch {
    void shutdown(1);
  }
});
process.stdin.on("end", () => void shutdown());
process.stdin.on("error", () => void shutdown(1));
process.stdout.on("error", () => void shutdown());
process.on("SIGTERM", () => void shutdown());
process.on("SIGINT", () => void shutdown());
function isObject2(value) {
  return typeof value === "object" && value !== null && !Array.isArray(value);
}
/**
 * @license
 * Copyright 2026 Qwen Team
 * SPDX-License-Identifier: Apache-2.0
 */
