// Force strict mode and setup for ESM
"use strict";
import {
  init_esbuild_shims
} from "./chunk-5O2XNYP6.js";
import {
  __name
} from "./chunk-J2S4EL5Y.js";

// packages/core/src/utils/shell-result.ts
init_esbuild_shims();
function isShellResultDisplay(value) {
  if (!value || typeof value !== "object" || Array.isArray(value)) return false;
  const v = value;
  return v["type"] === "shell_result" && v["version"] === 1 && typeof v["text"] === "string" && typeof v["output"] === "string" && typeof v["directory"] === "string" && ["exitCode", "signal", "pid"].every(
    (k) => v[k] === null || typeof v[k] === "number" && Number.isSafeInteger(v[k])
  ) && (v["error"] === null || typeof v["error"] === "string") && typeof v["outcome"] === "string" && ["completed", "failed", "cancelled", "timed_out"].includes(v["outcome"]) && typeof v["truncated"] === "boolean" && Array.isArray(v["notices"]) && v["notices"].length <= 32 && v["notices"].every((s) => typeof s === "string") && Array.isArray(v["outputFiles"]) && v["outputFiles"].length <= 32 && v["outputFiles"].every((s) => typeof s === "string") && v["directory"].length + v["outputFiles"].reduce((n, s) => n + s.length, 0) <= 8192;
}
__name(isShellResultDisplay, "isShellResultDisplay");
function mapShellResultText(result, map) {
  let truncated = result.truncated;
  const apply = /* @__PURE__ */ __name((value) => {
    const next = map(value);
    truncated ||= next !== value;
    return next;
  }, "apply");
  return {
    type: result.type,
    version: result.version,
    directory: result.directory,
    exitCode: result.exitCode,
    signal: result.signal,
    pid: result.pid,
    outcome: result.outcome,
    outputFiles: [...result.outputFiles],
    output: apply(result.output),
    text: apply(result.text),
    error: result.error === null ? null : apply(result.error),
    notices: result.notices.map(apply),
    truncated
  };
}
__name(mapShellResultText, "mapShellResultText");
function shellResultText(value) {
  return typeof value === "string" ? value : isShellResultDisplay(value) ? value.text : void 0;
}
__name(shellResultText, "shellResultText");

export {
  isShellResultDisplay,
  mapShellResultText,
  shellResultText
};
/**
 * @license
 * Copyright 2026 Qwen Team
 * SPDX-License-Identifier: Apache-2.0
 */
