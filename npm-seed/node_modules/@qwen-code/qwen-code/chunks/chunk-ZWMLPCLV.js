// Force strict mode and setup for ESM
"use strict";
import {
  assertShellSandboxCwd,
  encodeTextFileContentAsync
} from "./chunk-LWQKWO5I.js";
import {
  executeBwrap,
  sandboxAsset
} from "./chunk-2SENA3WV.js";
import {
  init_esbuild_shims
} from "./chunk-5O2XNYP6.js";
import {
  __name
} from "./chunk-J2S4EL5Y.js";

// packages/core/src/sandbox/runtime-file.ts
init_esbuild_shims();

// packages/core/src/sandbox/file-version.ts
init_esbuild_shims();
import { statSync } from "node:fs";
function getSandboxFileVersion(filePath) {
  try {
    const stats = statSync(filePath, { bigint: true });
    if (!stats.isFile()) {
      throw Object.assign(new Error("File tools require a regular file."), {
        code: stats.isDirectory() ? "EISDIR" : "EINVAL"
      });
    }
    return Object.freeze({
      dev: stats.dev.toString(),
      ino: stats.ino.toString(),
      size: stats.size.toString(),
      mtimeNs: stats.mtimeNs.toString(),
      ctimeNs: stats.ctimeNs.toString()
    });
  } catch (error) {
    if (error.code === "ENOENT") return null;
    throw error;
  }
}
__name(getSandboxFileVersion, "getSandboxFileVersion");

// packages/core/src/sandbox/file-worker-client.ts
init_esbuild_shims();

// packages/core/src/sandbox/file-worker-protocol.ts
init_esbuild_shims();
var MAX_FILE_HEADER_BYTES = 16 * 1024;
function encodeSandboxWriteRequest(request) {
  const header = Buffer.from(
    JSON.stringify({
      operation: request.operation,
      destination: request.destination,
      expected: request.expected,
      contentLength: request.content.length
    }) + "\n"
  );
  if (header.length > MAX_FILE_HEADER_BYTES)
    throw new Error("File worker header exceeds 16 KiB.");
  return Buffer.concat([header, request.content]);
}
__name(encodeSandboxWriteRequest, "encodeSandboxWriteRequest");

// packages/core/src/sandbox/file-worker-client.ts
async function writeSandboxFile(policy, request, signal) {
  signal.throwIfAborted();
  if (policy.filesystem === "read-only") {
    throw Object.assign(
      new Error("File writes are disabled by the read-only sandbox policy."),
      { code: "EROFS" }
    );
  }
  const stdin = encodeSandboxWriteRequest(request);
  let reply = "";
  let diagnostics = "";
  const appendBounded = /* @__PURE__ */ __name((current, chunk) => (current + chunk).slice(-MAX_FILE_HEADER_BYTES), "appendBounded");
  const handle = await executeBwrap(
    policy,
    {
      executable: process.execPath,
      args: [sandboxAsset("file-worker")],
      cwd: policy.workspace,
      env: { PATH: "/usr/bin:/bin", LANG: "C.UTF-8" },
      stdin
    },
    (event) => {
      if (event.type !== "data" || typeof event.chunk !== "string") return;
      if (event.stream === "stdout") reply = appendBounded(reply, event.chunk);
      else if (event.stream === "stderr")
        diagnostics = appendBounded(diagnostics, event.chunk);
    },
    signal,
    false,
    {},
    { streamStdout: true }
  );
  const result = await handle.result;
  if (result.sandboxStatus.state !== "confirmed" || result.error || result.aborted) {
    const detail = [result.error?.message, diagnostics || result.output].filter(Boolean).join("\n");
    throw new Error(
      `Sandbox file write failed (${result.sandboxStatus.state}): ${detail}`
    );
  }
  let parsedReply;
  try {
    parsedReply = JSON.parse(reply.trim());
  } catch {
    if (result.sandboxStatus.exitCode === 0)
      throw new Error("Invalid sandbox file worker reply.");
    throw new Error(
      `Sandbox file worker exited ${result.sandboxStatus.exitCode}: ${diagnostics || result.output || reply || "invalid reply"}`
    );
  }
  if (result.sandboxStatus.exitCode === 0 && parsedReply["ok"] === true) return;
  if (parsedReply["ok"] === false && typeof parsedReply["error"] === "string") {
    throw Object.assign(new Error(parsedReply["error"]), {
      ...typeof parsedReply["code"] === "string" ? { code: parsedReply["code"] } : {}
    });
  }
  throw new Error("Invalid sandbox file worker reply.");
}
__name(writeSandboxFile, "writeSandboxFile");

// packages/core/src/sandbox/runtime-file.ts
function captureRuntimeFileVersion(config, filePath) {
  if (!config.getShellExecutionSandbox?.()) return void 0;
  return getSandboxFileVersion(filePath);
}
__name(captureRuntimeFileVersion, "captureRuntimeFileVersion");
async function writeRuntimeFile(config, params, expected, signal) {
  const policy = config.getShellExecutionSandbox?.();
  if (!policy) {
    await config.getFileSystemService().writeTextFile(params);
    return;
  }
  signal.throwIfAborted();
  if (expected === void 0) throw new Error("Missing sandbox file version.");
  assertShellSandboxCwd(policy, config.getTargetDir());
  const content = await encodeTextFileContentAsync(
    params.path,
    params.content,
    params._meta
  );
  await writeSandboxFile(
    policy,
    {
      operation: "write",
      destination: params.path,
      content,
      expected
    },
    signal
  );
}
__name(writeRuntimeFile, "writeRuntimeFile");

export {
  captureRuntimeFileVersion,
  writeRuntimeFile
};
/**
 * @license
 * Copyright 2026 Qwen Team
 * SPDX-License-Identifier: Apache-2.0
 */
