import "./chunk-CFMZLE3L.js";
import "./chunk-UEYDN2YO.js";
import "./chunk-HD7O6WLV.js";
import "./chunk-7JDVJX7A.js";
import "./chunk-EAJHTQOM.js";
import "./chunk-5ETULJLP.js";
import "./chunk-NXRQI7RF.js";
import "./chunk-WZAD4ZNJ.js";
import "./chunk-IC63SGIX.js";
import "./chunk-CGIKEQFI.js";
import "./chunk-XZA32HII.js";
import "./chunk-J5LUMSAZ.js";
import "./chunk-HHJLM3WQ.js";
import "./chunk-LFIKUSEE.js";
import "./chunk-GNGQGEZD.js";
import "./chunk-WFNJIZ4G.js";
import "./chunk-MF436P6L.js";
import "./chunk-HDDAI7GZ.js";
import "./chunk-RYSJNY4B.js";
import {
  BaseToolInvocation,
  DeclarativeTool
} from "./chunk-5XEZQQG3.js";
import "./chunk-YPKYQHFS.js";
import {
  SchemaValidator
} from "./chunk-YDWJUBSY.js";
import {
  ToolNames
} from "./chunk-RSWOFJBY.js";
import "./chunk-XRL3EFYD.js";
import "./chunk-QFIRVSJL.js";
import "./chunk-BVNEEYJM.js";
import "./chunk-5R36APDE.js";
import "./chunk-GYAL5QAR.js";
import "./chunk-S34QJ6IR.js";
import "./chunk-MORSIWAC.js";
import "./chunk-K63OUFJ7.js";
import "./chunk-UKMWZ5NS.js";
import "./chunk-EZQTYYWM.js";
import "./chunk-MU2WLMYD.js";
import "./chunk-YQ3U5MUC.js";
import {
  init_esbuild_shims
} from "./chunk-5O2XNYP6.js";
import {
  __name
} from "./chunk-J2S4EL5Y.js";

// packages/core/src/tools/execution-tool.ts
init_esbuild_shims();
import { randomUUID } from "node:crypto";
var cacheGenerations = /* @__PURE__ */ new WeakMap();
async function synchronizeReadCache(environment, config) {
  let state = cacheGenerations.get(environment);
  if (!state) {
    state = {
      generation: config.getFileReadCache().getClearGeneration(),
      pending: Promise.resolve()
    };
    cacheGenerations.set(environment, state);
  }
  const generation = config.getFileReadCache().getClearGeneration();
  const current = state;
  current.pending = current.pending.catch(() => void 0).then(async () => {
    if (current.generation !== generation) {
      await environment.invalidateReadCache();
      current.generation = generation;
    }
  });
  await current.pending;
}
__name(synchronizeReadCache, "synchronizeReadCache");
var ExecutionToolInvocation = class extends BaseToolInvocation {
  constructor(owner, params) {
    super(params);
    this.owner = owner;
  }
  static {
    __name(this, "ExecutionToolInvocation");
  }
  id = randomUUID();
  prepared;
  details;
  callId;
  released;
  preparationAbort = new AbortController();
  modification;
  abortListeners = /* @__PURE__ */ new Map();
  setCallId(callId) {
    this.callId = callId;
    this.modification = this.owner.replaceInvocation(callId, this);
  }
  release() {
    return this.released ??= this.releaseOnce();
  }
  async releaseOnce() {
    for (const [signal, listener] of this.abortListeners)
      signal.removeEventListener("abort", listener);
    this.abortListeners.clear();
    this.preparationAbort.abort();
    try {
      if (this.prepared) {
        await this.prepared.catch(() => void 0);
        await this.owner.environment.release(
          this.id,
          AbortSignal.timeout(3e4)
        );
      }
    } finally {
      this.owner.finishInvocation(this.callId, this);
    }
  }
  getDescription() {
    return this.details?.description ?? this.owner.displayName;
  }
  toolLocations() {
    return this.details?.locations ?? [];
  }
  async prepare(signal) {
    signal.throwIfAborted();
    this.preparationAbort.signal.throwIfAborted();
    await synchronizeReadCache(this.owner.environment, this.owner.config);
    signal.throwIfAborted();
    this.preparationAbort.signal.throwIfAborted();
    if (!this.abortListeners.has(signal)) {
      const listener = /* @__PURE__ */ __name(() => {
        void this.release().catch(() => void 0);
      }, "listener");
      this.abortListeners.set(signal, listener);
      signal.addEventListener("abort", listener, { once: true });
    }
    this.prepared ??= this.owner.environment.prepare(
      {
        id: this.id,
        toolName: this.owner.name,
        params: this.modification?.originalParams ?? this.params,
        ...this.modification ? {
          modification: {
            oldContent: this.modification.oldContent,
            newContent: this.modification.newContent
          }
        } : {}
      },
      AbortSignal.any([signal, this.preparationAbort.signal])
    );
    this.details = await this.prepared;
    Object.assign(this.params, this.details.params);
  }
  async getDefaultPermission(signal = this.preparationAbort.signal) {
    try {
      await this.prepare(signal);
      const permission = await this.owner.environment.permission(
        this.id,
        AbortSignal.any([signal, this.preparationAbort.signal])
      );
      if (permission === "deny") await this.release();
      return permission;
    } catch (error) {
      await this.release().catch(() => void 0);
      throw error;
    }
  }
  async getConfirmationDetails(signal) {
    let details;
    try {
      await this.prepare(signal);
      details = await this.owner.environment.confirmation(this.id, signal);
    } catch (error) {
      await this.release().catch(() => void 0);
      throw error;
    }
    return {
      ...details,
      onConfirm: /* @__PURE__ */ __name(async (outcome, payload) => {
        const current = this.owner.currentInvocation(this.callId);
        if (current && current !== this) {
          const confirmation = await current.getConfirmationDetails(signal);
          await confirmation.onConfirm(outcome, payload);
          return;
        }
        if (outcome === "cancel" /* Cancel */) {
          await this.release().catch(() => void 0);
          return;
        }
        await this.owner.environment.confirm(this.id, outcome, payload, signal);
        if (outcome === "proceed_always" /* ProceedAlways */ && details.type === "edit") {
          this.owner.config.setApprovalMode("auto-edit" /* AUTO_EDIT */);
        }
      }, "onConfirm")
    };
  }
  async execute(signal, updateOutput) {
    try {
      await this.prepare(signal);
      const result = await this.owner.environment.execute(
        this.id,
        signal,
        updateOutput
      );
      return {
        llmContent: result.llmContent,
        returnDisplay: result.returnDisplay,
        ...result.outputBudgetApplied === true ? { outputBudgetApplied: true } : {},
        ...result.error ? { error: result.error } : {},
        persistedOutputFiles: [],
        resultFilePaths: []
      };
    } finally {
      await this.release().catch(() => void 0);
    }
  }
};
var ExecutionTool = class extends DeclarativeTool {
  constructor(original, environment, config) {
    super(
      original.name,
      original.displayName,
      original.name === ToolNames.WRITE_FILE ? original.description.replace(
        WRITE_FILE_ARTIFACT_DESCRIPTION,
        "Automatic session artifact registration is unavailable in this container session, including when record_as_artifact is true. Written files remain in the workspace."
      ) : original.description,
      original.kind,
      original.parameterSchema,
      original.isOutputMarkdown,
      original.canUpdateOutput,
      original.shouldDefer,
      original.alwaysLoad,
      original.searchHint
    );
    this.original = original;
    this.environment = environment;
    this.config = config;
    if (isModifiableDeclarativeTool(original)) {
      Object.defineProperty(this, "getModifyContext", {
        value: /* @__PURE__ */ __name((signal, callId) => {
          if (!callId || !this.invocations.has(callId))
            throw new Error("Container edits require an active tool call.");
          const originalContext = original.getModifyContext(signal);
          const snapshots = /* @__PURE__ */ new Map();
          const content = /* @__PURE__ */ __name((params) => {
            const key = JSON.stringify(params);
            let snapshot = snapshots.get(key);
            if (!snapshot) {
              snapshot = environment.modificationContent(
                original.name,
                params,
                signal
              );
              snapshots.set(key, snapshot);
            }
            return snapshot;
          }, "content");
          return {
            getFilePath: originalContext.getFilePath,
            getCurrentContent: /* @__PURE__ */ __name(async (params) => (await content(params)).current, "getCurrentContent"),
            getProposedContent: /* @__PURE__ */ __name(async (params) => (await content(params)).proposed, "getProposedContent"),
            createUpdatedParams: /* @__PURE__ */ __name((oldContent, newContent, params) => {
              signal.throwIfAborted();
              if (!this.invocations.has(callId))
                throw new Error("The tool call is no longer active.");
              const updated = original.name === ToolNames.NOTEBOOK_EDIT ? { ...params } : originalContext.createUpdatedParams(
                oldContent,
                newContent,
                params
              );
              this.modifications.set(callId, {
                oldContent,
                newContent,
                originalParams: structuredClone(params)
              });
              return updated;
            }, "createUpdatedParams")
          };
        }, "value")
      });
    }
  }
  static {
    __name(this, "ExecutionTool");
  }
  modifications = /* @__PURE__ */ new Map();
  invocations = /* @__PURE__ */ new Map();
  get schema() {
    return this.name === ToolNames.WRITE_FILE ? { ...this.original.schema, description: this.description } : this.original.schema;
  }
  get maxOutputChars() {
    return this.original.maxOutputChars;
  }
  get truncateKeep() {
    return this.original.truncateKeep;
  }
  toAutoClassifierInput(params) {
    return this.original.toAutoClassifierInput(params);
  }
  replaceInvocation(callId, invocation) {
    const modification = this.modifications.get(callId);
    this.modifications.delete(callId);
    const previous = this.invocations.get(callId);
    this.invocations.set(callId, invocation);
    if (previous && previous !== invocation)
      void previous.release().catch(() => void 0);
    return modification;
  }
  currentInvocation(callId) {
    return callId ? this.invocations.get(callId) : void 0;
  }
  finishInvocation(callId, invocation) {
    if (callId && this.invocations.get(callId) === invocation) {
      this.invocations.delete(callId);
      this.modifications.delete(callId);
    }
  }
  validateToolParams(params) {
    return SchemaValidator.validate(this.schema.parametersJsonSchema, params);
  }
  build(params) {
    const error = this.validateToolParams(params);
    if (error) throw new Error(error);
    return new ExecutionToolInvocation(this, params);
  }
};
function wrapExecutionTool(original, environment, config) {
  return new ExecutionTool(original, environment, config);
}
__name(wrapExecutionTool, "wrapExecutionTool");
export {
  wrapExecutionTool
};
/**
 * @license
 * Copyright 2026 Qwen Team
 * SPDX-License-Identifier: Apache-2.0
 */
