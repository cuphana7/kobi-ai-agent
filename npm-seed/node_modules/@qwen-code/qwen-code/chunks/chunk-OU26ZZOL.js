// Force strict mode and setup for ESM
"use strict";
import {
  require_shell_quote
} from "./chunk-QCG3JVIW.js";
import {
  getCurrentAgentId,
  isTopLevelSession,
  spawnBlockReason
} from "./chunk-6GI7NAZS.js";
import {
  getTeammateContext,
  isTeammate
} from "./chunk-S6LOFUVP.js";
import {
  normalizeMcpToolName,
  sanitizeToolNameForProvider
} from "./chunk-WZAD4ZNJ.js";
import {
  ToolNames
} from "./chunk-RSWOFJBY.js";
import {
  createDebugLogger
} from "./chunk-GYAL5QAR.js";
import {
  isNodeError
} from "./chunk-S34QJ6IR.js";
import {
  init_esbuild_shims
} from "./chunk-5O2XNYP6.js";
import {
  __commonJS,
  __name,
  __toESM
} from "./chunk-J2S4EL5Y.js";

// node_modules/picomatch/lib/constants.js
var require_constants = __commonJS({
  "node_modules/picomatch/lib/constants.js"(exports, module) {
    "use strict";
    init_esbuild_shims();
    var WIN_SLASH = "\\\\/";
    var WIN_NO_SLASH = `[^${WIN_SLASH}]`;
    var DEFAULT_MAX_EXTGLOB_RECURSION = 0;
    var DOT_LITERAL = "\\.";
    var PLUS_LITERAL = "\\+";
    var QMARK_LITERAL = "\\?";
    var SLASH_LITERAL = "\\/";
    var ONE_CHAR = "(?=.)";
    var QMARK = "[^/]";
    var END_ANCHOR = `(?:${SLASH_LITERAL}|$)`;
    var START_ANCHOR = `(?:^|${SLASH_LITERAL})`;
    var DOTS_SLASH = `${DOT_LITERAL}{1,2}${END_ANCHOR}`;
    var NO_DOT = `(?!${DOT_LITERAL})`;
    var NO_DOTS = `(?!${START_ANCHOR}${DOTS_SLASH})`;
    var NO_DOT_SLASH = `(?!${DOT_LITERAL}{0,1}${END_ANCHOR})`;
    var NO_DOTS_SLASH = `(?!${DOTS_SLASH})`;
    var QMARK_NO_DOT = `[^.${SLASH_LITERAL}]`;
    var STAR = `${QMARK}*?`;
    var SEP = "/";
    var POSIX_CHARS = {
      DOT_LITERAL,
      PLUS_LITERAL,
      QMARK_LITERAL,
      SLASH_LITERAL,
      ONE_CHAR,
      QMARK,
      END_ANCHOR,
      DOTS_SLASH,
      NO_DOT,
      NO_DOTS,
      NO_DOT_SLASH,
      NO_DOTS_SLASH,
      QMARK_NO_DOT,
      STAR,
      START_ANCHOR,
      SEP
    };
    var WINDOWS_CHARS = {
      ...POSIX_CHARS,
      SLASH_LITERAL: `[${WIN_SLASH}]`,
      QMARK: WIN_NO_SLASH,
      STAR: `${WIN_NO_SLASH}*?`,
      DOTS_SLASH: `${DOT_LITERAL}{1,2}(?:[${WIN_SLASH}]|$)`,
      NO_DOT: `(?!${DOT_LITERAL})`,
      NO_DOTS: `(?!(?:^|[${WIN_SLASH}])${DOT_LITERAL}{1,2}(?:[${WIN_SLASH}]|$))`,
      NO_DOT_SLASH: `(?!${DOT_LITERAL}{0,1}(?:[${WIN_SLASH}]|$))`,
      NO_DOTS_SLASH: `(?!${DOT_LITERAL}{1,2}(?:[${WIN_SLASH}]|$))`,
      QMARK_NO_DOT: `[^.${WIN_SLASH}]`,
      START_ANCHOR: `(?:^|[${WIN_SLASH}])`,
      END_ANCHOR: `(?:[${WIN_SLASH}]|$)`,
      SEP: "\\"
    };
    var POSIX_REGEX_SOURCE = {
      __proto__: null,
      alnum: "a-zA-Z0-9",
      alpha: "a-zA-Z",
      ascii: "\\x00-\\x7F",
      blank: " \\t",
      cntrl: "\\x00-\\x1F\\x7F",
      digit: "0-9",
      graph: "\\x21-\\x7E",
      lower: "a-z",
      print: "\\x20-\\x7E ",
      punct: "\\-!\"#$%&'()\\*+,./:;<=>?@[\\]^_`{|}~",
      space: " \\t\\r\\n\\v\\f",
      upper: "A-Z",
      word: "A-Za-z0-9_",
      xdigit: "A-Fa-f0-9"
    };
    module.exports = {
      DEFAULT_MAX_EXTGLOB_RECURSION,
      MAX_LENGTH: 1024 * 64,
      POSIX_REGEX_SOURCE,
      // regular expressions
      REGEX_BACKSLASH: /\\(?![*+?^${}(|)[\]])/g,
      REGEX_NON_SPECIAL_CHARS: /^[^@![\].,$*+?^{}()|\\/]+/,
      REGEX_SPECIAL_CHARS: /[-*+?.^${}(|)[\]]/,
      REGEX_SPECIAL_CHARS_BACKREF: /(\\?)((\W)(\3*))/g,
      REGEX_SPECIAL_CHARS_GLOBAL: /([-*+?.^${}(|)[\]])/g,
      REGEX_REMOVE_BACKSLASH: /(?:\[.*?[^\\]\]|\\(?=.))/g,
      // Replace globs with equivalent patterns to reduce parsing time.
      REPLACEMENTS: {
        __proto__: null,
        "***": "*",
        "**/**": "**",
        "**/**/**": "**"
      },
      // Digits
      CHAR_0: 48,
      /* 0 */
      CHAR_9: 57,
      /* 9 */
      // Alphabet chars.
      CHAR_UPPERCASE_A: 65,
      /* A */
      CHAR_LOWERCASE_A: 97,
      /* a */
      CHAR_UPPERCASE_Z: 90,
      /* Z */
      CHAR_LOWERCASE_Z: 122,
      /* z */
      CHAR_LEFT_PARENTHESES: 40,
      /* ( */
      CHAR_RIGHT_PARENTHESES: 41,
      /* ) */
      CHAR_ASTERISK: 42,
      /* * */
      // Non-alphabetic chars.
      CHAR_AMPERSAND: 38,
      /* & */
      CHAR_AT: 64,
      /* @ */
      CHAR_BACKWARD_SLASH: 92,
      /* \ */
      CHAR_CARRIAGE_RETURN: 13,
      /* \r */
      CHAR_CIRCUMFLEX_ACCENT: 94,
      /* ^ */
      CHAR_COLON: 58,
      /* : */
      CHAR_COMMA: 44,
      /* , */
      CHAR_DOT: 46,
      /* . */
      CHAR_DOUBLE_QUOTE: 34,
      /* " */
      CHAR_EQUAL: 61,
      /* = */
      CHAR_EXCLAMATION_MARK: 33,
      /* ! */
      CHAR_FORM_FEED: 12,
      /* \f */
      CHAR_FORWARD_SLASH: 47,
      /* / */
      CHAR_GRAVE_ACCENT: 96,
      /* ` */
      CHAR_HASH: 35,
      /* # */
      CHAR_HYPHEN_MINUS: 45,
      /* - */
      CHAR_LEFT_ANGLE_BRACKET: 60,
      /* < */
      CHAR_LEFT_CURLY_BRACE: 123,
      /* { */
      CHAR_LEFT_SQUARE_BRACKET: 91,
      /* [ */
      CHAR_LINE_FEED: 10,
      /* \n */
      CHAR_NO_BREAK_SPACE: 160,
      /* \u00A0 */
      CHAR_PERCENT: 37,
      /* % */
      CHAR_PLUS: 43,
      /* + */
      CHAR_QUESTION_MARK: 63,
      /* ? */
      CHAR_RIGHT_ANGLE_BRACKET: 62,
      /* > */
      CHAR_RIGHT_CURLY_BRACE: 125,
      /* } */
      CHAR_RIGHT_SQUARE_BRACKET: 93,
      /* ] */
      CHAR_SEMICOLON: 59,
      /* ; */
      CHAR_SINGLE_QUOTE: 39,
      /* ' */
      CHAR_SPACE: 32,
      /*   */
      CHAR_TAB: 9,
      /* \t */
      CHAR_UNDERSCORE: 95,
      /* _ */
      CHAR_VERTICAL_LINE: 124,
      /* | */
      CHAR_ZERO_WIDTH_NOBREAK_SPACE: 65279,
      /* \uFEFF */
      /**
       * Create EXTGLOB_CHARS
       */
      extglobChars(chars) {
        return {
          "!": { type: "negate", open: "(?:(?!(?:", close: `))${chars.STAR})` },
          "?": { type: "qmark", open: "(?:", close: ")?" },
          "+": { type: "plus", open: "(?:", close: ")+" },
          "*": { type: "star", open: "(?:", close: ")*" },
          "@": { type: "at", open: "(?:", close: ")" }
        };
      },
      /**
       * Create GLOB_CHARS
       */
      globChars(win32) {
        return win32 === true ? WINDOWS_CHARS : POSIX_CHARS;
      }
    };
  }
});

// node_modules/picomatch/lib/utils.js
var require_utils = __commonJS({
  "node_modules/picomatch/lib/utils.js"(exports) {
    "use strict";
    init_esbuild_shims();
    var {
      REGEX_BACKSLASH,
      REGEX_REMOVE_BACKSLASH,
      REGEX_SPECIAL_CHARS,
      REGEX_SPECIAL_CHARS_GLOBAL
    } = require_constants();
    exports.isObject = (val) => val !== null && typeof val === "object" && !Array.isArray(val);
    exports.hasRegexChars = (str) => REGEX_SPECIAL_CHARS.test(str);
    exports.isRegexChar = (str) => str.length === 1 && exports.hasRegexChars(str);
    exports.escapeRegex = (str) => str.replace(REGEX_SPECIAL_CHARS_GLOBAL, "\\$1");
    exports.toPosixSlashes = (str) => str.replace(REGEX_BACKSLASH, "/");
    exports.isWindows = () => {
      if (typeof navigator !== "undefined" && navigator.platform) {
        const platform = navigator.platform.toLowerCase();
        return platform === "win32" || platform === "windows";
      }
      if (typeof process !== "undefined" && process.platform) {
        return process.platform === "win32";
      }
      return false;
    };
    exports.removeBackslashes = (str) => {
      return str.replace(REGEX_REMOVE_BACKSLASH, (match) => {
        return match === "\\" ? "" : match;
      });
    };
    exports.escapeLast = (input, char, lastIdx) => {
      const idx = input.lastIndexOf(char, lastIdx);
      if (idx === -1) return input;
      if (input[idx - 1] === "\\") return exports.escapeLast(input, char, idx - 1);
      return `${input.slice(0, idx)}\\${input.slice(idx)}`;
    };
    exports.removePrefix = (input, state = {}) => {
      let output = input;
      if (output.startsWith("./")) {
        output = output.slice(2);
        state.prefix = "./";
      }
      return output;
    };
    exports.wrapOutput = (input, state = {}, options = {}) => {
      const prepend = options.contains ? "" : "^";
      const append = options.contains ? "" : "$";
      let output = `${prepend}(?:${input})${append}`;
      if (state.negated === true) {
        output = `(?:^(?!${output}).*$)`;
      }
      return output;
    };
    exports.basename = (path2, { windows } = {}) => {
      const segs = path2.split(windows ? /[\\/]/ : "/");
      const last = segs[segs.length - 1];
      if (last === "") {
        return segs[segs.length - 2];
      }
      return last;
    };
  }
});

// node_modules/picomatch/lib/scan.js
var require_scan = __commonJS({
  "node_modules/picomatch/lib/scan.js"(exports, module) {
    "use strict";
    init_esbuild_shims();
    var utils = require_utils();
    var {
      CHAR_ASTERISK,
      /* * */
      CHAR_AT,
      /* @ */
      CHAR_BACKWARD_SLASH,
      /* \ */
      CHAR_COMMA,
      /* , */
      CHAR_DOT,
      /* . */
      CHAR_EXCLAMATION_MARK,
      /* ! */
      CHAR_FORWARD_SLASH,
      /* / */
      CHAR_LEFT_CURLY_BRACE,
      /* { */
      CHAR_LEFT_PARENTHESES,
      /* ( */
      CHAR_LEFT_SQUARE_BRACKET,
      /* [ */
      CHAR_PLUS,
      /* + */
      CHAR_QUESTION_MARK,
      /* ? */
      CHAR_RIGHT_CURLY_BRACE,
      /* } */
      CHAR_RIGHT_PARENTHESES,
      /* ) */
      CHAR_RIGHT_SQUARE_BRACKET
      /* ] */
    } = require_constants();
    var isPathSeparator = /* @__PURE__ */ __name((code) => {
      return code === CHAR_FORWARD_SLASH || code === CHAR_BACKWARD_SLASH;
    }, "isPathSeparator");
    var depth = /* @__PURE__ */ __name((token) => {
      if (token.isPrefix !== true) {
        token.depth = token.isGlobstar ? Infinity : 1;
      }
    }, "depth");
    var scan = /* @__PURE__ */ __name((input, options) => {
      const opts = options || {};
      const length = input.length - 1;
      const scanToEnd = opts.parts === true || opts.scanToEnd === true;
      const slashes = [];
      const tokens = [];
      const parts = [];
      let str = input;
      let index = -1;
      let start = 0;
      let lastIndex = 0;
      let isBrace = false;
      let isBracket = false;
      let isGlob = false;
      let isExtglob = false;
      let isGlobstar = false;
      let braceEscaped = false;
      let backslashes = false;
      let negated = false;
      let negatedExtglob = false;
      let finished = false;
      let braces = 0;
      let prev;
      let code;
      let token = { value: "", depth: 0, isGlob: false };
      const eos = /* @__PURE__ */ __name(() => index >= length, "eos");
      const peek = /* @__PURE__ */ __name(() => str.charCodeAt(index + 1), "peek");
      const advance = /* @__PURE__ */ __name(() => {
        prev = code;
        return str.charCodeAt(++index);
      }, "advance");
      while (index < length) {
        code = advance();
        let next;
        if (code === CHAR_BACKWARD_SLASH) {
          backslashes = token.backslashes = true;
          code = advance();
          if (code === CHAR_LEFT_CURLY_BRACE) {
            braceEscaped = true;
          }
          continue;
        }
        if (braceEscaped === true || code === CHAR_LEFT_CURLY_BRACE) {
          braces++;
          while (eos() !== true && (code = advance())) {
            if (code === CHAR_BACKWARD_SLASH) {
              backslashes = token.backslashes = true;
              advance();
              continue;
            }
            if (code === CHAR_LEFT_CURLY_BRACE) {
              braces++;
              continue;
            }
            if (braceEscaped !== true && code === CHAR_DOT && (code = advance()) === CHAR_DOT) {
              isBrace = token.isBrace = true;
              isGlob = token.isGlob = true;
              finished = true;
              if (scanToEnd === true) {
                continue;
              }
              break;
            }
            if (braceEscaped !== true && code === CHAR_COMMA) {
              isBrace = token.isBrace = true;
              isGlob = token.isGlob = true;
              finished = true;
              if (scanToEnd === true) {
                continue;
              }
              break;
            }
            if (code === CHAR_RIGHT_CURLY_BRACE) {
              braces--;
              if (braces === 0) {
                braceEscaped = false;
                isBrace = token.isBrace = true;
                finished = true;
                break;
              }
            }
          }
          if (scanToEnd === true) {
            continue;
          }
          break;
        }
        if (code === CHAR_FORWARD_SLASH) {
          slashes.push(index);
          tokens.push(token);
          token = { value: "", depth: 0, isGlob: false };
          if (finished === true) continue;
          if (prev === CHAR_DOT && index === start + 1) {
            start += 2;
            continue;
          }
          lastIndex = index + 1;
          continue;
        }
        if (opts.noext !== true) {
          const isExtglobChar = code === CHAR_PLUS || code === CHAR_AT || code === CHAR_ASTERISK || code === CHAR_QUESTION_MARK || code === CHAR_EXCLAMATION_MARK;
          if (isExtglobChar === true && peek() === CHAR_LEFT_PARENTHESES) {
            isGlob = token.isGlob = true;
            isExtglob = token.isExtglob = true;
            finished = true;
            if (code === CHAR_EXCLAMATION_MARK && index === start) {
              negatedExtglob = true;
            }
            if (scanToEnd === true) {
              while (eos() !== true && (code = advance())) {
                if (code === CHAR_BACKWARD_SLASH) {
                  backslashes = token.backslashes = true;
                  code = advance();
                  continue;
                }
                if (code === CHAR_RIGHT_PARENTHESES) {
                  isGlob = token.isGlob = true;
                  finished = true;
                  break;
                }
              }
              continue;
            }
            break;
          }
        }
        if (code === CHAR_ASTERISK) {
          if (prev === CHAR_ASTERISK) isGlobstar = token.isGlobstar = true;
          isGlob = token.isGlob = true;
          finished = true;
          if (scanToEnd === true) {
            continue;
          }
          break;
        }
        if (code === CHAR_QUESTION_MARK) {
          isGlob = token.isGlob = true;
          finished = true;
          if (scanToEnd === true) {
            continue;
          }
          break;
        }
        if (code === CHAR_LEFT_SQUARE_BRACKET) {
          while (eos() !== true && (next = advance())) {
            if (next === CHAR_BACKWARD_SLASH) {
              backslashes = token.backslashes = true;
              advance();
              continue;
            }
            if (next === CHAR_RIGHT_SQUARE_BRACKET) {
              isBracket = token.isBracket = true;
              isGlob = token.isGlob = true;
              finished = true;
              break;
            }
          }
          if (scanToEnd === true) {
            continue;
          }
          break;
        }
        if (opts.nonegate !== true && code === CHAR_EXCLAMATION_MARK && index === start) {
          negated = token.negated = true;
          start++;
          continue;
        }
        if (opts.noparen !== true && code === CHAR_LEFT_PARENTHESES) {
          isGlob = token.isGlob = true;
          if (scanToEnd === true) {
            while (eos() !== true && (code = advance())) {
              if (code === CHAR_LEFT_PARENTHESES) {
                backslashes = token.backslashes = true;
                code = advance();
                continue;
              }
              if (code === CHAR_RIGHT_PARENTHESES) {
                finished = true;
                break;
              }
            }
            continue;
          }
          break;
        }
        if (isGlob === true) {
          finished = true;
          if (scanToEnd === true) {
            continue;
          }
          break;
        }
      }
      if (opts.noext === true) {
        isExtglob = false;
        isGlob = false;
      }
      let base = str;
      let prefix = "";
      let glob = "";
      if (start > 0) {
        prefix = str.slice(0, start);
        str = str.slice(start);
        lastIndex -= start;
      }
      if (base && isGlob === true && lastIndex > 0) {
        base = str.slice(0, lastIndex);
        glob = str.slice(lastIndex);
      } else if (isGlob === true) {
        base = "";
        glob = str;
      } else {
        base = str;
      }
      if (base && base !== "" && base !== "/" && base !== str) {
        if (isPathSeparator(base.charCodeAt(base.length - 1))) {
          base = base.slice(0, -1);
        }
      }
      if (opts.unescape === true) {
        if (glob) glob = utils.removeBackslashes(glob);
        if (base && backslashes === true) {
          base = utils.removeBackslashes(base);
        }
      }
      const state = {
        prefix,
        input,
        start,
        base,
        glob,
        isBrace,
        isBracket,
        isGlob,
        isExtglob,
        isGlobstar,
        negated,
        negatedExtglob
      };
      if (opts.tokens === true) {
        state.maxDepth = 0;
        if (!isPathSeparator(code)) {
          tokens.push(token);
        }
        state.tokens = tokens;
      }
      if (opts.parts === true || opts.tokens === true) {
        let prevIndex;
        for (let idx = 0; idx < slashes.length; idx++) {
          const n = prevIndex ? prevIndex + 1 : start;
          const i = slashes[idx];
          const value = input.slice(n, i);
          if (opts.tokens) {
            if (idx === 0 && start !== 0) {
              tokens[idx].isPrefix = true;
              tokens[idx].value = prefix;
            } else {
              tokens[idx].value = value;
            }
            depth(tokens[idx]);
            state.maxDepth += tokens[idx].depth;
          }
          if (idx !== 0 || value !== "") {
            parts.push(value);
          }
          prevIndex = i;
        }
        if (prevIndex && prevIndex + 1 < input.length) {
          const value = input.slice(prevIndex + 1);
          parts.push(value);
          if (opts.tokens) {
            tokens[tokens.length - 1].value = value;
            depth(tokens[tokens.length - 1]);
            state.maxDepth += tokens[tokens.length - 1].depth;
          }
        }
        state.slashes = slashes;
        state.parts = parts;
      }
      return state;
    }, "scan");
    module.exports = scan;
  }
});

// node_modules/picomatch/lib/parse.js
var require_parse = __commonJS({
  "node_modules/picomatch/lib/parse.js"(exports, module) {
    "use strict";
    init_esbuild_shims();
    var constants = require_constants();
    var utils = require_utils();
    var {
      MAX_LENGTH,
      POSIX_REGEX_SOURCE,
      REGEX_NON_SPECIAL_CHARS,
      REGEX_SPECIAL_CHARS_BACKREF,
      REPLACEMENTS
    } = constants;
    var expandRange = /* @__PURE__ */ __name((args, options) => {
      if (typeof options.expandRange === "function") {
        return options.expandRange(...args, options);
      }
      args.sort();
      const value = `[${args.join("-")}]`;
      try {
        new RegExp(value);
      } catch (ex) {
        return args.map((v) => utils.escapeRegex(v)).join("..");
      }
      return value;
    }, "expandRange");
    var syntaxError = /* @__PURE__ */ __name((type, char) => {
      return `Missing ${type}: "${char}" - use "\\\\${char}" to match literal characters`;
    }, "syntaxError");
    var splitTopLevel = /* @__PURE__ */ __name((input) => {
      const parts = [];
      let bracket = 0;
      let paren = 0;
      let quote = 0;
      let value = "";
      let escaped = false;
      for (const ch of input) {
        if (escaped === true) {
          value += ch;
          escaped = false;
          continue;
        }
        if (ch === "\\") {
          value += ch;
          escaped = true;
          continue;
        }
        if (ch === '"') {
          quote = quote === 1 ? 0 : 1;
          value += ch;
          continue;
        }
        if (quote === 0) {
          if (ch === "[") {
            bracket++;
          } else if (ch === "]" && bracket > 0) {
            bracket--;
          } else if (bracket === 0) {
            if (ch === "(") {
              paren++;
            } else if (ch === ")" && paren > 0) {
              paren--;
            } else if (ch === "|" && paren === 0) {
              parts.push(value);
              value = "";
              continue;
            }
          }
        }
        value += ch;
      }
      parts.push(value);
      return parts;
    }, "splitTopLevel");
    var isPlainBranch = /* @__PURE__ */ __name((branch) => {
      let escaped = false;
      for (const ch of branch) {
        if (escaped === true) {
          escaped = false;
          continue;
        }
        if (ch === "\\") {
          escaped = true;
          continue;
        }
        if (/[?*+@!()[\]{}]/.test(ch)) {
          return false;
        }
      }
      return true;
    }, "isPlainBranch");
    var normalizeSimpleBranch = /* @__PURE__ */ __name((branch) => {
      let value = branch.trim();
      let changed = true;
      while (changed === true) {
        changed = false;
        if (/^@\([^\\()[\]{}|]+\)$/.test(value)) {
          value = value.slice(2, -1);
          changed = true;
        }
      }
      if (!isPlainBranch(value)) {
        return;
      }
      return value.replace(/\\(.)/g, "$1");
    }, "normalizeSimpleBranch");
    var hasRepeatedCharPrefixOverlap = /* @__PURE__ */ __name((branches) => {
      const values = branches.map(normalizeSimpleBranch).filter(Boolean);
      for (let i = 0; i < values.length; i++) {
        for (let j = i + 1; j < values.length; j++) {
          const a = values[i];
          const b = values[j];
          const char = a[0];
          if (!char || a !== char.repeat(a.length) || b !== char.repeat(b.length)) {
            continue;
          }
          if (a === b || a.startsWith(b) || b.startsWith(a)) {
            return true;
          }
        }
      }
      return false;
    }, "hasRepeatedCharPrefixOverlap");
    var parseRepeatedExtglob = /* @__PURE__ */ __name((pattern, requireEnd = true) => {
      if (pattern[0] !== "+" && pattern[0] !== "*" || pattern[1] !== "(") {
        return;
      }
      let bracket = 0;
      let paren = 0;
      let quote = 0;
      let escaped = false;
      for (let i = 1; i < pattern.length; i++) {
        const ch = pattern[i];
        if (escaped === true) {
          escaped = false;
          continue;
        }
        if (ch === "\\") {
          escaped = true;
          continue;
        }
        if (ch === '"') {
          quote = quote === 1 ? 0 : 1;
          continue;
        }
        if (quote === 1) {
          continue;
        }
        if (ch === "[") {
          bracket++;
          continue;
        }
        if (ch === "]" && bracket > 0) {
          bracket--;
          continue;
        }
        if (bracket > 0) {
          continue;
        }
        if (ch === "(") {
          paren++;
          continue;
        }
        if (ch === ")") {
          paren--;
          if (paren === 0) {
            if (requireEnd === true && i !== pattern.length - 1) {
              return;
            }
            return {
              type: pattern[0],
              body: pattern.slice(2, i),
              end: i
            };
          }
        }
      }
    }, "parseRepeatedExtglob");
    var buildCharClassStar = /* @__PURE__ */ __name((chars) => {
      const source = chars.length === 1 ? utils.escapeRegex(chars[0]) : `[${chars.map((ch) => utils.escapeRegex(ch)).join("")}]`;
      return `${source}*`;
    }, "buildCharClassStar");
    var getStarExtglobSequenceChars = /* @__PURE__ */ __name((pattern) => {
      let index = 0;
      const chars = [];
      while (index < pattern.length) {
        const match = parseRepeatedExtglob(pattern.slice(index), false);
        if (!match || match.type !== "*") {
          return;
        }
        const branches = splitTopLevel(match.body).map((branch2) => branch2.trim());
        if (branches.length !== 1) {
          return;
        }
        const branch = normalizeSimpleBranch(branches[0]);
        if (!branch || branch.length !== 1) {
          return;
        }
        chars.push(branch);
        index += match.end + 1;
      }
      if (chars.length < 1) {
        return;
      }
      return chars;
    }, "getStarExtglobSequenceChars");
    var repeatedExtglobRecursion = /* @__PURE__ */ __name((pattern) => {
      let depth = 0;
      let value = pattern.trim();
      let match = parseRepeatedExtglob(value);
      while (match) {
        depth++;
        value = match.body.trim();
        match = parseRepeatedExtglob(value);
      }
      return depth;
    }, "repeatedExtglobRecursion");
    var analyzeRepeatedExtglob = /* @__PURE__ */ __name((body, options) => {
      if (options.maxExtglobRecursion === false) {
        return { risky: false };
      }
      const max = typeof options.maxExtglobRecursion === "number" ? options.maxExtglobRecursion : constants.DEFAULT_MAX_EXTGLOB_RECURSION;
      const branches = splitTopLevel(body).map((branch) => branch.trim());
      if (branches.length > 1) {
        if (branches.some((branch) => branch === "") || branches.some((branch) => /^[*?]+$/.test(branch)) || hasRepeatedCharPrefixOverlap(branches)) {
          return { risky: true };
        }
      }
      const safeChars = [];
      let sawStarSequence = false;
      let combinable = true;
      for (const branch of branches) {
        const chars = getStarExtglobSequenceChars(branch);
        if (chars) {
          sawStarSequence = true;
          safeChars.push(...chars);
          continue;
        }
        const literal = normalizeSimpleBranch(branch);
        if (literal && literal.length === 1) {
          safeChars.push(literal);
          continue;
        }
        combinable = false;
        if (repeatedExtglobRecursion(branch) > max) {
          return { risky: true };
        }
      }
      if (sawStarSequence) {
        return combinable ? { risky: true, safeOutput: buildCharClassStar([...new Set(safeChars)]) } : { risky: true };
      }
      return { risky: false };
    }, "analyzeRepeatedExtglob");
    var parse2 = /* @__PURE__ */ __name((input, options) => {
      if (typeof input !== "string") {
        throw new TypeError("Expected a string");
      }
      input = REPLACEMENTS[input] || input;
      const opts = { ...options };
      const max = typeof opts.maxLength === "number" ? Math.min(MAX_LENGTH, opts.maxLength) : MAX_LENGTH;
      let len = input.length;
      if (len > max) {
        throw new SyntaxError(`Input length: ${len}, exceeds maximum allowed length: ${max}`);
      }
      const bos = { type: "bos", value: "", output: opts.prepend || "" };
      const tokens = [bos];
      const capture = opts.capture ? "" : "?:";
      const PLATFORM_CHARS = constants.globChars(opts.windows);
      const EXTGLOB_CHARS = constants.extglobChars(PLATFORM_CHARS);
      const {
        DOT_LITERAL,
        PLUS_LITERAL,
        SLASH_LITERAL,
        ONE_CHAR,
        DOTS_SLASH,
        NO_DOT,
        NO_DOT_SLASH,
        NO_DOTS_SLASH,
        QMARK,
        QMARK_NO_DOT,
        STAR,
        START_ANCHOR
      } = PLATFORM_CHARS;
      const globstar = /* @__PURE__ */ __name((opts2) => {
        return `(${capture}(?:(?!${START_ANCHOR}${opts2.dot ? DOTS_SLASH : DOT_LITERAL}).)*?)`;
      }, "globstar");
      const nodot = opts.dot ? "" : NO_DOT;
      const qmarkNoDot = opts.dot ? QMARK : QMARK_NO_DOT;
      let star = opts.bash === true ? globstar(opts) : STAR;
      if (opts.capture) {
        star = `(${star})`;
      }
      if (typeof opts.noext === "boolean") {
        opts.noextglob = opts.noext;
      }
      const state = {
        input,
        index: -1,
        start: 0,
        dot: opts.dot === true,
        consumed: "",
        output: "",
        prefix: "",
        backtrack: false,
        negated: false,
        brackets: 0,
        braces: 0,
        parens: 0,
        quotes: 0,
        globstar: false,
        tokens
      };
      input = utils.removePrefix(input, state);
      len = input.length;
      const extglobs = [];
      const braces = [];
      const stack = [];
      let prev = bos;
      let value;
      const eos = /* @__PURE__ */ __name(() => state.index === len - 1, "eos");
      const peek = state.peek = (n = 1) => input[state.index + n];
      const advance = state.advance = () => input[++state.index] || "";
      const remaining = /* @__PURE__ */ __name(() => input.slice(state.index + 1), "remaining");
      const consume = /* @__PURE__ */ __name((value2 = "", num = 0) => {
        state.consumed += value2;
        state.index += num;
      }, "consume");
      const append = /* @__PURE__ */ __name((token) => {
        state.output += token.output != null ? token.output : token.value;
        consume(token.value);
      }, "append");
      const negate = /* @__PURE__ */ __name(() => {
        let count = 1;
        while (peek() === "!" && (peek(2) !== "(" || peek(3) === "?")) {
          advance();
          state.start++;
          count++;
        }
        if (count % 2 === 0) {
          return false;
        }
        state.negated = true;
        state.start++;
        return true;
      }, "negate");
      const increment = /* @__PURE__ */ __name((type) => {
        state[type]++;
        stack.push(type);
      }, "increment");
      const decrement = /* @__PURE__ */ __name((type) => {
        state[type]--;
        stack.pop();
      }, "decrement");
      const push = /* @__PURE__ */ __name((tok) => {
        if (prev.type === "globstar") {
          const isBrace = state.braces > 0 && (tok.type === "comma" || tok.type === "brace");
          const isExtglob = tok.extglob === true || extglobs.length && (tok.type === "pipe" || tok.type === "paren");
          if (tok.type !== "slash" && tok.type !== "paren" && !isBrace && !isExtglob) {
            state.output = state.output.slice(0, -prev.output.length);
            prev.type = "star";
            prev.value = "*";
            prev.output = star;
            state.output += prev.output;
          }
        }
        if (extglobs.length && tok.type !== "paren") {
          extglobs[extglobs.length - 1].inner += tok.value;
        }
        if (tok.value || tok.output) append(tok);
        if (prev && prev.type === "text" && tok.type === "text") {
          prev.output = (prev.output || prev.value) + tok.value;
          prev.value += tok.value;
          return;
        }
        tok.prev = prev;
        tokens.push(tok);
        prev = tok;
      }, "push");
      const extglobOpen = /* @__PURE__ */ __name((type, value2) => {
        const token = { ...EXTGLOB_CHARS[value2], conditions: 1, inner: "" };
        token.prev = prev;
        token.parens = state.parens;
        token.output = state.output;
        token.startIndex = state.index;
        token.tokensIndex = tokens.length;
        const output = (opts.capture ? "(" : "") + token.open;
        increment("parens");
        push({ type, value: value2, output: state.output ? "" : ONE_CHAR });
        push({ type: "paren", extglob: true, value: advance(), output });
        extglobs.push(token);
      }, "extglobOpen");
      const extglobClose = /* @__PURE__ */ __name((token) => {
        const literal = input.slice(token.startIndex, state.index + 1);
        const body = input.slice(token.startIndex + 2, state.index);
        const analysis = analyzeRepeatedExtglob(body, opts);
        if ((token.type === "plus" || token.type === "star") && analysis.risky) {
          const safeOutput = analysis.safeOutput ? (token.output ? "" : ONE_CHAR) + (opts.capture ? `(${analysis.safeOutput})` : analysis.safeOutput) : void 0;
          const open = tokens[token.tokensIndex];
          open.type = "text";
          open.value = literal;
          open.output = safeOutput || utils.escapeRegex(literal);
          for (let i = token.tokensIndex + 1; i < tokens.length; i++) {
            tokens[i].value = "";
            tokens[i].output = "";
            delete tokens[i].suffix;
          }
          state.output = token.output + open.output;
          state.backtrack = true;
          push({ type: "paren", extglob: true, value, output: "" });
          decrement("parens");
          return;
        }
        let output = token.close + (opts.capture ? ")" : "");
        let rest;
        if (token.type === "negate") {
          let extglobStar = star;
          if (token.inner && token.inner.length > 1 && token.inner.includes("/")) {
            extglobStar = globstar(opts);
          }
          if (extglobStar !== star || eos() || /^\)+$/.test(remaining())) {
            output = token.close = `)$))${extglobStar}`;
          }
          if (token.inner.includes("*") && (rest = remaining()) && /^\.[^\\/.]+$/.test(rest)) {
            const expression = parse2(rest, { ...options, fastpaths: false }).output;
            output = token.close = `)${expression})${extglobStar})`;
          }
          if (token.prev.type === "bos") {
            state.negatedExtglob = true;
          }
        }
        push({ type: "paren", extglob: true, value, output });
        decrement("parens");
      }, "extglobClose");
      if (opts.fastpaths !== false && !/(^[*!]|[/()[\]{}"])/.test(input)) {
        let backslashes = false;
        let output = input.replace(REGEX_SPECIAL_CHARS_BACKREF, (m, esc, chars, first, rest, index) => {
          if (first === "\\") {
            backslashes = true;
            return m;
          }
          if (first === "?") {
            if (esc) {
              return esc + first + (rest ? QMARK.repeat(rest.length) : "");
            }
            if (index === 0) {
              return qmarkNoDot + (rest ? QMARK.repeat(rest.length) : "");
            }
            return QMARK.repeat(chars.length);
          }
          if (first === ".") {
            return DOT_LITERAL.repeat(chars.length);
          }
          if (first === "*") {
            if (esc) {
              return esc + first + (rest ? star : "");
            }
            return star;
          }
          return esc ? m : `\\${m}`;
        });
        if (backslashes === true) {
          if (opts.unescape === true) {
            output = output.replace(/\\/g, "");
          } else {
            output = output.replace(/\\+/g, (m) => {
              return m.length % 2 === 0 ? "\\\\" : m ? "\\" : "";
            });
          }
        }
        if (output === input && opts.contains === true) {
          state.output = input;
          return state;
        }
        state.output = utils.wrapOutput(output, state, options);
        return state;
      }
      while (!eos()) {
        value = advance();
        if (value === "\0") {
          continue;
        }
        if (value === "\\") {
          const next = peek();
          if (next === "/" && opts.bash !== true) {
            continue;
          }
          if (next === "." || next === ";") {
            continue;
          }
          if (!next) {
            value += "\\";
            push({ type: "text", value });
            continue;
          }
          const match = /^\\+/.exec(remaining());
          let slashes = 0;
          if (match && match[0].length > 2) {
            slashes = match[0].length;
            state.index += slashes;
            if (slashes % 2 !== 0) {
              value += "\\";
            }
          }
          if (opts.unescape === true) {
            value = advance();
          } else {
            value += advance();
          }
          if (state.brackets === 0) {
            push({ type: "text", value });
            continue;
          }
        }
        if (state.brackets > 0 && (value !== "]" || prev.value === "[" || prev.value === "[^")) {
          if (opts.posix !== false && value === ":") {
            const inner = prev.value.slice(1);
            if (inner.includes("[")) {
              prev.posix = true;
              if (inner.includes(":")) {
                const idx = prev.value.lastIndexOf("[");
                const pre = prev.value.slice(0, idx);
                const rest2 = prev.value.slice(idx + 2);
                const posix = POSIX_REGEX_SOURCE[rest2];
                if (posix) {
                  prev.value = pre + posix;
                  state.backtrack = true;
                  advance();
                  if (!bos.output && tokens.indexOf(prev) === 1) {
                    bos.output = ONE_CHAR;
                  }
                  continue;
                }
              }
            }
          }
          if (value === "[" && peek() !== ":" || value === "-" && peek() === "]") {
            value = `\\${value}`;
          }
          if (value === "]" && (prev.value === "[" || prev.value === "[^")) {
            value = `\\${value}`;
          }
          if (opts.posix === true && value === "!" && prev.value === "[") {
            value = "^";
          }
          prev.value += value;
          append({ value });
          continue;
        }
        if (state.quotes === 1 && value !== '"') {
          value = utils.escapeRegex(value);
          prev.value += value;
          append({ value });
          continue;
        }
        if (value === '"') {
          state.quotes = state.quotes === 1 ? 0 : 1;
          if (opts.keepQuotes === true) {
            push({ type: "text", value });
          }
          continue;
        }
        if (value === "(") {
          increment("parens");
          push({ type: "paren", value });
          continue;
        }
        if (value === ")") {
          if (state.parens === 0 && opts.strictBrackets === true) {
            throw new SyntaxError(syntaxError("opening", "("));
          }
          const extglob = extglobs[extglobs.length - 1];
          if (extglob && state.parens === extglob.parens + 1) {
            extglobClose(extglobs.pop());
            continue;
          }
          push({ type: "paren", value, output: state.parens ? ")" : "\\)" });
          decrement("parens");
          continue;
        }
        if (value === "[") {
          if (opts.nobracket === true || !remaining().includes("]")) {
            if (opts.nobracket !== true && opts.strictBrackets === true) {
              throw new SyntaxError(syntaxError("closing", "]"));
            }
            value = `\\${value}`;
          } else {
            increment("brackets");
          }
          push({ type: "bracket", value });
          continue;
        }
        if (value === "]") {
          if (opts.nobracket === true || prev && prev.type === "bracket" && prev.value.length === 1) {
            push({ type: "text", value, output: `\\${value}` });
            continue;
          }
          if (state.brackets === 0) {
            if (opts.strictBrackets === true) {
              throw new SyntaxError(syntaxError("opening", "["));
            }
            push({ type: "text", value, output: `\\${value}` });
            continue;
          }
          decrement("brackets");
          const prevValue = prev.value.slice(1);
          if (prev.posix !== true && prevValue[0] === "^" && !prevValue.includes("/")) {
            value = `/${value}`;
          }
          prev.value += value;
          append({ value });
          if (opts.literalBrackets === false || utils.hasRegexChars(prevValue)) {
            continue;
          }
          const escaped = utils.escapeRegex(prev.value);
          state.output = state.output.slice(0, -prev.value.length);
          if (opts.literalBrackets === true) {
            state.output += escaped;
            prev.value = escaped;
            continue;
          }
          prev.value = `(${capture}${escaped}|${prev.value})`;
          state.output += prev.value;
          continue;
        }
        if (value === "{" && opts.nobrace !== true) {
          increment("braces");
          const open = {
            type: "brace",
            value,
            output: "(",
            outputIndex: state.output.length,
            tokensIndex: state.tokens.length
          };
          braces.push(open);
          push(open);
          continue;
        }
        if (value === "}") {
          const brace = braces[braces.length - 1];
          if (opts.nobrace === true || !brace) {
            push({ type: "text", value, output: value });
            continue;
          }
          let output = ")";
          if (brace.dots === true) {
            const arr = tokens.slice();
            const range = [];
            for (let i = arr.length - 1; i >= 0; i--) {
              tokens.pop();
              if (arr[i].type === "brace") {
                break;
              }
              if (arr[i].type !== "dots") {
                range.unshift(arr[i].value);
              }
            }
            output = expandRange(range, opts);
            state.backtrack = true;
          }
          if (brace.comma !== true && brace.dots !== true) {
            const out = state.output.slice(0, brace.outputIndex);
            const toks = state.tokens.slice(brace.tokensIndex);
            brace.value = brace.output = "\\{";
            value = output = "\\}";
            state.output = out;
            for (const t of toks) {
              state.output += t.output || t.value;
            }
          }
          push({ type: "brace", value, output });
          decrement("braces");
          braces.pop();
          continue;
        }
        if (value === "|") {
          if (extglobs.length > 0) {
            extglobs[extglobs.length - 1].conditions++;
          }
          push({ type: "text", value });
          continue;
        }
        if (value === ",") {
          let output = value;
          const brace = braces[braces.length - 1];
          if (brace && stack[stack.length - 1] === "braces") {
            brace.comma = true;
            output = "|";
          }
          push({ type: "comma", value, output });
          continue;
        }
        if (value === "/") {
          if (prev.type === "dot" && state.index === state.start + 1) {
            state.start = state.index + 1;
            state.consumed = "";
            state.output = "";
            tokens.pop();
            prev = bos;
            continue;
          }
          push({ type: "slash", value, output: SLASH_LITERAL });
          continue;
        }
        if (value === ".") {
          if (state.braces > 0 && prev.type === "dot") {
            if (prev.value === ".") prev.output = DOT_LITERAL;
            const brace = braces[braces.length - 1];
            prev.type = "dots";
            prev.output += value;
            prev.value += value;
            brace.dots = true;
            continue;
          }
          if (state.braces + state.parens === 0 && prev.type !== "bos" && prev.type !== "slash") {
            push({ type: "text", value, output: DOT_LITERAL });
            continue;
          }
          push({ type: "dot", value, output: DOT_LITERAL });
          continue;
        }
        if (value === "?") {
          const isGroup = prev && prev.value === "(";
          if (!isGroup && opts.noextglob !== true && peek() === "(" && peek(2) !== "?") {
            extglobOpen("qmark", value);
            continue;
          }
          if (prev && prev.type === "paren") {
            const next = peek();
            let output = value;
            if (prev.value === "(" && !/[!=<:]/.test(next) || next === "<" && !/<([!=]|\w+>)/.test(remaining())) {
              output = `\\${value}`;
            }
            push({ type: "text", value, output });
            continue;
          }
          if (opts.dot !== true && (prev.type === "slash" || prev.type === "bos")) {
            push({ type: "qmark", value, output: QMARK_NO_DOT });
            continue;
          }
          push({ type: "qmark", value, output: QMARK });
          continue;
        }
        if (value === "!") {
          if (opts.noextglob !== true && peek() === "(") {
            if (peek(2) !== "?" || !/[!=<:]/.test(peek(3))) {
              extglobOpen("negate", value);
              continue;
            }
          }
          if (opts.nonegate !== true && state.index === 0) {
            negate();
            continue;
          }
        }
        if (value === "+") {
          if (opts.noextglob !== true && peek() === "(" && peek(2) !== "?") {
            extglobOpen("plus", value);
            continue;
          }
          if (prev && prev.value === "(" || opts.regex === false) {
            push({ type: "plus", value, output: PLUS_LITERAL });
            continue;
          }
          if (prev && (prev.type === "bracket" || prev.type === "paren" || prev.type === "brace") || state.parens > 0) {
            push({ type: "plus", value });
            continue;
          }
          push({ type: "plus", value: PLUS_LITERAL });
          continue;
        }
        if (value === "@") {
          if (opts.noextglob !== true && peek() === "(" && peek(2) !== "?") {
            push({ type: "at", extglob: true, value, output: "" });
            continue;
          }
          push({ type: "text", value });
          continue;
        }
        if (value !== "*") {
          if (value === "$" || value === "^") {
            value = `\\${value}`;
          }
          const match = REGEX_NON_SPECIAL_CHARS.exec(remaining());
          if (match) {
            value += match[0];
            state.index += match[0].length;
          }
          push({ type: "text", value });
          continue;
        }
        if (prev && (prev.type === "globstar" || prev.star === true)) {
          prev.type = "star";
          prev.star = true;
          prev.value += value;
          prev.output = star;
          state.backtrack = true;
          state.globstar = true;
          consume(value);
          continue;
        }
        let rest = remaining();
        if (opts.noextglob !== true && /^\([^?]/.test(rest)) {
          extglobOpen("star", value);
          continue;
        }
        if (prev.type === "star") {
          if (opts.noglobstar === true) {
            consume(value);
            continue;
          }
          const prior = prev.prev;
          const before = prior.prev;
          const isStart = prior.type === "slash" || prior.type === "bos";
          const afterStar = before && (before.type === "star" || before.type === "globstar");
          if (opts.bash === true && (!isStart || rest[0] && rest[0] !== "/")) {
            push({ type: "star", value, output: "" });
            continue;
          }
          const isBrace = state.braces > 0 && (prior.type === "comma" || prior.type === "brace");
          const isExtglob = extglobs.length && (prior.type === "pipe" || prior.type === "paren");
          if (!isStart && prior.type !== "paren" && !isBrace && !isExtglob) {
            push({ type: "star", value, output: "" });
            continue;
          }
          while (rest.slice(0, 3) === "/**") {
            const after = input[state.index + 4];
            if (after && after !== "/") {
              break;
            }
            rest = rest.slice(3);
            consume("/**", 3);
          }
          if (prior.type === "bos" && eos()) {
            prev.type = "globstar";
            prev.value += value;
            prev.output = globstar(opts);
            state.output = prev.output;
            state.globstar = true;
            consume(value);
            continue;
          }
          if (prior.type === "slash" && prior.prev.type !== "bos" && !afterStar && eos()) {
            state.output = state.output.slice(0, -(prior.output + prev.output).length);
            prior.output = `(?:${prior.output}`;
            prev.type = "globstar";
            prev.output = globstar(opts) + (opts.strictSlashes ? ")" : "|$)");
            prev.value += value;
            state.globstar = true;
            state.output += prior.output + prev.output;
            consume(value);
            continue;
          }
          if (prior.type === "slash" && prior.prev.type !== "bos" && rest[0] === "/") {
            const end = rest[1] !== void 0 ? "|$" : "";
            state.output = state.output.slice(0, -(prior.output + prev.output).length);
            prior.output = `(?:${prior.output}`;
            prev.type = "globstar";
            prev.output = `${globstar(opts)}${SLASH_LITERAL}|${SLASH_LITERAL}${end})`;
            prev.value += value;
            state.output += prior.output + prev.output;
            state.globstar = true;
            consume(value + advance());
            push({ type: "slash", value: "/", output: "" });
            continue;
          }
          if (prior.type === "bos" && rest[0] === "/") {
            prev.type = "globstar";
            prev.value += value;
            prev.output = `(?:^|${SLASH_LITERAL}|${globstar(opts)}${SLASH_LITERAL})`;
            state.output = prev.output;
            state.globstar = true;
            consume(value + advance());
            push({ type: "slash", value: "/", output: "" });
            continue;
          }
          state.output = state.output.slice(0, -prev.output.length);
          prev.type = "globstar";
          prev.output = globstar(opts);
          prev.value += value;
          state.output += prev.output;
          state.globstar = true;
          consume(value);
          continue;
        }
        const token = { type: "star", value, output: star };
        if (opts.bash === true) {
          token.output = ".*?";
          if (prev.type === "bos" || prev.type === "slash") {
            token.output = nodot + token.output;
          }
          push(token);
          continue;
        }
        if (prev && (prev.type === "bracket" || prev.type === "paren") && opts.regex === true) {
          token.output = value;
          push(token);
          continue;
        }
        if (state.index === state.start || prev.type === "slash" || prev.type === "dot") {
          if (prev.type === "dot") {
            state.output += NO_DOT_SLASH;
            prev.output += NO_DOT_SLASH;
          } else if (opts.dot === true) {
            state.output += NO_DOTS_SLASH;
            prev.output += NO_DOTS_SLASH;
          } else {
            state.output += nodot;
            prev.output += nodot;
          }
          if (peek() !== "*") {
            state.output += ONE_CHAR;
            prev.output += ONE_CHAR;
          }
        }
        push(token);
      }
      while (state.brackets > 0) {
        if (opts.strictBrackets === true) throw new SyntaxError(syntaxError("closing", "]"));
        state.output = utils.escapeLast(state.output, "[");
        decrement("brackets");
      }
      while (state.parens > 0) {
        if (opts.strictBrackets === true) throw new SyntaxError(syntaxError("closing", ")"));
        state.output = utils.escapeLast(state.output, "(");
        decrement("parens");
      }
      while (state.braces > 0) {
        if (opts.strictBrackets === true) throw new SyntaxError(syntaxError("closing", "}"));
        state.output = utils.escapeLast(state.output, "{");
        decrement("braces");
      }
      if (opts.strictSlashes !== true && (prev.type === "star" || prev.type === "bracket")) {
        push({ type: "maybe_slash", value: "", output: `${SLASH_LITERAL}?` });
      }
      if (state.backtrack === true) {
        state.output = "";
        for (const token of state.tokens) {
          state.output += token.output != null ? token.output : token.value;
          if (token.suffix) {
            state.output += token.suffix;
          }
        }
      }
      return state;
    }, "parse");
    parse2.fastpaths = (input, options) => {
      const opts = { ...options };
      const max = typeof opts.maxLength === "number" ? Math.min(MAX_LENGTH, opts.maxLength) : MAX_LENGTH;
      const len = input.length;
      if (len > max) {
        throw new SyntaxError(`Input length: ${len}, exceeds maximum allowed length: ${max}`);
      }
      input = REPLACEMENTS[input] || input;
      const {
        DOT_LITERAL,
        SLASH_LITERAL,
        ONE_CHAR,
        DOTS_SLASH,
        NO_DOT,
        NO_DOTS,
        NO_DOTS_SLASH,
        STAR,
        START_ANCHOR
      } = constants.globChars(opts.windows);
      const nodot = opts.dot ? NO_DOTS : NO_DOT;
      const slashDot = opts.dot ? NO_DOTS_SLASH : NO_DOT;
      const capture = opts.capture ? "" : "?:";
      const state = { negated: false, prefix: "" };
      let star = opts.bash === true ? ".*?" : STAR;
      if (opts.capture) {
        star = `(${star})`;
      }
      const globstar = /* @__PURE__ */ __name((opts2) => {
        if (opts2.noglobstar === true) return star;
        return `(${capture}(?:(?!${START_ANCHOR}${opts2.dot ? DOTS_SLASH : DOT_LITERAL}).)*?)`;
      }, "globstar");
      const create = /* @__PURE__ */ __name((str) => {
        switch (str) {
          case "*":
            return `${nodot}${ONE_CHAR}${star}`;
          case ".*":
            return `${DOT_LITERAL}${ONE_CHAR}${star}`;
          case "*.*":
            return `${nodot}${star}${DOT_LITERAL}${ONE_CHAR}${star}`;
          case "*/*":
            return `${nodot}${star}${SLASH_LITERAL}${ONE_CHAR}${slashDot}${star}`;
          case "**":
            return nodot + globstar(opts);
          case "**/*":
            return `(?:${nodot}${globstar(opts)}${SLASH_LITERAL})?${slashDot}${ONE_CHAR}${star}`;
          case "**/*.*":
            return `(?:${nodot}${globstar(opts)}${SLASH_LITERAL})?${slashDot}${star}${DOT_LITERAL}${ONE_CHAR}${star}`;
          case "**/.*":
            return `(?:${nodot}${globstar(opts)}${SLASH_LITERAL})?${DOT_LITERAL}${ONE_CHAR}${star}`;
          default: {
            const match = /^(.*?)\.(\w+)$/.exec(str);
            if (!match) return;
            const source2 = create(match[1]);
            if (!source2) return;
            return source2 + DOT_LITERAL + match[2];
          }
        }
      }, "create");
      const output = utils.removePrefix(input, state);
      let source = create(output);
      if (source && opts.strictSlashes !== true) {
        source += `${SLASH_LITERAL}?`;
      }
      return source;
    };
    module.exports = parse2;
  }
});

// node_modules/picomatch/lib/picomatch.js
var require_picomatch = __commonJS({
  "node_modules/picomatch/lib/picomatch.js"(exports, module) {
    "use strict";
    init_esbuild_shims();
    var scan = require_scan();
    var parse2 = require_parse();
    var utils = require_utils();
    var constants = require_constants();
    var isObject = /* @__PURE__ */ __name((val) => val && typeof val === "object" && !Array.isArray(val), "isObject");
    var picomatch2 = /* @__PURE__ */ __name((glob, options, returnState = false) => {
      if (Array.isArray(glob)) {
        const fns = glob.map((input) => picomatch2(input, options, returnState));
        const arrayMatcher = /* @__PURE__ */ __name((str) => {
          for (const isMatch of fns) {
            const state2 = isMatch(str);
            if (state2) return state2;
          }
          return false;
        }, "arrayMatcher");
        return arrayMatcher;
      }
      const isState = isObject(glob) && glob.tokens && glob.input;
      if (glob === "" || typeof glob !== "string" && !isState) {
        throw new TypeError("Expected pattern to be a non-empty string");
      }
      const opts = options || {};
      const posix = opts.windows;
      const regex = isState ? picomatch2.compileRe(glob, options) : picomatch2.makeRe(glob, options, false, true);
      const state = regex.state;
      delete regex.state;
      let isIgnored = /* @__PURE__ */ __name(() => false, "isIgnored");
      if (opts.ignore) {
        const ignoreOpts = { ...options, ignore: null, onMatch: null, onResult: null };
        isIgnored = picomatch2(opts.ignore, ignoreOpts, returnState);
      }
      const matcher = /* @__PURE__ */ __name((input, returnObject = false) => {
        const { isMatch, match, output } = picomatch2.test(input, regex, options, { glob, posix });
        const result = { glob, state, regex, posix, input, output, match, isMatch };
        if (typeof opts.onResult === "function") {
          opts.onResult(result);
        }
        if (isMatch === false) {
          result.isMatch = false;
          return returnObject ? result : false;
        }
        if (isIgnored(input)) {
          if (typeof opts.onIgnore === "function") {
            opts.onIgnore(result);
          }
          result.isMatch = false;
          return returnObject ? result : false;
        }
        if (typeof opts.onMatch === "function") {
          opts.onMatch(result);
        }
        return returnObject ? result : true;
      }, "matcher");
      if (returnState) {
        matcher.state = state;
      }
      return matcher;
    }, "picomatch");
    picomatch2.test = (input, regex, options, { glob, posix } = {}) => {
      if (typeof input !== "string") {
        throw new TypeError("Expected input to be a string");
      }
      if (input === "") {
        return { isMatch: false, output: "" };
      }
      const opts = options || {};
      const format = opts.format || (posix ? utils.toPosixSlashes : null);
      let match = input === glob;
      let output = match && format ? format(input) : input;
      if (match === false) {
        output = format ? format(input) : input;
        match = output === glob;
      }
      if (match === false || opts.capture === true) {
        if (opts.matchBase === true || opts.basename === true) {
          match = picomatch2.matchBase(input, regex, options, posix);
        } else {
          match = regex.exec(output);
        }
      }
      return { isMatch: Boolean(match), match, output };
    };
    picomatch2.matchBase = (input, glob, options, posix = options && options.windows) => {
      const regex = glob instanceof RegExp ? glob : picomatch2.makeRe(glob, options);
      return regex.test(utils.basename(input, { windows: posix }));
    };
    picomatch2.isMatch = (str, patterns, options) => picomatch2(patterns, options)(str);
    picomatch2.parse = (pattern, options) => {
      if (Array.isArray(pattern)) return pattern.map((p) => picomatch2.parse(p, options));
      return parse2(pattern, { ...options, fastpaths: false });
    };
    picomatch2.scan = (input, options) => scan(input, options);
    picomatch2.compileRe = (state, options, returnOutput = false, returnState = false) => {
      if (returnOutput === true) {
        return state.output;
      }
      const opts = options || {};
      const prepend = opts.contains ? "" : "^";
      const append = opts.contains ? "" : "$";
      let source = `${prepend}(?:${state.output})${append}`;
      if (state && state.negated === true) {
        source = `^(?!${source}).*$`;
      }
      const regex = picomatch2.toRegex(source, options);
      if (returnState === true) {
        regex.state = state;
      }
      return regex;
    };
    picomatch2.makeRe = (input, options = {}, returnOutput = false, returnState = false) => {
      if (!input || typeof input !== "string") {
        throw new TypeError("Expected a non-empty string");
      }
      let parsed = { negated: false, fastpaths: true };
      if (options.fastpaths !== false && (input[0] === "." || input[0] === "*")) {
        parsed.output = parse2.fastpaths(input, options);
      }
      if (!parsed.output) {
        parsed = parse2(input, options);
      }
      return picomatch2.compileRe(parsed, options, returnOutput, returnState);
    };
    picomatch2.toRegex = (source, options) => {
      try {
        const opts = options || {};
        return new RegExp(source, opts.flags || (opts.nocase ? "i" : ""));
      } catch (err) {
        if (options && options.debug === true) throw err;
        return /$^/;
      }
    };
    picomatch2.constants = constants;
    module.exports = picomatch2;
  }
});

// node_modules/picomatch/index.js
var require_picomatch2 = __commonJS({
  "node_modules/picomatch/index.js"(exports, module) {
    "use strict";
    init_esbuild_shims();
    var pico = require_picomatch();
    var utils = require_utils();
    function picomatch2(glob, options, returnState = false) {
      if (options && (options.windows === null || options.windows === void 0)) {
        options = { ...options, windows: utils.isWindows() };
      }
      return pico(glob, options, returnState);
    }
    __name(picomatch2, "picomatch");
    Object.assign(picomatch2, pico);
    module.exports = picomatch2;
  }
});

// packages/core/src/permissions/rule-parser.ts
init_esbuild_shims();
var import_picomatch = __toESM(require_picomatch2(), 1);
var import_shell_quote = __toESM(require_shell_quote(), 1);
import path from "node:path";
import fs from "node:fs";
import os from "node:os";
var debugLogger = createDebugLogger("PERMISSIONS");
function toPosixPath(p) {
  return p.replace(/\\/g, "/");
}
__name(toPosixPath, "toPosixPath");
var TOOL_NAME_ALIASES = {
  // Exec tool
  exec: "exec",
  Exec: "exec",
  ExecTool: "exec",
  // Shell tool
  run_shell_command: "run_shell_command",
  Shell: "run_shell_command",
  ShellTool: "run_shell_command",
  Bash: "run_shell_command",
  // Claude Code compatibility
  // Edit tool — "Edit" is also a meta-category covering edit + write_file
  edit: "edit",
  Edit: "edit",
  EditTool: "edit",
  // Notebook Edit tool — also matched by "Edit" meta-category rules
  notebook_edit: "notebook_edit",
  NotebookEdit: "notebook_edit",
  NotebookEditTool: "notebook_edit",
  // Write File tool — also matched by "Edit" meta-category rules
  write_file: "write_file",
  WriteFile: "write_file",
  WriteFileTool: "write_file",
  Write: "write_file",
  // Read File tool — "Read" is also a meta-category covering read_file + grep + glob + list_directory
  read_file: "read_file",
  ReadFile: "read_file",
  ReadFileTool: "read_file",
  Read: "read_file",
  // Zoom Image tool — also matched by "Read" meta-category rules
  zoom_image: "zoom_image",
  ZoomImage: "zoom_image",
  ZoomImageTool: "zoom_image",
  // Grep tool — also matched by "Read" meta-category rules
  grep_search: "grep_search",
  Grep: "grep_search",
  GrepTool: "grep_search",
  search_file_content: "grep_search",
  // legacy
  SearchFiles: "grep_search",
  // legacy display name
  // Glob tool — also matched by "Read" meta-category rules
  glob: "glob",
  Glob: "glob",
  GlobTool: "glob",
  FindFiles: "glob",
  // legacy display name
  // List Directory tool — also matched by "Read" meta-category rules
  list_directory: "list_directory",
  ListFiles: "list_directory",
  ListFilesTool: "list_directory",
  ReadFolder: "list_directory",
  // legacy display name
  // TodoList tool (wire name todo_write; class TodoWriteTool)
  todo_write: "todo_write",
  TodoList: "todo_write",
  // Legacy display name (renamed from "TodoWrite")
  TodoWrite: "todo_write",
  TodoWriteTool: "todo_write",
  // WebFetch tool
  web_fetch: "web_fetch",
  WebFetch: "web_fetch",
  WebFetchTool: "web_fetch",
  // WebSearch tool
  web_search: "web_search",
  WebSearch: "web_search",
  WebSearchTool: "web_search",
  // ReadMcpResource tool
  read_mcp_resource: "read_mcp_resource",
  ReadMcpResource: "read_mcp_resource",
  ReadMcpResourceTool: "read_mcp_resource",
  // Agent (subagent) tool
  agent: "agent",
  Agent: "agent",
  AgentTool: "agent",
  // Legacy aliases for the agent tool (renamed from "task")
  task: "agent",
  Task: "agent",
  TaskTool: "agent",
  // Skill tool
  skill: "skill",
  Skill: "skill",
  SkillTool: "skill",
  // ExitPlanMode tool
  exit_plan_mode: "exit_plan_mode",
  ExitPlanMode: "exit_plan_mode",
  ExitPlanModeTool: "exit_plan_mode",
  // EnterPlanMode tool
  enter_plan_mode: "enter_plan_mode",
  EnterPlanMode: "enter_plan_mode",
  EnterPlanModeTool: "enter_plan_mode",
  // LSP tool
  lsp: "lsp",
  Lsp: "lsp",
  LspTool: "lsp",
  // Monitor tool
  monitor: "monitor",
  Monitor: "monitor",
  MonitorTool: "monitor",
  // Send Message tool (teams)
  send_message: "send_message",
  SendMessage: "send_message",
  SendMessageTool: "send_message",
  // Goal tools — the display name of get_goal is "Goal" (see ToolDisplayNames)
  get_goal: "get_goal",
  Goal: "get_goal",
  GetGoal: "get_goal",
  update_goal: "update_goal",
  UpdateGoal: "update_goal",
  UpdateGoalTool: "update_goal",
  propose_goal: "propose_goal",
  ProposeGoal: "propose_goal",
  ProposeGoalTool: "propose_goal",
  // Save Memory tool
  save_memory: "save_memory",
  SaveMemory: "save_memory",
  SaveMemoryTool: "save_memory",
  // Ask User Question tool
  ask_user_question: "ask_user_question",
  AskUserQuestion: "ask_user_question",
  AskUserQuestionTool: "ask_user_question",
  // Cron tools
  cron_create: "cron_create",
  CronCreate: "cron_create",
  cron_list: "cron_list",
  CronList: "cron_list",
  cron_delete: "cron_delete",
  CronDelete: "cron_delete",
  // Loop wakeup tool
  loop_wakeup: "loop_wakeup",
  LoopWakeup: "loop_wakeup",
  LoopWakeupTool: "loop_wakeup",
  // Create Sub Session tool
  create_sub_session: "create_sub_session",
  CreateSubSession: "create_sub_session",
  CreateSubSessionTool: "create_sub_session",
  // List Agents tool
  list_agents: "list_agents",
  ListAgents: "list_agents",
  ListAgentsTool: "list_agents",
  // Task lifecycle tools (teams)
  task_stop: "task_stop",
  TaskStop: "task_stop",
  task_create: "task_create",
  TaskCreate: "task_create",
  task_update: "task_update",
  TaskUpdate: "task_update",
  task_list: "task_list",
  TaskList: "task_list",
  // Team tools
  team_create: "team_create",
  TeamCreate: "team_create",
  team_delete: "team_delete",
  TeamDelete: "team_delete",
  team_plan_approval: "team_plan_approval",
  TeamPlanApproval: "team_plan_approval",
  // Image generation tool
  image_gen: "image_gen",
  ImageGen: "image_gen",
  ImageGenTool: "image_gen",
  // Omni media tools
  omni_downsample_image: "omni_downsample_image",
  DownsampleImage: "omni_downsample_image",
  omni_downscale_video: "omni_downscale_video",
  DownscaleVideo: "omni_downscale_video",
  omni_downsample_audio: "omni_downsample_audio",
  DownsampleAudio: "omni_downsample_audio",
  omni_extract_keyframes: "omni_extract_keyframes",
  ExtractKeyframes: "omni_extract_keyframes",
  omni_extract_audio: "omni_extract_audio",
  ExtractAudio: "omni_extract_audio",
  omni_clip_video: "omni_clip_video",
  ClipVideo: "omni_clip_video",
  omni_convert_image: "omni_convert_image",
  ConvertImage: "omni_convert_image",
  omni_transcribe_audio: "omni_transcribe_audio",
  TranscribeAudio: "omni_transcribe_audio",
  omni_clip_image: "omni_clip_image",
  ClipImage: "omni_clip_image",
  omni_clip_audio: "omni_clip_audio",
  ClipAudio: "omni_clip_audio",
  omni_caption_image: "omni_caption_image",
  CaptionImage: "omni_caption_image",
  omni_caption_audio: "omni_caption_audio",
  CaptionAudio: "omni_caption_audio",
  omni_ocr_image: "omni_ocr_image",
  OcrImage: "omni_ocr_image",
  omni_understand_video_segments: "omni_understand_video_segments",
  UnderstandVideoSegments: "omni_understand_video_segments",
  omni_recall_media_memory: "omni_recall_media_memory",
  RecallMediaMemory: "omni_recall_media_memory",
  // Tool search tool
  tool_search: "tool_search",
  ToolSearch: "tool_search",
  ToolSearchTool: "tool_search",
  // Deferred-tool invocation bridge
  tool_call: "tool_call",
  ToolCall: "tool_call",
  ToolCallTool: "tool_call",
  // Structured output (synthetic --json-schema contract)
  structured_output: "structured_output",
  StructuredOutput: "structured_output",
  // Worktree tools
  enter_worktree: "enter_worktree",
  EnterWorktree: "enter_worktree",
  exit_worktree: "exit_worktree",
  ExitWorktree: "exit_worktree",
  // Workflow / artifact tools
  workflow: "workflow",
  Workflow: "workflow",
  artifact: "artifact",
  Artifact: "artifact",
  record_artifact: "record_artifact",
  record_source: "record_source",
  RecordSource: "record_source",
  RecordArtifact: "record_artifact",
  // Report Findings tool
  report_findings: "report_findings",
  ReportFindings: "report_findings",
  request_shutdown: "request_shutdown",
  RequestShutdown: "request_shutdown",
  // Display image tool
  display_image: "display_image",
  DisplayImage: "display_image",
  // Legacy edit tool name
  replace: "edit"
};
var SHELL_TOOL_NAMES = /* @__PURE__ */ new Set([
  "run_shell_command",
  "monitor"
]);
var READ_TOOLS = /* @__PURE__ */ new Set([
  "read_file",
  "zoom_image",
  "grep_search",
  "glob",
  "list_directory"
]);
var EDIT_TOOLS = /* @__PURE__ */ new Set(["edit", "write_file", "notebook_edit"]);
var WEBFETCH_TOOLS = /* @__PURE__ */ new Set(["web_fetch"]);
function resolveToolName(rawName) {
  return Object.hasOwn(TOOL_NAME_ALIASES, rawName) ? TOOL_NAME_ALIASES[rawName] : rawName;
}
__name(resolveToolName, "resolveToolName");
var TOOL_NAME_ALIASES_BY_CANONICAL = (() => {
  const byCanonical = /* @__PURE__ */ new Map();
  for (const [alias, canonical] of Object.entries(TOOL_NAME_ALIASES)) {
    const aliases = byCanonical.get(canonical);
    if (aliases) {
      aliases.push(alias);
    } else {
      byCanonical.set(canonical, [alias]);
    }
  }
  return byCanonical;
})();
function getToolNameAliases(canonicalName) {
  return TOOL_NAME_ALIASES_BY_CANONICAL.get(canonicalName) ?? [];
}
__name(getToolNameAliases, "getToolNameAliases");
function getSpecifierKind(canonicalToolName) {
  if (SHELL_TOOL_NAMES.has(canonicalToolName)) {
    return "command";
  }
  if (READ_TOOLS.has(canonicalToolName) || EDIT_TOOLS.has(canonicalToolName)) {
    return "path";
  }
  if (WEBFETCH_TOOLS.has(canonicalToolName)) {
    return "domain";
  }
  return "literal";
}
__name(getSpecifierKind, "getSpecifierKind");
function toolMatchesRuleToolName(ruleToolName, contextToolName) {
  if (ruleToolName === contextToolName) {
    return true;
  }
  if (ruleToolName === "read_file" && READ_TOOLS.has(contextToolName)) {
    return true;
  }
  if (ruleToolName === "edit" && EDIT_TOOLS.has(contextToolName)) {
    return true;
  }
  if (ruleToolName === "run_shell_command" && contextToolName === "monitor") {
    return true;
  }
  return false;
}
__name(toolMatchesRuleToolName, "toolMatchesRuleToolName");
function parseRule(raw) {
  const trimmed = raw.trim();
  const openParen = trimmed.indexOf("(");
  if (openParen === -1) {
    const canonicalName2 = resolveToolName(trimmed);
    return {
      raw: trimmed,
      toolName: canonicalName2
    };
  }
  const toolPart = trimmed.substring(0, openParen).trim();
  if (!trimmed.endsWith(")")) {
    return { raw: trimmed, toolName: resolveToolName(toolPart), invalid: true };
  }
  let rawSpecifier = trimmed.substring(openParen + 1, trimmed.length - 1);
  const canonicalName = resolveToolName(toolPart);
  const specifierKind = rawSpecifier ? getSpecifierKind(canonicalName) : void 0;
  if (specifierKind === "command") {
    rawSpecifier = rawSpecifier.replace(/:(\*)/g, " $1");
  }
  let specifier = rawSpecifier;
  let toolParamMatchers;
  if (specifierKind === "literal" && !canonicalName.startsWith("mcp__") && rawSpecifier.includes(":")) {
    const parts = rawSpecifier.split(",").map((p) => p.trim());
    const plainParts = [];
    const matchers = [];
    for (const part of parts) {
      const colonIdx = part.indexOf(":");
      if (colonIdx > 0) {
        const key = part.substring(0, colonIdx).trim();
        const valuePattern = part.substring(colonIdx + 1).trim();
        if (key && /^[a-zA-Z_][a-zA-Z0-9_]*$/.test(key)) {
          if (valuePattern === "") {
            debugLogger.warn(
              `Empty valuePattern in rule "${trimmed}": key="${key}" will only match empty strings. Use "*" to match any value.`
            );
          }
          matchers.push({ key, valuePattern });
          continue;
        } else if (key && !/^[a-zA-Z_][a-zA-Z0-9_]*$/.test(key)) {
          debugLogger.warn(
            `Invalid key "${key}" in rule "${trimmed}": keys must match /^[a-zA-Z_][a-zA-Z0-9_]*$/. Hyphens and dots are not supported.`
          );
        }
      }
      plainParts.push(part);
    }
    if (matchers.length > 0) {
      toolParamMatchers = matchers;
      specifier = plainParts.join(",").trim() || void 0;
    }
  } else if (specifierKind !== "literal" && rawSpecifier.includes(":") && !rawSpecifier.startsWith("domain:")) {
    debugLogger.warn(
      `key:value syntax is only supported for literal-specifier tools (got ${specifierKind} for "${canonicalName}")`
    );
  }
  return {
    raw: trimmed,
    toolName: canonicalName,
    specifier,
    specifierKind,
    toolParamMatchers
  };
}
__name(parseRule, "parseRule");
function parseRules(raws) {
  return raws.filter((r) => r && r.trim()).map(parseRule).map((r) => {
    if (r.invalid) {
      debugLogger.warn(
        `Ignoring malformed rule (unbalanced parentheses): ${r.raw}`
      );
    }
    return r;
  });
}
__name(parseRules, "parseRules");
var CANONICAL_TO_RULE_DISPLAY = {
  // Read meta-category
  read_file: "Read",
  zoom_image: "Read",
  grep_search: "Read",
  glob: "Read",
  list_directory: "Read",
  // Edit meta-category
  edit: "Edit",
  write_file: "Edit",
  notebook_edit: "Edit",
  // Shell
  run_shell_command: "Bash",
  // Monitor
  monitor: "Monitor",
  // Web
  web_fetch: "WebFetch",
  web_search: "WebSearch",
  read_mcp_resource: "ReadMcpResource",
  // Agent / Skill
  agent: "Agent",
  skill: "Skill",
  // Others
  save_memory: "SaveMemory",
  todo_write: "TodoList",
  lsp: "Lsp",
  exit_plan_mode: "ExitPlanMode",
  enter_plan_mode: "EnterPlanMode"
};
function getRuleDisplayName(canonicalToolName) {
  return CANONICAL_TO_RULE_DISPLAY[canonicalToolName] ?? canonicalToolName;
}
__name(getRuleDisplayName, "getRuleDisplayName");
var FILE_TARGETED_TOOLS = /* @__PURE__ */ new Set([
  "read_file",
  "zoom_image",
  "edit",
  "write_file",
  "notebook_edit"
]);
function buildPermissionRules(ctx) {
  const canonicalName = resolveToolName(ctx.toolName);
  const displayName = getRuleDisplayName(canonicalName);
  const kind = getSpecifierKind(canonicalName);
  switch (kind) {
    case "command":
      if (ctx.command) {
        return [`${displayName}(${ctx.command})`];
      }
      return [displayName];
    case "path":
      if (ctx.filePath) {
        const dirPath = FILE_TARGETED_TOOLS.has(canonicalName) ? path.dirname(ctx.filePath) : ctx.filePath;
        const specifier = dirPath.startsWith("/") ? `/${dirPath}/**` : `${dirPath}/**`;
        return [`${displayName}(${specifier})`];
      }
      return [displayName];
    case "domain":
      if (ctx.domain) {
        return [`${displayName}(${ctx.domain})`];
      }
      return [displayName];
    case "literal":
    default: {
      if (canonicalName.startsWith("mcp__")) {
        return [displayName];
      }
      const parts = [];
      if (ctx.specifier) parts.push(ctx.specifier);
      const stableParamKeys = /* @__PURE__ */ new Set(["model", "subagent_type", "skill"]);
      if (ctx.toolParams) {
        for (const key of stableParamKeys) {
          const v = ctx.toolParams[key];
          if (typeof v === "string" || typeof v === "number") {
            if (ctx.specifier && String(v) === ctx.specifier) continue;
            parts.push(`${key}:${v}`);
          }
        }
      }
      if (parts.length > 0) return [`${displayName}(${parts.join(",")})`];
      return [displayName];
    }
  }
}
__name(buildPermissionRules, "buildPermissionRules");
var DISPLAY_NAME_TO_VERB = {
  Read: "read files",
  Edit: "edit files",
  Bash: "run commands",
  Monitor: "monitor commands",
  WebFetch: "fetch from",
  WebSearch: "search the web",
  Agent: "use agent",
  Skill: "use skill",
  SaveMemory: "save memory",
  TodoList: "write todos",
  Lsp: "use LSP",
  ExitPlanMode: "exit plan mode",
  EnterPlanMode: "enter plan mode"
};
function cleanPathSpecifier(specifier) {
  let cleaned = specifier;
  cleaned = cleaned.replace(/\/\*\*$/, "/").replace(/\/\*$/, "/");
  if (cleaned.startsWith("//")) {
    cleaned = cleaned.substring(1);
  }
  if (!cleaned.endsWith("/")) {
    cleaned += "/";
  }
  return cleaned;
}
__name(cleanPathSpecifier, "cleanPathSpecifier");
function buildHumanReadableRuleLabel(rules) {
  if (!rules.length) return "";
  const parts = [];
  for (const rule of rules) {
    const parenIdx = rule.indexOf("(");
    if (parenIdx === -1) {
      const verb2 = DISPLAY_NAME_TO_VERB[rule] ?? rule.toLowerCase();
      parts.push(verb2);
      continue;
    }
    const displayName = rule.substring(0, parenIdx);
    const specifier = rule.substring(parenIdx + 1, rule.length - 1);
    const verb = DISPLAY_NAME_TO_VERB[displayName] ?? displayName.toLowerCase();
    const canonicalName = Object.entries(CANONICAL_TO_RULE_DISPLAY).find(
      ([, v]) => v === displayName
    )?.[0];
    const kind = canonicalName ? getSpecifierKind(canonicalName) : "literal";
    switch (kind) {
      case "path": {
        const cleanPath = cleanPathSpecifier(specifier);
        parts.push(`${verb} in ${cleanPath}`);
        break;
      }
      case "command": {
        const cmdVerb = DISPLAY_NAME_TO_VERB[displayName] ?? "run";
        const verbWord = cmdVerb.split(" ")[0];
        parts.push(`${verbWord} '${specifier}' commands`);
        break;
      }
      case "domain":
        parts.push(`${verb} ${specifier}`);
        break;
      case "literal":
      default:
        parts.push(`${verb} "${specifier}"`);
        break;
    }
  }
  return parts.join(", ");
}
__name(buildHumanReadableRuleLabel, "buildHumanReadableRuleLabel");
var SHELL_OPERATORS = ["&&", "||", ";;", "|&", "|", ";", "&", "\n"];
var BASH_WORD_SEPARATORS = [" ", "	", "\n"];
function trimBashWordSeparators(text) {
  let start = 0;
  let end = text.length;
  while (start < end && BASH_WORD_SEPARATORS.includes(text[start])) {
    start++;
  }
  while (end > start && BASH_WORD_SEPARATORS.includes(text[end - 1])) {
    end--;
  }
  return text.slice(start, end);
}
__name(trimBashWordSeparators, "trimBashWordSeparators");
function precedingBackslashCount(command, index) {
  let count = 0;
  for (let k = index - 1; k >= 0 && command[k] === "\\"; k--) {
    count++;
  }
  return count;
}
__name(precedingBackslashCount, "precedingBackslashCount");
function isAsyncOperator(command, index) {
  if (command[index + 1] === ">") {
    return false;
  }
  for (let j = index - 1; j >= 0; j--) {
    const ch = command[j];
    if (BASH_WORD_SEPARATORS.includes(ch)) {
      continue;
    }
    if (ch === ">" || ch === "<") {
      return precedingBackslashCount(command, j) % 2 === 1;
    }
    return true;
  }
  return true;
}
__name(isAsyncOperator, "isAsyncOperator");
var CR_IS_WHOLE_REDIRECT_TARGET = /(^|[ \t])(?:<<-?|1>>|1>|>>|>|2>>|2>|&>>|&>|<)\r$/;
function splitCompoundCommandSegments(command) {
  const segments = [];
  let inSingle = false;
  let inDouble = false;
  let escaped = false;
  let lastSplit = 0;
  let arithmeticDepth = 0;
  for (let i = 0; i < command.length; i++) {
    const ch = command[i];
    if (escaped) {
      escaped = false;
      continue;
    }
    if (ch === "\\") {
      escaped = true;
      continue;
    }
    if (ch === "'" && !inDouble) {
      inSingle = !inSingle;
      continue;
    }
    if (ch === '"' && !inSingle) {
      inDouble = !inDouble;
      continue;
    }
    if (inSingle || inDouble) {
      continue;
    }
    if (ch === "(" && command[i + 1] === "(") {
      arithmeticDepth++;
      i++;
      continue;
    }
    if (arithmeticDepth > 0 && ch === ")" && command[i + 1] === ")") {
      arithmeticDepth--;
      i++;
      continue;
    }
    for (const op of SHELL_OPERATORS) {
      if (command.substring(i, i + op.length) !== op) {
        continue;
      }
      if (op === "&" && (arithmeticDepth > 0 || !isAsyncOperator(command, i))) {
        continue;
      }
      const raw = command.substring(lastSplit, i);
      const dropsLineEndingCR = op === "\n" && !CR_IS_WHOLE_REDIRECT_TARGET.test(raw);
      const segment = trimBashWordSeparators(
        dropsLineEndingCR ? raw.replace(/\r$/, "") : raw
      );
      if (segment) {
        segments.push({ command: segment, terminator: op });
      }
      lastSplit = i + op.length;
      i = lastSplit - 1;
      break;
    }
  }
  const lastSegment = trimBashWordSeparators(command.substring(lastSplit));
  if (lastSegment) {
    segments.push({ command: lastSegment, terminator: "" });
  }
  return segments;
}
__name(splitCompoundCommandSegments, "splitCompoundCommandSegments");
function splitCompoundCommand(command) {
  const commands = splitCompoundCommandSegments(command).map(
    (segment) => segment.command
  );
  return commands.length > 0 ? commands : [command];
}
__name(splitCompoundCommand, "splitCompoundCommand");
function matchesCommandPattern(pattern, command) {
  const normalizedCommand = stripLeadingVariableAssignments(command);
  if (pattern === "*") {
    return true;
  }
  if (!pattern.includes("*")) {
    return normalizedCommand === pattern || normalizedCommand.startsWith(pattern + " ");
  }
  let regex = "^";
  let pos = 0;
  while (pos < pattern.length) {
    const starIdx = pattern.indexOf("*", pos);
    if (starIdx === -1) {
      regex += escapeRegex(pattern.substring(pos));
      break;
    }
    const literalBefore = pattern.substring(pos, starIdx);
    if (starIdx > 0 && pattern[starIdx - 1] === " ") {
      const literalWithoutTrailingSpace = literalBefore.slice(0, -1);
      regex += escapeRegex(literalWithoutTrailingSpace);
      regex += "( .*)?";
    } else {
      regex += escapeRegex(literalBefore);
      regex += ".*";
    }
    pos = starIdx + 1;
  }
  regex += "$";
  try {
    return new RegExp(regex, "s").test(normalizedCommand);
  } catch {
    return normalizedCommand === pattern;
  }
}
__name(matchesCommandPattern, "matchesCommandPattern");
function matchesParamValuePattern(pattern, value) {
  const normalizedPattern = pattern.toLowerCase();
  const normalizedValue = value.toLowerCase();
  if (normalizedPattern === "*") {
    return true;
  }
  if (!normalizedPattern.includes("*")) {
    return normalizedValue === normalizedPattern;
  }
  const segments = normalizedPattern.split("*");
  let pos = 0;
  if (segments[0].length > 0) {
    if (!normalizedValue.startsWith(segments[0])) {
      return false;
    }
    pos = segments[0].length;
  }
  for (let i = 1; i < segments.length - 1; i++) {
    const seg = segments[i];
    if (seg.length === 0) continue;
    const idx = normalizedValue.indexOf(seg, pos);
    if (idx === -1) {
      return false;
    }
    pos = idx + seg.length;
  }
  const last = segments[segments.length - 1];
  if (last.length > 0) {
    if (normalizedValue.length - pos < last.length) {
      return false;
    }
    return normalizedValue.endsWith(last);
  }
  return true;
}
__name(matchesParamValuePattern, "matchesParamValuePattern");
function evaluateParamMatchers(matchers, toolParams, ruleRaw) {
  if (!toolParams) {
    debugLogger.debug(`Param matcher rule "${ruleRaw}" skipped: no toolParams`);
    return false;
  }
  for (const matcher of matchers) {
    if (!Object.hasOwn(toolParams, matcher.key)) {
      debugLogger.debug(
        `Param matcher failed: rule="${ruleRaw}" key=${matcher.key} expected="${matcher.valuePattern}" actual=missing`
      );
      return false;
    }
    const actualValue = toolParams[matcher.key];
    if (actualValue === void 0 || actualValue === null) {
      debugLogger.debug(
        `Param matcher failed: rule="${ruleRaw}" key=${matcher.key} expected="${matcher.valuePattern}" actual=null/undefined`
      );
      return false;
    }
    if (typeof actualValue !== "string" && typeof actualValue !== "number") {
      debugLogger.debug(
        `Param matcher skipped: rule="${ruleRaw}" key=${matcher.key} value is ${typeof actualValue}`
      );
      return false;
    }
    const actualStr = String(actualValue);
    if (!matchesParamValuePattern(matcher.valuePattern, actualStr)) {
      debugLogger.debug(
        `Param matcher failed: rule="${ruleRaw}" key=${matcher.key} expected="${matcher.valuePattern}" actual="${actualStr}"`
      );
      return false;
    }
  }
  return true;
}
__name(evaluateParamMatchers, "evaluateParamMatchers");
function escapeRegex(s) {
  return s.replace(/[.+?^${}()|[\]\\]/g, "\\$&");
}
__name(escapeRegex, "escapeRegex");
var ENV_ASSIGNMENT_REGEX = /^[A-Za-z_][A-Za-z0-9_]*=/;
function stripLeadingVariableAssignments(command) {
  const trimmed = command.trim();
  if (!trimmed) {
    return trimmed;
  }
  try {
    const tokens = [];
    for (const token of (0, import_shell_quote.parse)(trimmed)) {
      if (typeof token === "string") {
        tokens.push(token);
      } else if (token && typeof token === "object" && "op" in token && typeof token.op === "string") {
        tokens.push(token.op);
      }
    }
    let firstCommandToken = 0;
    while (firstCommandToken < tokens.length && ENV_ASSIGNMENT_REGEX.test(tokens[firstCommandToken])) {
      firstCommandToken++;
    }
    return tokens.slice(firstCommandToken).join(" ");
  } catch {
    return trimmed;
  }
}
__name(stripLeadingVariableAssignments, "stripLeadingVariableAssignments");
function resolvePathPattern(specifier, projectRoot, cwd) {
  if (specifier.startsWith("//")) {
    return specifier.substring(1);
  }
  if (specifier.startsWith("~/")) {
    return toPosixPath(path.join(os.homedir(), specifier.substring(2)));
  }
  if (specifier.startsWith("/")) {
    return toPosixPath(path.join(projectRoot, specifier.substring(1)));
  }
  if (specifier.startsWith("./")) {
    return toPosixPath(path.join(cwd, specifier.substring(2)));
  }
  return toPosixPath(path.join(cwd, specifier));
}
__name(resolvePathPattern, "resolvePathPattern");
var LINE_TERMINATOR_PLACEHOLDER = "";
function neutralizeLineTerminators(value) {
  return value.replace(/[\r\n\u2028\u2029]/g, LINE_TERMINATOR_PLACEHOLDER);
}
__name(neutralizeLineTerminators, "neutralizeLineTerminators");
function matchesPathPattern(specifier, filePath, projectRoot, cwd, matchMode = "lexical") {
  const resolvedPattern = resolvePathPattern(specifier, projectRoot, cwd);
  const patterns = matchMode === "canonical" ? getCanonicalPatternCandidates(resolvedPattern) : [resolvedPattern];
  const matchers = patterns.map(
    (pattern) => (0, import_picomatch.default)(neutralizeLineTerminators(pattern), {
      dot: true,
      nocase: false
    })
  );
  const paths = matchMode === "canonical" ? getCanonicalPathMatchCandidates(filePath, cwd) : [toPosixPath(filePath)];
  return paths.some(
    (candidate) => matchers.some((isMatch) => isMatch(neutralizeLineTerminators(candidate)))
  );
}
__name(matchesPathPattern, "matchesPathPattern");
function getCanonicalPatternCandidates(resolvedPattern) {
  const candidates = /* @__PURE__ */ new Set([resolvedPattern]);
  const { base } = import_picomatch.default.scan(resolvedPattern);
  const canonicalBase = realpathNearestExisting(base);
  if (canonicalBase !== void 0) {
    candidates.add(
      `${toPosixPath(canonicalBase)}${resolvedPattern.slice(base.length)}`
    );
  }
  return [...candidates];
}
__name(getCanonicalPatternCandidates, "getCanonicalPatternCandidates");
function getCanonicalPathMatchCandidates(filePath, cwd) {
  const candidates = /* @__PURE__ */ new Set([toPosixPath(filePath)]);
  const absolutePath = resolveWithoutNormalizing(cwd, filePath);
  const canonicalPath = realpathNearestExisting(absolutePath);
  if (canonicalPath !== void 0) {
    candidates.add(toPosixPath(canonicalPath));
  }
  return [...candidates];
}
__name(getCanonicalPathMatchCandidates, "getCanonicalPathMatchCandidates");
function resolveWithoutNormalizing(base, filePath) {
  return path.isAbsolute(filePath) ? filePath : `${base}${/[\\/]$/.test(base) ? "" : path.sep}${filePath}`;
}
__name(resolveWithoutNormalizing, "resolveWithoutNormalizing");
function realpathNearestExisting(filePath) {
  let current = filePath;
  const missingSegments = [];
  let symlinkHops = 0;
  const maxSymlinkHops = 40;
  while (true) {
    try {
      return path.join(fs.realpathSync.native(current), ...missingSegments);
    } catch (error) {
      if (!isNodeError(error) || error.code !== "ENOENT" && error.code !== "ENOTDIR") {
        return void 0;
      }
    }
    try {
      if (fs.lstatSync(current).isSymbolicLink()) {
        if (symlinkHops++ >= maxSymlinkHops) {
          return void 0;
        }
        const target = fs.readlinkSync(current);
        const parent2 = fs.realpathSync.native(path.dirname(current));
        current = resolveWithoutNormalizing(parent2, target);
        continue;
      }
    } catch (error) {
      if (!isNodeError(error) || error.code !== "ENOENT" && error.code !== "ENOTDIR") {
        return void 0;
      }
    }
    const parent = path.dirname(current);
    if (parent === current) {
      return void 0;
    }
    missingSegments.unshift(path.basename(current));
    current = parent;
  }
}
__name(realpathNearestExisting, "realpathNearestExisting");
function matchesDomainPattern(specifier, domain) {
  const pattern = specifier.startsWith("domain:") ? specifier.substring(7).trim() : specifier.trim();
  if (!pattern || !domain) {
    return false;
  }
  const normalizedDomain = domain.toLowerCase();
  const normalizedPattern = pattern.toLowerCase();
  if (normalizedDomain === normalizedPattern) {
    return true;
  }
  if (normalizedDomain.endsWith("." + normalizedPattern)) {
    return true;
  }
  return false;
}
__name(matchesDomainPattern, "matchesDomainPattern");
function matchesMcpPattern(pattern, toolName) {
  if (pattern === toolName) {
    return true;
  }
  if (!pattern.endsWith("*") && pattern.split("__").length >= 3 && normalizeMcpToolName(pattern) === normalizeMcpToolName(toolName)) {
    return true;
  }
  if (pattern.endsWith("*")) {
    const prefix = sanitizeToolNameForProvider(pattern.slice(0, -1));
    return sanitizeToolNameForProvider(toolName).startsWith(prefix);
  }
  const patternParts = pattern.split("__");
  const toolParts = toolName.split("__");
  if (patternParts.length === 2 && toolParts.length >= 3 && patternParts[0] === toolParts[0] && sanitizeToolNameForProvider(patternParts[1]) === sanitizeToolNameForProvider(toolParts[1])) {
    return true;
  }
  return false;
}
__name(matchesMcpPattern, "matchesMcpPattern");
function matchesToolPattern(pattern, toolName) {
  return toolName.startsWith("mcp__") ? matchesMcpPattern(pattern, toolName) : pattern === toolName;
}
__name(matchesToolPattern, "matchesToolPattern");
function matchesRule(rule, toolName, command, filePath, domain, pathContext, specifier, toolParams, toolAliases, pathMatchMode = "lexical") {
  const canonicalCtxToolName = resolveToolName(toolName);
  if (rule.invalid) {
    return false;
  }
  if (rule.toolName.startsWith("mcp__") || canonicalCtxToolName.startsWith("mcp__")) {
    const matchesLegacyExactName = !rule.toolName.endsWith("*") && rule.toolName.split("__").length >= 3 && (toolAliases ?? []).some(
      (alias) => rule.toolName === resolveToolName(alias)
    );
    const matchesMcpName = matchesMcpPattern(rule.toolName, canonicalCtxToolName) || matchesLegacyExactName;
    if (!matchesMcpName) {
      return false;
    }
    if (rule.specifier) {
      debugLogger.debug(
        `MCP rule "${rule.raw}" has specifier "${rule.specifier}" \u2014 MCP tool names already encode server identity; specifier not supported`
      );
      return false;
    }
    if (rule.toolParamMatchers?.length) {
      if (!evaluateParamMatchers(rule.toolParamMatchers, toolParams, rule.raw)) {
        return false;
      }
    }
    return true;
  }
  if (!toolMatchesRuleToolName(rule.toolName, canonicalCtxToolName)) {
    return false;
  }
  if (!rule.specifier && !rule.toolParamMatchers?.length) {
    return true;
  }
  const kind = rule.specifierKind ?? getSpecifierKind(rule.toolName);
  let specifierMatched = true;
  if (rule.specifier) {
    switch (kind) {
      case "command": {
        if (command === void 0) {
          return false;
        }
        specifierMatched = matchesCommandPattern(rule.specifier, command);
        break;
      }
      case "path": {
        if (filePath === void 0) {
          return false;
        }
        const ctx = pathContext ?? {
          projectRoot: process.cwd(),
          cwd: process.cwd()
        };
        specifierMatched = matchesPathPattern(
          rule.specifier,
          filePath,
          ctx.projectRoot,
          ctx.cwd,
          pathMatchMode
        );
        break;
      }
      case "domain": {
        if (domain === void 0) {
          return false;
        }
        specifierMatched = matchesDomainPattern(rule.specifier, domain);
        break;
      }
      case "literal":
      default: {
        const value = command ?? specifier;
        specifierMatched = value !== void 0 && value === rule.specifier;
        break;
      }
    }
  }
  if (!specifierMatched) {
    return false;
  }
  if (rule.toolParamMatchers?.length) {
    if (!evaluateParamMatchers(rule.toolParamMatchers, toolParams, rule.raw)) {
      return false;
    }
  }
  return true;
}
__name(matchesRule, "matchesRule");

// packages/core/src/agents/runtime/subagent-plan-tool-policy.ts
init_esbuild_shims();
var SUBAGENT_PLAN_LIFECYCLE_TOOLS = /* @__PURE__ */ new Set([
  ToolNames.ENTER_PLAN_MODE,
  ToolNames.EXIT_PLAN_MODE
]);
var EXCLUDED_TOOLS_FOR_SUBAGENTS = /* @__PURE__ */ new Set([
  ToolNames.AGENT,
  ToolNames.CRON_CREATE,
  ToolNames.CRON_LIST,
  ToolNames.CRON_DELETE,
  ToolNames.LIST_AGENTS,
  ToolNames.TASK_STOP,
  ToolNames.SEND_MESSAGE,
  ToolNames.TEAM_CREATE,
  ToolNames.TEAM_DELETE,
  ToolNames.TEAM_PLAN_APPROVAL,
  ToolNames.REQUEST_SHUTDOWN,
  ToolNames.TASK_CREATE,
  ToolNames.TASK_UPDATE,
  ToolNames.TASK_LIST,
  ToolNames.TODO_WRITE,
  ...SUBAGENT_PLAN_LIFECYCLE_TOOLS,
  // Worktree management belongs to the parent session — a subagent must
  // never enter or exit the user's worktree state independently.
  ToolNames.ENTER_WORKTREE,
  ToolNames.EXIT_WORKTREE,
  // V1 session artifacts and sources are owned by the parent daemon session.
  ToolNames.ARTIFACT,
  ToolNames.RECORD_ARTIFACT,
  ToolNames.RECORD_SOURCE,
  // FIX-8 (SEC-I1): WORKFLOW is excluded to prevent unbounded recursive
  // fan-out: a subagent spawned by Workflow that calls Workflow would create
  // O(k^n) subagents.
  ToolNames.WORKFLOW
]);
var EXCLUDED_TOOLS_FOR_TEAMMATES = /* @__PURE__ */ new Set([
  ToolNames.AGENT,
  ToolNames.CRON_CREATE,
  ToolNames.CRON_LIST,
  ToolNames.CRON_DELETE,
  ToolNames.LIST_AGENTS,
  ToolNames.TASK_STOP,
  ToolNames.TEAM_CREATE,
  ToolNames.TEAM_DELETE,
  ToolNames.TEAM_PLAN_APPROVAL,
  ToolNames.REQUEST_SHUTDOWN,
  ToolNames.TODO_WRITE,
  ...SUBAGENT_PLAN_LIFECYCLE_TOOLS,
  // Worktree management belongs to the parent session.
  ToolNames.ENTER_WORKTREE,
  ToolNames.EXIT_WORKTREE,
  ToolNames.RECORD_SOURCE,
  // Same recursion guard as EXCLUDED_TOOLS_FOR_SUBAGENTS: the teammate
  // identity propagates through AsyncLocalStorage into anything it
  // spawns, so prepareTools() would keep choosing THIS exclusion set
  // for nested agents — without WORKFLOW here, a teammate-launched
  // workflow re-arms the O(k^n) fan-out the subagent set prevents.
  ToolNames.WORKFLOW
]);
function getExcludedToolsForCurrentContext() {
  if (!isTeammate()) {
    return EXCLUDED_TOOLS_FOR_SUBAGENTS;
  }
  if (!isPlanRequiredTeammateContext()) {
    return EXCLUDED_TOOLS_FOR_TEAMMATES;
  }
  const excluded = new Set(EXCLUDED_TOOLS_FOR_TEAMMATES);
  excluded.delete(ToolNames.EXIT_PLAN_MODE);
  return excluded;
}
__name(getExcludedToolsForCurrentContext, "getExcludedToolsForCurrentContext");
var READ_ONLY_INSPECTION_TOOLS = [
  ToolNames.READ_FILE,
  ToolNames.GREP,
  ToolNames.GLOB,
  ToolNames.LS,
  ToolNames.LSP,
  ToolNames.TOOL_SEARCH,
  ToolNames.READ_MCP_RESOURCE
];
var PLAN_REQUIRED_TEAMMATE_PRE_APPROVAL_TOOLS = /* @__PURE__ */ new Set([
  ToolNames.EXIT_PLAN_MODE,
  ToolNames.TASK_LIST,
  ...READ_ONLY_INSPECTION_TOOLS
]);
var PRE_APPROVAL_TASK_CLAIM_KEYS = /* @__PURE__ */ new Set([
  "taskId",
  "status",
  "owner",
  "addBlocks",
  "addBlockedBy"
]);
function isSubagentLikeExecutionContext() {
  return getCurrentAgentId() !== null || isTeammate();
}
__name(isSubagentLikeExecutionContext, "isSubagentLikeExecutionContext");
function matchesAgentToolBlocklist(blocklist, toolName) {
  if (!blocklist?.length) {
    return false;
  }
  return blocklist.some(
    (pattern) => toolName.startsWith("mcp__") ? matchesMcpPattern(pattern, toolName) : pattern === toolName
  );
}
__name(matchesAgentToolBlocklist, "matchesAgentToolBlocklist");
function isToolExcludedForCurrentContext(toolName, maxSubagentDepth) {
  if (toolName === ToolNames.AGENT) {
    if (maxSubagentDepth === void 0) {
      return true;
    }
    const nestingAllowed = !isTopLevelSession() && spawnBlockReason(maxSubagentDepth) === null;
    return !nestingAllowed;
  }
  return getExcludedToolsForCurrentContext().has(toolName);
}
__name(isToolExcludedForCurrentContext, "isToolExcludedForCurrentContext");
function getExcludedToolUnavailableMessage(toolName) {
  return `Tool "${toolName}" is not available to this agent.`;
}
__name(getExcludedToolUnavailableMessage, "getExcludedToolUnavailableMessage");
function isPlanRequiredTeammateContext() {
  return getTeammateContext()?.planModeRequired === true;
}
__name(isPlanRequiredTeammateContext, "isPlanRequiredTeammateContext");
function isPlanRequiredTeammateAwaitingApproval(config) {
  return isPlanRequiredTeammateContext() && config.getApprovalMode() === "plan" /* PLAN */;
}
__name(isPlanRequiredTeammateAwaitingApproval, "isPlanRequiredTeammateAwaitingApproval");
function isPlanLifecycleToolUnavailableInSubagent(toolName) {
  if (!isSubagentLikeExecutionContext()) return false;
  if (toolName === ToolNames.ENTER_PLAN_MODE) return true;
  if (toolName === ToolNames.EXIT_PLAN_MODE) {
    return !isPlanRequiredTeammateContext();
  }
  return false;
}
__name(isPlanLifecycleToolUnavailableInSubagent, "isPlanLifecycleToolUnavailableInSubagent");
function shouldUsePlanOnlyReminderInSubagentContext() {
  return isSubagentLikeExecutionContext() && !isPlanRequiredTeammateContext();
}
__name(shouldUsePlanOnlyReminderInSubagentContext, "shouldUsePlanOnlyReminderInSubagentContext");
function isLeaderOnlyToolUnavailableInSubagent(toolName) {
  return isSubagentLikeExecutionContext() && toolName === ToolNames.TEAM_PLAN_APPROVAL;
}
__name(isLeaderOnlyToolUnavailableInSubagent, "isLeaderOnlyToolUnavailableInSubagent");
function getLeaderOnlyToolUnavailableMessage(toolName) {
  return `${toolName} is only available to the team leader. Subagents and teammates cannot approve teammate plans.`;
}
__name(getLeaderOnlyToolUnavailableMessage, "getLeaderOnlyToolUnavailableMessage");
function getPlanRequiredTeammatePreApprovalMessage(toolName) {
  return `${toolName} is not available while this plan-required teammate is waiting for leader approval. Finish investigation, call exit_plan_mode with the proposed plan, and wait for the leader to approve it before taking execution actions.`;
}
__name(getPlanRequiredTeammatePreApprovalMessage, "getPlanRequiredTeammatePreApprovalMessage");
function isPlanRequiredTeammatePreApprovalAllowedTool(toolName, params) {
  if (PLAN_REQUIRED_TEAMMATE_PRE_APPROVAL_TOOLS.has(toolName)) {
    return true;
  }
  if (toolName !== ToolNames.TASK_UPDATE) {
    return false;
  }
  return isPreApprovalClaimOnlyTaskUpdate(params);
}
__name(isPlanRequiredTeammatePreApprovalAllowedTool, "isPlanRequiredTeammatePreApprovalAllowedTool");
function isPreApprovalClaimOnlyTaskUpdate(params) {
  if (typeof params !== "object" || params === null || Array.isArray(params)) {
    return false;
  }
  const taskParams = params;
  for (const key of Object.keys(taskParams)) {
    if (!PRE_APPROVAL_TASK_CLAIM_KEYS.has(key)) {
      return false;
    }
  }
  const agentName = getTeammateContext()?.agentName;
  return typeof taskParams["taskId"] === "string" && taskParams["status"] === "in_progress" && (taskParams["owner"] === void 0 || taskParams["owner"] === agentName) && isAbsentOrEmptyArray(taskParams["addBlocks"]) && isAbsentOrEmptyArray(taskParams["addBlockedBy"]);
}
__name(isPreApprovalClaimOnlyTaskUpdate, "isPreApprovalClaimOnlyTaskUpdate");
function isAbsentOrEmptyArray(value) {
  return value === void 0 || Array.isArray(value) && value.length === 0;
}
__name(isAbsentOrEmptyArray, "isAbsentOrEmptyArray");
function getSubagentPlanToolUnavailableMessage(toolName) {
  return `${toolName} is not available inside subagents or team agents. Plan mode is owned by the caller/main session; return your plan, findings, or constraints to the caller in your normal response instead of entering or exiting plan mode.`;
}
__name(getSubagentPlanToolUnavailableMessage, "getSubagentPlanToolUnavailableMessage");
function buildSubagentPlanToolBlockedResult(toolName, logTag, logger) {
  const message = getSubagentPlanToolUnavailableMessage(toolName);
  logger.warn(
    `[${logTag}] Blocked plan lifecycle tool call from subagent: ${toolName}`
  );
  return {
    llmContent: message,
    returnDisplay: message,
    error: { message }
  };
}
__name(buildSubagentPlanToolBlockedResult, "buildSubagentPlanToolBlockedResult");

export {
  require_picomatch2 as require_picomatch,
  SHELL_TOOL_NAMES,
  resolveToolName,
  getToolNameAliases,
  toolMatchesRuleToolName,
  parseRule,
  parseRules,
  getRuleDisplayName,
  buildPermissionRules,
  buildHumanReadableRuleLabel,
  splitCompoundCommandSegments,
  splitCompoundCommand,
  matchesToolPattern,
  matchesRule,
  SUBAGENT_PLAN_LIFECYCLE_TOOLS,
  EXCLUDED_TOOLS_FOR_SUBAGENTS,
  READ_ONLY_INSPECTION_TOOLS,
  isSubagentLikeExecutionContext,
  matchesAgentToolBlocklist,
  isToolExcludedForCurrentContext,
  getExcludedToolUnavailableMessage,
  isPlanRequiredTeammateContext,
  isPlanRequiredTeammateAwaitingApproval,
  isPlanLifecycleToolUnavailableInSubagent,
  shouldUsePlanOnlyReminderInSubagentContext,
  isLeaderOnlyToolUnavailableInSubagent,
  getLeaderOnlyToolUnavailableMessage,
  getPlanRequiredTeammatePreApprovalMessage,
  isPlanRequiredTeammatePreApprovalAllowedTool,
  getSubagentPlanToolUnavailableMessage,
  buildSubagentPlanToolBlockedResult
};
/**
 * @license
 * Copyright 2025 Google LLC
 * SPDX-License-Identifier: Apache-2.0
 */
/**
 * @license
 * Copyright 2025 Qwen
 * SPDX-License-Identifier: Apache-2.0
 */
