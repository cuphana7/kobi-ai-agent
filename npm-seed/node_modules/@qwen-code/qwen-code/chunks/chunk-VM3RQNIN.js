// Force strict mode and setup for ESM
"use strict";
import {
  getExcludedToolUnavailableMessage,
  getLeaderOnlyToolUnavailableMessage,
  getSubagentPlanToolUnavailableMessage,
  isLeaderOnlyToolUnavailableInSubagent,
  isPlanLifecycleToolUnavailableInSubagent,
  isSubagentLikeExecutionContext,
  isToolExcludedForCurrentContext
} from "./chunk-OU26ZZOL.js";
import {
  BaseDeclarativeTool,
  BaseToolInvocation
} from "./chunk-5XEZQQG3.js";
import {
  ToolDisplayNames,
  ToolNames,
  canonicalToolName
} from "./chunk-RSWOFJBY.js";
import {
  init_esbuild_shims
} from "./chunk-5O2XNYP6.js";
import {
  __name
} from "./chunk-J2S4EL5Y.js";

// packages/core/src/tools/tool-call.ts
init_esbuild_shims();
var DEFERRED_TOOL_CALL_REFUSAL_PREFIX = "[tool_call bridge refused] ";
var DEFERRED_TOOL_CALL_CANCELLATION_PREFIX = "[tool_call bridge cancelled] ";
function bridgeRefusal(message) {
  return new Error(`${DEFERRED_TOOL_CALL_REFUSAL_PREFIX}${message}`);
}
__name(bridgeRefusal, "bridgeRefusal");
async function resolveDeferredToolCall(registry, envelope, options) {
  let bridge;
  try {
    bridge = await registry.ensureTool(ToolNames.TOOL_CALL);
  } catch (error) {
    return {
      error: bridgeRefusal(
        `tool_call could not be loaded: ${error instanceof Error ? error.message : String(error)}`
      ),
      errorType: "tool_not_registered" /* TOOL_NOT_REGISTERED */
    };
  }
  if (!bridge) {
    return {
      error: bridgeRefusal("tool_call is not registered in this session."),
      errorType: "tool_not_registered" /* TOOL_NOT_REGISTERED */
    };
  }
  let invocation;
  try {
    invocation = bridge.build(envelope);
  } catch (error) {
    return {
      error: bridgeRefusal(
        error instanceof Error ? error.message : String(error)
      ),
      errorType: "invalid_tool_params" /* INVALID_TOOL_PARAMS */
    };
  }
  let targetName = canonicalToolName(invocation.params.name);
  const lower = targetName.toLowerCase();
  const registered = registry.getAllToolNames?.() ?? [];
  let match;
  for (const name of registered) {
    if (name.toLowerCase() === lower) {
      match = name;
    }
  }
  if (match !== void 0) {
    targetName = match;
  }
  if (targetName === ToolNames.TOOL_CALL || targetName === ToolNames.TOOL_SEARCH) {
    return {
      error: bridgeRefusal(
        `tool_call cannot invoke bridge tool "${targetName}".`
      ),
      errorType: "invalid_tool_params" /* INVALID_TOOL_PARAMS */,
      targetName
    };
  }
  let target;
  try {
    target = await registry.ensureTool(targetName);
  } catch (error) {
    return {
      error: bridgeRefusal(
        `Deferred tool "${invocation.params.name}" could not be loaded: ${error instanceof Error ? error.message : String(error)}`
      ),
      errorType: "tool_not_registered" /* TOOL_NOT_REGISTERED */
    };
  }
  if (!target) {
    const remedy = registry.getTool(ToolNames.TOOL_SEARCH) ? " Run tool_search again to inspect the available tools." : " No deferred-tool discovery is available in this session.";
    return {
      error: bridgeRefusal(
        `Deferred tool "${invocation.params.name}" is not registered in this session.${remedy}`
      ),
      errorType: "tool_not_registered" /* TOOL_NOT_REGISTERED */
    };
  }
  if (isPlanLifecycleToolUnavailableInSubagent(target.name)) {
    return {
      error: bridgeRefusal(getSubagentPlanToolUnavailableMessage(target.name)),
      errorType: "execution_denied" /* EXECUTION_DENIED */
    };
  }
  if (isLeaderOnlyToolUnavailableInSubagent(target.name)) {
    return {
      error: bridgeRefusal(getLeaderOnlyToolUnavailableMessage(target.name)),
      errorType: "execution_denied" /* EXECUTION_DENIED */
    };
  }
  if (isSubagentLikeExecutionContext() && isToolExcludedForCurrentContext(target.name, options?.maxSubagentDepth)) {
    return {
      error: bridgeRefusal(getExcludedToolUnavailableMessage(target.name)),
      errorType: "execution_denied" /* EXECUTION_DENIED */
    };
  }
  if (!registry.isDeferredAndHidden(target.name)) {
    return {
      error: bridgeRefusal(
        `Tool "${target.name}" is already visible to the model or is not deferred. Call it directly instead of using tool_call.`
      ),
      errorType: "invalid_tool_params" /* INVALID_TOOL_PARAMS */,
      targetName: target.name
    };
  }
  if (registry.isToolDeclared?.(target.name) === false) {
    return {
      error: bridgeRefusal(
        `Deferred tool "${target.name}" is not declared in this session, so it cannot be invoked via tool_call.`
      ),
      errorType: "execution_denied" /* EXECUTION_DENIED */
    };
  }
  if (!registry.getTool(ToolNames.TOOL_SEARCH)) {
    return {
      error: bridgeRefusal(
        `Deferred tool "${target.name}" is unreachable in this session: tool_search is not registered, so the ToolSearch + ToolCall bridge is incomplete and deferred tools cannot be invoked via tool_call.`
      ),
      errorType: "execution_denied" /* EXECUTION_DENIED */
    };
  }
  return {
    tool: target,
    arguments: structuredClone(invocation.params.arguments)
  };
}
__name(resolveDeferredToolCall, "resolveDeferredToolCall");
var ToolCallInvocation = class extends BaseToolInvocation {
  static {
    __name(this, "ToolCallInvocation");
  }
  getDescription() {
    return this.params.name;
  }
  execute(_signal) {
    const message = "tool_call must be dispatched through the tool scheduler so the underlying tool keeps its permissions, hooks, and approvals.";
    return Promise.resolve({
      llmContent: `Error: ${message}`,
      returnDisplay: message,
      error: { message, type: "execution_failed" /* EXECUTION_FAILED */ }
    });
  }
};
var ToolCallTool = class _ToolCallTool extends BaseDeclarativeTool {
  constructor(registry) {
    super(
      _ToolCallTool.Name,
      ToolDisplayNames.TOOL_CALL,
      "Invokes a deferred tool after its schema has been reviewed with tool_search. Pass the exact deferred tool name and arguments matching the reviewed schema. Permissions, hooks, and approvals apply to the underlying tool.",
      "other" /* Other */,
      {
        type: "object",
        properties: {
          name: {
            type: "string",
            description: "Exact deferred tool name returned by tool_search.",
            minLength: 1
          },
          arguments: {
            type: "object",
            description: "Arguments matching the deferred tool schema returned by tool_search."
          }
        },
        required: ["name", "arguments"],
        additionalProperties: false
      },
      true,
      false,
      false,
      true,
      "deferred bridge invoke execute"
    );
    this.registry = registry;
  }
  static {
    __name(this, "ToolCallTool");
  }
  static Name = ToolNames.TOOL_CALL;
  toAutoClassifierInput(params) {
    const rawName = typeof params.name === "string" ? params.name : "";
    const targetName = canonicalToolName(rawName);
    if (rawName === "") {
      return { name: targetName };
    }
    let target = this.registry?.getTool(targetName);
    if (!target && this.registry) {
      const lower = targetName.toLowerCase();
      for (const name of this.registry.getAllToolNames?.() ?? []) {
        if (name.toLowerCase() === lower) {
          target = this.registry.getTool(name);
        }
      }
    }
    if (!target) {
      return { name: targetName };
    }
    if (target.name === ToolNames.TOOL_CALL) {
      return { name: target.name };
    }
    try {
      const projected = target.toAutoClassifierInput(
        structuredClone(params.arguments)
      );
      if (projected === "") {
        return { name: target.name };
      }
      if (projected === void 0) {
        return {
          name: target.name,
          arguments: structuredClone(params.arguments)
        };
      }
      return { name: target.name, arguments: projected };
    } catch {
      return { name: target.name };
    }
  }
  createInvocation(params) {
    return new ToolCallInvocation(params);
  }
};

export {
  DEFERRED_TOOL_CALL_REFUSAL_PREFIX,
  DEFERRED_TOOL_CALL_CANCELLATION_PREFIX,
  resolveDeferredToolCall,
  ToolCallTool
};
/**
 * @license
 * Copyright 2025 Qwen Team
 * SPDX-License-Identifier: Apache-2.0
 */
