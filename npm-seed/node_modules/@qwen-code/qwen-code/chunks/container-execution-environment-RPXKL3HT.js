// Force strict mode and setup for ESM
"use strict";
import {
  hasRootlessMarker
} from "./chunk-APKWAYCI.js";
import {
  ExecutionCleanupError
} from "./chunk-27CVFKVI.js";
import {
  isShellResultDisplay
} from "./chunk-4M7RD5QQ.js";
import {
  require_shell_quote
} from "./chunk-QCG3JVIW.js";
import {
  Mutex
} from "./chunk-VBT4BMCQ.js";
import {
  resolveWorkspacePath
} from "./chunk-RYSJNY4B.js";
import {
  ToolNames
} from "./chunk-RSWOFJBY.js";
import {
  Storage,
  createDebugLogger
} from "./chunk-GYAL5QAR.js";
import "./chunk-S34QJ6IR.js";
import "./chunk-K63OUFJ7.js";
import {
  init_esbuild_shims
} from "./chunk-5O2XNYP6.js";
import {
  __name,
  __toESM
} from "./chunk-J2S4EL5Y.js";

// packages/core/src/services/container-execution-environment.ts
init_esbuild_shims();
var import_shell_quote = __toESM(require_shell_quote(), 1);
import {
  execFile,
  spawn
} from "node:child_process";
import { randomUUID } from "node:crypto";
import {
  access,
  lstat,
  mkdir,
  mkdtemp,
  realpath,
  rm,
  rmdir,
  writeFile
} from "node:fs/promises";
import { homedir, tmpdir } from "node:os";
import { isAbsolute, join, relative, sep } from "node:path";
import { createInterface } from "node:readline";
var debugLogger = createDebugLogger("ContainerExecutionEnvironment");
function isPackageInstallation(command) {
  if (/[\n\r$`]/.test(command)) return false;
  try {
    const words = (0, import_shell_quote.parse)(command);
    if (!words.every((word) => typeof word === "string")) return false;
    const [program, action] = words;
    return program === "npm" && (action === "install" || action === "ci") || program === "pnpm" && action === "install" || program === "yarn" && action === "install";
  } catch {
    return false;
  }
}
__name(isPackageInstallation, "isPackageInstallation");
function runtimeCommand(options, args, signal) {
  return new Promise((resolveResult, reject) => {
    execFile(
      options.runtime,
      args,
      {
        env: options.runtimeEnv,
        cwd: tmpdir(),
        // Creating a cold image can pull it first; the caller controls cancellation.
        timeout: args[0] === "create" ? 0 : 3e4,
        signal,
        maxBuffer: 1024 * 1024,
        encoding: "utf8"
      },
      (error, stdout, stderr) => {
        if (error)
          reject(
            new Error(
              `${options.runtime} ${args[0]} failed: ${stderr || error.message}` + (args[0] === "create" && !signal?.aborted ? ` Check image ${options.image}; try ${options.runtime} pull ${options.image}.` : "")
            )
          );
        else resolveResult(stdout);
      }
    );
  });
}
__name(runtimeCommand, "runtimeCommand");
function workerContainerArguments(options, worker, name, rootless, gitMask, network = false) {
  const args = [
    "create",
    "--init",
    "--interactive",
    "--name",
    name,
    "--cap-drop",
    "ALL",
    "--security-opt",
    "no-new-privileges",
    "--volume",
    `${worker.workspace}:${worker.workspace}`,
    "--volume",
    `${options.bundleDirectory}:/opt/qwen-executor:ro`,
    "--workdir",
    worker.workspace,
    "--tmpfs",
    `${options.containerHome}:rw,mode=1777`
  ];
  if (worker.outputDirectory)
    args.push(
      "--volume",
      `${worker.outputDirectory}:${worker.outputDirectory}`
    );
  args.push("--volume", `${gitMask}:${join(worker.workspace, ".git")}:ro`);
  if (!network) args.push("--network", "none");
  const uid = process.getuid?.();
  const gid = process.getgid?.();
  if (!rootless && uid !== void 0 && gid !== void 0)
    args.push("--user", `${uid}:${gid}`);
  for (const value of options.environment) args.push("--env", value);
  args.push(
    "--entrypoint",
    "node",
    options.image,
    "/opt/qwen-executor/execution-worker.js",
    JSON.stringify(worker)
  );
  return args;
}
__name(workerContainerArguments, "workerContainerArguments");
async function readGitEntry(workspace) {
  const entry = await lstat(join(workspace, ".git")).catch(
    (error) => {
      if (error.code !== "ENOENT") throw error;
      return void 0;
    }
  );
  if (entry?.isSymbolicLink())
    throw new Error("A symlinked .git entry cannot be safely mounted.");
  if (entry && !entry.isDirectory() && !entry.isFile())
    throw new Error("The .git entry must be a regular file or directory.");
  return entry;
}
__name(readGitEntry, "readGitEntry");
var gitMountPointMutex = new Mutex();
var gitMountPoints = /* @__PURE__ */ new Map();
async function acquireGitMountPoint(workspace) {
  return gitMountPointMutex.runExclusive(async () => {
    const path = join(workspace, ".git");
    const entry = await readGitEntry(workspace);
    let point = gitMountPoints.get(path);
    if (point) {
      if (!entry || entry.dev !== point.dev || entry.ino !== point.ino) {
        throw new Error(
          "The shared .git mount point changed while still in use."
        );
      }
    } else if (!entry) {
      await mkdir(path);
      const created = await lstat(path);
      if (!created.isDirectory()) {
        throw new Error("The .git mount point changed during creation.");
      }
      point = { users: 0, dev: created.dev, ino: created.ino };
      gitMountPoints.set(path, point);
    }
    const directory = !entry || entry.isDirectory();
    if (!point) return { directory, release: void 0 };
    const owned = point;
    owned.users++;
    let released = false;
    return {
      directory,
      release: /* @__PURE__ */ __name(() => gitMountPointMutex.runExclusive(async () => {
        if (released) return;
        if (owned.users === 1) {
          const current = await lstat(path).catch(
            (error) => {
              if (error.code !== "ENOENT") throw error;
              return void 0;
            }
          );
          if (current?.isDirectory() && current.dev === owned.dev && current.ino === owned.ino) {
            await rmdir(path).catch((error) => {
              if (error.code !== "ENOENT" && error.code !== "ENOTEMPTY")
                throw error;
            });
          }
          gitMountPoints.delete(path);
        }
        owned.users--;
        released = true;
      }), "release")
    };
  });
}
__name(acquireGitMountPoint, "acquireGitMountPoint");
async function validateGitMask(workspace, gitMask) {
  const entry = await readGitEntry(workspace);
  if ((!entry || entry.isDirectory()) !== (await lstat(gitMask)).isDirectory()) {
    throw new Error(
      "The workspace .git entry changed type; restart the container agent."
    );
  }
}
__name(validateGitMask, "validateGitMask");
var ContainerWorker = class {
  constructor(options) {
    this.options = options;
  }
  static {
    __name(this, "ContainerWorker");
  }
  process;
  pending = /* @__PURE__ */ new Map();
  failure;
  disposal;
  starting;
  stderr = "";
  name = `qwen-agent-${randomUUID()}`;
  start(worker, rootless, gitMask, network, signal) {
    this.starting = this.startInternal(
      worker,
      rootless,
      gitMask,
      network,
      signal
    );
    return this.starting;
  }
  async startInternal(worker, rootless, gitMask, network, signal) {
    signal.throwIfAborted();
    await validateGitMask(worker.workspace, gitMask);
    signal.throwIfAborted();
    if (this.disposal)
      throw new Error("Container executor was disposed during startup.");
    debugLogger.debug(
      `Creating ${this.options.runtime} container ${this.name} with image ${this.options.image} (may pull the image; cancellation is available).`
    );
    await runtimeCommand(
      this.options,
      workerContainerArguments(
        this.options,
        worker,
        this.name,
        rootless,
        gitMask,
        network
      ),
      signal
    );
    await validateGitMask(worker.workspace, gitMask);
    signal.throwIfAborted();
    if (this.disposal)
      throw new Error("Container executor was disposed during startup.");
    const child = spawn(
      this.options.runtime,
      ["start", "--attach", "--interactive", this.name],
      {
        env: this.options.runtimeEnv,
        cwd: tmpdir(),
        stdio: ["pipe", "pipe", "pipe"]
      }
    );
    this.process = child;
    child.stderr.on("data", (data) => {
      this.stderr = (this.stderr + data.toString()).slice(-8192);
    });
    child.on("error", (error) => this.fail(error));
    child.on(
      "close",
      () => this.fail(new Error(`Container executor exited. ${this.stderr}`))
    );
    child.stdin.on("error", (error) => this.fail(error));
    const lines = createInterface({ input: child.stdout });
    lines.on("line", (line) => {
      try {
        const reply = JSON.parse(line);
        const pending = this.pending.get(reply.id);
        if (!pending) return;
        if ("update" in reply) {
          pending.update?.(reply.update);
          return;
        }
        this.pending.delete(reply.id);
        if ("error" in reply) pending.reject(new Error(reply.error));
        else pending.resolve(reply.result);
      } catch {
        this.fail(
          new Error(
            "Container executor returned an invalid protocol response."
          )
        );
      }
    });
    await this.request(
      { method: "invalidateReadCache" },
      AbortSignal.any([signal, AbortSignal.timeout(3e4)])
    );
  }
  request(request, signal, update) {
    if (this.failure) return Promise.reject(this.failure);
    if (this.disposal || !this.process)
      return Promise.reject(new Error("Container executor is closed."));
    signal.throwIfAborted();
    const id = randomUUID();
    return new Promise((resolveResult, reject) => {
      const aborted = /* @__PURE__ */ __name(() => {
        const pending = this.pending.get(id);
        if (!pending) return;
        this.pending.delete(id);
        this.process?.stdin.write(`${JSON.stringify({ cancel: id })}
`);
        pending.reject(
          new Error(
            request.method === "execute" ? "Container execution cancelled; an interrupted write may have completed. Do not automatically retry. No operation was replayed." : `Container ${request.method} cancelled. No operation was replayed.`
          )
        );
      }, "aborted");
      this.pending.set(id, {
        resolve: /* @__PURE__ */ __name((value) => {
          signal.removeEventListener("abort", aborted);
          resolveResult(value);
        }, "resolve"),
        reject: /* @__PURE__ */ __name((error) => {
          signal.removeEventListener("abort", aborted);
          reject(error);
        }, "reject"),
        update
      });
      signal.addEventListener("abort", aborted, { once: true });
      this.process.stdin.write(`${JSON.stringify({ id, request })}
`);
    });
  }
  fail(error) {
    this.failure ??= new Error(
      `${error.message} Execution outcome may be unknown; no operation was replayed.`
    );
    for (const pending of this.pending.values()) pending.reject(this.failure);
    this.pending.clear();
  }
  dispose() {
    this.fail(new Error("Container executor disposed."));
    this.disposal ??= this.remove().catch((error) => {
      this.disposal = void 0;
      throw error;
    });
    return this.disposal;
  }
  async remove() {
    await this.starting?.catch(() => void 0);
    this.process?.kill("SIGKILL");
    try {
      await runtimeCommand(this.options, ["rm", "-f", this.name]);
    } catch (error) {
      if (!/No such container|no container with (?:name or )?ID/i.test(
        String(error)
      )) {
        throw new ExecutionCleanupError(
          `Could not remove container ${this.name}; preserve its workspace until cleanup succeeds: ${String(error)}`
        );
      }
    }
  }
};
var ContainerExecutionEnvironment = class _ContainerExecutionEnvironment {
  constructor(options, workerOptions, rootless, temporaryDirectory, gitMask, primary) {
    this.options = options;
    this.workerOptions = workerOptions;
    this.rootless = rootless;
    this.temporaryDirectory = temporaryDirectory;
    this.gitMask = gitMask;
    this.primary = primary;
    this.workers.add(primary);
  }
  static {
    __name(this, "ContainerExecutionEnvironment");
  }
  get outputDirectory() {
    return join(this.temporaryDirectory, "output");
  }
  workers = /* @__PURE__ */ new Set();
  invocations = /* @__PURE__ */ new Map();
  disposal;
  releaseGitMountPoint;
  closed = false;
  static async create(config, options, signal) {
    signal.throwIfAborted();
    const workspace = await realpath(config.getWorkingDir());
    const bundleDirectory = await realpath(options.bundleDirectory);
    const temporaryRoot = await realpath(tmpdir());
    if (process.platform === "win32" || workspace.includes(":") || bundleDirectory.includes(":") || temporaryRoot.includes(":")) {
      throw new Error(
        "Container execution requires Unix paths without volume separators."
      );
    }
    for (const protectedDirectory of [
      homedir(),
      Storage.getGlobalQwenDir(),
      Storage.getRuntimeBaseDir(),
      temporaryRoot
    ]) {
      const canonical = resolveWorkspacePath(protectedDirectory);
      const fromWorkspace = relative(workspace, canonical);
      if (fromWorkspace === "" || !fromWorkspace.startsWith(`..${sep}`) && fromWorkspace !== ".." && !isAbsolute(fromWorkspace)) {
        throw new Error(
          "Container workspace must not contain the host home, Qwen credentials or runtime directory, or the temporary directory."
        );
      }
    }
    for (const directory of [bundleDirectory, ...options.trustedDirectories]) {
      const overlap = [
        relative(workspace, directory),
        relative(directory, workspace)
      ].some(
        (path) => path === "" || !path.startsWith(`..${sep}`) && path !== ".." && !isAbsolute(path)
      );
      if (overlap) {
        throw new Error(
          "Container workspace must not overlap the trusted CLI bundle or dependency directories. Run an independent CLI installation outside the workspace."
        );
      }
    }
    await access(join(bundleDirectory, "execution-worker.js"));
    const resolvedOptions = { ...options, bundleDirectory };
    const info = await runtimeCommand(
      resolvedOptions,
      ["info", "--format", "{{json .}}"],
      signal
    );
    const serverErrors = JSON.parse(info).ServerErrors;
    if (serverErrors?.length) {
      throw new Error(
        `${options.runtime} info failed: ${serverErrors.join("; ")}`
      );
    }
    const rootless = hasRootlessMarker(info);
    const lines = config.getTruncateToolOutputLines();
    const threshold = config.getTruncateToolOutputThreshold();
    const workerOptions = {
      workspace,
      sessionId: `executor-${randomUUID()}`,
      // Zero disables truncation; Infinity would become null on the wire.
      truncateToolOutputLines: Number.isFinite(lines) ? lines : 0,
      truncateToolOutputThreshold: config.isTruncateToolOutputThresholdExplicit() ? Number.isFinite(threshold) ? threshold : 0 : void 0,
      fileReadCacheDisabled: config.getFileReadCacheDisabled(),
      fileFiltering: config.getFileFilteringOptions(),
      defaultFileEncoding: config.getDefaultFileEncoding(),
      shellDefaultTimeoutMs: config.getShellDefaultTimeoutMs(),
      shellHeartbeatIntervalMs: config.getShellHeartbeatIntervalMs(),
      maxBufferedOutputBytes: config.getShellExecutionConfig().maxBufferedOutputBytes
    };
    await readGitEntry(workspace);
    const temporaryDirectory = await mkdtemp(
      join(temporaryRoot, "qwen-agent-executor-")
    );
    const gitMask = join(temporaryDirectory, "git-mask");
    const primary = new ContainerWorker(resolvedOptions);
    const environment = new _ContainerExecutionEnvironment(
      resolvedOptions,
      workerOptions,
      rootless,
      temporaryDirectory,
      gitMask,
      primary
    );
    try {
      await mkdir(environment.outputDirectory);
      workerOptions.outputDirectory = environment.outputDirectory;
      const gitMountPoint = await acquireGitMountPoint(workspace);
      environment.releaseGitMountPoint = gitMountPoint.release;
      if (gitMountPoint.directory) await mkdir(gitMask);
      else await writeFile(gitMask, "");
      await primary.start(workerOptions, rootless, gitMask, false, signal);
      return environment;
    } catch (error) {
      await environment.dispose().catch((cleanupError) => {
        throw new ExecutionCleanupError(
          `${String(error)}; cleanup failed: ${String(cleanupError)}`,
          { cause: error, retryCleanup: /* @__PURE__ */ __name(() => environment.dispose(), "retryCleanup") }
        );
      });
      throw error;
    }
  }
  async prepare(request, signal) {
    if (this.closed) throw new Error("Container executor is closed.");
    let worker = this.primary;
    if (request.toolName === ToolNames.SHELL && typeof request.params["command"] === "string" && isPackageInstallation(request.params["command"])) {
      if (request.params["is_background"])
        throw new Error(
          "Network-enabled package installations must run in the foreground."
        );
      worker = new ContainerWorker(this.options);
      this.workers.add(worker);
      try {
        await worker.start(
          this.workerOptions,
          this.rootless,
          this.gitMask,
          true,
          signal
        );
      } catch (error) {
        await worker.dispose().catch((cleanupError) => {
          throw new ExecutionCleanupError(
            `${String(error)}; cleanup failed: ${String(cleanupError)}`,
            { cause: error }
          );
        });
        this.workers.delete(worker);
        throw error;
      }
    }
    this.invocations.set(request.id, worker);
    try {
      return await worker.request({ method: "prepare", request }, signal);
    } catch (error) {
      await this.release(request.id, AbortSignal.timeout(3e4)).catch(
        (cleanupError) => {
          throw new ExecutionCleanupError(
            `${String(error)}; cleanup failed: ${String(cleanupError)}`,
            { cause: error }
          );
        }
      );
      throw error;
    }
  }
  invocation(id) {
    const worker = this.invocations.get(id);
    if (!worker) throw new Error("Unknown container invocation.");
    return worker;
  }
  permission(id, signal) {
    return this.invocation(id).request(
      { method: "permission", invocationId: id },
      signal
    );
  }
  confirmation(id, signal) {
    return this.invocation(id).request(
      { method: "confirmation", invocationId: id },
      signal
    );
  }
  confirm(id, outcome, payload, signal) {
    return this.invocation(id).request(
      { method: "confirm", invocationId: id, outcome, payload },
      signal
    );
  }
  async execute(id, signal, update) {
    const worker = this.invocation(id);
    let result;
    try {
      result = await worker.request(
        { method: "execute", invocationId: id },
        signal,
        update
      );
    } catch (error) {
      await this.release(id, AbortSignal.timeout(3e4)).catch(
        () => void 0
      );
      throw error;
    }
    this.invocations.delete(id);
    if (worker !== this.primary) {
      try {
        await worker.dispose();
        this.workers.delete(worker);
      } catch (error) {
        const notice = `

[Container cleanup failed after tool execution: ${String(error)}. The tool result above is still valid; do not automatically retry the command.]`;
        delete result.outputBudgetApplied;
        result.llmContent = typeof result.llmContent === "string" ? result.llmContent + notice : [
          ...Array.isArray(result.llmContent) ? result.llmContent : [result.llmContent],
          { text: notice }
        ];
        if (typeof result.returnDisplay === "string") {
          result.returnDisplay += notice;
        } else if (isShellResultDisplay(result.returnDisplay)) {
          result.returnDisplay = {
            ...result.returnDisplay,
            text: result.returnDisplay.text + notice,
            notices: [...result.returnDisplay.notices, notice.trim()]
          };
        }
        if (result.error) result.error.message += notice;
      }
    }
    return result;
  }
  modificationContent(toolName, params, signal) {
    return this.primary.request(
      { method: "modificationContent", toolName, params },
      signal
    );
  }
  async release(id, signal) {
    const worker = this.invocations.get(id);
    if (!worker) return;
    this.invocations.delete(id);
    if (worker !== this.primary) {
      await worker.dispose();
      this.workers.delete(worker);
    } else
      await worker.request({ method: "release", invocationId: id }, signal);
  }
  async invalidateReadCache(paths) {
    await this.primary.request(
      { method: "invalidateReadCache", paths },
      AbortSignal.timeout(3e4)
    );
  }
  dispose() {
    this.closed = true;
    this.disposal ??= (async () => {
      const resources = `${[...this.workers].map((worker) => worker.name).join(", ")} (${this.options.runtime}); temporary directory ${this.temporaryDirectory}`;
      const notice = setTimeout(() => {
        console.warn(
          `Container cleanup is still pending: ${resources}. Keep the workspace and verify these containers have stopped before manual removal.`
        );
      }, 500);
      notice.unref?.();
      try {
        const results = await Promise.allSettled(
          [...this.workers].map((worker) => worker.dispose())
        );
        const failure = results.find((result) => result.status === "rejected");
        if (failure?.status === "rejected") throw failure.reason;
        this.invocations.clear();
        this.workers.clear();
        await this.releaseGitMountPoint?.();
        await rm(this.temporaryDirectory, { recursive: true, force: true });
      } catch (error) {
        console.warn(
          `Container cleanup failed: ${resources}: ${String(error)}`
        );
        throw error;
      } finally {
        clearTimeout(notice);
      }
    })().catch((error) => {
      this.disposal = void 0;
      throw error;
    });
    return this.disposal;
  }
};
export {
  ContainerExecutionEnvironment,
  isPackageInstallation,
  workerContainerArguments
};
/**
 * @license
 * Copyright 2026 Qwen Team
 * SPDX-License-Identifier: Apache-2.0
 */
