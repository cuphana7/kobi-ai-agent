// Force strict mode and setup for ESM
"use strict";
import {
  t
} from "./chunk-S5SS7OXV.js";
import {
  computeThresholds,
  estimateContextTextTokens,
  formatContextFileDisplayPath,
  getMainSessionBaseSystemPrompt,
  stripTerminalControlSequences
} from "./chunk-LWQKWO5I.js";
import {
  qualifySkillName
} from "./chunk-JXNPH2EP.js";
import {
  isMediaPolicyToolHiddenFromModel
} from "./chunk-MNU36NGH.js";
import {
  resolveSlimmingConfig
} from "./chunk-GC3J5W2O.js";
import {
  DiscoveredMCPTool,
  uiTelemetryService
} from "./chunk-HZU2DMPO.js";
import {
  DEFAULT_TOKEN_LIMIT
} from "./chunk-CGIKEQFI.js";
import {
  buildAvailableSkillsReminder,
  getStartupContextLength
} from "./chunk-GNGQGEZD.js";
import {
  buildSkillLlmContent
} from "./chunk-WFNJIZ4G.js";
import {
  ToolNames
} from "./chunk-RSWOFJBY.js";
import {
  isSubpath
} from "./chunk-GYAL5QAR.js";
import {
  getErrorMessage
} from "./chunk-S34QJ6IR.js";
import {
  init_esbuild_shims
} from "./chunk-5O2XNYP6.js";
import {
  __name
} from "./chunk-J2S4EL5Y.js";

// packages/cli/src/ui/commands/contextCommand.ts
init_esbuild_shims();
import * as path from "node:path";

// packages/cli/src/utils/extension-mention.ts
init_esbuild_shims();
import * as fs from "node:fs/promises";
var EXTENSION_REF_PREFIX = "ext:";
var EXTENSION_CONTEXT_BUDGET = 2e5;
var EXTENSION_CONTEXT_FILE_CAP = 5e4;
function parseExtensionRef(pathName) {
  if (!pathName.startsWith(EXTENSION_REF_PREFIX)) return null;
  const name = pathName.slice(EXTENSION_REF_PREFIX.length);
  if (!name) return null;
  return { name };
}
__name(parseExtensionRef, "parseExtensionRef");
function buildExtensionRef(extensionName) {
  return `${EXTENSION_REF_PREFIX}${extensionName}`;
}
__name(buildExtensionRef, "buildExtensionRef");
function matchExtensionByRef(name, extensions) {
  const lower = name.toLowerCase();
  return extensions.find(
    (ext) => ext.name.toLowerCase() === lower || ext.config.name.toLowerCase() === lower
  );
}
__name(matchExtensionByRef, "matchExtensionByRef");
var BIDI_CONTROL_RE = /[‎‏؜⁦⁧⁨⁩‪‫‬‭‮]/g;
function sanitizeDisplayText(raw) {
  const stripped = stripTerminalControlSequences(raw).replace(BIDI_CONTROL_RE, "").replace(/\s+/g, " ").trim();
  return stripped.length > 0 ? stripped : null;
}
__name(sanitizeDisplayText, "sanitizeDisplayText");
function getSanitizedExtensionDisplayName(extension) {
  return sanitizeDisplayText(extension.displayName || extension.name) || extension.name;
}
__name(getSanitizedExtensionDisplayName, "getSanitizedExtensionDisplayName");
function buildExtensionContextText(extension) {
  const displayName = getSanitizedExtensionDisplayName(extension);
  const lines = [];
  lines.push(
    `--- Extension: ${displayName} (untrusted third-party content) ---`
  );
  if (extension.config.description) {
    const desc = sanitizeDisplayText(extension.config.description);
    if (desc) {
      lines.push(desc);
      lines.push("");
    }
  }
  const capabilities = [];
  if (extension.skills && extension.skills.length > 0) {
    const skillNames = extension.skills.map(
      (s) => sanitizeDisplayText(qualifySkillName(extension.name, s.name)) || s.name
    ).join(", ");
    capabilities.push(`- Skills: ${skillNames} (invoke via /<skill-name>)`);
  }
  if (extension.mcpServers && Object.keys(extension.mcpServers).length > 0) {
    const serverNames = Object.keys(extension.mcpServers).map((n) => sanitizeDisplayText(n) || n).join(", ");
    capabilities.push(`- MCP Servers: ${serverNames}`);
  }
  if (extension.agents && extension.agents.length > 0) {
    const agentNames = extension.agents.map((a) => sanitizeDisplayText(a.name) || a.name).join(", ");
    capabilities.push(`- Agents: ${agentNames}`);
  }
  if (capabilities.length > 0) {
    lines.push("Available capabilities from this extension:");
    lines.push(...capabilities);
    lines.push("");
  }
  lines.push(`--- End Extension: ${displayName} ---`);
  return lines.join("\n");
}
__name(buildExtensionContextText, "buildExtensionContextText");
async function buildExtensionMentionContext(extension, options) {
  let contextText = buildExtensionContextText(extension);
  let remainingBudget = options.remainingBudget;
  if (extension.contextFiles.length === 0) {
    return { text: contextText, remainingBudget };
  }
  const fileReads = await Promise.allSettled(
    extension.contextFiles.map(async (contextFilePath) => {
      let realPath;
      let realExtPath;
      try {
        realPath = await fs.realpath(contextFilePath);
        realExtPath = await fs.realpath(extension.path);
      } catch {
        options.onDebugMessage?.(
          `Skipping unreadable context file: ${contextFilePath}`
        );
        return null;
      }
      if (!isSubpath(realExtPath, realPath)) {
        options.onDebugMessage?.(
          `Skipping context file outside extension directory: ${contextFilePath}`
        );
        return null;
      }
      return fs.readFile(realPath, {
        encoding: "utf-8",
        signal: options.signal
      });
    })
  );
  for (let i = 0; i < fileReads.length; i++) {
    const outcome = fileReads[i];
    if (outcome.status === "rejected") {
      options.onDebugMessage?.(
        `Failed to read extension context file ${extension.contextFiles[i]}: ${getErrorMessage(outcome.reason)}`
      );
      continue;
    }
    const content = outcome.value;
    if (!content || !content.trim()) continue;
    if (remainingBudget <= 0) {
      options.onDebugMessage?.(
        "Extension context budget exhausted, skipping remaining files."
      );
      break;
    }
    const cap = Math.min(EXTENSION_CONTEXT_FILE_CAP, remainingBudget);
    const cappedContent = content.length > cap ? content.slice(0, cap) + "\n... (truncated)" : content;
    contextText += `

${cappedContent}`;
    remainingBudget -= cappedContent.length;
  }
  return { text: contextText, remainingBudget };
}
__name(buildExtensionMentionContext, "buildExtensionMentionContext");

// packages/cli/src/ui/commands/contextCommand.ts
function currentTier(tokens, thresholds) {
  if (tokens >= thresholds.hard) return "hard";
  if (tokens >= thresholds.auto) return "auto";
  if (tokens >= thresholds.warn) return "warn";
  return "safe";
}
__name(currentTier, "currentTier");
function extensionContextFileOwners(config, workingDir) {
  const owners = /* @__PURE__ */ new Map();
  for (const extension of config.getActiveExtensions?.() ?? []) {
    const displayName = getSanitizedExtensionDisplayName(extension);
    for (const contextFile of extension.contextFiles ?? []) {
      const absolutePath = path.resolve(workingDir, contextFile);
      const fileLabel = formatContextFileDisplayPath(
        absolutePath,
        extension.path
      );
      owners.set(
        absolutePath,
        `${t("Extension")}: ${displayName} \xB7 ${fileLabel}`
      );
    }
  }
  return owners;
}
__name(extensionContextFileOwners, "extensionContextFileOwners");
function parseMemoryFiles(memoryContent, workingDir, extensionOwners = /* @__PURE__ */ new Map()) {
  if (!memoryContent || memoryContent.trim().length === 0) return [];
  const results = [];
  const regex = /--- Context from: (.+?) ---\n([\s\S]*?)--- End of Context from: \1 ---/g;
  let match;
  while ((match = regex.exec(memoryContent)) !== null) {
    const filePath = match[1];
    const content = match[2];
    const absolutePath = path.resolve(workingDir, filePath);
    const owner = extensionOwners.get(absolutePath);
    results.push({
      // An extension's file is named by its extension rather than by a path
      // under the install directory, which is what makes the row actionable:
      // the reader can disable or migrate that extension.
      path: owner ?? formatContextFileDisplayPath(absolutePath, workingDir),
      tokens: estimateContextTextTokens(content)
    });
  }
  if (results.length === 0 && memoryContent.trim().length > 0) {
    results.push({
      path: t("memory"),
      tokens: estimateContextTextTokens(memoryContent)
    });
  }
  return results;
}
__name(parseMemoryFiles, "parseMemoryFiles");
function unescapeXml(text) {
  return text.replace(/&lt;/g, "<").replace(/&gt;/g, ">").replace(/&quot;/g, '"').replace(/&apos;/g, "'").replace(/&amp;/g, "&");
}
__name(unescapeXml, "unescapeXml");
var AVAILABLE_SKILLS_OPEN = "<available_skills>";
var NO_SKILLS_NOTICE = "No skills are currently available.";
var SKILL_LISTING_ENTRY = /<skill>\n<name>\n([\s\S]*?)\n<\/name>[\s\S]*?<\/skill>/g;
function isSkillListingText(text) {
  return text.includes(AVAILABLE_SKILLS_OPEN) || text.includes(NO_SKILLS_NOTICE);
}
__name(isSkillListingText, "isSkillListingText");
function measureSkillListing(text) {
  const byName = /* @__PURE__ */ new Map();
  for (const match of text.matchAll(SKILL_LISTING_ENTRY)) {
    const name = unescapeXml(match[1]);
    byName.set(name.toLowerCase(), {
      name,
      tokens: estimateContextTextTokens(match[0])
    });
  }
  return { tokens: estimateContextTextTokens(text), byName };
}
__name(measureSkillListing, "measureSkillListing");
function mergeSkillListing(into, from) {
  into.tokens += from.tokens;
  for (const [name, entry] of from.byName) {
    into.byName.set(name, entry);
  }
}
__name(mergeSkillListing, "mergeSkillListing");
function measureTailSkillListings(conversation) {
  const listing = { tokens: 0, byName: /* @__PURE__ */ new Map() };
  const billedTexts = /* @__PURE__ */ new Set();
  for (const content of conversation) {
    for (const part of content.parts ?? []) {
      const text = part.text;
      if (typeof text !== "string" || !isSkillListingText(text)) continue;
      const measured = measureSkillListing(text);
      if (measured.byName.size === 0) continue;
      mergeSkillListing(listing, measured);
      billedTexts.add(text);
    }
  }
  return { listing, billedTexts };
}
__name(measureTailSkillListings, "measureTailSkillListings");
function measureStartupPrelude(prelude) {
  const skillListing = { tokens: 0, byName: /* @__PURE__ */ new Map() };
  let startupContextTokens = 0;
  for (const content of prelude) {
    for (const part of content.parts ?? []) {
      if (typeof part.text !== "string") continue;
      if (isSkillListingText(part.text)) {
        mergeSkillListing(skillListing, measureSkillListing(part.text));
      } else {
        startupContextTokens += estimateContextTextTokens(part.text);
      }
    }
  }
  return { skillListing, startupContextTokens };
}
__name(measureStartupPrelude, "measureStartupPrelude");
function estimateFunctionResponseTokens(part, imageTokenEstimate) {
  const response = part.functionResponse;
  let tokens = estimateContextTextTokens(
    JSON.stringify({
      id: response.id,
      name: response.name,
      response: response.response
    })
  );
  const nested = response.parts;
  if (Array.isArray(nested)) {
    for (const inner of nested) {
      if (inner.inlineData || inner.fileData) {
        tokens += imageTokenEstimate;
      } else if (typeof inner.text === "string") {
        tokens += estimateContextTextTokens(inner.text);
      }
    }
  }
  return tokens;
}
__name(estimateFunctionResponseTokens, "estimateFunctionResponseTokens");
function isBilledSkillBody(part, billedSkillBodies) {
  if (billedSkillBodies.size === 0) return false;
  const output = part.functionResponse?.response?.output;
  if (typeof output !== "string") return false;
  if (billedSkillBodies.has(output)) return true;
  for (const body of billedSkillBodies) {
    if (output.startsWith(`${body}
`)) return true;
  }
  return false;
}
__name(isBilledSkillBody, "isBilledSkillBody");
function estimateConversationTokens(conversation, billing) {
  let tokens = 0;
  for (const content of conversation) {
    for (const part of content.parts ?? []) {
      if (typeof part.text === "string") {
        if (billing.billedListingTexts.has(part.text)) continue;
        tokens += estimateContextTextTokens(part.text);
      } else if (part.inlineData || part.fileData) {
        tokens += billing.imageTokenEstimate;
      } else if (part.functionCall) {
        tokens += estimateContextTextTokens(JSON.stringify(part.functionCall));
      } else if (part.functionResponse) {
        if (part.functionResponse.name === ToolNames.SKILL && isBilledSkillBody(part, billing.billedSkillBodies)) {
          continue;
        }
        tokens += estimateFunctionResponseTokens(
          part,
          billing.imageTokenEstimate
        );
      }
    }
  }
  return tokens;
}
__name(estimateConversationTokens, "estimateConversationTokens");
async function collectContextData(config, showDetails) {
  const modelName = config.getModel() || "unknown";
  const contentGeneratorConfig = config.getContentGeneratorConfig();
  const contextWindowSize = contentGeneratorConfig.contextWindowSize ?? DEFAULT_TOKEN_LIMIT;
  const llmClient = config.getLlmClient?.();
  const activeChat = llmClient?.isInitialized?.() ? llmClient.getChat() : void 0;
  const apiTotalTokens = activeChat ? activeChat.getLastPromptTokenCount() : uiTelemetryService.getLastPromptTokenCount();
  const apiCachedTokens = activeChat?.getLastCachedContentTokenCount?.() ?? uiTelemetryService.getLastCachedContentTokenCount();
  const history = activeChat?.getHistoryShallow?.() ?? activeChat?.getHistory?.() ?? [];
  const preludeLength = getStartupContextLength(history);
  const prelude = measureStartupPrelude(history.slice(0, preludeLength));
  const conversationHistory = history.slice(preludeLength);
  const tailSkillListings = measureTailSkillListings(conversationHistory);
  let conversationTokens = 0;
  const systemPromptText = getMainSessionBaseSystemPrompt(config);
  const systemPromptTokens = estimateContextTextTokens(systemPromptText);
  const toolRegistry = config.getToolRegistry();
  const allTools = toolRegistry ? toolRegistry.getAllTools() : [];
  const toolDeclarations = toolRegistry ? toolRegistry.getFunctionDeclarations() : [];
  const toolsJsonStr = JSON.stringify(toolDeclarations);
  const allToolsTokens = estimateContextTextTokens(toolsJsonStr);
  const builtinTools = [];
  const mcpTools = [];
  for (const tool of allTools) {
    if (toolRegistry?.isDeferredAndHidden(tool.name)) {
      continue;
    }
    if (isMediaPolicyToolHiddenFromModel(config, tool)) {
      continue;
    }
    const toolJsonStr = JSON.stringify(tool.schema);
    const tokens = estimateContextTextTokens(toolJsonStr);
    if (tool instanceof DiscoveredMCPTool) {
      mcpTools.push({
        name: `${tool.serverName}__${tool.serverToolName || tool.name}`,
        tokens
      });
    } else if (tool.name !== ToolNames.SKILL) {
      builtinTools.push({
        name: tool.name,
        tokens
      });
    }
  }
  const memoryContent = config.getUserMemory();
  const memoryFiles = parseMemoryFiles(
    memoryContent,
    config.getWorkingDir(),
    extensionContextFileOwners(config, config.getWorkingDir())
  );
  const autoMemoryPrompt = config.getAutoMemoryPrompt();
  if (autoMemoryPrompt) {
    memoryFiles.push({
      path: t("auto memory"),
      tokens: estimateContextTextTokens(autoMemoryPrompt)
    });
  }
  const memoryFilesTokens = memoryFiles.reduce((sum, f) => sum + f.tokens, 0);
  const skillTool = allTools.find((tool) => tool.name === ToolNames.SKILL);
  const skillToolDefinitionTokens = skillTool ? estimateContextTextTokens(JSON.stringify(skillTool.schema)) : 0;
  const loadedSkillNames = skillTool && "getLoadedSkillNames" in skillTool ? skillTool.getLoadedSkillNames() : /* @__PURE__ */ new Set();
  const skillManager = config.getSkillManager();
  const skillConfigs = skillManager ? await skillManager.listSkills() : [];
  const enabledSkillNames = new Set(
    skillConfigs.filter((skill) => config.isSkillEnabled(skill)).map((skill) => skill.name.toLowerCase())
  );
  let skillListing = prelude.skillListing;
  if (!activeChat) {
    const reminder = await buildAvailableSkillsReminder(config);
    if (reminder) {
      skillListing = measureSkillListing(reminder.reminder);
    }
  }
  mergeSkillListing(skillListing, tailSkillListings.listing);
  let loadedBodiesTokens = 0;
  const billedSkillBodies = /* @__PURE__ */ new Set();
  const skills = skillConfigs.map((skill) => {
    const listingTokens = skillListing.byName.get(skill.name.toLowerCase())?.tokens ?? 0;
    const isLoaded = loadedSkillNames.has(skill.name);
    let bodyTokens;
    if (isLoaded && skill.body) {
      const baseDir = skill.filePath ? path.dirname(skill.filePath) : "";
      const body = buildSkillLlmContent(baseDir, skill.body);
      bodyTokens = estimateContextTextTokens(body);
      loadedBodiesTokens += bodyTokens;
      billedSkillBodies.add(body);
    }
    return {
      name: skill.name,
      tokens: listingTokens,
      loaded: isLoaded,
      bodyTokens
    };
  });
  const rowedNames = new Set(skillConfigs.map((s) => s.name.toLowerCase()));
  for (const [key, entry] of skillListing.byName) {
    if (rowedNames.has(key)) continue;
    rowedNames.add(key);
    enabledSkillNames.add(key);
    skills.push({ name: entry.name, tokens: entry.tokens });
  }
  conversationTokens = estimateConversationTokens(conversationHistory, {
    imageTokenEstimate: resolveSlimmingConfig(config.getChatCompression?.()).imageTokenEstimate,
    billedSkillBodies,
    billedListingTexts: tailSkillListings.billedTexts
  });
  const skillsTokens = skillToolDefinitionTokens + skillListing.tokens + loadedBodiesTokens;
  const startupContextTokens = prelude.startupContextTokens;
  const thresholds = computeThresholds(
    contextWindowSize,
    config.getAutoCompactThreshold()
  );
  const autocompactBuffer = Math.max(
    0,
    Math.round(contextWindowSize - thresholds.auto)
  );
  const rawOverhead = systemPromptTokens + allToolsTokens + memoryFilesTokens + skillListing.tokens + loadedBodiesTokens + startupContextTokens;
  const rawContent = rawOverhead + conversationTokens;
  const hasTokenCount = apiTotalTokens > 0;
  const isEstimated = !hasTokenCount || activeChat?.isLastPromptTokenCountEstimated() === true;
  const mcpToolsTotalTokens = mcpTools.reduce(
    (sum, tool) => sum + tool.tokens,
    0
  );
  let totalTokens;
  let displaySystemPrompt;
  let displayBuiltinTools;
  let displayMcpTools;
  let displayMemoryFiles;
  let displaySkills;
  let displayStartupContext;
  let messagesTokens;
  let unattributedTokens = 0;
  let freeSpace;
  let detailBuiltinTools;
  let detailMcpTools;
  let detailMemoryFiles;
  let detailSkills;
  if (!hasTokenCount) {
    totalTokens = 0;
    displaySystemPrompt = systemPromptTokens;
    displaySkills = skillsTokens;
    displayStartupContext = startupContextTokens;
    displayBuiltinTools = Math.max(
      0,
      allToolsTokens - skillToolDefinitionTokens - mcpToolsTotalTokens
    );
    displayMcpTools = mcpToolsTotalTokens;
    displayMemoryFiles = memoryFilesTokens;
    messagesTokens = 0;
    freeSpace = Math.max(0, contextWindowSize - rawContent - autocompactBuffer);
    detailBuiltinTools = builtinTools;
    detailMcpTools = mcpTools;
    detailMemoryFiles = memoryFiles;
    detailSkills = skills;
  } else {
    totalTokens = apiTotalTokens;
    const scale = rawOverhead > totalTokens ? totalTokens / rawOverhead : 1;
    const clampDeficit = Math.max(
      0,
      skillToolDefinitionTokens + mcpToolsTotalTokens - allToolsTokens
    );
    const clampedMcpTools = Math.max(0, mcpToolsTotalTokens - clampDeficit);
    const clampedSkills = skillsTokens - Math.max(0, clampDeficit - mcpToolsTotalTokens);
    const clampedBuiltinTools = Math.max(
      0,
      allToolsTokens - skillToolDefinitionTokens - clampedMcpTools
    );
    displaySystemPrompt = Math.floor(systemPromptTokens * scale);
    displayBuiltinTools = Math.floor(clampedBuiltinTools * scale);
    displayMcpTools = Math.floor(clampedMcpTools * scale);
    displayMemoryFiles = Math.floor(memoryFilesTokens * scale);
    displaySkills = Math.floor(clampedSkills * scale);
    displayStartupContext = Math.floor(startupContextTokens * scale);
    const attributedOverhead = displaySystemPrompt + displayBuiltinTools + displayMcpTools + displayMemoryFiles + displaySkills + displayStartupContext;
    if (scale < 1) {
      messagesTokens = Math.max(0, totalTokens - attributedOverhead);
    } else {
      messagesTokens = Math.min(
        conversationTokens,
        Math.max(0, totalTokens - attributedOverhead)
      );
      unattributedTokens = Math.max(
        0,
        totalTokens - attributedOverhead - messagesTokens
      );
    }
    freeSpace = Math.max(
      0,
      contextWindowSize - totalTokens - autocompactBuffer
    );
    const scaleDetail = /* @__PURE__ */ __name((items) => scale < 1 ? items.map((item) => ({
      ...item,
      tokens: Math.round(item.tokens * scale)
    })) : items, "scaleDetail");
    detailBuiltinTools = scaleDetail(builtinTools);
    detailMcpTools = scaleDetail(mcpTools);
    detailMemoryFiles = scaleDetail(memoryFiles);
    detailSkills = scale < 1 ? skills.map((item) => ({
      ...item,
      tokens: Math.round(item.tokens * scale),
      bodyTokens: item.bodyTokens ? Math.round(item.bodyTokens * scale) : void 0
    })) : skills;
  }
  const tierTokens = hasTokenCount ? apiTotalTokens : rawContent;
  const breakdown = {
    systemPrompt: displaySystemPrompt,
    builtinTools: displayBuiltinTools,
    mcpTools: displayMcpTools,
    memoryFiles: displayMemoryFiles,
    skills: displaySkills,
    startupContext: displayStartupContext,
    messages: messagesTokens,
    unattributed: unattributedTokens,
    cachedTokens: hasTokenCount ? apiCachedTokens : 0,
    freeSpace,
    autocompactBuffer,
    thresholds,
    currentTier: currentTier(tierTokens, thresholds)
  };
  return {
    type: "context_usage" /* CONTEXT_USAGE */,
    modelName,
    totalTokens,
    contextWindowSize,
    breakdown,
    builtinTools: showDetails ? detailBuiltinTools : [],
    mcpTools: showDetails ? detailMcpTools : [],
    memoryFiles: showDetails ? detailMemoryFiles : [],
    skills: showDetails ? detailSkills.filter(
      (skill) => enabledSkillNames.has(skill.name.toLowerCase())
    ) : [],
    isEstimated,
    showDetails
  };
}
__name(collectContextData, "collectContextData");
function fmtTokens(tokens) {
  if (tokens >= 1e3) {
    return `${(tokens / 1e3).toFixed(1)}k`;
  }
  return `${tokens}`;
}
__name(fmtTokens, "fmtTokens");
function fmtCategoryRow(label, tokens, contextWindowSize, indent = "  ") {
  const percentage = contextWindowSize > 0 ? (tokens / contextWindowSize * 100).toFixed(1) : "0.0";
  const right = `${fmtTokens(tokens)} tokens (${percentage}%)`;
  const leftPart = `${indent}${label}`;
  const totalWidth = 56;
  const dots = Math.max(1, totalWidth - leftPart.length - right.length);
  return `${leftPart}${" ".repeat(dots)}${right}`;
}
__name(fmtCategoryRow, "fmtCategoryRow");
function formatNum(n) {
  return Math.round(n).toLocaleString("en-US");
}
__name(formatNum, "formatNum");
function formatContextUsageText(data) {
  const {
    modelName,
    totalTokens,
    contextWindowSize,
    breakdown,
    builtinTools,
    mcpTools,
    memoryFiles,
    skills,
    isEstimated,
    showDetails
  } = data;
  const hasTokenCount = totalTokens > 0;
  const lines = [];
  lines.push("## Context Usage");
  lines.push("");
  if (!hasTokenCount) {
    lines.push("*No API response yet. Send a message to see actual usage.*");
    lines.push("");
    lines.push("**Estimated pre-conversation overhead**");
    lines.push(
      `Model: ${modelName}  Context window: ${fmtTokens(contextWindowSize)} tokens`
    );
    lines.push("");
  } else {
    lines.push(
      `Model: ${modelName}  Context window: ${fmtTokens(contextWindowSize)} tokens`
    );
    lines.push("");
    if (isEstimated) {
      lines.push(
        "*Token usage is estimated until provider usage is received.*"
      );
      lines.push("");
    }
    lines.push(fmtCategoryRow("Used", totalTokens, contextWindowSize));
    if ((breakdown.cachedTokens ?? 0) > 0) {
      lines.push(
        fmtCategoryRow(
          "Cached prefix",
          breakdown.cachedTokens,
          contextWindowSize,
          "  \u2514 "
        )
      );
    }
    lines.push(fmtCategoryRow("Free", breakdown.freeSpace, contextWindowSize));
    lines.push("");
    lines.push("**Compaction thresholds**");
    lines.push(
      `  Effective window:   ${formatNum(breakdown.thresholds.effectiveWindow)}  (window \u2212 ${formatNum(contextWindowSize - breakdown.thresholds.effectiveWindow)} reserve)`
    );
    lines.push(`  Warn threshold:     ${formatNum(breakdown.thresholds.warn)}`);
    lines.push(`  Auto threshold:     ${formatNum(breakdown.thresholds.auto)}`);
    lines.push(`  Hard threshold:     ${formatNum(breakdown.thresholds.hard)}`);
    lines.push(`  Current tier:       ${breakdown.currentTier}`);
    lines.push("");
    lines.push("**Usage by category**");
  }
  lines.push(
    fmtCategoryRow("System prompt", breakdown.systemPrompt, contextWindowSize)
  );
  lines.push(
    fmtCategoryRow("Built-in tools", breakdown.builtinTools, contextWindowSize)
  );
  if (breakdown.mcpTools > 0) {
    lines.push(
      fmtCategoryRow("MCP tools", breakdown.mcpTools, contextWindowSize)
    );
  }
  lines.push(
    fmtCategoryRow("Memory files", breakdown.memoryFiles, contextWindowSize)
  );
  lines.push(fmtCategoryRow("Skills", breakdown.skills, contextWindowSize));
  if ((breakdown.startupContext ?? 0) > 0) {
    lines.push(
      fmtCategoryRow(
        "Startup context",
        breakdown.startupContext,
        contextWindowSize
      )
    );
  }
  if (hasTokenCount) {
    lines.push(
      fmtCategoryRow("Messages", breakdown.messages, contextWindowSize)
    );
    if ((breakdown.unattributed ?? 0) > 0) {
      lines.push(
        fmtCategoryRow(
          "Unattributed",
          breakdown.unattributed,
          contextWindowSize
        )
      );
    }
  }
  if (showDetails) {
    const sortedBuiltin = [...builtinTools].sort((a, b) => b.tokens - a.tokens);
    const sortedMcp = [...mcpTools].sort((a, b) => b.tokens - a.tokens);
    const sortedMemory = [...memoryFiles].sort((a, b) => b.tokens - a.tokens);
    const sortedSkills = [...skills].sort((a, b) => {
      if (a.loaded !== b.loaded) return a.loaded ? -1 : 1;
      return b.tokens + (b.bodyTokens ?? 0) - (a.tokens + (a.bodyTokens ?? 0));
    });
    if (sortedBuiltin.length > 0) {
      lines.push("");
      lines.push("**Built-in tools**");
      for (const tool of sortedBuiltin) {
        lines.push(
          fmtCategoryRow(tool.name, tool.tokens, contextWindowSize, "  \u2514 ")
        );
      }
    }
    if (sortedMcp.length > 0) {
      lines.push("");
      lines.push("**MCP tools**");
      for (const tool of sortedMcp) {
        lines.push(
          fmtCategoryRow(tool.name, tool.tokens, contextWindowSize, "  \u2514 ")
        );
      }
    }
    if (sortedMemory.length > 0) {
      lines.push("");
      lines.push("**Memory files**");
      for (const file of sortedMemory) {
        lines.push(
          fmtCategoryRow(file.path, file.tokens, contextWindowSize, "  \u2514 ")
        );
      }
    }
    if (sortedSkills.length > 0) {
      lines.push("");
      lines.push("**Skills**");
      for (const skill of sortedSkills) {
        const label = skill.loaded ? `${skill.name} (active)` : skill.name;
        lines.push(
          fmtCategoryRow(label, skill.tokens, contextWindowSize, "  \u2514 ")
        );
        if (skill.loaded && skill.bodyTokens && skill.bodyTokens > 0) {
          lines.push(
            fmtCategoryRow(
              "body loaded",
              skill.bodyTokens,
              contextWindowSize,
              "    \u2514 "
            )
          );
        }
      }
    }
  } else {
    lines.push("");
    lines.push("*Run /context detail for per-item breakdown.*");
  }
  return lines.join("\n");
}
__name(formatContextUsageText, "formatContextUsageText");
var contextCommand = {
  name: "context",
  get description() {
    return t(
      'Show context window usage breakdown. Use "/context detail" for per-item breakdown.'
    );
  },
  kind: "built-in" /* BUILT_IN */,
  supportedModes: ["interactive", "non_interactive", "acp"],
  action: /* @__PURE__ */ __name(async (context, args) => {
    const normalizedArgs = args?.trim().toLowerCase();
    const showDetails = normalizedArgs === "detail" || normalizedArgs === "-d";
    const executionMode = context.executionMode ?? "interactive";
    const { config } = context.services;
    if (!config) {
      if (executionMode === "interactive") {
        context.ui.addItem(
          {
            type: "error" /* ERROR */,
            text: t("Config not loaded.")
          },
          Date.now()
        );
        return;
      }
      return {
        type: "message",
        messageType: "error",
        content: t("Config not loaded.")
      };
    }
    const contextUsageItem = await collectContextData(config, showDetails);
    if (executionMode === "interactive") {
      context.ui.addItem(contextUsageItem, Date.now());
      return;
    }
    return {
      type: "message",
      messageType: "info",
      content: formatContextUsageText(contextUsageItem)
    };
  }, "action"),
  subCommands: [
    {
      name: "detail",
      get description() {
        return t("Show per-item context usage breakdown.");
      },
      kind: "built-in" /* BUILT_IN */,
      supportedModes: ["interactive", "non_interactive", "acp"],
      action: /* @__PURE__ */ __name(async (context) => {
        await contextCommand.action(context, "detail");
      }, "action")
    }
  ]
};

export {
  EXTENSION_CONTEXT_BUDGET,
  parseExtensionRef,
  buildExtensionRef,
  matchExtensionByRef,
  sanitizeDisplayText,
  getSanitizedExtensionDisplayName,
  buildExtensionMentionContext,
  collectContextData,
  formatContextUsageText,
  contextCommand
};
/**
 * @license
 * Copyright 2025 Qwen
 * SPDX-License-Identifier: Apache-2.0
 */
