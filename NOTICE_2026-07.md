# 📢 Kobi 신규 기능 안내 (2026-07-03 ~ 2026-07-21)

`KB AI Assistant(Kobi)` 오프라인 설치 패키지에 추가된 신규 기능을 안내드립니다. 새 버전을 받아 재설치하시면 아래 기능을 바로 사용하실 수 있습니다.

---

## 1. Office(Excel/Word) 파일 편집 스킬 추가 (v20260721)

Office가 설치되어 있지 않은 PC에서도 순수 Node.js 라이브러리만으로 Excel(`.xlsx`)과 Word(`.docx`) 파일을 다룰 수 있는 `office-edit` 스킬이 새로 탑재되었습니다.

- **읽기**: Excel 시트/셀 내용 조회(`read-xlsx`), Word 문서 텍스트/마크다운 추출(`read-docx`)
- **부분 수정**: 기존 서식을 유지한 채 Excel 특정 셀 값만 변경(`edit-xlsx`), Word 특정 문구만 치환(`edit-docx`)
- **템플릿 채우기**: 보고서 등 기존 양식에 데이터를 채워 새 파일 생성(`fill-template`)
- **신규 생성** *(07-21 추가)*: 양식 없이 제목/표/문단 구조만 지정해 완전히 새로운 Excel/Word 파일을 처음부터 생성(`create-xlsx`, `create-docx`) — 기존 파일은 `--force` 옵션 없이는 덮어쓰지 않습니다.
- 필요한 라이브러리(exceljs, mammoth, docxtemplater, pizzip, docx)가 스킬 폴더에 이미 포함되어 있어 별도 설치나 네트워크 연결이 필요 없습니다.
- 안전 원칙: `--in-place`를 명시하지 않는 한 원본 파일을 덮어쓰지 않고 항상 새 파일로 저장합니다.

## 2. 시작 배너 & Kobi 브랜딩 적용 (v20260720)

Kobi 실행 시 상단에 `Kobi` / `KB AI Assistant` 배너와 전용 아스키 아트 로고가 표시되도록 변경되었습니다. 또한 실행 준비 과정 안내가 개선되어, `node.exe` 탐색 중에는 스피너 애니메이션(`Kobi 실행 준비 중입니다...`)이, 에이전트 시작 직전에는 `Kobi 에이전트를 시작합니다...` 메시지가 표시됩니다. *(스피너/시작 메시지는 v20260721에서 함께 반영)*

## 3. Computer Use 화면 읽기 전용 기능 추가 (v20260713, 보안팀 승인)

개발자가 열어둔 로그/에러창/실행결과 등 화면에 보이는 정보를 Kobi가 텍스트로 읽어 분석을 보조하는 **읽기 전용** 기능이 추가되었습니다. (v20260720에서 정식 활성화)

- 마우스 클릭·드래그·키보드 입력 등 PC를 직접 조작하는 툴은 전체 차단되며, `list_windows`, `get_accessibility_tree` 등 읽기 전용 조회 툴만 허용됩니다.
- 사내 LLM이 이미지(비전) 입력을 지원하지 않아, 스크린샷 대신 화면 UI를 텍스트 트리로 읽는 방식(`mode: "ax"`)을 사용합니다.
- 내부망 전용 단일 사용자 PC를 전제로 하며, 인증서·비밀번호·API 키·OTP 등 **자격증명 정보**는 화면에 보이더라도 원문 그대로 인용하지 않고 마스킹 처리합니다.
- 차단되지 않은 조회 툴도 호출 시마다 사용자 승인(Approval) 절차를 거칩니다.

## 4. Frism 형상관리시스템(CM) 연동 스킬 추가 (v20260709)

사내 표준 형상관리 솔루션인 Frism CM과 대화형으로 연동할 수 있는 `frism-cm` 스킬이 추가되었습니다.

- 사용자 인증/연결 상태 검증, CM 패키지 조회 및 자동 생성
- 소스 자원 체크아웃/체크인, 체크아웃 취소 및 잠금 해제
- 파일 변경 이력 버전 조회 및 특정 버전 코드 다운로드
- 스테이지(테스트) 환경 배포 반영 신청 (※ 운영 환경 반영은 보안 정책상 계속 차단)

## 5. Qwen Code 코어 모듈 업데이트

- 0.19.9 (v20260720) → **0.20.0** (v20260721)로 업데이트되었으며, 오프라인 npm 캐시도 함께 갱신되었습니다.

---

## 요약 표

| 버전 | 일자 | 구분 | 핵심 내용 |
| :--- | :--- | :--- | :--- |
| v20260721 | 2026-07-21 | 기능 추가/개선 | Office 편집 스킬에 `create-xlsx`/`create-docx` 추가, Qwen Code 0.20.0, 실행 안내 스피너 개선 |
| v20260721 | 2026-07-21 | 기능 추가 | Office(Excel/Word) 파일 편집 스킬(`office-edit`) 신규 탑재 |
| v20260720 | 2026-07-20 | 기능 추가/UI 개선 | 시작 배너·Kobi 브랜딩 적용, Computer Use 정식 활성화, Qwen Code 0.19.9 |
| v20260713 | 2026-07-13 | 기능 추가 (보안팀 승인) | Computer Use 화면 읽기 전용 기능 추가 |
| v20260709 | 2026-07-09 | 기능 추가 | Frism 형상관리시스템(CM) 연동 스킬 추가 |

> 자세한 전체 변경 이력은 `README.md`의 [12. 패키지 변경 이력 (Changelog)](README.md) 섹션을 참고해 주세요.
